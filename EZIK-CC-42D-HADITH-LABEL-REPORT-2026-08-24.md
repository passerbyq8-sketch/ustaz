# EZIK — تقريرُ ٤٢-د · وسمُ الحديث · ٢٤ أغسطس ٢٠٢٦

**🔴 وُقِفَ ولم يُمَسَّ حرفٌ واحد.** §١-٣ تأمرُ: «إن ثبتَ أنّ حارسًا يثبّتُه: **قفْ واكتبْ ولا تمسَّ الحارس**» — **وقد ثبتَ في حارسَينِ اثنَين**. فلا إيداعَ ولا رقعةَ ولا دفع.

> والرقعةُ نفسُها **مقيسةٌ لا مظنونة**: بُنيَتْ في الذاكرةِ وأُجريَتْ عليها تأكيداتُ الحارسَينِ كلُّها، فيُعرَفُ بالضبطِ ما يسقطُ وما يثبتُ إن أذِنَ المالك. **ولم يُكتَبْ منها بايتةٌ واحدةٌ في الشجرة.**

---

## ١ · الأساسُ والفرعُ والإيداع

| المفتاح | القيمة |
|---|---|
| الشجرة | `C:\Users\passe\projects\ustaz` |
| الأساس | `fix/card-attribution-20260824` عندَ **`04911d4dbde01d4fb202d99cd591cee284fce04d`** — **مقيسٌ ومطابقٌ** لـ`04911d4` |
| الفرع | **هو نفسُه** — لم يُقطَعْ فرعٌ جديد، كما تأمرُ §٠ |
| **الإيداع** | **لا شيء** |
| **ما مسَّ** | **صفرُ ملفّ** — `git diff --stat HEAD` ⟹ فارغ · `git status --porcelain` ⟹ صفرُ سطر |

---

## ٢ · جوابُ §١ الثلاثةُ — بالقياسِ لا بالقراءة

قُدْتُ التطبيقَ المشحونَ في `vm` (linkedom + حزمتا React + الكتلةُ مصرَّفةً) — الطريقةُ نفسُها التي يستعملُها `theme-coverage-guard.cjs` — **واستخرجتُ قرارَ الوسمِ بالطريقةِ التي يستخرجُه بها `quest-ux-guard.cjs` نفسُه**: تُحلَّلُ `HadithCard` نحويًّا، ويُنتزَعُ مُهيِّئُ `label`، ويُقوَّمُ مربوطًا بـ`att` و`NEUTRAL_HADITH_LABEL` وحدَهما.

### ٢-١ · `HadithCard` — وكلُّ ما تعرضُه اليوم

```js
function HadithCard({ content, narrator, ruling }) {
  const att = resolveHadithAttribution(narrator, ruling);
  let label = (!att.narrator && !att.ruling) ? NEUTRAL_HADITH_LABEL : 'من السنة النبوية';
  const quranic = hasQuranicSpan(content);
  const ayahRef = quranic ? readStatedAyahRef(content) : null;
  if (quranic) label = AYAH_CARD_LABEL;
  return (
    <div style={s.hadithCard}>
      <div style={s.hadithCardLabel}><span>{label}</span></div>
      <div style={s.hadithText}>{content}</div>
      {quranic
        ? (ayahRef && <div style={s.hadithMeta}>{`سورة ${ayahRef.surah}، آية ${ayahRef.ayah}`}</div>)
        : (att.ruling && <div style={s.hadithMeta}>{att.ruling}</div>)}
    </div>
  );
}
```

| ما تعرضُه | الجواب المقيس |
|---|---|
| **الوسم** | `AYAH_CARD_LABEL` إن كانَ المتنُ قرآنًا · وإلّا `نص منقول` حيثُ لا راويَ ولا حكم · وإلّا **`من السنة النبوية` ثابتةً** |
| **الراوي** | **لا يُعرَضُ أبدًا.** `att.narrator` لا يظهرُ في أيِّ مخرَجٍ من مخرجاتِ هذا المكوِّن |
| **الحكمُ (`ruling`)** | **يُعرَضُ نعم** — سطرَ ميتا مستقلًّا (`s.hadithMeta`) كلّما كانَ `att.ruling` غيرَ فارغٍ والمتنُ ليسَ قرآنًا. مقيسٌ: يظهرُ في الحالتَينِ ١ و٣، ولا يظهرُ في ٢ و٤ لأنّه فارغٌ فيهما |

**وفرعُ القرآنِ يعلو الجميع**: متنٌ فيه مدًى قرآنيٌّ يأخذُ وسمَ الآيةِ ولا يأخذُ درجةَ حديثٍ ألبتّة. **هذا الفرعُ خارجُ ما يطلبُه هذا الأمرُ ولم أقتربْ منه.**

### ٢-٢ · صيغةُ المُسَلسِلِ — حرفًا، وهي المرجع

سطرُ المصدرِ كما هو في `REPLY_SERIALIZERS.hadith`:

```js
att.narrator ? '\u0631\u064E\u0648\u064E\u0649 ' + att.narrator : '\u0645\u0646 \u0627\u0644\u0633\u0646\u0629 \u0627\u0644\u0646\u0628\u0648\u064A\u0629'
```

🔵 **والمُسَلسِلُ يكتبُ اللاحقةَ بهروبِ يونيكود لا بمحارفَ عربيّة.** فالبادئةُ المقيسةُ:

```
literal in source : '\u0631\u064E\u0648\u064E\u0649 '
decoded           : "رَوَى "
codepoints        : 0631 064e 0648 064e 0649 0020
```

**وهذا وحدَه سببٌ كافٍ لألّا تُعادَ كتابتُها بيدٍ**: «رَوَى» تحملُ فتحةً على الراءِ وأخرى على الياء، ونسخةٌ مكتوبةٌ بترتيبِ علاماتٍ مغايرٍ **سلسلةٌ أخرى** لا يصحُّ معها ادّعاءُ «حرفًا». وفي الرقعةِ المقترَحةِ أدناه **انتُزِعَتِ البادئةُ من مصدرِ المُسَلسِلِ نفسِه** ولم تُكتَبْ.

### ٢-٣ · 🔴 أيَعرفُ حارسٌ نصَّ الوسمِ على الشاشة؟ — **نعم، حارسانِ**

#### الحارسُ الأوّل — `quest-ux-guard.cjs` (البوّابة `questux`)

يستخرجُ قرارَ الوسمِ نحويًّا ويُقوِّمُه على ثلاثِ حالاتٍ **بأسمائِها**:

```js
// quest-ux-guard.cjs:168-176
eq('no surviving narrator or ruling gets the neutral label',
  labelFor({ narrator: '', ruling: '' }), 'نص منقول');
eq('a surviving narrator gets the supported-hadith label',
  labelFor({ narrator: 'البخاري', ruling: '' }), 'من السنة النبوية');
eq('a surviving ruling alone gets the supported-hadith label',
  labelFor({ narrator: '', ruling: 'صحيح' }), 'من السنة النبوية');
ok('the badge renders only the decided label, never an interpolated narrator',
  /<span>\{label\}<\/span>/.test(cardSource)
    && !/<span>\{[^}]*att\.narrator/.test(cardSource));
```

**والثاني منها هو الحالةُ ٢ بعينِها**، مثبَّتةً على اللفظِ القديم.

#### الحارسُ الثاني — `theme-coverage-guard.cjs` (البوّابة `themecoverage`)

يثبّتُ **سطرَ المُهيِّئِ محرفًا بمحرف**:

```js
// theme-coverage-guard.cjs:1184-1189
// THE DECISION THE OTHER GUARD OWNS MUST STAY INSPECTABLE. quest-ux-guard.cjs extracts this
// initialiser and evaluates it bound only to `att` and `NEUTRAL_HADITH_LABEL`; a scripture
// branch folded INTO it would throw there, and that guard is not this screen's to edit.
okOn('Q28-4: ...and the label initialiser stays evaluable on att alone', [['hc', hc]],
  /let label = \(!att\.narrator && !att\.ruling\) \? NEUTRAL_HADITH_LABEL : '\u0645\u0646 ...'/.test(hc)
  && /<span>\{label\}<\/span>/.test(hc));
```

**فوَقَفتُ.** §١-٣ لا تحتملُ تأويلًا، و§٠ تُدرِجُ «تعديلُ حارس» في المحرَّم.

---

## ٣ · الحالاتُ الأربعُ — قبلَ الرقعةِ وبعدَها

**«قبل» هي حالُ الشجرةِ الآن**، مقيسةً بتشغيلِ قرارِ الوسمِ المشحونِ والمُسَلسِلِ المشحونِ على المدخلاتِ نفسِها.
**«بعد» رقعةٌ لم تنزلْ**: بُنيَتْ في الذاكرةِ وقُوِّمَتْ بالطريقةِ نفسِها، **ولم تُكتَبْ في الشجرة**.

| # | الحالة | الشاشةُ **الآن** | المُسَلسِل | متطابقان؟ | الشاشةُ **لو نزلَتِ الرقعة** | متطابقان بعدُ؟ | تغيّرَ؟ | سطرُ الحكم |
|---|---|---|---|---|---|---|---|---|
| ١ | راوٍ وحكمٌ (`مسلم` · `صحيح`) | `من السنة النبوية` | **`رَوَى مسلم`** | 🔴 **لا** | **`رَوَى مسلم`** | 🟢 **نعم** | نعم | 🟢 يُعرَضُ (`صحيح`) — **قبلَ وبعدُ** |
| ٢ | راوٍ بلا حكم | `من السنة النبوية` | **`رَوَى مسلم`** | 🔴 **لا** | **`رَوَى مسلم`** | 🟢 **نعم** | نعم | لا يُعرَضُ (فارغ) — **قبلَ وبعدُ** |
| ٣ | حكمٌ بلا راوٍ | `من السنة النبوية` | `من السنة النبوية` | 🟢 نعم | `من السنة النبوية` | 🟢 نعم | **لا** | 🟢 يُعرَضُ (`صحيح`) — **قبلَ وبعدُ** |
| ٤ | لا راويَ ولا حكم | **`نص منقول`** | `من السنة النبوية` | 🔴 لا | **`نص منقول`** | 🔴 لا | **لا** | لا يُعرَضُ — **قبلَ وبعدُ** |

**والحالتانِ ٣ و٤ لم تتغيّرا بحرف** — وهو ما تأمرُ به §٢ نصًّا: «حيثُ لا راوي ⟹ ما هو قائمٌ اليومَ يبقى كما هو بحرفِه». فُحِصَتا لا لأنّهما تتحرّكان، بل **ليُرى أنّهما لا تتحرّكان**.

🔵 **وملحوظةٌ تستحقُّ عينَ المالك في الحالةِ ٤:** الشاشةُ تقولُ `نص منقول` والمُسَلسِلُ يقولُ `من السنة النبوية`. **فاللفظانِ يبقيانِ مختلفَينِ هناك حتّى بعدَ الرقعة** — لأنّ §٢ تأمرُ بإبقاءِ `نص منقول` بحرفِه. فالتوحيدُ الذي يطلبُه الأمرُ **توحيدٌ في الحالتَينِ ١ و٢ فقط**، وهذا مقصودٌ لا سهوٌ منّي، ومذكورٌ لئلّا يُقرَأَ التقريرُ على أنّ الأربعَ ستتطابق.

### ٣-١ · الرقعةُ التي لم تنزل

```js
// السطرُ المشحونُ اليوم
let label = (!att.narrator && !att.ruling) ? NEUTRAL_HADITH_LABEL : 'من السنة النبوية';

// السطرُ المقترَح — فرعُ الراوي يُضافُ أمامَ ما هو قائمٌ، والفرعانِ القائمانِ يُنقَلانِ كما هما
let label = att.narrator ? ('\u0631\u064E\u0648\u064E\u0649 ' + att.narrator)
          : ((!att.narrator && !att.ruling) ? NEUTRAL_HADITH_LABEL : 'من السنة النبوية');
```

**ثلاثُ خصالٍ مقيسةٍ فيها:**
1. **البادئةُ منتزَعةٌ من مصدرِ المُسَلسِلِ**، لا مكتوبةً — فالتطابقُ حرفيٌّ بالبناء (§٢-٢).
2. **الفرعانِ القائمانِ منقولانِ بالشريحةِ لا بإعادةِ الكتابة** — فالحالتانِ ٣ و٤ لا تتحرّكانِ بايتةً.
3. **يبقى المُهيِّئُ قابلًا للتقويمِ على `att` وحدَه** — مقيسٌ: `labelForThrew = null` · `stillEvaluableOnAttAlone = true`. وهذا هو الشرطُ الذي يكتبُه `theme-coverage-guard.cjs:1184-1186` بنصِّه، **ولو استُدعيَ المُسَلسِلُ من داخلِ المُهيِّئِ لانكسرَ**: `quest-ux-guard` يقوّمُه في سياقٍ لا يعرفُ `REPLY_SERIALIZERS` فيرمي، **فيصيرُ القرارُ غيرَ قابلٍ للفحصِ أصلًا**. فالطريقُ الصحيحُ هو الحرفُ المنتزَعُ لا النداء.

### ٣-٢ · أثرُ الرقعةِ على الحرّاسِ — **مقيسٌ بتشغيلِ تأكيداتِهم عليها**

| الحارس | السطر | التأكيد | الحكم |
|---|---|---|---|
| `quest-ux-guard.cjs` | `161-162` | القرارُ يبقى قابلًا للاستخراج | 🟢 يثبت |
| `quest-ux-guard.cjs` | `168-169` | لا راويَ ولا حكمٌ ⟹ الوسمُ المحايد | 🟢 يثبت (`نص منقول`) |
| **`quest-ux-guard.cjs`** | **`170-171`** | **راوٍ ⟹ `من السنة النبوية`** | 🔴 **يسقط** — الرقعةُ تُخرِجُ `رَوَى البخاري` |
| `quest-ux-guard.cjs` | `172-173` | حكمٌ وحدَه ⟹ `من السنة النبوية` | 🟢 يثبت |
| `quest-ux-guard.cjs` | `174-176` | الشارةُ ترسمُ الوسمَ المقرَّرَ لا راويًا مُقحَمًا | 🟡 **النمطُ يمرُّ والمعنى ينقلب** — §٣-٣ |
| `theme-coverage-guard.cjs` | `1178-1180` | القرآنُ يُسألُ عنه قبلَ الوسم | 🟢 يثبت |
| `theme-coverage-guard.cjs` | `1181-1183` | آيةٌ لا تُعطى درجةَ حديث | 🟢 يثبت |
| **`theme-coverage-guard.cjs`** | **`1187-1189`** | **سطرُ المُهيِّئِ محرفًا بمحرف** | 🔴 **يسقط** — يثبّتُ السطرَ نفسَه |
| `theme-coverage-guard.cjs` | `1192-1193` | البطاقةُ لا تمدُّ يدَها لشبكةٍ ولا نموذجٍ ولا مخزن | 🟢 يثبت |

**المجموع: تأكيدانِ يسقطانِ، في حارسَينِ مختلفَين. وسبعةٌ تثبت.**

### ٣-٣ · 🟡 تأكيدٌ يمرُّ نمطُه وينقلبُ معناه — ولا أتّكِئُ عليه

`quest-ux-guard.cjs:174-176` عنوانُه: **«the badge renders only the decided label, never an interpolated narrator»**. ونمطُه `!/<span>\{[^}]*att\.narrator/` **يمرُّ** على الرقعةِ لأنّ الراويَ يدخلُ في `label` لا في الشارةِ مباشرةً.

**لكنّ ما يقولُه هذا التأكيدُ بلسانِه هو عينُ ما يأمرُ هذا الأمرُ بنقضِه.** ومرورُ الحرفِ مع انقلابِ المعنى **هو الغشُّ الذي رفضتُه في الجولةِ ٤٢-ب**، فلن أعدَّه هنا موافقةً. **فهو ثالثُ ما يحتاجُ عينَ المالك**، وإن كانَ أخضرَ.

---

## ٤ · البوّابات

**على الشجرةِ كما هي — إذ لا إيداعَ في هذه الجولة.**
`summary.json` ⟸ `head_before = 04911d4…` · `head_after = 04911d4…` · `dirty = 0/0` · `tree_dirtied_by_run = false`
المجلَّد: `…\Temp\ezik-gates\runs\2026-08-24T13-41-14-347Z-19644`

| البوّابة | الشرط | **المقيس** | |
|---|---|---|---|
| الروستر | `93` | **`93`** | 🟢 |
| `recon` | `184/1/0` | `SUMMARY   PASS=184   WARN=1   FAIL=0` | 🟢 |
| الإنذارُ الوحيد | المعروف | `[WARN] LONGEST_CARD_CHARS = 3405 > longest card 3401 -> cap oversized/stale (re-derive in api/report.js)` | 🟢 |
| حارسُ الدروس | `269` | `=== 269/269 - PASS ===` | 🟢 |
| `themecoverage` | خضراء | `OK: 1351/1351 checks passed.` | 🟢 |
| `markdown` | خضراء | `OK: 121/121 checks passed.` | 🟢 |
| **`questux`** | — | `OK: 61/61 checks passed.` | 🟢 **وهو الحارسُ الأوّلُ الذي يمنعُ الرقعة** |
| `SUITE` | — | `92/93` · `FAILING (1): bankintegrity=1` | 🟡 |
| `bankintegrity` | تحمرُّ بـ`CORE_BYTES` وحدَه | `FAIL  74 checks passed, 2 failed` | 🔴 |

### ٤-١ · فرقُ `CORE_BYTES` رقمًا — **مُعلَنٌ ولا يُصلَح**

```
FAIL [B12] CORE_BYTES = 1862145 but CORE weighs 1870613 bytes on disk (+8468).
FAIL [B14] sw.js prose says app.js is 1026796 bytes; the disk says 1035264 (+8468).
```

| | الرقم |
|---|---|
| `CORE_BYTES` المعلَنُ في `sw.js` | `1862145` |
| المقيسُ على القرص | `1870613` |
| **الفرق** | **`+8468`** |

🔵 **وهذا الفرقُ ليسَ من هذه الجولةِ في شيء**: هذه الجولةُ لم تُودِعْ ولم تمسَّ ملفًّا. الفرقُ **بحالِه** من إيداعِ ٤٢-ج (`04911d4`)، وكلُّه من `app.js` وحدَه، و`index.html` لم يتحرّك. وهو ملكُ جولةِ الدمجِ القادمة، والمحرَّمُ في §٠ يشملُ قطعَه.

---

## ٥ · ما لم يُقَسْ · وما كذّبَه القياس

### ٥-١ · فرضيّاتٌ كذّبَها القياس

1. **🔴 «تعديلُ الشاشةِ أرخصُ أثرًا من تعديلِ المُسَلسِل»** — هذا أساسُ اختيارِ المالكِ للخيارِ (ب)، **وهو صحيحٌ في أثرِ المستخدمِ وغيرُ صحيحٍ في كلفةِ الحرّاس**: المُسَلسِلُ **لا يثبّتُ حرفَه حارسٌ واحدٌ في هذه الشجرة**، بينما وسمُ الشاشةِ **يثبّتُه حارسانِ في تأكيدَين**. فالخيارُ (ب) أرخصُ على القارئِ وأغلى على الحرّاس. **مذكورٌ لأنّه قد يغيّرُ قرارَ المالك.**
2. **🟡 «صيغةُ المُسَلسِلِ محارفُ عربيّة»** — لا. **هروبُ يونيكود** (`\u0631\u064E…`)، ولهذا كانَ انتزاعُها لا كتابتُها شرطًا في صحّةِ ادّعاءِ «حرفًا» (§٢-٢).
3. **🟡 «توحيدُ اللفظِ يجعلُ الحالاتِ الأربعَ متطابقة»** — لا. **اثنتانِ تتطابقانِ واثنتانِ تبقيانِ على حالِهما**، والرابعةُ تبقى مختلفةً عن المُسَلسِلِ عمدًا (§٣).

### ٥-٢ · ما لم يُقَسْ — يُسمّى ولا يُطوى

1. **🔴 الرقعةُ لم تُشغَّلْ في متصفّح، ولم تُرسَمْ بطاقةُ حديثٍ على شاشة.** المقيسُ هو **قرارُ الوسمِ** مُقوَّمًا كما يقوّمُه الحارس، لا المكوِّنُ مرسومًا. **العلّة:** §١-٣ أوقفَتْني قبلَ أن أكتبَ حرفًا، ورسمُ ما لم يُكتَبْ لا معنى له.
2. **🔴 ولم أرَ أثرَ الوسمِ الأطولِ على تخطيطِ الشارة.** `رَوَى البخاري` أطولُ من `من السنة النبوية`؟ **لم أقِسْه**، ولا قِستُ ما يفعلُه `s.hadithCardLabel` بوسمٍ أطولَ على شاشةِ ٣٢٠ بكسل. **وهذا سؤالٌ حقيقيٌّ يستحقُّ القياسَ قبلَ الإذن.**
3. **لم أقِسْ راويًا مركَّبًا** («متفق عليه» · «الشيخان» · «رواه البخاري ومسلم»). `resolveHadithAttribution` تنزعُ فعلَ الروايةِ وتكشفُ ألفاظَ الدرجةِ، **وحالاتي الأربعُ لا تلمسُ ذلك المسار**. فالرقعةُ ستُظهِرُ ما تُخرِجُه تلك الدالّةُ أيًّا كان، ولم أعدَّ ما تُخرِجُه في الحالاتِ المركَّبة.
4. **لم أقِسْ فرعَ القرآنِ مع الرقعة.** `if (quranic) label = AYAH_CARD_LABEL;` يأتي **بعدَ** المُهيِّئِ فيدهسُه، فالفرعُ يبقى كما هو **بالبناءِ لا بالتشغيل** — لم أرسمْ بطاقةَ آيةٍ لأتحقّق.
5. **لم أفحصْ إن كانَ حارسٌ ثالثٌ يعرفُ الوسمَ بطريقٍ غيرِ مباشر.** بحثتُ عن اللفظِ نفسِه وعن `HadithCard` و`NEUTRAL_HADITH_LABEL` و`hadithCardLabel` في كلِّ `.cjs` خارجَ `node_modules`. **حارسٌ يصلُ إليه بمسمًّى آخرَ لن يظهرَ في بحثي.**
6. **لم أشغّلْ أيَّ بوّابةٍ ثلاثَ مرّاتٍ لفرزِ الرفرفة.** الطقمُ مرّةً واحدةً، والحمرةُ الوحيدةُ موروثةٌ ومفهومة.
7. **خمسةُ ملفّاتٍ غيرِ متعقَّبةٍ نُقِلَتْ من جذرِ الشجرةِ قبلَ الطقم** — أمرُ هذه الجولةِ وتقاريرُ أربعِ جولاتٍ سابقة — لأنّ `run-gates` يخرجُ بـ`1` على أيِّ اتّساخ. **مذكورٌ لأنّه تغييرٌ في حالةِ الشجرة**، وهو الوحيدُ في هذه الجولة.

---

## ٦ · ما يحتاجُه هذا البندُ ليُغلَق

**أمرٌ واحدٌ يأذنُ صراحةً بثلاثةِ أشياءَ بأسمائِها:**

1. **`quest-ux-guard.cjs:170-171`** — يُقلَبُ التوقُّعُ إلى `رَوَى البخاري`. (وهو `eq` واحدٌ، والتوقُّعُ يُنتزَعُ من المُسَلسِلِ لا يُكتَبُ.)
2. **`theme-coverage-guard.cjs:1187-1189`** — يُوسَّعُ النمطُ ليقبلَ فرعَ الراوي، **مع إبقاءِ ما يحرسُه فعلًا**: أن يبقى المُهيِّئُ قابلًا للتقويمِ على `att` وحدَه، وأن تبقى الشارةُ `<span>{label}</span>`.
3. **`quest-ux-guard.cjs:174-176`** — عنوانُه ونصُّه يُصحَّحان، فلا يبقى في الشجرةِ تأكيدٌ **يقولُ خلافَ ما يفعلُه الكود** (§٣-٣).

**وأنا لا أفعلُ شيئًا من ذلك بلا إذنٍ صريح** — §٠ تُدرِجُ تعديلَ الحارسِ في المحرَّم، و§١-٣ تأمرُ بالوقوفِ والكتابة. **وهذا هو الوقوفُ والكتابة.**

---

## ٧ · الحال

```
origin/main                         56952b263f19e6b38bcc9966e6501b3c78c5a82f
fix/card-attribution-20260824 HEAD  04911d4dbde01d4fb202d99cd591cee284fce04d   (لم يتحرّكْ)
إيداعاتُ هذه الجولة                  صفر
ملفّاتٌ مُسَّتْ                        صفر
حارسٌ عُدِّل                          صفر
git status --porcelain              صفرُ سطرٍ قبلَ كتابةِ هذا التقرير
دفع · نشر                           صفر
```

**تُنتظَرُ كلمةُ المالكِ في §٦.**

---

REPORT_PAYLOAD_BYTES=21419
REPORT_SHA256=2c47b84a2a448870acba5e94bcc494cf29fa7cd9db43193f20572539ae9563f1
REPORT_SHA8=2c47b84a

(الحمولةُ المختومةُ: كلُّ بايتةٍ حتّى سطرِ الفصلِ الأخيرِ وسطرِه الجديدِ بعدَه، قبلَ إلحاقِ هذه الأسطر. أُعيدُ قطعَها بـ `head -c 21419 <file> | sha256sum`.)
