# تقريرٌ — عزك · عطبُ منسدلةِ المحتوى اليوميّ (٤٧-أ · الشكلُ لا المنطق)

**النافذة:** ن١ · **الوكيل:** Claude Code · **مجلَّدُ العمل:** `C:\Users\passe\projects\ustaz-cc-ui`
**الفرع:** `fix/daily-dropdown-20260822` · **الأساس:** `4c9531cb4687ed9f576fe047aa21b130f692b3ff`
**التاريخ:** ٢٢ أغسطس ٢٠٢٦ · **الدفع:** لم يقعْ، ولا مرّةً واحدة.

> **الحال: وقوفٌ.** `STOP_REASON=RED_GATE_AFTER_COMMIT + CURE_NEEDS_FILE_OUTSIDE_APP_JSX`
> العلاجُ نفسُه وقعَ كلُّه في `app.jsx` ونجحَ مقيسًا؛ والذي أوقفَ الجولةَ هو **مسكُ الدفاتر**
> بعدَه: البناءُ زادَ `app.js` سبعةَ عشرَ بايتًا، وعددُ بايتاتِه مثبَّتٌ **في `sw.js`** في موضعَين،
> و`sw.js` محظورٌ في §٢ حظرًا مطلقًا. لا حكمَ هنا؛ الحكمُ لصاحبِ الأمر.

---

## ٠ · نقلُ الأمرِ وتنظيفُ الجذر

| المفتاح | القيمة |
|---|---|
| `ORDER_MOVED` | `True` — إلى `C:\EZIK-STAGE\orders\ORDER-FIX-DROPDOWN.md` |

وثلاثةُ ملفّاتٍ أخرى كانت غيرَ متعقَّبةٍ في الجذرِ من جولاتٍ سابقةٍ **نُقِلَتْ ولم تُحذَفْ**، لأنّ
`DIRTY_LINES = 0` شرطٌ في §١ ولأنّ الملفَّ الغريبَ في الجذرِ يُسقِطُ `recon`:

| الملفّ | إلى |
|---|---|
| `ORDER-ROUND25-A.md` | `C:\EZIK-STAGE\orders\` |
| `BABEL-32-REPORT.md` | `C:\EZIK-STAGE\parked\` |
| `EZIK-CC-A-ROUND25-REPORT-2026-08-22.md` | `C:\EZIK-STAGE\parked\` |

---

## ١ · بوّاباتُ ما قبلَ العمل — مقيسةٌ على الشجرةِ نفسِها

| المفتاح | المقيس | الشرط | الحال |
|---|---|---|---|
| `HEAD` | `4c9531cb4687ed9f576fe047aa21b130f692b3ff` | مطابقةٌ للأساس | ✅ |
| `DIRTY_LINES` | `0` | `= 0` | ✅ |
| `APPJS_BYTES` | `947721` | `= 947721` | ✅ |
| `APPJS_SHA256` | `0cf2feb4ba7b17ffd4abcf0c6e751e4da3343f00a4ffbbcbbc72dea52d7edca4` | مطابقة | ✅ |
| `SW_BYTES` | `42994` | للشهادةِ فقط | ✅ |
| `APPJSX_EXISTS` | `True` | `= True` | ✅ |

وزيادةً: `645a3e0` — البندُ صاحبُ العطب — **داخلٌ في الأساس** (`ANCESTOR=True`)، فالعطبُ حاضرٌ
في الشجرةِ التي عملتُ عليها لا في فرعٍ آخر.

---

## ٢ · الطورُ ١ — القياسُ قبلَ أيِّ بايتة

### ٢/١ · مواضعُ المنسدلاتِ الثلاث

| الشاشة | السطرُ في `app.jsx` | المكوّنُ الحاوي |
|---|---|---|
| المصحف | `14761` | `PagedMushaf` (لوحُ «وردُ اليوم») |
| الأذكار | `4830` | `IstanaAdhkarBrowse` |
| المحفّظ | `15299` | `MemorizeScreen` (شاشةُ الاختيار) |

### ٢/٢ · الخصائصُ المقيسةُ على العنصرِ نفسِه — متطابقةٌ في الثلاث

| الخاصّيّة | المصحف | الأذكار | المحفّظ |
|---|---|---|---|
| `size` (سمةً) | غائبة | غائبة | غائبة |
| `size` (خاصّيّةً) | `0` | `0` | `0` |
| `multiple` | `false` | `false` | `false` |
| `className` | *فارغة* | *فارغة* | *فارغة* |
| الأسلوبُ الضمنيُّ | `s.memAyahSelect` | `s.memAyahSelect` | `s.memAyahSelect` |
| المحسوبُ منه | `flex: 1 1 0%` · `height: 40px` | نفسُه | نفسُه |
| عددُ الخياراتِ المرسومة | `120` | `133` | `115` |

`s.memAyahSelect` (سطر `16323`):
`{ flex: 1, height: 40, borderRadius: 12, border: '2px solid var(--line)', … }`

### ٢/٣ · الخصائصُ المقيسةُ على الحاويةِ الأمِّ المباشرة — **هنا يقعُ الفرق**

| الحاوية | المصحف | الأذكار | المحفّظ |
|---|---|---|---|
| العنصر | `div` (`s.wirdSheet`) | `div.theme-dark.adhkar3` (`s.eziaContainer`) | `div.ezsh-wrap` |
| `display` | `flex` | `flex` | **`block`** |
| `flex-direction` | `column` | `column` | — |
| `height` المصرَّح | *غيرُ مصرَّح* (تلقائيّ) | **`100dvh`** | *غيرُ مصرَّح* |
| `height` المقيس | `379px` | **`932px`** | `6921.94px` (طولُ المحتوى) |

### ٢/٤ · جدولُ الفرق، بنصِّه

> **حاضرٌ في الأذكارِ وغائبٌ عن أختَيها:** حاويةٌ `flex` باتّجاهِ `column` **وارتفاعٌ قاطعٌ**
> (`height: 100dvh` ⟸ `932px`). هذا وحدَه ما تفرَّدَتْ به.
>
> **حاضرٌ في المحفّظِ وغائبٌ عن الأذكار:** حاويةٌ `block`، فيها `flex` معطَّلٌ أصلًا و`height: 40`
> هو الحاكم.
>
> **حاضرٌ في المصحفِ وغائبٌ عن الأذكار:** حاويةُ `column` **بلا ارتفاعٍ قاطع**، فلا فراغَ يُوزَّعُ
> ويسقطُ العنصرُ إلى ارتفاعِ محتواه.
>
> **وصفرُ فرقٍ على العنصرِ نفسِه:** لا `size`، ولا `multiple`، ولا صنف، ولا أسلوبٌ ثانٍ.

### ٢/٥ · التصنيف

**`CAUSE=(ب)`** — صنفُ/خاصّيّةُ ارتفاعٍ في الحاويةِ تمتصُّ الارتفاع.

بالتفصيل: `flex: 1` اختصارٌ يحملُ `flex-basis: 0%`. كُتِبَ هذا المفتاحُ لشريطِ البدءِ في المحفّظ
(`s.ezqStartBar`) وهو حاويةُ `flex` باتّجاهِ **صفٍّ**، فيملأ العرض. وحينَ وُضِعَ العنصرُ نفسُه في
حاويةِ **عمودٍ ذاتِ ارتفاعٍ قاطع**، صارَ المحورُ الرئيسُ **رأسيًّا**: العنصرُ ينمو، و`height: 40`
المكتوبُ بجانبِه لا يُستعمَلُ ألبتّة. فليسَ العطبُ في `size` ولا في `multiple` ولا في ارتفاعٍ
مثبَّت — بل في أنّ **الاختصارَ ذاتَه انقلبَ معناه بانقلابِ محورِ الحاوية**.

---

## ٣ · الطورُ ٢ — الإصلاح: خاصّيّةٌ واحدةٌ في موضعٍ واحد

| البند | القيمة |
|---|---|
| الملفّاتُ المحرَّرةُ بيدٍ | `app.jsx` **وحدَه** |
| المواضع | سطرٌ واحد (`4831`) + تعليقٌ يشرحُ المقيس |
| التغيير | `style={s.memAyahSelect}` ⟸ `style={{ ...s.memAyahSelect, flex: 'none' }}` |
| `s.memAyahSelect` | **لم يُمَسّ** — شريطُ بدءِ المحفّظِ ما زالَ يملأُ صفَّه |
| المصحفُ والمحفّظُ | **لم يُمَسّا** في المصدرِ ألبتّة |

`flex: 'none'` هو `0 0 auto`: لا نموَّ ولا انكماش، وحجمُ الأساسِ يعودُ إلى `height` المصرَّحِ
أصلًا في المفتاحِ نفسِه. صفرُ تغييرٍ في منطقٍ، أو في قيمةٍ محفوظة، أو في ترتيبِ خيارات، أو في
مفتاحِ تخزين، أو في لفظٍ معروض.

---

## ٤ · الطورُ ٣ — القياسُ بعد

الآلة: كروم رأسيٌّ حقيقيٌّ عبرَ CDP، بسباكةِ `tools/stream-p1/ask-pin-and-enter-measure.cjs`
نفسِها (خادمٌ محلّيٌّ على المستودع، إعادةُ تشغيلٍ للاعتماديّات، بذرُ الموافقةِ والملفِّ الشخصيّ).
لم توجدْ في المستودعِ آلةٌ تقيسُ ارتفاعَ منسدلةٍ على هذه الشاشاتِ الثلاث، فبُنِيَ مسبارٌ مؤقّتٌ
**خارجَ المستودعِ** في `C:\EZIK-STAGE\probe\dropdown-measure.cjs`، وقِيسَ به الطرفانِ، **ثمّ حُذِفَ**
(`PROBE_DELETED=True`) ولم يُودَعْ في المستودع.

`Emulation.setDeviceMetricsOverride` — والمقيسُ داخلَ الصفحةِ `viewport = 430x932` في الجريانَين.

### ٤/١ · الارتفاعُ بالبكسل على ٤٣٠×٩٣٢

| الشاشة | قبل | بعد | الفرق | الشرط | الحال |
|---|---|---|---|---|---|
| **الأذكار** | **`439.5px`** | **`40px`** | **−399.5** | تعودُ إلى سطرٍ واحد | ✅ |
| المصحف | `32px` | `32px` | `0` | لا يزدادُ ولا ينكسر | ✅ |
| المحفّظ | `40px` | `40px` | `0` | لا يزدادُ ولا ينكسر | ✅ |

وشاهدُ العلّةِ في المحسوبِ نفسِه: `flex-grow` في الأذكارِ `1 ⟸ 0` و`flex-basis` `0% ⟸ auto`،
بينما بقيَ الاثنانِ في المصحفِ والمحفّظِ على `1` و`0%` كما كانا — أي **لم يُمَسّا فعلًا**، لا قولًا.

### ٤/٢ · الشكلُ لم ينكسر

| المقيس | قبل | بعد |
|---|---|---|
| عددُ خياراتِ المصحف | `120` | `120` |
| عددُ خياراتِ الأذكار | `133` | `133` |
| عددُ خياراتِ المحفّظ | `115` | `115` |
| قيمةُ منسدلةِ المصحفِ المعروضة | `surah:36` | `surah:36` |
| قيمةُ منسدلةِ المحفّظِ المعروضة | `78` | `78` |

### ٤/٣ · الاختيارُ المحفوظ

بُذِرَ السجلُّ قبلَ إقلاعِ التطبيقِ ولم يُكتَبْ بعدَها، وقُرِئَ من `localStorage` في الجريانَين:

```
ezik_daily_wird_v1 = {"mushaf":{"mode":"surah","surah":36},"adhkar":{"cat":"morning","title":"x"},"memorize":{"surah":78}}
```

`SAVED_CHOICE_MATCH=True` — **متطابقٌ حرفًا** قبلَ التعديلِ وبعدَه، وقرأتْه الشاشتانِ اللتان
تحملانِ قيمةً حقيقيّةً وعرضتاها كما هي.

### ٤/٤ · أعدادُ الحراسِ الثلاثةِ — ستّةُ أرقام

| الحارس | قبل | بعد | الحال |
|---|---|---|---|
| `themecoverage` (`theme-coverage-guard.cjs`) | `1341/1341` | `1341/1341` | ✅ لم ينخفض |
| `wird` (`tools/wird-guard.cjs`) | `1122` ناجحة · `0` ساقطة | `1122` ناجحة · `0` ساقطة | ✅ لم ينخفض |
| `i18nui` (`guards/i18n-ui-guard.cjs`) | `277/277` | `277/277` | ✅ لم ينخفض |

«قبل» قِيسَ على الشجرةِ نظيفةً عندَ الأساسِ نفسِه، لا نُقِلَ من وثيقة.

---

## ٥ · الطورُ ٤ — السلسلةُ بترتيبِها

| الخطوة | النتيجة |
|---|---|
| `node tools/build-app.cjs` | `app.js` — `947738` بايتًا · `sha256 8b2a8e3c8f8206824ef5adb00d29ee48dccfd026f57a0ecc1170d67e5aa9a53e` |
| `node tools/build-app.cjs --check` | `OK: app.js is exactly what this source builds` — **المبنيُّ = ما على القرصِ حرفًا** |
| `node C:\Users\passe\tdz-scan.cjs` | `TDZ_COUNT=0` (`PARSE_OK=1` · `SCOPES=1338`) |
| الإيداعُ محلّيًّا، بأسماءِ الملفّاتِ صراحةً | `git add app.jsx app.js` ⟸ `78d8a01` |
| `git status --porcelain` بعدَ الإيداع | `0` سطرًا |
| `node tools/run-gates.cjs` على شجرةٍ **نظيفةٍ تمامًا** | **`89/90`** · `recon: PASS=182 WARN=1 FAIL=0` · `tree after: 0 dirty` |
| البوّابةُ الساقطة | **`bankintegrity`** |
| رفعةُ مخزنِ الشحنة | **لم تقعْ** — `ezik-v16` كما هي |
| الدفع | **لم يقعْ** |

### ٥/١ · البوّابةُ الساقطةُ بنصِّ سطرَيها

```
FAIL [B12] CORE_BYTES = 1781119 but CORE weighs 1781136 bytes on disk (+17).
FAIL [B14] sw.js prose says app.js is 947721 bytes; the disk says 947738 (+17).
```

`+17` في الموضعَينِ هي بعينِها زيادةُ `app.js` (`947721 ⟸ 947738`)، وهي حصيلةُ البناءِ من
`app.jsx` المصحَّح. والعددانِ المثبَّتانِ يسكنانِ **`sw.js` وحدَه**:

| الموضع | السطر | النصّ |
|---|---|---|
| `sw.js:96` | نثرٌ داخلَ تعليقِ CORE | `/  (index.html) 120617 + app.js 947721 + icon-watermark.png 368386` |
| `sw.js:112` | ثابتٌ مصرَّح | `const CORE_BYTES = 1781119;` |

**و§٢ تقولُ: «صفرُ `sw.js`».** فالعلاجُ التامُّ يستلزمُ ملفًّا خارجَ `app.jsx`، وهو الملفُّ
المحظورُ بعينِه.

### ٥/٢ · ما لم يُفعَلْ، عمدًا

- **لم يُحرَّرْ `sw.js`** — لا نثرًا ولا ثابتًا. §٢.
- **لم تُخفَضْ أرضيّةُ حارسٍ ولا عددُ تأكيدٍ** لتمرَّ الدفعة. §٢.
- **لم يُدفَعْ شيء**، ولم تُرفَعْ نسخةُ مخزنِ الشحنة. §٠/§٣.
- **لم يُتراجَعْ عن الإيداع**: `78d8a01` باقٍ على فرعِه ليقرأَه صاحبُ الأمرِ ويحكم؛ والفرعُ
  **أحمرُ**، فلا يُدمَجُ ولا يُدفَعُ حتّى يُقرَّرَ في `sw.js`.

---

## ٦ · ما لم يُقَسْ، بعلّتِه

1. **الفرقُ بينَ ٣٢ و٤٠ بكسلًا في منسدلةِ المصحف.** قِيسَ ولم يُصلَحْ. سببُه من جنسِ سببِ
   الأذكارِ نفسِه (`flex-basis: 0%` في حاويةِ عمود)، غيرَ أنّ حاويتَه بلا ارتفاعٍ قاطعٍ فسقطَ
   العنصرُ إلى ارتفاعِ محتواه بدلَ أن ينمو. ولمّا كانَ شرطُ القبولِ في §٣ صريحًا: «المصحفُ
   والمحفّظُ **لا يزدادُ ارتفاعُهما**»، وكانَ إصلاحُه زيادةً مقدارُها ثمانيةُ بكسلات، تُرِكَ كما
   هو. **قرارٌ مقصود، لا سهو.**
2. **الخطُّ في القياس.** حُجِبَتْ خطوطُ Google في المسبارِ (لا شبكةَ مضمونةٌ في جريانٍ غيرِ
   تفاعليّ)، فرُسِمَ النصُّ بخطٍّ بديل. لا يمسُّ هذا الحكم: الارتفاعُ المقيسُ محكومٌ بـ`height: 40`
   أو بنموِّ `flex`، لا بمقاييسِ الخطّ، والفرقُ المقيسُ كانَ ٣٩٩٫٥ بكسلًا لا بضعةَ أعشار.
3. **`WARN=1` في `recon`.** حاضرةٌ في الجريانِ نفسِه ولم تُفحَصْ: البوّابةُ تُبلِغُ `FAIL=0`،
   والتحذيرُ خارجَ نطاقِ هذا الأمر.
4. **الشاشاتُ في وضعِ LTR أو بعرضٍ آخرَ غيرِ ٤٣٠.** §٣ عيَّنَ `430×932` وحدَه، فلم يُقَسْ سواه.
5. **صنفُ الأذكارِ الحقيقيُّ في المنسدلة.** البذرةُ حملَتْ `cat: "morning"` وهو ليس معرِّفًا في
   متجرِ الأذكار، فعُرِضَ «بلا اختيار» في الطرفَين. لا يمسُّ هذا القياسَ — الارتفاعُ قِيسَ
   والخياراتُ ١٣٣ في الطرفَين — لكنّ «عرضُ فئةٍ محفوظةٍ حقيقيّةٍ» لم يُقَسْ.
6. **لماذا وقعَ الخطأُ أصلًا:** لم يُقَسْ أنّ ثلاثةَ مواضعَ تشاركُ مفتاحَ أسلوبٍ كُتِبَ لحاويةِ
   صفٍّ واحدةٍ. لم يُبْنَ حارسٌ يمنعُ تكرارَه — ذلك تغييرٌ خارجَ نطاقِ هذا الأمر.

---

## ٧ · الخلاصةُ بالمفاتيح

```
ORDER_MOVED=True
GATE_HEAD=PASS  GATE_DIRTY=PASS  GATE_APPJS_BYTES=PASS  GATE_APPJS_SHA256=PASS  GATE_APPJSX=PASS
CAUSE=(b)  container flex-column with a definite height absorbs `flex: 1 1 0%`
FILES_EDITED_BY_HAND=app.jsx
ADHKAR_BEFORE=439.5px  ADHKAR_AFTER=40px
MUSHAF_BEFORE=32px     MUSHAF_AFTER=32px
MEMORIZE_BEFORE=40px   MEMORIZE_AFTER=40px
SAVED_CHOICE_MATCH=True
THEMECOVERAGE=1341/1341 -> 1341/1341
WIRD=1122/0 -> 1122/0
I18NUI=277/277 -> 277/277
BUILD_CHECK=OK  APPJS_BYTES_AFTER=947738
TDZ_COUNT=0
GATES=89/90  RECON_FAIL=0  FAILING=bankintegrity
CACHE_STORE_BUMPED=False
PUSHED=False
PROBE_DELETED=True
STOP_REASON=RED_GATE_AFTER_COMMIT + CURE_NEEDS_FILE_OUTSIDE_APP_JSX (sw.js:96, sw.js:112)
FINAL_HEAD=78d8a01
```

`FINAL_HEAD` أعلاه هو رأسُ إيداعِ الشِّفرة. هذا التقريرُ يُودَعُ فوقَه ولا يحملُ شِفرةً؛ وحصيلةُ
البوّاباتِ المذكورةُ قِيسَتْ على الشجرةِ نظيفةً عندَ `78d8a01` بالضبط.

---
PAYLOAD_BYTES=16438 SHA256=3448a007fff0c19f57316387e949406f18374da71a35a4649f9d3d7bd34461b9
