# تقريرُ ٢٤-ج — وسيطُ تصفّحِ الدروس

STATUS=COMMITTED_RED
REPORT_DATE=2026-08-23
ORDER=C:\EZIK-ORDERS\ORDER-24C-B-PROXY.md
BRANCH=feat/lessons-browse-proxy-24c
BASE=a332cb3bfc43067f88f4878654a9a3846d07cb14

## ٠ · بوّابةُ الدخول

قِيسَت البوّابةُ قبلَ أيِّ كتابة:

```text
git rev-parse origin/main
a332cb3bfc43067f88f4878654a9a3846d07cb14

git status --porcelain
<صفرُ سطر>

git checkout -B feat/lessons-browse-proxy-24c a332cb3
Switched to a new branch 'feat/lessons-browse-proxy-24c'
```

إذًا لم يتحرّك الأساس، وقُطِع الفرعُ المطلوبُ من `a332cb3` قبلَ أيِّ فعلٍ في
ملفّاتِ العمل. طبعَ Git تحذيرَ صلاحيةٍ عن ملفِّ التجاهلِ العامّ
`C:\Users\passe\.config\git\ignore`؛ لم يكن سطرَ حالةٍ ولم يغيّر نتيجةَ أنَّ
الشجرةَ كانت نظيفة.

## ١ · الجردُ المقيسُ قبلَ أيِّ رقعةٍ تنفيذيّة

### ١-١ قالبُ `api/lessons-search.js`

قُرئ الملفُ كاملًا من الأساسِ المثبّت. الحقائقُ الحرفيّةُ فيه:

- رابطُ الخدمةِ في ثابتٍ محلّيٍّ اسمُه `SEARCH_URL` وقيمتُه
  `https://lib.ezik.app/lessons/search`. **ليس للرابطِ متغيّرُ بيئةٍ في الملف.**
- الرمزُ يُقرأ من `process.env.SEARCH_API_TOKEN`. وهذه هي مجموعةُ أسماءِ
  `process.env` في الملف كلِّه: `{SEARCH_API_TOKEN}`.
- لذلك معنى «صفرُ متغيّرِ بيئةٍ جديد» المقيسُ هنا هو أن يقرأَ الوسيطُ الجديدُ
  `SEARCH_API_TOKEN` نفسَه وألّا يضيفَ أيَّ اسمٍ آخرَ تحتَ `process.env`؛ أمّا
  عنوانُ `/browse` فيبقى ثابتًا محلّيًّا على نسقِ `SEARCH_URL`.
- `TIMEOUT_MS=12000`.
- `MAX_Q_CHARS=400`.
- `LIMIT_MAX=10`.
- البنيةُ المرجعيّةُ تقرأ الجسمَ النصّيَّ أو الكائن، ترفض غيرَ `POST` بـ`405`،
  ترفض المدخلَ الفاسدَ بـ`400`، ترفض غيابَ الرمزِ بـ`503`، وتحوّل فشلَ الاتصالِ
  أو حالةَ المنبعِ أو جسمَه غيرَ المقروءِ إلى `502`. النداءُ محكومٌ بـ
  `AbortSignal.timeout(TIMEOUT_MS)`.
- `shapeSearchResponse` دالّةٌ صافيةٌ مُصدَّرةٌ كي يختبرَ الحارسُ التشكيلَ بلا
  شبكة. وهي لا تمرّرُ غلافَ المنبعِ أو صفوفَه مرورًا أعمى.

### ١-٢ شكلُ `gates.json` وتسجيلُ الحارس

قُرئ `gates.json` نفسُه وحُلِّل، لا أسماءُ مفاتيحَ مفترضة:

- الجذرُ **مصفوفة JSON** طولُها الآن `92`.
- سجلُّ الحارسِ العاديِّ كائنٌ بالمفاتيحِ `name` و`script` و`args`؛
  `args` سلسلةٌ، وتوجدُ في الروستر حالةٌ ذاتُ مفتاحٍ اختياريٍّ `companions`.
- آخرُ ثلاثةِ سجلاتٍ المقيسة: `bootinvariants` ثم `lessonssearch` ثم
  `fiqhindex`.
- `tools/run-gates.cjs` يقرأ المصفوفةَ من `gates.json`، ويشغّل كلَّ سجلٍ
  بالتتابع هكذا: `node <script> ...<args>` من جذرِ المستودع، ثم يطبع
  `<passed>/<total> EXIT=0` ويُخرج الدليلَ خارجَ شجرةِ العمل.
- تسجيلُ هذا العملِ سيكونُ سجلًّا واحدًا بالنمطِ الفعليِّ نفسِه:

  ```json
  {
    "name": "lessonsbrowse",
    "script": "guards/lessons-browse-guard.cjs",
    "args": ""
  }
  ```

  وبذلك يكونُ تغيّرُ الروسترِ المقصودُ `92 -> 93`، بسطرِ التسجيلِ وحدَه في
  `gates.json`.

### ١-٣ نسقُ القائمةِ البيضاءِ في `lib/lessons-source-card.js`

قُرئ الملفُ كاملًا. لا ينسخُ الكائنَ ولا يشتقُ مفاتيحَه. المساعدُ `carry`
يحملُ اسمًا واحدًا فقط إذا كان موجودًا، ثم يستدعيه `buildLessonCard` بالأسماءِ
المكتوبةِ صراحةً: `title` ثم `scholar_id` ثم `url`، ويضيفُ `content_type`
بالاسمِ بعدَ فحصِه. الحقلُ الغائبُ يبقى غائبًا، والحقلُ غيرُ المقيسِ لا يُختَرعُ
ولا يعبر. وسيبني وسيطُ التصفّحِ قوائمَه البيضاءَ بالطريقةِ ذاتِها: أسماءٌ
صريحةٌ لكلِّ مستوى، بلا `Object.keys` أو `Object.entries` أو `Object.values`
أو نشرِ كائنٍ.

## ٢ · التنفيذ

أُضيف `api/lessons-browse.js` على العقدِ المجمّد:

- `POST /api/lessons-browse` يرسل نداءً واحدًا بـ`POST` إلى
  `https://lib.ezik.app/lessons/browse`، ويحمل الرمزَ نفسَه من
  `SEARCH_API_TOKEN` في `authorization: Bearer <token>`.
- المستوياتُ المقبولةُ محصورةٌ في `scholars` و`series` و`lessons`. غيرُها
  `400` قبلَ أيِّ نداء.
- `scholar_id` مطلوبٌ في `series` و`lessons`، ووجودُ `series` مطلوبٌ في
  `lessons`، لكنَّ قيمتَها `""` مقبولةٌ وتُمرَّرُ كما هي.
- `normalizePage` لا يُبقي إلّا عددًا صحيحًا موجبًا؛ وما سواه يصبح `1`.
- الطلباتُ الخارجةُ تُبنى بأسماءِ الحقولِ وحدَها: `level`؛ أو `level ·
  scholar_id · page`؛ أو `level · scholar_id · series · page`.
- الردودُ تُبنى بدوالٍ منفصلةٍ لكلِّ مستوى. صفُّ العلماء يحملُ
  `scholar_id · count`، وصفُّ السلاسلِ `series · count`، وصفُّ الدروسِ
  `unit_id · title · url`. والغلافُ إمّا `rows · total` أو `rows · total ·
  page · pages`.
- لا يوجد نسخٌ أعمى ولا `Object.keys`/`entries`/`values` ولا نشرٌ في منطقةِ
  التشكيل. كلُّ حقلٍ محمولٌ باسمِه.
- `405` و`400` و`503` و`502` لها أجسامٌ ثابتة. مهلةُ المنبعِ `12000ms`،
  والردُّ الناجحُ يحمل `Cache-Control: private, no-store`.
- مساراتُ السجلِ الستةُ لا تربطُ استثناءً في `catch` ولا تقرأُ رسالةَ
  استثناء. سجّلت أكوادًا ثابتة. كشفَت بوّابةُ `telemetrytext` لاحقًا أنَّ
  اسمَ الحقلِ المطبوعَ `code` غيرُ موجودٍ في قائمتِها القديمة؛ لم أرقّعْه
  بعدَ تفعيلِ شرطِ التوقّف، والتفصيلُ في §٥.

## ٣ · الحارسُ والفيكستشر

أُضيف `guards/fixtures-lessons-browse.json` بثلاثةِ ردودٍ اصطناعيّةٍ محليّة.
لا يحتوي الملفُ حرفًا عربيًّا، ويزرعُ العلامةَ
`LESSON-TEXT-FIXTURE-MUST-NOT-PASS` في `snippet` و`quote_text` و`body` وفي
حقولٍ مجهولة، كي يكونَ أيُّ تسرّبٍ ظاهرًا.

أُضيف `guards/lessons-browse-guard.cjs`. استوردَ الدالّةَ الحقيقيّة، واستبدل
`globalThis.fetch` بوصلةٍ محليّةٍ في كلِّ نداء، فلم يخرجْ إلى الشبكة. نتيجتُه
المحفوظة:

```text
$ node guards/lessons-browse-guard.cjs
exit: 0
MUTANTS_KILLED=2/2
ASSERTIONS=81/81
=== PASS ===
```

التأكيداتُ الـ`81` تثبتُ نصَّ المصدرِ وسلوكَه: المستوياتَ الثلاثةَ وحدَها؛
مواضعَ `405/400/503/502`؛ مساواةَ مجموعاتِ القوائمِ البيضاءِ في المستوياتِ
الثلاثة؛ غيابَ حقولِ المتنِ والحقولِ المجهولة؛ غيابَ النسخِ والنشرِ؛ تساوي
مجموعةِ أسماءِ البيئةِ مع `lessons-search.js`؛ العنوانَ والرمزَ والمهلة؛
طلباتِ السلكِ؛ تطبيعَ الصفحة؛ قبولَ السلسلةِ الفارغة؛ الأجسامَ الثابتة؛ وعدمَ
تسرّبِ الرمزِ أو نصِّ الاستثناء.

المسخانِ المحمّلانِ من الذاكرةِ، بلا كتابةٍ على القرص:

1. إضافةُ `snippet` إلى تشكيلِ صفِّ العلماء؛ حملَ العلامةَ الممنوعةَ فقتلَه
   فحصُ مساواةِ المجموعةِ وفحصُ التسرّب.
2. استبدالُ حصرِ `LEVELS` بفحصِ أنَّ المستوى نصٌّ؛ قبِلَ مستوىً رابعًا
   `archive` بنداءٍ خارجيٍّ، فقتلَه فحصُ `400`/صفرِ نداء.

## ٤ · الروسترُ والحدود

كان `gates.json` مصفوفةً من `92` سجلًّا، وصار مصفوفةً من `93` بإضافةِ سجلٍ
واحد:

```json
{"name":"lessonsbrowse","script":"guards/lessons-browse-guard.cjs","args":""}
```

أي إنَّ الروسترَ تحرّك حرفيًّا `92 -> 93`. قبلَ إيداعِ التنفيذ كان
`git status --porcelain` أربعةَ أسطرٍ وحدَها:

```text
 M gates.json
?? api/lessons-browse.js
?? guards/fixtures-lessons-browse.json
?? guards/lessons-browse-guard.cjs
```

والفرقُ من `a332cb3` إلى إيداعِ التنفيذ، عدا التقريرِ المطلوب، هو هذه
الملفّاتُ الأربعةُ وحدَها. أعاد الأمرُ الآتي صفرَ سطر:

```text
git diff --name-only a332cb3 HEAD -- app.jsx app.js index.html api/lessons-search.js sw.js lib
<صفرُ سطر>
```

لم أُشغّل بناءَ التطبيق، ولم ألمس `app.jsx` أو `app.js` أو `index.html` أو
أيَّ مسارٍ ممنوع.

## ٥ · البوّاباتُ الثلاثُ حرفيًّا

### ٥-١ `node tools/run-gates.cjs` — أحمرُ، وتفعّلَ شرطُ التوقّف

شُغّل بعدَ إيداعِ التنفيذ على `ed12562ae2e55abfc1d3e5326a8619c16782748b`
وشجرةٍ نظيفة:

```text
=== gate suite — 93 gates from gates.json ===
HEAD:     ed12562ae2e55abfc1d3e5326a8619c16782748b
tree:     0 dirty path(s)
evidence: C:\Users\passe\AppData\Local\Temp\ezik-gates\runs\2026-08-23T11-24-51-682Z-12244

recon                EXIT=1  (714ms)
bankintegrity        EXIT=1  (224ms)
questux              EXIT=1  (1605ms)
takhrij              EXIT=1  (129303ms)
guardhonesty         EXIT=1  (1579ms)
telemetrytext        EXIT=1  (211ms)
lessonsbrowse        EXIT=0  (54ms)

=== SUITE: 87/93 EXIT=0 ===
recon:    SUMMARY   PASS=182   WARN=1   FAIL=3
tree after: 0 dirty path(s)
FAILING (6): recon=1, bankintegrity=1, questux=1, takhrij=1, guardhonesty=1, telemetrytext=1
```

إذًا لم تتحقق `93/93 EXIT=0`. والحارسُ الجديدُ نفسُه أخضرُ `81/81`، لكنَّ
`bankintegrity` أحمرُ؛ وهذا هو شرطُ التوقّفِ الصريحُ في الأمر. خرجَ سجلُّه:

```text
FAIL [B12] CORE_BYTES = 1802087 but CORE weighs 1802385 bytes on disk (+298).
FAIL [B14] sw.js prose says vendor/react-dom.umd.js is 131835 bytes; the disk says 132102 (+267).
FAIL [B14] sw.js prose says vendor/react.umd.js is 10751 bytes; the disk says 10782 (+31).
FAIL  74 checks passed, 3 failed.
```

وقِيسَت العلّةُ قراءةً فقط: كائنا Git للملفّين `131835` و`10751` بايتًا،
أمّا نسختا العمل فـ`132102` و`10782`؛ مجموعُ فرقِ نهايةِ السطر `298` بايتًا.
لا يظهر أيٌّ منهما في فرقِ هذا الفرع، وإصلاحُ `sw.js` أو ملفّاتِ المورد خارجُ
§٤. لذلك لم أصلحْه ولم أُعِدْ الطقم.

والحمراتُ الأخرى محفوظةٌ بلا تجميل:

- `recon`: `PASS=182 WARN=1 FAIL=3` — توقّع `92` لا `93`، وطلبَ رصدَ
  `guards/lessons-browse-guard.cjs` في `.gitattributes`، ورأى تقريرَ التنفيذِ
  العامَّ قديمًا. الملفاتُ الثلاثةُ اللازمةُ لهذه الإصلاحات خارجُ سطحِ §٤.
- `guardhonesty`: `227/228`؛ `G-01 current gate roster matches recon count`،
  `expected 92, got 93`.
- `takhrij`: `101/102`؛ فشلَت `ORIGINAL_GATE_SET_MATCH` داخلَ طقمِ
  `stored-DEEN` لأنَّه يثبّتُ مجموعةَ الـ`92`.
- `telemetrytext`: `PASS=20 FAIL=1`؛ ستةُ مواضعَ في الملفِّ الجديدِ طبعت
  حقلًا اسمُه `code` لم تُراجِعْه قائمتُها. هذا عيبٌ في النتيجةِ الحالية،
  لكنَّ الأمرَ أوجبَ التوقّفَ عندَ حمرةِ `bankintegrity`، فلم أكتبْ رقعةً
  بعدها.
- `questux`: تعطّلَ بعد `7` تأكيداتٍ خضراءَ وصفرِ فشلِ تأكيد، برسالة
  `Page.enable aborted: read ECONNRESET` في محاولتَي إرفاقِ المتصفّح.

### ٥-٢ `node recon-audit.cjs`

لم أشغّله نداءً مستقلًّا بعدَ حمرةِ `bankintegrity` لأنَّ الأمرَ قال إنّها
توقفُ الوكيل. لكنَّ الروسترَ كان قد شغّل الأمرَ نفسَه وحفظ خرجَه الحرفيَّ:

```text
$ node recon-audit.cjs
exit: 1
SUMMARY   PASS=182   WARN=1   FAIL=3
```

إذًا لم يتحقق `FAIL=0`، ولم أزعمْ أنَّه تحقق.

### ٥-٣ `git status --porcelain`

بعدَ الطقمِ وقبلَ استكمالِ هذا التقرير طبعَ صفرَ سطر؛ وأثبتَ الروسترُ أيضًا
`tree after: 0 dirty path(s)`. سيُقاسُ وضعُ الشجرةِ بعدَ إيداعِ التقريرِ في
التسليمِ، لأنَّ الإيداعَ لا يستطيع أن يحتويَ قياسَ رأسِه النهائي.

## ٦ · الإيداعات

```text
5d026c04d9aa4c9d72533a247d1c71fb5a53d874 docs: record lessons browse proxy measurements
ed12562ae2e55abfc1d3e5326a8619c16782748b feat: add lessons browse proxy and guard
```

الإيداعُ الأوّلُ حفظَ جردَ §١ قبلَ أولِ رقعةٍ تنفيذيّة. الثانيُ حملَ الملفاتِ
الأربعةَ المأذونةَ وحدَها. سيحملُ إيداعٌ ثالثٌ هذا التقريرَ النهائي؛ لا يمكنُ
كتابةُ SHA إيداعٍ داخلَ الملفِّ الذي يكوّنُ ذلك الـSHA بلا مرجعٍ ذاتيٍّ، لذلك
يُطبعُ في التسليمِ بعدَ الإيداع.

`git add` استُعملَ بأسماءِ المساراتِ الصريحةِ فقط. لم يُشغّل `git add .`،
ولم يُشغّل `git push` أو دمج.

## ٧ · ما لم أقِسْه — بعلّتِه

1. **لم أُجرِ نداءً حيًّا إلى `/browse`.** الاختبارُ كلُّه بالفيكستشرِ
   ووصلةِ `fetch` محليّة. قد لا تكونُ الخدمةُ قد نشرت المسارَ بعدُ، ولا يوجد
   في هذا التقرير ادّعاءُ نداءٍ حيٍّ لم يقع.
2. **لم أقِس نشرَ Vercel أو سلوكَ واجهةٍ.** النشرُ والواجهةُ خارجُ هذا
   المسار، و`app.jsx` و`app.js` و`index.html` ممنوعةُ المسّ.
3. **لم أُعِد تشغيلَ الطقمِ بعدَ حمرتِه.** `bankintegrity` شرطُ توقفٍ صريح،
   وإصلاحُه يمسُّ أسطحًا غيرَ مأذونة.
4. **لم أشغّل `recon-audit.cjs` مستقلًّا بعدَ التوقف.** نتيجتُه داخلَ الطقم
   مقيسةٌ ومحفوظةٌ، أمّا نداءٌ ثانٍ فلم يقع.
5. **لم أصلح تثبيتاتِ العدد `92`.** `recon-audit.cjs` و`.gitattributes`
   وتقريرُ التنفيذِ العامُ وحارسُ الصدق وطقمُ `stored-DEEN` خارجُ قائمةِ
   الملفاتِ الأربعة، رغم أنَّ الانتقالَ المطلوبَ إلى `93` كشفَ تثبيتاتِها.
6. **لم أصلح رفضَ `telemetrytext` لحقلِ السجلِّ `code`.** هو عيبٌ مقيسٌ في
   النتيجة، لكنَّ رقعتَه ستقع بعدَ شرطِ التوقّف؛ يلزم إذنٌ جديدٌ لاستئنافِ
   المسار.
7. **لم أقِس SHA إيداعِ التقريرِ داخلَ حمولتِه.** هذا مرجعٌ ذاتيٌّ مستحيل؛
   يُقاسُ بعدَ الإيداع ويُطبعُ في التسليم.

## ٨ · الخلاصة

الوسيطُ والحارسُ والفيكستشرُ وسجلُّ الروستر بُنيت وأُودعت ضمنَ السطحِ
المأذون، والحارسُ الجديدُ أخضرُ `81/81` ومسخاه مقتولان `2/2`. لكنَّ بوابةَ
الطقمِ المطلوبةَ لم تمر: `87/93`، و`bankintegrity` أحمرُ بفارقِ نسخةِ عملٍ
`+298`، كما اصطدمَ الانتقالُ `92 -> 93` بتثبيتاتٍ قائمةٍ خارجَ السطحِ.
الحالةُ لذلك **COMMITTED_RED** وليست نجاحًا، وتوقّفتُ بلا لمسِ ملفٍ ممنوعٍ
وبلا دفعٍ.

---

**تعريفُ الختم:** `REPORT_SHA8` هو أوّلُ ثمانيةِ محارفَ من SHA-256 لحمولةِ
UTF-8 ذاتِ نهاياتِ `LF`. والحمولةُ هي بايتاتُ هذا الملفِّ كلُّها إلى ما قبلَ
سطرِ `---` الذي يفتحُ كتلةَ الختم، منتهيةً بمحرفِ سطرٍ واحدٍ بعدَ آخرِ سطرٍ
في §٨.

REPORT_PAYLOAD_BYTES=17246
REPORT_SHA8=ba97aa3a

## ٨ · الختم

PENDING_AFTER_FINAL_REPORT
