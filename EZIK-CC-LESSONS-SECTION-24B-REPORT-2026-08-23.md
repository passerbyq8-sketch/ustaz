# تقريرُ ٢٤-ب — قسمُ الدروسِ في عزك

**التاريخ:** ٢٣ أغسطس ٢٠٢٦ · **الفرع:** `feat/lessons-section-24b` من `origin/main`
**كلُّ رقمٍ هنا مقيسٌ في هذه الجلسة**، ومخرجُ كلِّ خطوةٍ مطبوعٌ. وما لم يُقَسْ فله §٨.
**لا دفعَ ولا دمج.**

---

## ٠ · بوّابةُ الدخول — كما قِستُها

```
--- origin/main ---   3eb5aff60542f467cb0d10ddb165f090623ff0ed   EXIT=0
--- main ---          3eb5aff60542f467cb0d10ddb165f090623ff0ed   EXIT=0
--- status ---        (صفرُ سطر)                                  EXIT=0
app.jsx     bytes=1063427 sha8=d69f3e2f
app.js      bytes=955775  sha8=0acf4443
index.html  bytes=120617  sha8=0933a9cb
```

| الشيء | القيمةُ المطلوبة | ما قِستُه | الحكم |
|---|---|---|---|
| `git rev-parse origin/main` | `3eb5aff6…` | `3eb5aff60542f467cb0d10ddb165f090623ff0ed` | مطابق |
| `git status --porcelain` | صفرُ سطر | صفرُ سطر | مطابق |
| `app.jsx` | `1,063,427` · `d69f3e2f` | `1063427` · `d69f3e2f` | مطابق |
| `app.js` | `955,775` · `0acf4443` | `955775` · `0acf4443` | مطابق |
| `index.html` | `120,617` · `0933a9cb` | `120617` · `0933a9cb` | مطابق |

الخمسةُ مطابقةٌ، فمضيتُ إلى §١.

---

## ١ · جردُ التنقّل — مقيسٌ قبلَ أيِّ رقعة

### ١-١ المراسي، أعيدَ قياسُها ولم تُنقَلْ

| المرسى | ما يقولُه الأمر | ما قِستُه | الحكم |
|---|---|---|---|
| `FatwaScreen` | `3741` | `app.jsx:3741` | مطابق |
| كتلةُ ثوابتِه | `3579-3718` | مطابقٌ في المدى — **لكنَّ الأسماءَ ليست كما كُتِبَتْ** | انظرْ أدناه |
| `EZIK_ROOT_SCREENS` | `5232` | `app.jsx:5232` = `['loading', 'onboarding', 'chat']` | مطابق |
| `EZIK_SHEET_SCREENS` | `5236` | `app.jsx:5236` = `['parentGate', 'parentDashboard', 'settings', 'favorites']` | مطابق |
| `replaceEzikScreen` | `6725` | `app.jsx:6725` | مطابق |
| `screen === '…'` | **٥٠ موضعًا** | **١٧ موضعًا** (`grep -c "screen === '"`) | **مختلف** |

**تصحيحانِ مقيسان، لا اعتراضان:**

1. **أسماءُ ثوابتِ الفتاوى تحملُ بادئةَ `EZIK_`**: المقيسُ `EZIK_FATWA_API_BASE` (`3579`) · `EZIK_FATWA_ACTIONS` (`3596`) · `EZIK_FATWA_ALL` (`3708`) · `EZIK_FATWA_PAGE_SIZE` (`3709`) · `EZIK_FATWA_SCHOLARS_KEY` (`3718`). لا وجودَ في الملفِّ لاسمٍ يبدأُ بـ`FATWA_` عاريًا: `grep -n "^const FATWA_"` ⟹ صفرُ نتيجة.
2. **مواضعُ `screen === '…'` سبعةَ عشرَ لا خمسينَ.** الرقمُ مطبوعٌ أعلاه؛ ولم أبنِ على أيٍّ من العددين، فالمخيطُ يُقاسُ بالاسمِ لا بالعدد.

### ١-٢ مفتاحُ شاشةِ الفتاوى وسجلُّه — **وهو في السجلِّ غيرِ المتوقَّع: لا هذا ولا ذاك**

- **المفتاحُ النصّيّ:** `'fatwa'`.
- **في أيِّ السجلَّين؟ في لا واحدٍ منهما.** مقيسٌ: `EZIK_ROOT_SCREENS` لا تحويه، و`EZIK_SHEET_SCREENS` لا تحويه.

**ولماذا؟ لأنّ السجلَّينِ ليسا دليلَ شاشاتٍ، بل جدولا استثناء.** `ezikBackTarget` (`app.jsx:5237`) دالّةٌ نقيّةٌ تقرأُ الشاشةَ المغادَرةَ وتردُّ وجهتَها:

```
if (EZIK_ROOT_SCREENS.indexOf(cur) !== -1) return null;   // جذرٌ: لا أبَ له
if (cur === 'call') return 'chat';
if (cur === 'home') return 'chat';
if (EZIK_SHEET_SCREENS.indexOf(cur) !== -1) { … return sheetOrigin … }   // ورقةٌ: تعودُ لفاتحِها
// Every feature section: home, never the chat.
return 'home';
```

فالسطرُ الأخيرُ — بنصِّه: **«كلُّ قسمِ ميزةٍ: البيت، لا المحادثة»** — هو ما يحكمُ `fatwa`، ولذلك لا تحتاجُ أن تُذكَرَ في سجلّ. **فوضعتُ `lessons` حيثُ `fatwa`: في لا واحدٍ منهما**، وهذا هو «السجلُّ نفسُه» بأصدقِ معانيه. ولم أخترعْ سجلًّا ثالثًا؛ والسطرُ الأخيرُ هو بعينِه ما يجعلُ الثالثَ غيرَ لازم. والحارسُ يبرهنُ هذا الاختيارَ بتأكيدين (٥ب-١).

### ١-٣ من أينَ يدخلُها القارئ

| الطريق | السطرُ المقيس | الشكل |
|---|---|---|
| **بطاقةُ الوحدةِ في البيت** (المدخلُ القانونيّ) | `app.jsx:3290` | `{ id: 'fatwa', label: EZH_FATWA, icon: EZH_ICON_FATWA, onClick: v.onOpenFatwa, meta: null }` داخلَ `ezHomeModules` |
| المعالجُ الذي تُمرَّرُ إليه | `app.jsx:9211` | `<Home … onOpenFatwa={() => setScreen('fatwa')} … />` |
| الرسم | `app.jsx:9255` | `if (screen === 'fatwa') return <FatwaScreen … />;` |
| **مدخلٌ ثانٍ مختلفُ الشكل** | `app.jsx:8444` | «اقتراحاتُ الأقسام» في رصيفِ المحادثة — `{ id: 'fatwa', …, go: () => setScreen('fatwa') }` |

الأمرُ يطلبُ **مدخلًا واحدًا** للدروس، فأخذتُ الأوّلَ — بطاقةَ البيت — وتركتُ الثاني. (وله ذيلٌ في §٨.)

### ١-٤ وسائطُ `api/lessons-search.js` — مقيسةٌ من الخادمِ نفسِه

المعالجُ يقرأُ من جسمِ الطلبِ **حقلين لا ثالثَ**:

```
const body = readBody(req);
const q = typeof body.q === 'string' ? body.q.trim() : '';
if (!q) return res.status(400).json(BAD_REQUEST_BODY);
const limit = normalizeLimit(body.limit);
```

| الوسيط | مقبول؟ | المقيس |
|---|---|---|
| `q` | نعم | نصٌّ، يُشذَّبُ، وفارغُه `400`؛ ويُقَصُّ عندَ `MAX_Q_CHARS = 400` قبلَ المنبع |
| `limit` | نعم | صحيحٌ، افتراضُه `10`، وسقفُه `LIMIT_MAX = 10` |
| `offset` · `page` · `scholar` · `content_type` · أيُّ مرشِّح | **لا** | لا يُقرَأُ منها شيءٌ ألبتّة |

وما يُرسَلُ إلى المنبعِ هو `JSON.stringify({ q: q.slice(0, MAX_Q_CHARS), limit })` — لا ثالثَ.

**فما بُنِيَ على ذلك:** صندوقُ بحثٍ واحدٌ، ويُطلَبُ `limit: 10` وهو أقصى ما يمكن.
**وما امتنعَ لعدمِه:** **لا صفحاتِ ولا زرَّ «المزيد» ولا مرشِّحَ شيخٍ ولا نوعٍ**. وليسَ هذا زهدًا: بسقفٍ عشرةٍ وبلا `offset` **لا توجدُ صفحةٌ ثانيةٌ تُطلَب**، وزرُّ «المزيد» يكونُ زرًّا لا يفعلُ شيئًا. والحارسُ يبرهنُ هذا **من مصدرِ الخادمِ نفسِه** لا من قولي: يجمعُ كلَّ `body.<name>` في `api/lessons-search.js` ويؤكِّدُ أنّ المجموعةَ تساوي `limit,q` بالضبط، ثمّ يؤكِّدُ أنّ الشاشةَ لا تحملُ اسمًا من أسماءِ التصفّح.

---

## ٢ · العائقُ الذي كشفَه القياسُ قبلَ أن أكتبَ سطرًا

**مفتاحُ شاشةٍ جديدٌ يُسجَّلُ في ملفَّينِ خارجَ §٤.** `theme-coverage-guard.cjs` يعلنُ في نثرِه (السطور ١٠٤-١٠٨) أنّ `html` عندَه هو **العميلُ المشحونُ** منذُ البند ٣٢ — أي `app.jsx` — ثمّ يستخرجُ الشاشاتِ بـ`for (const m of html.matchAll(/if \(screen === '([a-zA-Z]+)'/g))` ويقابلُها بجدولِ `INDEX_SCREENS` وبجدولِ `EZIK-THEME-33-HANDOFF.md`.

المقيسُ قبلَ الرقعة: `INDEX_SCREENS` **١٣ مفتاحًا**، والمناولةُ تقولُ `### index.html — ١٣ شاشة، صفر legacy` وتَسردُ الثلاثةَ عشرَ نفسَها.

فسطرٌ واحدٌ جديدٌ `if (screen === 'lessons')` يُحمِّرُ:
- `every reachable index.html screen is in the inventory` ⟹ `['lessons']`
- `the inventory covers 13 index.html screens` ⟹ ١٣ مقابلَ ١٤

وإصلاحُه يقتضي `theme-coverage-guard.cjs` (و§٤ يمنعُ «أيَّ حارسٍ آخر») و`EZIK-THEME-33-HANDOFF.md` (وليسَ في قائمةِ ما يُمَسّ). و§٥ لا يأذنُ إلّا بحمرةِ `bankintegrity`.

**فعرضتُ الأمرَ على المالكِ قبلَ كتابةِ سطرٍ واحدٍ، وكانَ القرارُ: تُوسَّعُ §٤ لهذا التسجيلِ وحدَه**، على أن يكونَ في إيداعٍ مستقلٍّ. وهذا ما جرى، وتفصيلُه في §٥.

---

## ٣ · ما بُنِيَ

### ٣-١ الكتلةُ ومواضعُ الوصلِ الثلاثة

| الشيء | السطرُ بعدَ الرقعة |
|---|---|
| كتلةُ القسمِ كلُّها | `app.jsx:3612-3797`، محدودةٌ بسياجينِ من عندِها: `// ITEM 24-B -- THE LESSONS SECTION` و`… -- END OF THE LESSONS SECTION` |
| كتلةُ الثوابتِ الخاصّة | `app.jsx:3638-3652` — `EZIK_LESSONS_SCREEN_ENDPOINT` · `_LIMIT = 10` · `_MIN_Q = 3` · `_TIMEOUT_MS = 12000` · وأسماءُ الحالاتِ الأربع |
| القائمةُ البيضاء | `ezikLessonsScreenRows` — `app.jsx:3655` |
| النداء | `ezikLessonsScreenSearch` — `app.jsx:3675` |
| الشاشة | `LessonsScreen` — `app.jsx:3696` |
| **الوصلُ ١**: بطاقةُ الوحدةِ في البيت | `app.jsx:3326` — `{ id: 'lessons', … }` تحتَ صفِّ الفتاوى مباشرةً |
| **الوصلُ ٢**: المعالج | `app.jsx:9437` — `onOpenLessons={() => setScreen('lessons')}` |
| **الوصلُ ٣**: الرسم | `app.jsx:9486` — `if (screen === 'lessons') return <LessonsScreen onBack={goEzikBack} />;` |

**ولماذا وُضِعَتِ الكتلةُ قبلَ كتلةِ الفتاوى لا داخلَها؟** لأنّ `K3` في `theme-coverage-guard.cjs` يقتطعُ ميزةَ الفتاوى شريحةً واحدةً — من تعريفِ ثابتِ عنوانِها إلى رايةِ الأذكار — **ويحدُّ طولَها بـ`> 7000 && < 22000`**. المقيسُ قبلَ الرقعةِ `20135`، فالمتّسعُ `1865` محرفًا فقط. فوضعُ ٩ آلافِ محرفٍ داخلَها كانَ سيُحمِّرَ `K3`. **والمقيسُ بعدَ الرقعة: `20135` — لم تتغيّرْ بايتةً واحدةً.**

**ومصيدةٌ وقعتُ فيها وقِستُها:** أوّلُ محاولةٍ وضعَتِ الكتلةَ في الموضعِ الصحيحِ، ثمّ **اقتبسَ تعليقي حرفيًّا الاسمَ الذي يفتحُ `K3` عليه شريحتَه**. فصارَ `indexOf` يجدُ تعليقي أوّلًا، وابتلعتِ الشريحةُ كتلتي كلَّها: `fatwaSrc.length` قفزَ إلى `29427`. أعدتُ صياغةَ التعليقِ ليُسمّيَ الثابتَ نثرًا ولا يهجّئَه، فعادَ `20135`. والتعليقُ نفسُه يحذّرُ القادمَ من هذا بالنصّ.

### ٣-٢ الشاشةُ والمعجم

الشاشةُ تُركَّبُ على **`EzShell`** — القوقعةُ المشتركةُ التي تستعملُها الإعداداتُ والمحفّظُ وفهرسُ المصحف — فتأخذُ هويّةَ التطبيقِ ولوحَه الداكنَ وشريطَ رجوعِه بلا لونٍ ولا خطٍّ ولا بنيةٍ جديدة. وهذا **مُلزِمٌ لا مُختار**: `index.html` خارجُ الحدودِ في §٤، فلا يمكنُ إضافةُ قاعدةِ CSS واحدة.

**١١ مفتاحًا جديدًا في المعجمِ، في نصفيه معًا** — لا نصَّ حرًّا في الشاشة: `module.lessons` · `module.lessons.sub` · `lessons.searchPlaceholder` · `lessons.searchAria` · `lessons.searchButton` · `lessons.minChars` · `lessons.loading` · `lessons.empty` · `lessons.error` · `lessons.retry` · `lessons.resultsAria`. والحارسُ يعدُّ كلَّ مفتاحٍ **مرّتين** في الملفِّ — مرّةً لكلِّ لغةٍ — ويسقُطُ إن نقصَ واحدٌ من جهةٍ.

وأضفتُ `lessons` إلى **كلا** موضعَيْ بناءِ `EZIST_SUB` (`app.jsx:107` داخلَ `ezLangRelabel` و`app.jsx:3324`)، حتّى لا يرثَ عيبًا قائمًا في الملفِّ وصفتُه في §٨.

### ٣-٣ العرضُ والحالاتُ الثلاث

**ثلاثةُ حقولٍ لا رابعَ**: `title` نصًّا · `scholar_id` تحتَه · `url` رابطًا خارجيًّا بـ`target="_blank"` و`rel="noopener noreferrer"`. مقيسٌ بمساواةِ مجموعة: `hit.*` المقروءةُ في الكتلةِ = `["scholar_id","title","url"]`. ولا `snippet` ولا `excerpt` ولا `matn` ولا أيٌّ من الستّةِ الأخرى (`score` · `unit_id` · `tier` · `usage` · `citation_allowed` · `content_type`)، ولا `Object.keys`/`entries`/`values` ولا `for…in` ولا `JSON.stringify(hit`.

| الحال | ما يظهر | المقيس |
|---|---|---|
| جارٍ | ثلاثُ نقاطٍ بنسقِ `s.dot` القائمِ في الملفِّ + `lessons.loading` | `state === EZIK_LESSONS_LOADING` |
| صفرُ نتيجة | `lessons.empty` سطرًا واحدًا | `state === EZIK_LESSONS_DONE && rows.length === 0` |
| فشل | `lessons.error` + زرُّ `lessons.retry` | `state === EZIK_LESSONS_FAILED` |

ولأنّ الشاشةَ يجبُ أن **تفرِّقَ** بينَ الرفِّ الفارغِ والطلبِ الفاشلِ — بخلافِ بطاقةِ الذيلِ التي تصمتُ في الحالين — فالنداءُ يردُّ نتيجةً مُميَّزةً `{ ok, rows }` لا قائمةً عاريةً. و`ok:false` تشملُ: حالًا غيرَ `200` · جسمًا غيرَ مقروءٍ · انقطاعًا · وإجهاضَ الاثنتي عشرةَ ثانية. **ورسالةُ الخادمِ الخامُ لا تبلغُ القارئَ لأنّ الشاشةَ لا تقرؤُها أصلًا**: تقرأُ رمزَ الحالِ ولا شيءَ غيرَه — والحارسُ يعدُّ مواضعَ `.error` في الكتلةِ ويؤكِّدُ أنّها **واحدةٌ**، وهي مفتاحُ المعجمِ نفسُه.

وزرُّ الإعادةِ يُعيدُ **الاستعلامَ الذي فشلَ** (`lastQueryRef.current`) لا ما في الصندوقِ الآن.

### ٣-٤ خارجُ النطاق، كما أُمِرَ

لا تبويبَ تصفّحٍ بالسلسلة · لا شجرةَ أبوابٍ · لا تشغيلَ وسائطَ ولا تنزيل. ولم أكتبْ لأيٍّ منها بذرةً ولا مكانًا.

---

## ٤ · الحارس

| الشيء | قبل | بعد |
|---|---|---|
| مواضعُ `check(` في المصدر | `135` | `183` |
| التأكيداتُ عندَ التشغيل | `158/158 - PASS` | **`206/206 - PASS`** |
| البايتات | `42913` | `58597` · `sha8=8cf60203` |
| المسوخ | ٣ (كلُّها في أقسامِ الخادمِ و٢٤-أ) | ٦ — الثلاثةُ كما هي، وثلاثةٌ جديدةٌ للقسمِ |

**قسمٌ جديدٌ واحدٌ `5B`**، أُدرِجَ بينَ القسمِ ٥ والقسمِ ٦. **وقسمُ ٢٤-أ (القسم ٥) لم يُمَسَّ بحرف**، ولا نُزِعَ حارسٌ ولا أُضعِفَ ولا أُضيفَ ملفُّ حارسٍ جديد — **الروسترُ ٩٢**.

ما صارَ يبرهنُه `5B`:

1. **التسجيلُ ومدخلٌ واحد** — الشاشةُ تُرسَمُ على مفتاحِها؛ والمفتاحُ يُقارَنُ في موضعٍ **واحد**؛ وصفُّ الوحدةِ **واحد**؛ و`setScreen('lessons')` **واحد**؛ والصفُّ مبنيٌّ في مصفوفةِ الوحداتِ الواحدةِ **بجوارِ صفِّ الفتاوى**؛ و`lessons` في **لا واحدٍ** من السجلَّينِ **كما أنّ `fatwa` في لا واحدٍ منهما** (يُقرَأُ السجلّانِ من المصدرِ ويُفتَّشُ فيهما)؛ والسطرُ الذي يوجّهُ كليهما ما زالَ موجودًا.
2. **القائمةُ البيضاءُ بمساواةِ مجموعة** + الكلماتُ الممنوعةُ التسعُ + صيغُ التعدادِ الستّ. **مسخٌ ١**: قارئٌ يأخذُ `snippet` ⟹ يسقُطُ في المجموعةِ **وفي** قائمةِ الكلماتِ معًا.
3. **`AbortController`**: يُنشَأُ وتُمرَّرُ إشارتُه · وبحثٌ ثانٍ يقطعُ الأوّلَ · وهبوطٌ متأخّرٌ يُسقَطُ · ومغادرةُ الشاشةِ تقطعُ الطلب. **مسخٌ ٢**: نزعُ المتحكّمِ ⟹ يسقُطُ أوّلُ تأكيدٍ.
4. **الحالاتُ الثلاث** حاضرةٌ كلٌّ بسطرِها من المعجم، وقيمةُ الحالِ الواحدةُ تحكمُ كلَّ فرعٍ (`loading` مرّتين، `done` مرّتين، `failed` مرّةً، و`idle` **صفرًا** — فالشاشةُ قبلَ أوّلِ بحثٍ لا ترسمُ شيئًا). **مسخٌ ٣**: حذفُ سطرِ «لا نتائج» ⟹ تسقُطُ الحالُ الثانية.

وزيادةً: الأرضيّةُ والسقفُ والمهلة؛ ووسائطُ الخادمِ مقروءةً **من `api/lessons-search.js` نفسِه**؛ وأنّ الواجهةَ تنادي الطريقَ من **موضعين اثنين** لا ثالثَ (البطاقةُ والشاشة)؛ وأنّ الكتلتين تحتفظانِ بسقفَيهما ومهلتَيهما المختلفتين؛ وأنّ الحزمةَ المبنيّةَ تحملُ القسمَ.

---

## ٥ · التسجيلُ في جردِ السماتِ — الإيداعُ المستقلّ

بإذنِ المالكِ، وفي إيداعٍ وحدَه `e07d0eb`:

| الملفّ | التغيير |
|---|---|
| `theme-coverage-guard.cjs` | `INDEX_SCREENS` + صفُّ `lessons` (`shell: 'istana'`) |
| `theme-coverage-guard.cjs` | `K2`: `13` ⟹ `14` istana، والمجموعةُ المسمّاةُ تكسبُ `lessons` |
| `theme-coverage-guard.cjs` | `N30` المرآة: `13/0/3` ⟹ `14/0/3` |
| `theme-coverage-guard.cjs` | **`K4` جديدٌ بجوارِ `K3`** |
| `EZIK-THEME-33-HANDOFF.md` | `### index.html — ١٣ شاشة` ⟹ `١٤ شاشة`، وصفٌّ جديدٌ في الجدول |

**والتصنيفُ مكتسَبٌ لا مُعلَن**، على انضباطِ الملفِّ نفسِه: `K4` يبرهنُ أنّ الشاشةَ تُركَّبُ على `EzShell` بعنوانٍ ومعالجِ رجوعٍ ولافتةِ رجوع، وأنّها **لا تسمّي لونًا حرفيًّا**، وأنّها **لا تضيفُ مفردةَ `.lsn*` واحدةً** إلى ورقةِ الأنماط. فـ`istana` في الجدولِ نتيجةُ فحصٍ كسائرِ الصفوف. **ولم تُخفَضْ أرضيّةٌ ولا نُزِعَ تأكيدٌ ولا استُثنيَ ملفّ.** المقيس: `themecoverage` ⟹ `OK: 1346/1346 checks passed.`

---

## ٦ · الملفّاتُ بعدَ العمل

| الملفّ | قبل | بعد | الفرق |
|---|---|---|---|
| `app.jsx` | `1063427` · `d69f3e2f` | `1077614` · `d4103039` | `+14187` |
| `app.js` | `955775` · `0acf4443` | `968689` · `0cd7f9ce` | `+12914` |
| `guards/lessons-search-guard.cjs` | `42913` | `58597` · `8cf60203` | `+15684` |
| `index.html` | `120617` · `0933a9cb` | `120617` · `0933a9cb` | **`0`** |

`app.js` **مولَّدٌ بالأداةِ ولم يُحرَّرْ بيد**. و`index.html` لم يُمَسَّ بحرف.

---

## ٧ · البوّاباتُ الأربع — على شجرةٍ نظيفةٍ بعدَ الإيداع

### ١) البناءُ والتحقّق ⟹ **`OK`**

```
built     968689 bytes  0cd7f9ceb58728103c7dc1f1a16f28c5b98596504f4bed6fe5ca87857a443e3f
on disk   968689 bytes  0cd7f9ceb58728103c7dc1f1a16f28c5b98596504f4bed6fe5ca87857a443e3f
OK: app.js is exactly what this source builds
BUILD_CHECK_EXIT=0
```

### ٢) `node tools/run-gates.cjs` ⟹ **`91/92`، بحمرةِ `bankintegrity` وحدَها**

```
=== SUITE: 91/92 EXIT=0 ===
recon:    SUMMARY   PASS=184   WARN=1   FAIL=0
tree after: 0 dirty path(s)
FAILING (1): bankintegrity=1
  bankintegrity  ->  C:\Users\passe\AppData\Local\Temp\ezik-gates\runs\2026-08-23T10-21-23-234Z-10072\gate-bankintegrity.log
evidence: C:\Users\passe\AppData\Local\Temp\ezik-gates\runs\2026-08-23T10-21-23-234Z-10072
GATES_EXIT=1
```

**و`bankintegrity` كاملةً، وهي الحمرةُ المُعلَنةُ سلفًا:**

```
FAIL [B12] CORE_BYTES = 1789173 but CORE weighs 1802087 bytes on disk (+12914).
      The quota pre-check is measured against a number that stopped being true.
      Repair it with `node tools/core-bytes.cjs --write` and re-cut the sw.js
      digest in the SAME commit -- never by hand from the comment table.
FAIL [B14] sw.js prose says app.js is 955775 bytes; the disk says 968689 (+12914).
      A comment that states a size is what the next person re-cuts CORE_BYTES from,
      so it is wrong in exactly the way the constant used to be.
FAIL  74 checks passed, 2 failed.
```

**والسببُ نموُّ `app.js` ولا شيءَ غيرُه، وهذا مقيسٌ لا مُسلَّمٌ به.** التأكيدانِ يعلنانِ الفرقَ نفسَه `+12914`، وهو بعينِه `968689 − 955775`. وبحسابٍ على القيمِ المقيسة: `1802087 − 968689 + 955775 = 1789173` — وهو المُعلَنُ في `sw.js` بالضبط. فلا بقيّةَ في هذه الحمرةِ لشيءٍ آخر.

ولم أُصلِحْها: إعادةُ قطعِ `CORE_BYTES` وختمِ `sw.js` **ملكُ جولةِ الدمجِ وحدَها** (البند ١١٢)، و`sw.js` و`quest-bank-integrity-guard.cjs` خارجَ حدودي في §٤.

### ٣) `node recon-audit.cjs` ⟹ **`FAIL=0`**

```
==================================================================
 SUMMARY   PASS=184   WARN=1   FAIL=0
 No structural FAILs. WARNs are eyeball items, not necessarily bugs.
==================================================================
RECON_EXIT=0
```

و`WARN` واحدٌ، هو المعروفُ نفسُه، مطبوعٌ ولم يُصلَحْ:

```
[WARN] LONGEST_CARD_CHARS = 3405 > longest card 3401 -> cap oversized/stale (re-derive in api/report.js)
```

### ٤) `git status --porcelain` ⟹ **صفرُ سطر**

الملفّاتُ التي مُسَّتْ: `app.jsx` · `app.js` · `guards/lessons-search-guard.cjs` في `6fcaa36`، و`theme-coverage-guard.cjs` · `EZIK-THEME-33-HANDOFF.md` في `e07d0eb`. و`git add` بأسماءَ صريحةٍ في الإيداعين، لا `git add .`.

### ٧-٥ حمرةٌ ثانيةٌ ظهرَتْ وزالَتْ — وهي مِن فعلي، لا من البيئة

التشغيلةُ **الأولى** للطقمِ أعطَتْ `90/92` بحمرتَينِ: `bankintegrity` و**`recon`**. وسجلُّ `recon` كاملًا في موضعِ الخلل:

```
[WARN] MIXED line endings in EZIK-THEME-33-HANDOFF.md (CRLF=135, lone LF=1) -- anchor-matching hazard
[FAIL] implementation report drift: RECON summary is stale (current 182/2/0)
```

**التشخيصُ مقيسٌ لا مُخمَّن.** نسخةُ العملِ من المناولةِ `CRLF`، وسكربتي كتبَ السطرَ الجديدَ بـ`\n` عاريًا — فصارَ في الملفِّ سطرٌ واحدٌ شاذٌّ. و`recon` يفحصُ خلطَ نهاياتِ الأسطر، فانقلبَ تأكيدٌ من `PASS` إلى `WARN`؛ وفحصُ «تقريرِ التنفيذ» يقابلُ سطرَ `RECON PASS=183 WARN=1 FAIL=0` (`EZIK-RFC-V0.5-R2-IMPLEMENTATION-REPORT.md:366`) بالعدّادِ قبلَه، فاختلَّ العددُ فسقطَ.

أصلحتُ نهايةَ السطرِ إلى `CRLF`، والمقيسُ بعدَه: `CRLF=136 loneLF=0`. ثمّ زالَتِ الحمرتانِ معًا وعادَ `recon` إلى `184/1/0`.

**ونقطةٌ تستحقُّ التسجيل:** الإصلاحُ **لا يظهرُ في `git diff` ألبتّة**. المقيس: الكائنُ المُودَعُ `CRLF=0 loneLF=136` قبلَ الإصلاحِ وبعدَه — git يُسوّي عندَ الإيداع. فالعطبُ كانَ في **نسخةِ العملِ وحدَها**، ولم يدخلِ التاريخَ قطُّ، ومع ذلك كانَ يُحمِّرُ بوّابةً على شجرةٍ يقولُ `git status` إنّها نظيفة. ولذلك لا إيداعَ لهذا الإصلاح: لا شيءَ فيه يُودَع.

---

## ٨ · ما لم أقِسْه — بعلّتِه

1. **لم أفتحْ متصفّحًا ولم أرَ الشاشةَ مرسومةً.** لا لقطةَ شاشةٍ ولا تشغيلَ حيًّا: كلُّ ما أعرفُه عن الرسمِ مقروءٌ من المصدرِ ومن الحرّاس. فتموضعُ الصندوقِ، وتباينُ الألوانِ على شاشةٍ حقيقيّةٍ، وسلوكُ البطاقةِ السادسةِ في شبكةِ البيتِ على هاتفٍ ضيّق — **لم يُقَسْ**. **العلّة:** لا متصفّحَ في هذا المسار.
2. **لم أنادِ `/api/lessons-search` حيًّا.** لم يقعْ في هذه الجلسةِ نداءُ شبكةٍ واحد. ما قِستُه هو **مصدرُ** الخادمِ: الحقلانِ اللذانِ يقرؤُهما، والسقفُ، والمهلة. أمّا أنّ الخدمةَ تردُّ عشرةً فعلًا لاستعلامٍ حقيقيّ، وأنّ زمنَها يحتملُ الاثنتي عشرةَ ثانيةَ — **فمنقولٌ من أوامرَ سابقةٍ ولم أقِسْه اليوم**.
3. **الشاشةُ تستعملُ `EzShell` ولا مفرداتِ CSS لها.** فهي تشبهُ الإعداداتِ والمحفّظَ لا شاشةَ الفتاوى ذاتَ `.ezf-*`. هذا **قيدٌ لا اختيار** — `index.html` خارجَ §٤ — ولم أقِسْ كيفَ يبدو الفرقُ لعينِ قارئٍ ينتقلُ بينَ القسمين.
4. **المدخلُ الثاني للأقسامِ لم يُمَسَّ، وتعليقُه صارَ خطأً.** `app.jsx:8661` ما زالَ يقول: *«this app has no lessons section -- the five modules are memorize, adhkar, mushaf, treasure and fatwa»*. صارَ هذا **غيرَ صحيحٍ بفعلِ هذه الجولة**. لم أصحّحْه لأنّ §٢-١ تأمرُ بمدخلٍ **واحدٍ**، وتحريرُ ذلك التعليقِ يجرُّ إلى سؤالِ إضافةِ مدخلٍ ثانٍ وهو خارجَ الأمر. **أرفعُها للمالك.**
5. **عيبانِ قائمانِ وجدتُّهما ولم أُصلحْهما.** (أ) `ezLangRelabel` (`app.jsx:107`) يعيدُ بناءَ `EZIST_SUB` **بدونِ** `prayer` و`library`، بينما التعريفُ في `app.jsx:3324` يحويهما — فوصفُ «الصلاةِ والقبلة» يختفي بعدَ تبديلِ اللغة. (ب) `EZIST_SUB_LIBRARY` ما زالَ في الملفِّ وواجهةُ المكتبةِ نُزِعَتْ في ٢٢ أغسطس. **العلّة:** كلاهما خارجَ ما أُمِرتُ به. وقد أضفتُ `lessons` إلى **الموضعين** حتّى لا يرثَ العيبَ الأوّل، ولم أقِسْ أثرَ (أ) على شاشةٍ حيّة.
6. **لم أقِسْ زمنَ إقلاعٍ ولا حجمَ نقلٍ.** `app.jsx` كبُرَ `+14187` و`app.js` `+12914`؛ أثرُ ذلك على زمنِ التحليلِ أو أوّلِ رسمٍ **لم يُقَسْ**.
7. **لم أُشغِّلِ الطقمَ إلّا مرّتين** — واحدةً كشفَتْ حمرةَ `recon` وواحدةً بعدَ إصلاحِها. فبوّاباتٌ معروفةٌ بالتذبذبِ (`lockpackage` بملحِها العشوائيّ، و`questux` وما يعتمدُ على Chrome) لم تُقَسْ إلّا في هاتين، وقد خرجَتا بـ`EXIT=0` في الثانية.
8. **`REPORT_SHA8` مقطوعٌ على بايتاتِ `LF`.** لا رصدَ لـ`*.md` في `.gitattributes` (وهو خارجَ §٤ على كلِّ حال)، فسحبٌ على آلةٍ بـ`core.autocrlf=true` يُنزِلُ هذا الملفَّ `CRLF` فتختلفُ البصمة. **لم أقِسْ** ذلك السحب.

---

## ٩ · الإيداعات

| البصمةُ القصيرة | ما فيه |
|---|---|
| `6fcaa36` | `feat: the lessons section -- a search screen in ezik (item 24-b)` — `app.jsx` · `app.js` · `guards/lessons-search-guard.cjs` |
| `e07d0eb` | `chore: register the lessons screen in the theme inventory (item 24-b)` — `theme-coverage-guard.cjs` · `EZIK-THEME-33-HANDOFF.md` |

الفرعُ `feat/lessons-section-24b` مقطوعٌ من `3eb5aff`. **لا `git push`. لا دمج.**

---

## ١٠ · خلاصةٌ في سطرين

قسمُ الدروسِ شاشةٌ قائمةٌ بذاتِها على المفتاحِ `lessons`، يدخلُها القارئُ من بطاقةٍ في البيتِ بجوارِ الفتاوى، تبحثُ بنداءٍ واحدٍ قابلٍ للإجهاضِ وترسمُ ثلاثةَ حقولٍ مكتوبةٍ بالاسمِ وثلاثَ حالاتٍ من المعجم — بلا صفحاتٍ ولا مرشِّحاتٍ لأنّ الخادمَ لا يقرأُ وسيطًا لهما. والحارسُ نما من ١٥٨ تأكيدًا إلى ٢٠٦ بثلاثةِ مسوخٍ جديدةٍ، والروسترُ ٩٢. والبوّاباتُ ثلاثٌ خضراءُ والرابعةُ `91/92` بحمرةِ `bankintegrity` وحدَها وبسببِها المُعلَنِ سلفًا: `app.js` كبُرَ `+12914`.

---

**تعريفُ الختم:** `REPORT_SHA8` هو `sha256[0:8]` للحمولة، والحمولةُ هي بايتاتُ هذا الملفِّ **إلى ما قبلَ سطرِ `---` الذي يفتحُ كتلةَ الختمِ هذه** — أي التقريرُ كلُّه منتهيًا بآخرِ سطرٍ من §١٠، بمحرفِ سطرٍ واحدٍ في آخرِه، وبترميزِ `UTF-8` وبنهاياتِ أسطرِ `LF`. وطولُها مطبوعٌ في `REPORT_PAYLOAD_BYTES` أدناه، فيمكنُ إعادةُ القياسِ حرفيًّا.

REPORT_PAYLOAD_BYTES=29878
REPORT_SHA8=80155977
