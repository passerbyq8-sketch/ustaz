# تقريرُ تنفيذٍ — عزك · الشاملةُ · **٢٥-ب · الإتمام**: سطرُ «لا نتيجة» يعودُ حقلًا من الخادم

**النافذة:** ن١ · وكيلُ Claude Code · `C:\Users\passe\projects\ustaz-cc-ui`
**التاريخ:** 2026-08-22
**الفرع:** `feat/lib-search-16b-20260822`
**استُؤنِفَ من:** `7fec4793` — ولم يُعَدْ شيءٌ ممّا تمّ.

> ✅ **الحصيلة: منجَزٌ ومودَعٌ.** الحارسُ `169 → 198`. الحمراءُ الوحيدةُ `bankintegrity` برقمَي `sw.js` وحدَهما. **صفرُ دفع.**

---

## ٠ · نقلُ الأمر

`ORDER-LIBSEARCH-16B-C.md` نُقِلَ إلى `C:\EZIK-STAGE\orders\` **قبلَ أيِّ فعلٍ آخر** ⟹ `ORDER_MOVED=True`.

---

## ١ · بوّاباتُ ما قبلَ العمل

| المفتاح | المطلوب | **المقيس** | الحال |
|---|---|---|---|
| `CURRENT_BRANCH` | `feat/lib-search-16b-20260822` | `feat/lib-search-16b-20260822` | ✅ |
| `HEAD` | `7fec4793` أو سليلُه | `7fec4793f44dbcce29e21f51c5a694cb8bf463c2` (هو نفسُه) | ✅ |
| `DIRTY_LINES` | `0` | `0` (بعدَ نقلِ الأمر) | ✅ |
| `GUARD_ASSERTIONS_BEFORE` | `169` مقيسًا | **`169/169 - PASS`** | ✅ |

---

## ٢ · الثابتُ الجديد

`lib/lib-source-card.js` — `+7/-0` سطرًا، ولا شيءَ حُذِف.

```js
export const EMPTY_TEXT = 'لا نتيجةَ لهذا البحث.';
```

موضوعٌ **بينَ `DEGRADED_TEXT` و`TRUNCATED_TAG`**، بالأسلوبِ نفسِه: تعليقٌ يقولُ متى يُقالُ ولمَ، ثمّ سطرٌ واحد. وسِجِلُّه سِجِلُّ أختَيه — جملةٌ خبريّةٌ قصيرةٌ منتهيةٌ بنقطة.

**والثابتانِ القائمانِ لم يُمَسّا بايتةً** — مقيسٌ صراحةً في جريانِ الترقيع:

```
SISTERS_UNTOUCHED=true
REFUSED_TEXT  = "تعذّرَ توسيعُ البحث."
DEGRADED_TEXT = "النتائجُ مبتورةٌ عندَ سقفِ البحث."
EMPTY_TEXT    = "لا نتيجةَ لهذا البحث."
```

---

## ٣ · الأسبقيّةُ على الخادم — مقيسةً صفًّا صفًّا

`api/lib-search.js` — `+29/-10` سطرًا. الاستيرادُ ضمَّ `EMPTY_TEXT`، والاسمُ أُعلِنَ في القائمةِ المصدَّرة:

```js
export const ADDED_RESPONSE_FIELDS = Object.freeze(['refused_text', 'degraded_text', 'empty_text']);
```

والقرارُ كُتِبَ **سلسلةً مرتَّبةً واحدة** لا ثلاثةَ `if` مستقلّة، لأنّ الترتيبَ **هو** العقد. والمقيسُ بتشغيلِ `shapeSearchResponse` الحقيقيّةِ على الفيكستشرات:

| # | الحال | المطلوب | **المُرسَلُ مقيسًا** |
|---|---|---|---|
| ١ | `refused === true` | `refused_text` وحدَه | `refused_text` ✅ |
| ٢ | `degraded_reason` **مع** إصابات | `degraded_text` | `degraded_text` ✅ |
| ٣ | **صفرُ إصاباتٍ وغيرُ مرفوض** | `empty_text` وحدَه | `empty_text` ✅ |
| ٤ | إصاباتٌ وغيرُ مبتور | لا شيءَ من الثلاثة | `(none)` ✅ |

وحالتانِ حدّيّتانِ لم يُسمِّهما الجدولُ صراحةً، وقد حُسِمتا على نصِّ الأمرِ وقِيستا:

| الحال | **المُرسَلُ مقيسًا** | لِمَ |
|---|---|---|
| مرفوضٌ **مع** إصابات | `refused_text` | الصفُّ الأوّلُ يقولُ «وحدَه»، فالرفضُ يسبقُ كلَّ شيء |
| مبتورٌ بـ**صفرِ** إصابات | `empty_text` | الصفُّ الثاني مشروطٌ بـ«مع إصاباتٍ»، والصفُّ الثالثُ يقولُ «وحدَه». ولا نتائجَ لبتْرٍ أن يصفَها |

🔴 **ولا يجتمعُ `empty_text` مع إصابةٍ أبدًا ولا مع `refused_text` أبدًا** — ومعها توكيدٌ أعمُّ: **جسمٌ واحدٌ لا يحملُ أكثرَ من جملةٍ واحدةٍ**، مقيسًا على الأجسامِ الستّةِ كلِّها.

**والسبعةَ عشرَ حقلًا وحقولُ الجوابِ الأصليّةُ بلا نقص** — توكيداتُ القسمِ C والقسمِ F قائمةٌ كما هي وخضراء.

---

## ٤ · العميلُ — والترتيبُ الذي كان لا بدَّ منه

`app.jsx` — `+11/-2` سطرًا.

الورقةُ ترسمُ `empty_text` **كما وصل**. ولكنّ التغييرَ لم يكنْ سطرَ رسمٍ فقط: **فرعُ «صفرُ إصابات» انتقلَ فوقَ فرعِ «مبتور»** في `ezLibViewState`، ليطابقَ ترتيبَ الخادمِ حرفًا.

**ولمَ ذلك ليس تجميلًا:** الخادمُ يرسلُ **واحدةً** من الثلاثِ بقاعدةٍ مرتَّبة. فلو صنَّفَ العرضُ جسمًا تصنيفًا مخالفًا — مثلًا عدَّ جسمًا مبتورًا بصفرِ إصاباتٍ «مبتورًا» — لطلبَ `degraded_text` والخادمُ أرسلَ `empty_text`، **فلا يجدُ حقلَه ولا يرسمُ شيئًا**، وهو الفراغُ عينُه الذي جاءَ هذا الأمرُ ليغلقَه. والحارسُ يقيسُ هذا الاتّفاقَ مصفوفةً على خمسةِ أشكالٍ (§٥).

**و`CARD_TEXT_IN_APPJSX = 0` باقٍ:** نصُّ `EMPTY_TEXT` **صفرُ ظهورٍ في `app.jsx` وفي `app.js`** — الورقةُ تحملُ اسمَ الحقلِ `'empty_text'` لا كلماتِه. وغابَ الحقلُ ⟹ لا يُرسَمُ شيءٌ ولا يُختلَقُ سطر.

---

## ٥ · الحارسُ — `169 → 198`

| | قبل | **بعد** |
|---|---|---|
| عددُ التأكيدات | `169` | **`198`** |
| الحصيلة | `169/169 - PASS` | **`198/198 - PASS`** |

**ولم يُخفَّفْ تأكيدٌ واحد.** ثلاثةُ توكيداتٍ قائمةٍ أُعيدَ توجيهُها لأنّ الحقيقةَ التي تصفُها تغيَّرَتْ، وكلُّها **أضيقُ لا أوسع**:

* «الاثنانِ اللذانِ قد تُضيفُهما هما بالضبط…» ⟹ **الثلاثة** بالضبط، من القائمةِ المصدَّرةِ نفسِها
* «الحالُ الفارغةُ لا تسمّي حقلًا» ⟹ **تسمّي `empty_text`**، وهو ما صارَ صحيحًا
* «ولا واحدةَ من الجملتَينِ على 200 عاديّ» ⟹ **ولا واحدةَ من الثلاث**

**والقسمُ F8 جديدٌ كلُّه (٢٦ توكيدًا)** ويحوي الأربعةَ الملزِمةَ في §٣ من الأمر:

| # | التوكيدُ الملزِم | الحال |
|---|---|---|
| ١ | صفرُ إصاباتٍ وغيرُ مرفوضٍ ⟹ `empty_text` حاضرٌ **ومطابقٌ لثابتِ الوحدةِ حرفًا** | ✅ |
| ٢ | إصاباتٌ حاضرةٌ ⟹ `empty_text` غائبٌ | ✅ |
| ٣ | `refused === true` ⟹ `empty_text` غائبٌ و`refused_text` حاضرٌ | ✅ |
| ٤ | نصُّ `EMPTY_TEXT` صفرُ ظهورٍ في `app.jsx` | ✅ (وصفرٌ في `app.js` كذلك) |

ومعها: «وحدَه» على الجسمِ الفارغِ · «جملةٌ واحدةٌ على الأكثرِ» على الأجسامِ الستّة · والحالتانِ الحدّيّتانِ · **ومصفوفةُ الاتّفاق**: على خمسةِ أشكالٍ، هل يطلبُ العرضُ الحقلَ الذي أرسلَه الخادمُ فعلًا، وهل يرسمُه.
وفي القسم G توكيدانِ جديدانِ على **الشاشةِ المرسومة**: الجملةُ تظهرُ في الحالِ الفارغةِ، ولا تظهرُ نتيجةٌ بجانبِها، ولا تظهرُ الجملةُ حينَ تكونُ هناكَ إصابات.

### طفرةُ العضّ — المطلوبةُ في §٣، على القرصِ ثمّ رُدَّتْ

| الخطوة | العدد | الخروج |
|---|---|---|
| قبلَ الطفرة | **`198/198 - PASS`** | `0` |
| جُعِلَ الخادمُ يُرسِلُ `empty_text` **مع** إصاباتٍ (`} else if (!shownHits.length) {` ⟸ `} else if (true) {`) | **`186/198 - FAIL`** — سقطَ اثنا عشرَ توكيدًا، منها اثنانِ على **الشاشةِ المرسومة** | `1` |
| رُدَّ الأصل (`RESTORED_IDENTICAL=true`) | **`198/198 - PASS`** | `0` |

وفي الحارسِ نفسِه طفرةٌ داخليّةٌ دائمةٌ بالمعنى ذاتِه (خادمٌ مطفَّرٌ في الذاكرةِ عبرَ `data:` URL) تتحقّقُ أوّلًا من أنّ الطفرةَ **ليست عاطلة**، بجانبِ طفرتَي جولةِ التصحيحِ السابقتَين.

---

## ٦ · شرطُ القبول

| المفتاح | **المقيس** |
|---|---|
| `GUARD_ASSERTIONS_AFTER` | **198** (`>= 169`) · والأربعةُ الجديدةُ خضراء · والطفرةُ عضَّتْ |
| `CARD_TEXT_IN_APPJSX` | **0** — على جملِ الوحدةِ الأربعِ (بما فيها `EMPTY_TEXT`) وجملِ الدالّةِ الأربع |
| `DYNAMIC_IMPORT_SITES_AFTER` | **0** — في `app.jsx` و`app.js` |
| `LIB_HOST_IN_APPJSX` | **0** |
| `CALLS_ON_BOOT` | **0** — بشجرةِ البناءِ وعلى ورقةٍ مرسومةٍ (`gNet.length === 0`) |
| `PAGE_SHOWN_WHEN_NOT_CITABLE` | **0** — في البطاقةِ، وفي السطرِ، وعلى الشاشة |
| `build-app.cjs --check` | `960498` بايتة · `4269b5c939f633bef66fccc84567ed0e94cf77d9ae26313f9b38b5352e44942c` — مطابق |
| `TDZ_COUNT` | **0** · `SCOPES=1355` · `MODE=script` · **بمسارٍ صريحٍ مطبوع:** `C:/Users/passe/projects/ustaz-cc-ui/app.jsx` (`SRC_CHARS=1020339`) |
| الإيداعُ أوّلًا | نعم، ثمّ السلسلةُ على شجرةٍ نظيفة (`0 dirty path(s)`) |
| الدفع | **صفر** |

### الأرضيّاتُ — قبلَ وبعد

| | قبل (`7fec4793`) | **بعد** (`4cce746a`) |
|---|---|---|
| `run-gates` | 89/90 | **89/90** |
| `recon` | `PASS=182 WARN=1 FAIL=0` | `PASS=182 WARN=1 FAIL=0` |
| `babel` | `EXIT=0` | ✅ `EXIT=0` |
| `themecoverage` | `EXIT=0` | ✅ `EXIT=0` |
| `wird` | `EXIT=0` | ✅ `EXIT=0` |
| `i18nui` | `EXIT=0` | ✅ `EXIT=0` |
| `noemptyanswer` | `EXIT=0` | ✅ `EXIT=0` |
| حارسُ ١٦-أ | `169/169` | ✅ **`198/198`** |

**لم تنخفضْ أرضيّةٌ ولم يسقطْ فحصٌ كان أخضر.**

### الحمراءُ الوحيدةُ — `bankintegrity`، نصُّها حرفًا

```
  FAIL [B12] CORE_BYTES = 1781136 but CORE weighs 1793896 bytes on disk (+12760).
        The quota pre-check is measured against a number that stopped being true.
        Repair it with `node tools/core-bytes.cjs --write` and re-cut the sw.js
        digest in the SAME commit -- never by hand from the comment table.
  FAIL [B14] sw.js prose says app.js is 947738 bytes; the disk says 960498 (+12760).
        A comment that states a size is what the next person re-cuts CORE_BYTES from,
        so it is wrong in exactly the way the constant used to be.
FAIL  74 checks passed, 2 failed.
```

رقمانِ في `sw.js` وحدَهما، وكلاهما `+12760` وهو نموُّ `app.js` بعينِه (`947738 → 960498`). **صفرُ بايتةٍ في `sw.js`.**

---

## ٧ · ما لم يُقَسْ، بعلّتِه

1. **لم يُجرَّبْ نداءٌ حيٌّ — التوكنُ في الإنتاجِ وحدَه.** كلُّ جسمٍ في هذا التقريرِ أنتجَتْه الدالّةُ الحقيقيّةُ من فيكستشراتٍ محلّيّة.
2. **لم يُقَسْ أنّ خدمةَ المكتبةِ تُرجِعُ فعلًا `hits: []` لبحثٍ بلا نتيجة.** الصفُّ الثالثُ مبنيٌّ على `Array.isArray(out.hits) ? … : []`، فجوابٌ يُغفِلُ `hits` رأسًا يُعامَلُ كصفرِ إصاباتٍ أيضًا — وهو الاتّجاهُ الآمن، لكنّه لم يُشاهَدْ من الخدمةِ الحقيقيّة.
3. **الحالتانِ الحدّيّتانِ (§٣) اجتهادٌ على نصِّ الأمرِ لا نصٌّ فيه.** جدولُ الأمرِ لم يُسمِّ «مرفوضٌ مع إصابات» ولا «مبتورٌ بصفرِ إصابات»؛ حُسِمتا على كلمةِ «وحدَه» في الصفَّينِ الأوّلِ والثالثِ وعلى شرطِ «مع إصاباتٍ» في الثاني. **وهما مقيستانِ ومثبَّتتانِ بتوكيدٍ**، فإن أرادَ صاحبُ الأمرِ غيرَهما فالتغييرُ سطرٌ واحدٌ في الدالّةِ وسطرٌ في العرض.
4. **لم يُقَسْ نصُّ الحالِ الفارغةِ على قارئٍ.** ظهورُه على الشاشةِ مقيسٌ في القسم G؛ أمّا كونُه الصياغةَ الأنسبَ فحكمٌ لا قياس.
5. **لم تُقَسْ فروقُ الخطِّ ولا اتّجاهُ النصِّ على شاشةٍ حقيقيّة.** `linkedom` بلا `getComputedStyle`، ولم تُلتقطْ صورة.
6. **لم يُقَسْ زمنٌ ألبتّة.**

---

## ٨ · الرأسُ الأخير

| المفتاح | القيمة |
|---|---|
| الرأسُ المُستأنَفُ منه | `7fec4793f44dbcce29e21f51c5a694cb8bf463c2` |
| إيداعُ الإتمام | **`4cce746ab3ab021b1d722a9444d4552317bb3dfb`** |
| `FINAL_HEAD` | **`4cce746ab3ab021b1d722a9444d4552317bb3dfb`** — وعليه قِيسَتِ السلسلةُ على شجرةٍ نظيفة |
| الدفع | **صفر** |

ويُودَعُ هذا التقريرُ فوقَه، وSHA إيداعِه مطبوعٌ على الطرفيّةِ بعدَ الختم.

---

## ٩ · في الختمِ وسطورِه

🔴 **الختمُ محسوبٌ على بايتاتِ LF**، و`core.autocrlf` **مفعَّلٌ** في هذه الشجرة، فسحبٌ جديدٌ على وِندوز يكتبُ هذا الملفَّ بـCRLF ولن يُعيدَ الرقم. **فيُعادُ إنتاجُه من كائنِ git:**

```
git show <commit>:EZIK-CC-LIBSEARCH-16B-C-REPORT-2026-08-22.md
```

وقد أُعيدَ إنتاجُه في هذا الجريانِ **من القرصِ ومن كائنِ git كلَيهما**، وأعطيا الرقمَ نفسَه.

<!-- PAYLOAD-END -->

```
PAYLOAD_BYTES = 14381
SHA256        = a749909cb47f73842c54e55ad25d2dbe83291c70b853efca9fda4c945f658b6b
ALGO          = sha256 over every byte of this file up to and including the line <!-- PAYLOAD-END --> and its newline
ENCODING      = LF. core.autocrlf is on in this tree: reproduce from the git object, not the working copy.
```
