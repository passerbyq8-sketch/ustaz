# تقريرُ ٢٤-ج — شاشتا التصفّحِ في قسمِ الدروس

**التاريخ:** ٢٣ أغسطس ٢٠٢٦ · **الفرع:** `feat/lessons-browse-ui-24c` من `a332cb3`
**كلُّ رقمٍ هنا مقيسٌ في هذه الجلسة**، ومخرجُ كلِّ خطوةٍ مطبوعٌ. وما لم يُقَسْ فله §٩.
**لا دفعَ ولا دمجَ ولا `git add .`.**

---

## ٠ · بوّابةُ الدخول — كما قِستُها

```
--- git rev-parse origin/main ---   a332cb3bfc43067f88f4878654a9a3846d07cb14
--- git status --porcelain ---      (صفرُ سطر)
--- git checkout -B feat/lessons-browse-ui-24c a332cb3 ---
Switched to a new branch 'feat/lessons-browse-ui-24c'
--- git rev-parse HEAD ---          a332cb3bfc43067f88f4878654a9a3846d07cb14

app.jsx     bytes=1077614  lines=17015  sha8=d4103039
app.js      bytes=968689   lines=5293   sha8=0cd7f9ce
index.html  bytes=120617   lines=1685   sha8=0933a9cb
```

| الشيء | المطلوب | ما قِستُه | الحكم |
|---|---|---|---|
| `git rev-parse origin/main` | `a332cb3b…` | `a332cb3bfc43067f88f4878654a9a3846d07cb14` | مطابق |
| `git status --porcelain` | صفرُ سطر | صفرُ سطر | مطابق |
| الشجرةُ راكدةٌ على `2586860` | قطعُ الفرعِ أوّلُ فعل | قُطِعَ الفرعُ من `a332cb3` قبلَ أيِّ قراءةٍ للمصدر | مطابق |

**`BASE_MOVED` لم يُطبَعْ**: `origin/main` هو ما نصَّ عليه الأمرُ حرفًا بحرف.

---

## ١ · القياسُ قبلَ الكتابةِ — الفخُّ المقيسُ يومَ ٢٤-ب

كتلةُ ٢٤-ب قرأتُها كلَّها أوّلًا. سطرا سياجِها كانا:

```
app.jsx:3612   // ITEM 24-B -- THE LESSONS SECTION
app.jsx:3797   // ITEM 24-B -- END OF THE LESSONS SECTION
```

ومفاتيحُ المعجمِ الأحدَ عشرَ التي تحملُها: `module.lessons` · `module.lessons.sub` ·
`lessons.searchPlaceholder` · `lessons.searchAria` · `lessons.searchButton` · `lessons.minChars` ·
`lessons.loading` · `lessons.empty` · `lessons.error` · `lessons.retry` · `lessons.resultsAria`.

### الفخُّ `K3` — `fatwaSrc.length` قبلَ وبعدُ

`theme-coverage-guard.cjs:1814` يفتحُ شريحةَ الفتاوى بـ`html.indexOf('const EZIK_FATWA_API_BASE =')`
ويغلقُها على بانرِ الأذكار، ثمّ يحدُّ طولَها `> 7000 && < 22000` في السطرِ ١٨٢٨.

```
fatwaSrc.length  قبلَ الكتابة   = 20135      (السعةُ المتبقّيةُ حتّى الحدِّ: 1865 بايتًا فقط)
fatwaSrc.length  بعدَ الكتابة   = 20135      (فرقُ الصفر)
```

**لم يتغيّرْ ولا بايتٍ واحد.** وسببانِ اثنانِ حفظاه: كتلتي تفتحُ **فوقَ** موضعِ الشريحةِ لا داخلَها
(`fatwaAt` انتقلَ من `213497` إلى `235145` وهو إزاحةٌ لا طولٌ)، و**اسمُ ذلك الثابتِ لا يُهجَّأُ في
كتلتي ولا في تعليقٍ منها** — فالتعليقُ الذي يحملُه كان سينقلُ بدايةَ الشريحةِ إلى سطري ويبتلعُ القسمَ كلَّه.
ما فعلتُه بدَلَه: وصفتُه نثرًا («a locator comment») ولم أكتبْه.

### والحدُّ الآخرُ الذي كان لا بدَّ من قياسِه: `K4`

`theme-coverage-guard.cjs:1843` يحدُّ شريحةَ ٢٤-ب نفسَها `> 3000 && < 14000`.

```
lessonsSrc.length  قبلُ = 9267     بعدُ = 12519     (المتبقّي حتّى الحدِّ: 1481)
```

وشرطاه الحرفيّانِ في السطرِ ١٨٤٩ باقيانِ كما هما، ولذلك لم يتغيّرْ سطرُ `EzShell` بحرف:

```
/<EzShell title=\{ezT\('module\.lessons'\)\} onBack=\{onBack\} backLabel=\{A2_BACK\}>/   ⟹ true
/function LessonsScreen\(\{ onBack \}\)/                                              ⟹ true
```

**وهذانِ الشرطانِ هما ما شكَّلَ التصميمَ كلَّه**، لا ذوقي: `onBack={onBack}` مطلوبٌ حرفيًّا، فلم يكنْ
لي أن أُمرِّرَ إلى الصدفةِ دالَّةً محسوبةً باسمٍ آخر. فصارَ `LessonsScreen` غلافًا يُسلِّمُ طريقَ البيتِ
باسمِ `onHome`، و`LessonsSection` هو الذي يُعرِّفُ `onBack` محلّيًّا ويملكُ السُّلَّم.

---

## ٢ · الكتلُ ومواضعُ الوصل

### أ · لماذا كتلةٌ ثالثةٌ ولمَ فوقَ ٢٤-ب لا داخلَها

قسمُ `5B` في الحارسِ يفحصُ ما بينَ سياجَي ٢٤-ب بقائمةِ كلماتٍ محرَّمةٍ اسمُها `PAGER_WORDS` —
`offset` · `page` · `pageSize` · `nextPage` · `loadMore` · `hasMore` — **وهو محقٌّ في تحريمِها هناك**،
لأنّ طريقَ البحثِ يقرأُ حقلَي متنٍ اثنينِ ولا صفحةَ له، فزرٌّ مربوطٌ بثالثٍ يُرسِلُ ما يسقطُ على الأرض.
والتصفّحُ طريقٌ آخرُ بعقدٍ آخر، فهو **منطقةٌ ثالثةٌ** من الملفِّ لا داخلَ تلكَ ولا داخلَ بطاقةِ ٢٤-أ.
وموضعُها **فوقَ** شريحةِ الفتاوى لا تحتَها، لِما في §١.

### ب · المواضعُ بعدَ الكتابة

| الموضع | السطر | ماذا |
|---|---|---|
| `app.jsx` | `3649` | `// ITEM 24-C -- THE LESSONS BROWSE PANE` — يفتحُ الكتلةَ الجديدةَ ملاصقًا لسياجِ ٢٤-ب من فوقِه |
| `app.jsx` | `3792` | `function LessonsBrowsePane({ backRef })` |
| `app.jsx` | `3968` | `// ITEM 24-C -- END OF THE LESSONS BROWSE PANE` |
| `app.jsx` | `3971` | `// ITEM 24-B -- THE LESSONS SECTION` (سياجُ ٢٤-ب، لم يُمَسَّ) |
| `app.jsx` | `4056` | `const EZIK_LESSONS_TAB_BROWSE = 'browse';` |
| `app.jsx` | `4065` | `function LessonsScreen({ onBack })` — غلافٌ من ثلاثةِ أسطر |
| `app.jsx` | `4070` | `function LessonsSection({ onHome })` — التبويبانِ والسُّلَّم |
| `app.jsx` | `4121` | `const searchPane = (` — رفعُ جسدِ البحثِ إلى قيمةٍ، **بلا تغييرِ سطرٍ واحدٍ منه** |
| `app.jsx` | `4189` | `<div role="tablist" …>` |
| `app.jsx` | `4212` | `// ITEM 24-B -- END OF THE LESSONS SECTION` |
| `app.jsx` | `245`–`261` | سبعةَ عشرَ مفتاحًا في النصفِ العربيّ، بعدَ `lessons.resultsAria` |
| `app.jsx` | `474`–`490` | السبعةَ عشرَ نفسُها في النصفِ الإنجليزيّ |
| `app.jsx` | `16963`–`16973` | إحدى عشرةَ مفتاحَ نمطٍ `lsb*` بجوارِ `lsn*`، كلُّ لونٍ فيها رمزُ `--a3-*` قائمٌ |
| `guards/lessons-search-guard.cjs` | `752`–`1120` | القسمُ `5C` |
| `guards/lessons-search-guard.cjs` | رأسُ الملفِّ | خمسةُ أسطرِ روسترٍ تصفُ `5C` بعدَ سطرِ `5B` |

**الرفعُ إلى `searchPane` هو كلُّ ما أصابَ البحثَ.** الأسطرُ من `<form role="search"` إلى آخرِ
`) : null}` قبلَ إغلاقِ الصدفةِ سليمةٌ بايتًا ببايت، ولم أُعِدْ إزاحتَها؛ لُفَّتْ في `React.Fragment`
وحسب. وقسمُ `5B` بأكملِه ما زالَ يمرُّ (٢٠٦ تأكيدًا خضراءَ)، وهو البرهانُ لا دعواي.

### ج · العقدُ كما بنيتُ عليه

```
{"level":"scholars"}                                        ⟹ {rows:[{scholar_id,count}], total}
{"level":"series","scholar_id":"…","page":1}                ⟹ {rows:[{series,count}], total,page,pages}
{"level":"lessons","scholar_id":"…","series":"…","page":1}  ⟹ {rows:[{unit_id,title,url}], total,page,pages}
```

المستوى الأوّلُ **لا يحملُ صفحةً ألبتّة** لأنّ العقدَ لا يذكرُها له، وهو ما يوافقُ «بلا صفحات» في الأمر.
والقائمةُ البيضاءُ **مكتوبةٌ بالاسمِ لكلِّ مستوًى** في ثلاثِ دوالَّ منفصلةٍ، ولا `Object.keys` ولا
`Object.entries` ولا `Object.assign` ولا رسمٌ تلقائيٌّ في الكتلةِ كلِّها.

**`unit_id` يُقرَأُ ولا يُرسَمُ**: هو هويّةُ الصفِّ في القائمةِ (`key={row.unit || i}`) ولا شيءَ غيرُ ذلك،
والحارسُ يعدُّ ظهورَه فيبرهنُ أنّه واحدٌ. و**صفرُ نصِّ درسٍ** في المستوياتِ الثلاثةِ: اسمُ شيخٍ، اسمُ
سلسلةٍ، عنوانُ درسٍ، ورابطٌ خارجيٌّ — لا خامسَ.

---

## ٣ · مفاتيحُ المعجمِ الجديدةُ — سبعةَ عشرَ، في النصفَينِ معًا

| المفتاح | العربيّة | الإنجليزيّة |
|---|---|---|
| `lessons.tabBrowse` | تصفُّح | Browse |
| `lessons.tabSearch` | بحث | Search |
| `lessons.tabsAria` | تصفُّحُ الدروسِ أو البحثُ فيها | Browse the lessons or search them |
| `lessons.scholarsTitle` | المشايخ | Scholars |
| `lessons.scholarsAria` | قائمةُ المشايخ | The list of scholars |
| `lessons.seriesAria` | سلاسلُ الشيخ | The scholar’s series |
| `lessons.lessonsAria` | دروسُ السلسلة | The lessons of the series |
| `lessons.miscSeries` | دروسٌ متفرّقة | Assorted lessons |
| `lessons.countLessons` | `{n}` درسًا | `{n}` lessons |
| `lessons.browseLoading` | يُحمِّلُ الآنَ… | Loading… |
| `lessons.browseEmpty` | لا شيءَ هنا. | Nothing here. |
| `lessons.browseError` | تعذّرَ التحميلُ الآنَ. | This could not load just now. |
| `lessons.browseRetry` | أعِدِ المحاولة | Try again |
| `lessons.pagerAria` | تنقُّلُ الصفحات | Page navigation |
| `lessons.pagePrev` | السابق | Previous |
| `lessons.pageNext` | التالي | Next |
| `lessons.pageOf` | صفحةُ `{n}` من `{of}` | Page `{n}` of `{of}` |

و`i18nui` — وهو الذي يقايسُ نصفَي المعجمِ مفتاحًا بمفتاحٍ، ويمنعُ التكرارَ، ويشترطُ أن يكونَ كلُّ
`{placeholder}` على الجانبَينِ — أخضرُ (`EXIT=0`).

**صفرُ نصٍّ حرٍّ**، وهو مبرهنٌ مرّتَينِ لا مرّةً: بعدَ نزعِ التعليقاتِ **لا يبقى في كتلةِ التصفّحِ محرفٌ
عربيٌّ واحد** (فكلُّ كلمةٍ تُرسَمُ نداءُ `ezT`)، وكلُّ نصٍّ مقتبَسٍ باقٍ فيها إمّا مفتاحُ معجمٍ يبدأُ
بـ`lessons.` وإمّا واحدٌ من سبعةَ عشرَ لفظًا تقنيًّا مسمًّى بالاسمِ — فجملةٌ تُكتَبُ بالإنجليزيّةِ،
وهي ما لا يراه الفحصُ الأوّلُ، يُسقِطُها الثاني.

---

## ٤ · ما صارَ يبرهنُه `5C` — ومسوخُه

`guards/lessons-search-guard.cjs` نما من **٢٠٦** تأكيدًا إلى **٢٦٧** (`+61`). **قسما ٥ و`5B` لم
يُمَسّا بحرف** — قرأتُ متغيّراتِهما (`appJsx` · `appJs` · `screenCode` · `screenFrom` · `drawFrom` ·
`stripComments` · `FORBIDDEN_IN_DRAW` · `ENUMERATORS` · `SNIPPET_MARKER`) ولم أكتبْ فيهما.
**ولم يُضَفْ ملفُّ حارسٍ، والروسترُ ٩٢ كما كان.**

`5C` مقسومٌ تسعَ مجموعات:

| المجموعة | ماذا تبرهن |
|---|---|
| `5C` رأسًا | الكتلةُ محدودةٌ بسياجَيها، جسدُ كودٍ لا تعليقٌ، و**ثلاثُ مناطقَ منفصلةٍ لا اثنتان**: بطاقةُ ٢٤-أ، وقسمُ ٢٤-ب، وهذه |
| `5C-1` | الطريقُ مسمًّى مرّةً واحدةً وهو `/api/lessons-browse` · نداءُ `fetch` واحدٌ على ذلك الثابتِ لا غير · `POST` بجسدِ `JSON` · و**الكتلةُ لا تمدُّ يدَها إلى طريقِ البحثِ** · وكلماتُ المستوياتِ الثلاثةِ ثوابتُ العقدِ نفسُها |
| `5C-2` | **متنُ الطلبِ لكلِّ مستوًى بمساواةِ مجموعةٍ**: الأوّلُ `level` وحدَه (**ولا صفحةَ**)، والثاني `level,page,scholar_id`، والثالثُ `level,page,scholar_id,series` — ولا مستوًى يخترعُ `limit` أو `q` أو `offset` أو `kind` أو `sort` أو `content_type` |
| `5C-3` | **القوائمُ البيضاءُ الثلاثُ بمساواةِ مجموعةٍ، مُشغَّلةً حقًّا**: الدوالُّ تُقتطَعُ من `app.jsx` وتُقيَّمُ وتُقادُ بفيكستشرٍ **كلُّ صفٍّ فيه يحملُ حقولَ المتنِ الثمانيةَ** (`snippet` · `excerpt` · `matn` · `content_type` · `score` · `tier` · `usage` · `citation_allowed`) — ولا واحدٌ منها ينجو من أيِّ مستوًى، ولا العلامةُ المزروعةُ فيها. ويُبرهَنُ أنّ القوائمَ **مكتوبةٌ لا مُعدَّدةٌ**، وأنّ الصفوفَ المقروءةَ هي الستّةُ المسمّاةُ لا سابعَ، وأنّ `unit_id` لا يصلُ إلى عنصرٍ مرسومٍ |
| `5C-4` | **سلّةُ المتفرّقةِ آخرًا دائمًا** — والفيكستشرُ يرسلُها **أوّلًا** عمدًا، فتطبيقٌ يرسمُ ما أُعطِيَ كان سينجحُ في اختبارٍ فيكستشرُه مرتَّبٌ سلفًا |
| `5C-5` | `AbortController` في مسارِ التصفّحِ: الإشارةُ تُسلَّمُ لـ`fetch` · وتنظيفُ الأثرِ هو ما يقطعُ الطلبَ المعلَّقَ · وكلُّ انتقالٍ بينَ المستوياتِ تابعٌ من توابعِ ذلك الأثرِ · **وهبوطٌ متأخّرٌ يُسقَطُ بجيلٍ** · واثنتا عشرةَ ثانيةً في ثابتٍ مسمًّى |
| `5C-6` | **الحالاتُ الثلاثُ**، وهي واحدةٌ لكلِّ المستوياتِ: تحميلٌ · لا نتائجَ · فشلٌ مع إعادةٍ — كلُّها من المعجمِ، ولا نصَّ خطأٍ من الخادمِ يبلغُ القارئَ ألبتّة (`.error` تظهرُ صفرَ مرّةٍ في الكتلة) |
| `5C-7` | **السُّلَّمُ رُتبةً واحدةً**: القسمُ يعرضُ ضغطةَ الرجوعِ على الكتلةِ **قبلَ** أن يذهبَ إلى البيتِ · دروسٌ ⟵ سلاسلُ · سلاسلُ ⟵ مشايخُ · والمشايخُ أرضٌ تُردُّ عندَها الضغطةُ · و**طرفا المدى معطَّلانِ لا مخفيّانِ** (الزرّانِ يُرسَمانِ معًا ثمّ يُعطَّلانِ، ولا شرطَ `pages > 1` في الملفِّ) · ولا صفحاتِ للمشايخِ لأنّ العقدَ يُرسِلُهم كاملينَ |
| `5C-8` | سبعةَ عشرَ مفتاحًا في النصفَينِ · صفرُ محرفٍ عربيٍّ ناجٍ في الكتلةِ · كلُّ مقتبَسٍ مفتاحٌ أو لفظٌ تقنيٌّ مسمًّى · والحزمةُ المبنيّةُ تحملُ الكتلةَ لا المصدرُ وحدَه |
| `5C-9` | المسوخُ الأربعةُ |

### المسوخُ الأربعةُ — وكلُّ واحدٍ يُبرهَنُ مسخًا حقيقيًّا قبلَ أن يُقاد

الطفرةُ العقيمُ تُبلِّغُ نجاحًا كاذبًا: التأكيدُ تحتَها يعضُّ الأصلَ فتُقرَأُ الجولةُ خضراءَ. فكلُّ مسخٍ
أدناه يُثبَتُ أوّلًا أنّه غيَّرَ المصدرَ، ثمّ يُقاد.

| المسخ | ما يُفسِدُه | التأكيدُ الذي يعضُّ |
|---|---|---|
| **٤** | قارئُ الدروسِ يُمرِّرُ الصفَّ كما هو (`out.push(row)`) بدلَ تسميةِ حقولِه — وهو شكلُ القائمةِ المُعدَّدةِ من الخارج | **يُقاد فعليًّا**: المخرجُ يُسرِّبُ العلامةَ المزروعةَ وحقولَ المتنِ الثمانيةَ جميعًا |
| **٥** | السلّةُ تُلحَقُ في **المقدّمة** (`loose.concat(named)`) | **يُقاد فعليًّا**: آخرُ الصفوفِ لم يعدْ السلّةَ، وأوّلُها صارَ هي |
| **٦** | `AbortController` يُنزَعُ من مسارِ التصفّحِ ويُستبدَلُ بكائنٍ خاملٍ | فحصُ الإجهاضِ الأوّلُ يسقط |
| **٧** | السُّلَّمُ يقفزُ رُتبتَينِ: الدروسُ ترجعُ إلى المشايخِ رأسًا | فحصُ «رُتبةً واحدةً لا رُتبتَينِ» يسقط |

والمسوخُ الأربعةُ **جديدةٌ**؛ مسوخُ ٥ و`5B` الثلاثةُ (١ · ٢ · ٣) وما في الأقسامِ ١ و٣ و٤ باقيةٌ على حالِها.

---

## ٥ · `app.jsx` و`app.js` قبلَ وبعدُ

| الملفّ | قبلُ | بعدُ | الفرق |
|---|---|---|---|
| `app.jsx` | `1077614` بايتًا · `17015` سطرًا · `d4103039` | `1101363` · `17444` · `a135a626` | `+23749` بايتًا · `+429` سطرًا |
| `app.js` | `968689` · `5293` · `0cd7f9ce` | `989166` · `5391` · `ef0d81ad` | `+20477` بايتًا · `+98` سطرًا |
| `guards/lessons-search-guard.cjs` | `58597` · `894` · `8cf60203` | `83828` · `1268` · `2f8f934b` | `+25231` بايتًا · `+374` سطرًا |
| `index.html` | `120617` · `1685` · `0933a9cb` | **لم يُمَسّ** | `0` |

`app.js` **مولَّدٌ** بـ`node tools/build-app.cjs` لا يدًا، ولا قاعدةَ `CSS` واحدةً أُضيفت:
`K4` يبرهنُ في السطرِ ١٨٥٨ أنّ `index.html` لم يكسبْ قاعدةَ `.lsn*` واحدةً، وكتلتي ترسمُ من
كائنِ `s` نفسِه بأحدَ عشرَ مفتاحًا بادئتُها `lsb`، كلُّ لونٍ فيها رمزُ `--a3-*` قائمٌ.

---

## ٦ · البوّاباتُ الأربعُ — حرفيًّا، على شجرةٍ نظيفةٍ بعدَ الإيداعِ

### ١ · البناءُ ثمّ `--check`

```
$ node tools/build-app.cjs
source (LF-normalised)   1101363 bytes  (CR removed: 0)
runtime                  classic  (preset-react, sourceType=script)
wrote                    app.js  989166 bytes  in 496ms
sha256                   ef0d81adc80748d002d7306de5be369a49c8b4331a790c8d075c004804e4a43e

$ node tools/build-app.cjs --check
built     989166 bytes  ef0d81adc80748d002d7306de5be369a49c8b4331a790c8d075c004804e4a43e
on disk   989166 bytes  ef0d81adc80748d002d7306de5be369a49c8b4331a790c8d075c004804e4a43e
OK: app.js is exactly what this source builds
```

### ٢ · `node tools/run-gates.cjs`

```
=== SUITE: 91/92 EXIT=0 ===
recon:    SUMMARY   PASS=184   WARN=1   FAIL=0
tree after: 0 dirty path(s)
FAILING (1): bankintegrity=1
```

**واحدةٌ حمراءُ، وهي `bankintegrity` وحدَها، وبسببِ نموِّ `app.js` لا غير** — وهو المقبولُ المنصوصُ
عليه في §٥ من الأمر. تفصيلُها في §٧.

### ٣ · `node recon-audit.cjs`

```
 SUMMARY   PASS=184   WARN=1   FAIL=0
```

**`FAIL=0`** كما اشترطَ الأمر.

🔵 وملاحظةٌ تستحقُّ التدوينَ: قبلَ الإيداعِ كانت هذه البوّابةُ تطبعُ `PASS=182 WARN=1 FAIL=1`
وعلّتُها `implementation report drift: RECON summary is stale`. وليسَ ذلك انحدارًا: الشجرةُ
المتّسخةُ تُنزِلُ فحصَ «working tree clean» من `PASS` إلى `INFO`، فينقصُ عدُّ النجاحاتِ واحدًا،
فيختلفُ عن الرقمِ المسجَّلِ في `EZIK-RFC-V0.5-R2-IMPLEMENTATION-REPORT.md:366` فيسقطُ فحصُ الانجرافِ.
**الإيداعُ وحدَه أصلحَها**، ولم يُمَسَّ ذلك التقريرُ ولا `gates.json`.

### ٤ · `git status --porcelain` قبلَ الإيداع

```
 M app.js
 M app.jsx
 M guards/lessons-search-guard.cjs
```

**الملفّاتُ الثلاثةُ وحدَها.** ولا `index.html` ولا `api/**` ولا `lib/**` ولا `sw.js` ولا
`gates.json` ولا حارسٌ آخر.

---

## ٧ · `bankintegrity` كاملةً

```
  FAIL [B12] CORE_BYTES = 1802087 but CORE weighs 1822564 bytes on disk (+20477).
        The quota pre-check is measured against a number that stopped being true.
        Repair it with `node tools/core-bytes.cjs --write` and re-cut the sw.js
        digest in the SAME commit -- never by hand from the comment table.
  FAIL [B14] sw.js prose says app.js is 968689 bytes; the disk says 989166 (+20477).
        A comment that states a size is what the next person re-cuts CORE_BYTES from,
        so it is wrong in exactly the way the constant used to be.

FAIL  74 checks passed, 2 failed.
```

`+20477` في السطرَينِ هو **بعينِه** فرقُ `app.js` في §٥: `989166 − 968689 = 20477`. فالحمرةُ
سببُها نموُّ `app.js` **وحدَه**، ولا شيءَ غيرَه سقطَ في السبعةِ والسبعينَ فحصًا. وإعادةُ القطعِ
(`tools/core-bytes.cjs --write` وإعادةُ ختمِ `sw.js`) **ملكُ جولةِ الدمجِ وحدَها — البندُ ١١٢** —
و`sw.js` خارجَ حدودي، فلم أمسَّه. **ولم أخفضْ أرضيّةً ولم أنزعْ تأكيدًا.**

---

## ٨ · الإيداعات

```
b580cec  feat(lessons): the section grows a browse tab -- scholar, series, lessons
```

إيداعٌ واحدٌ للملفّاتِ الثلاثةِ، ثمّ هذا التقريرُ في إيداعٍ ثانٍ بعدَ البوّاباتِ.
**لا `git push`. لا دمجَ. ولم يُستعمَلْ `git add .` ألبتّة** — الملفّاتُ الثلاثةُ سُمِّيتْ بأسمائِها.

---

## ٩ · ما لم أقِسْه — وعلّتُه

1. **`/api/lessons-browse` لم يُنادَ حيًّا، ولا مرّةً واحدةً.** الملفُّ **غيرُ موجودٍ في شجرتي**
   (`ls api/lessons-browse.js ⟹ No such file or directory`)، وهو ملكُ وكيلٍ آخرَ يعملُ الآنَ
   بالتوازي كما نصَّ الأمر. فكلُّ ما في §٤ من برهانٍ سلوكيٍّ **فيكستشرٌ محلّيٌّ**: الدوالُّ تُقتطَعُ
   من `app.jsx` وتُقيَّمُ وتُقادُ بصفوفٍ مصوغةٍ على العقدِ المكتوبِ في الأمر. **ولا أدَّعي نداءً حيًّا
   لم يقعْ**، ولا أدَّعي شيئًا عن سلوكِ الخادمِ: ما برهنتُه هو جانبُ الواجهةِ من العقدِ وحدَه.
2. **لم أشغِّلْ الواجهةَ في متصفِّح.** لا لقطةَ شاشةٍ ولا مشيَ نقراتٍ: البرهانُ كلُّه ساكنٌ ومُشغَّلُ
   دوالَّ، وثلاثُ بوّاباتٍ (`i18nui` · `themecoverage` · `a11y`) هي التي تقودُ المستندَ لا أنا.
   فترتيبُ التبويبَينِ بصريًّا، ومقاسُ اللمسِ على جهازٍ حقيقيٍّ، لم يُقاسا.
3. **ترتيبُ المشايخِ تنازليًّا بالعددِ لم أُثبِتْه** — لأنّه **حكمُ الخادمِ**، لا حكمي: الكتلةُ ترسمُ
   الصفوفَ بالترتيبِ الذي وصلَتْ به ولا تُعيدُ فرزَها، وذلك مذكورٌ في تعليقِ المستوى الأوّلِ.
   الشيءُ الوحيدُ الذي أُعيدُ ترتيبَه هو سلّةُ المتفرّقةِ، وهو مبرهنٌ في `5C-4`.
4. **حجمُ الصفحةِ عشرةٌ لم أُرسِلْه ولم أقِسْه.** العقدُ لا يحملُ حقلًا له، فالخادمُ يملكُه؛ والكتلةُ
   تسألُ عن رقمِ صفحةٍ وتقرأُ `pages` كما جاءت. زرٌّ يُرسِلُ `pageSize` كان سيُرسِلُ ما لا يُقرَأ.
5. **عودةُ القارئِ إلى التبويبِ لا تحفظُ موضعَه في السُّلَّمِ.** التبويبُ غيرُ المختارِ لا يُركَّبُ
   أصلًا — وهو ما يجعلُ طلبَه المعلَّقَ يُقطَعُ بنفسِ التنظيفِ الذي يقطعُه عندَ مغادرةِ الشاشةِ —
   فالرجوعُ إليه يبدأُ من المشايخِ. هذا اختيارٌ لا سهو، وهو غيرُ مقيسٍ عندَ قارئٍ حقيقيّ.
6. **`B12`/`B14` لم أُصلِحْهما ولم أقِسْ أثرَ إصلاحِهما** — البندُ ١١٢ يجعلُهما لجولةِ الدمجِ،
   و`sw.js` خارجَ حدودي.

---

**تعريفُ الختم:** `REPORT_SHA8` هو `sha256[0:8]` للحمولة، والحمولةُ هي بايتاتُ هذا الملفِّ **إلى ما قبلَ سطرِ `---` الذي يفتحُ كتلةَ الختمِ هذه** — أي التقريرُ كلُّه منتهيًا بآخرِ سطرٍ من §٩، بمحرفِ سطرٍ واحدٍ في آخرِه، وبترميزِ `UTF-8` وبنهاياتِ أسطرِ `LF`. وطولُها مطبوعٌ في `REPORT_PAYLOAD_BYTES` أدناه، فيمكنُ إعادةُ القياسِ حرفيًّا.

REPORT_PAYLOAD_BYTES=24973
REPORT_SHA8=c61e716a
