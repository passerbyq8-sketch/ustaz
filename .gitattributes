# Al-Murabbi - line-ending pins.
# Defect 40, MEASURED in session 28: a fresh clone with core.autocrlf=true injects
# 77 CR into worship-golden.json (SHA8 A13C34BA -> F48CB7C7) and 55 CR into
# worship-display.json (64B8A2C9 -> 9A56CC5B), and gate 7 exits 1.

# Byte-compared or SHA-pinned by the guards. LF forever, on every machine.
worship-golden.json      text eol=lf
worship-display.json     text eol=lf
quran-golden.json        text eol=lf
referral-golden.json     text eol=lf
adhkar.json              text eol=lf
lib/data/adhkar.json     text eol=lf

# recon-audit requires bare-LF = 0 inside index.html. Pure CRLF, on every machine.
index.html               text eol=crlf

# Not measured by any guard. Pinned only so every machine agrees -- and so the local
# bytes match what git stores and Vercel serves, which is LF for both today.
privacy.html             text eol=lf
sw.js                    text eol=lf
.gitattributes           text eol=lf

# D09, MEASURED: sw.js and manifest.json are BOTH SHA-256 sealed now, by gate
# `bankintegrity` (check B10), and the seal is cut on LF bytes -- so the note above
# ("not measured by any guard") no longer holds for either of them. manifest.json
# carried no pin at all, so a clone with core.autocrlf=true checked it out with 16 CR
# (533 -> 549 bytes) and the seal broke on a machine that had changed nothing. sw.js
# was pinned on line 20 already but had drifted to CRLF in the working tree (4944 ->
# 5044) and the seal had been cut on those CRLF bytes, which is the same defect from
# the other end: it held on this machine and nowhere else.
manifest.json            text eol=lf

# Zero-newline by design (gate 5 depends on it). Git must never rewrap these.
quran-uthmani.json       -text
mushaf-layout.json       -text

# Binary. Never convert.
*.gz                     binary
quest-data/*.json -text

# Defect 40 extended: these nine guard scripts were brought under git tracking in
# bug 43. Pin them to LF so a fresh clone with core.autocrlf=true cannot inject CR
# into them (a CR shift would corrupt the byte counts the guards measure).
worship-guard.cjs         text eol=lf
quran-guard.cjs           text eol=lf
layout-guard.cjs          text eol=lf
babel-gate.cjs            text eol=lf
runtime-gate.cjs          text eol=lf
recon-audit.cjs           text eol=lf
build-worship-display.cjs text eol=lf
referral-guard.cjs        text eol=lf
classifier-guard.cjs      text eol=lf
gates.json text eol=lf
EZIK-RFC-V0.5-R2-IMPLEMENTATION-REPORT.md text eol=lf

# Gate 10 (hafs-map-guard) and the correspondence map it SHA-pins. Defect 40 again:
# the map's sha256 is asserted by the gate, so a fresh clone with core.autocrlf=true
# would inject CR, move the hash and fail the gate on a machine that changed nothing.
hafs-map-guard.cjs        text eol=lf
data/mushaf-hafs-map.json text eol=lf
build-golden-md.cjs text eol=lf
golden-set-worship.md text eol=lf
build-mushaf-layout.cjs text eol=lf
esc.cjs text eol=lf
flip-stream-flag.cjs text eol=lf
quran-verify.cjs text eol=lf
lib/daycap.js text eol=lf
lib/ratelimit.js text eol=lf
api/unlock.js text eol=crlf

# Gate 11 (call-mode-guard). Same reason as every gate above: it brace-slices index.html,
# so a clone that injected CR into the GATE itself would shift nothing -- but a gate that
# is not LF-pinned is the one file in the roster whose bytes nobody agrees on.
call-mode-guard.cjs text eol=lf

# Gate 12 (chat-history-guard). Same reason as every gate above.
chat-history-guard.cjs text eol=lf

# Gate 13 (markdown-guard). Same reason as every gate above.
markdown-guard.cjs text eol=lf

# Gate 14 (quest-reveal-guard). Same reason as every gate above. Its golden lives under
# quest-data/, already covered by the "quest-data/*.json -text" rule further up.
quest-reveal-guard.cjs text eol=lf

# Gate 15 (quran-quest-guard). Same reason as every gate above. Its golden also lives
# under quest-data/, covered by the "quest-data/*.json -text" rule further up.
quran-quest-guard.cjs text eol=lf

# Gate 16 (prayer-quest-guard). Same reason as every gate above. Its golden also lives
# under quest-data/, covered by the "quest-data/*.json -text" rule further up.
prayer-quest-guard.cjs text eol=lf

# Gate 17 (quest-bank-integrity-guard). Same reason as every gate above. Its golden also
# lives under quest-data/, covered by the "quest-data/*.json -text" rule further up.
quest-bank-integrity-guard.cjs text eol=lf

# Gate 18 (quest-content-review-guard). Same reason as every gate above. Its manifest also
# lives under quest-data/, covered by the "quest-data/*.json -text" rule further up.
quest-content-review-guard.cjs text eol=lf

# Gate 19 (theme-coverage-guard). Same reason as every gate above: it is compared byte-for-byte
# against what the app ships, so its own line endings must not drift under core.autocrlf.
theme-coverage-guard.cjs text eol=lf

# Gate 20 (chat-ux-guard). Same reason as every gate above: it reads and re-runs the shipped
# babel block, so its own line endings must not drift under core.autocrlf.
chat-ux-guard.cjs text eol=lf

# Gate 21 (a11y-guard). Same reason as every gate above: it re-runs the shipped babel block, so
# its own line endings must not drift under core.autocrlf.
a11y-guard.cjs text eol=lf

# Gate 22 (quest-ux-guard). It seals the question bank by hash and drives the shipped page in a
# real browser, so its own line endings must not drift under core.autocrlf.
quest-ux-guard.cjs text eol=lf

# Gate 23 (attribution-guard). Same reason as every gate above. Its two modules are pinned for
# the same reason lib/daycap.js is: the gate reads them as text and asserts on their contents, so
# a fresh clone with core.autocrlf=true must not shift a byte of them.
attribution-guard.cjs text eol=lf
lib/attribution.js text eol=lf
lib/binothaimeen.js text eol=lf

# Gate 24 (claim-guard) and the two captured islamweb pages it measures against. Same reason as
# every gate above: the fixtures are matched as text, so a CR injected by core.autocrlf would
# change what the gate is comparing.
claim-guard.cjs text eol=lf
data/claim-fixtures.json text eol=lf
lib/claim-gate.js text eol=lf
lib/duration.js text eol=lf

# Gate 25 (source-registry-guard) and the three modules it asserts on. Same reason as every
# gate above: the guard reads these files as text and matches on their contents, so a CR
# injected by core.autocrlf on a fresh clone must not shift a byte of them.
source-registry-guard.cjs text eol=lf
lib/source-registry.js text eol=lf
lib/source-purpose.js text eol=lf
lib/source-page-gates.js text eol=lf
lib/retrieve.js text eol=lf
tools/source-live-smoke.cjs text eol=lf

# Gate 26 (brave-query-guard) and the query builder it measures. Same reason as every gate
# above: the guard reads these files as text and asserts on their contents.
brave-query-guard.cjs text eol=lf
lib/brave-query.js text eol=lf

# Gate 27 (smart-retrieval-guard) and the request planner it measures.
smart-retrieval-guard.cjs text eol=lf
lib/ask-plan.js text eol=lf
api/ask.js text eol=lf

# Gates 28-32 (the ledger-RAG guards) and every module they read as text. Same reason as every
# gate above, and one more that is specific to this path: lib/ledger/segment.js records UTF-8
# BYTE offsets into page text, and lib/ledger/gates.js re-derives every span from them. A CR
# injected by core.autocrlf on a fresh clone would shift those offsets and turn a correct
# extraction into a failing Gate 1 on a machine that changed nothing.
ledger-contract-guard.cjs text eol=lf
ledger-retrieval-guard.cjs text eol=lf
ledger-gates-guard.cjs text eol=lf
ledger-runtime-guard.cjs text eol=lf
ledger-fixtures-guard.cjs text eol=lf
ledger-seam-guard.cjs text eol=lf
data/ledger-fixtures.json text eol=lf
data/ledger-fixture-results.json text eol=lf
tools/ledger-live-eval.cjs text eol=lf
lib/ledger/*.js text eol=lf

# Gates 34-35 (the RFC v0.5-R2 policy guards) and the shared policy core they measure. Same
# reason as every gate above, and one specific to this batch: lib/policy/entities.js and
# lib/policy/age.js match Arabic on WHOLE-WORD boundaries, and a CR injected by core.autocrlf on a
# fresh clone would sit inside a word run and change what those boundaries see.
guards/rfc-v05r2-guard.cjs text eol=lf
guards/rfc-v05r2-runtime-guard.cjs text eol=lf
guards/rfc-v05r2-wiring-guard.cjs text eol=lf
guards/rfc-v05r2-round3-guard.cjs text eol=lf
guards/rfc-v05r2-mode-guard.cjs text eol=lf
guards/rfc-v05r2-historical-guard.cjs text eol=lf
guards/rfc-v05r2-consistency-guard.cjs text eol=lf
lib/policy/*.js text eol=lf
lib/legacy-policy-flag.js text eol=lf
guards/rfc-v05r2-entity-world-guard.cjs text eol=lf
guards/scholar-registry-drift-guard.cjs text eol=lf
guards/shipped-reality-guard.cjs text eol=lf

# Batch 2. Same reason as every gate above, and one specific to this module: lib/page-match.js
# matches Arabic ḥāl markers as substrings of a word run, and a CR injected by core.autocrlf on a
# fresh clone would sit inside one and change what those runs see.
guards/page-match-guard.cjs text eol=lf
lib/page-match.js text eol=lf
guards/takhrij-lock-guard.cjs text eol=lf
lib/takhrij-lock.js text eol=lf
# The same defect as lib/route-classify.js below, found by fixing that one: with the route seam
# repaired, gate takhrij went 84/87 -> 85/87 and uncovered two more assertions of its own that had
# been dark behind the earlier abort. takhrij-lock-guard.cjs:425 mutates lib/finalized-sse-writer.js
# by a THREE-LINE template literal, so every \n in the seam has to match; the file was stored LF,
# pinned nowhere, and checked out with 396 CR. `mutation precondition` is the guard's own honest
# statement that its mutant was not applied, and the mutant assertion behind it failed for the same
# reason. Neither the file nor the guard was touched by either parallel branch -- both blobs are
# byte-identical to d2c5828 -- so this is baseline debt, uncovered here and closed here.
lib/finalized-sse-writer.js text eol=lf
lib/frozen-text.js text eol=lf
guards/quoted-phrase-guard.cjs text eol=lf
guards/adapted-corpus-guard.cjs text eol=lf
guards/dead-domains-guard.cjs text eol=lf
guards/floors-and-filters-guard.cjs text eol=lf
guards/source-liveness-guard.cjs text eol=lf
guards/source-attribution-guard.cjs text eol=lf
guards/referral-tail-guard.cjs text eol=lf
guards/name-presence-guard.cjs text eol=lf
guards/retrieval-observability-guard.cjs text eol=lf
tools/source-liveness.cjs text eol=lf
data/source-liveness.json text eol=lf

# The AI-consent gate (Apple 5.1.1(i) / 5.1.2(i)). Pinned for the same reason every module above
# is: tools/ai-consent-probe.cjs READS lib/ai-consent.js as text and asserts that the version
# string in it equals the one in index.html. That mirror check is the only thing standing between
# a client and a server that disagree about which disclosure was accepted, and a stray CR is
# exactly the kind of invisible difference that would break it in a fresh clone and nowhere else.
lib/ai-consent.js text eol=lf
tools/ai-consent-probe.cjs text eol=lf
guards/ruling-source-guard.cjs text eol=lf
guards/world-parity-guard.cjs text eol=lf
tools/wird-guard.cjs text eol=lf
guards/voice-safety-guard.cjs text eol=lf

# D14: madina-hafs-guard joins the roster, and recon requires every gate script to be
# pinned here. The file itself is not touched by that item -- only checked out as LF
# from here on, like every other gate.
tools/madina-hafs-guard.cjs text eol=lf

# D15: i18n-ui-guard joins the roster, same mechanical requirement as D14 above.
guards/i18n-ui-guard.cjs text eol=lf

# D41: adhkar-twins-guard joins the roster, same mechanical requirement as D14/D15 above.
# It matters more here than for most: this gate compares the two adhkar.json copies by RAW
# BYTES, so a CR introduced on one side by core.autocrlf and not the other IS the failure it
# reports. Both copies are already pinned eol=lf above, and so is the guard itself.
guards/adhkar-twins-guard.cjs text eol=lf

# D02ب: the system prompt module and the gate that pins it. lib/system-prompt.js carries ~79KB
# of vocalised Arabic whose bytes are fingerprinted by that gate -- a CR injected by
# core.autocrlf would change every pinned SHA on a fresh clone and fail the gate for a reason
# that has nothing to do with the prompt.
lib/system-prompt.js text eol=lf
lib/reader-fields.js text eol=lf
guards/system-prompt-parity-guard.cjs text eol=lf

# D13/D12/D06: the lock package gate. It drives api/unlock.js, api/parent-code.js and the
# founder verifier against a store double and compares digests it computes itself, so a CR
# introduced by core.autocrlf on one side of a comparison and not the other is a failure with
# nothing to do with the locks.
guards/lock-package-guard.cjs text eol=lf

# D6أ: the source-honesty gate. It compares user-agent strings byte for byte and drives the
# fetcher to read the header actually sent, so a CR introduced by core.autocrlf on one side of
# a comparison and not the other is a failure with nothing to do with honesty.
guards/source-honesty-guard.cjs text eol=lf
guards/search-budget-p0-guard.cjs text eol=lf
guards/hybrid-live-fatwa-guard.cjs text eol=lf
lib/fatwa-service.js text eol=lf
lib/hybrid-deen.js text eol=lf
# The missing sibling of the three above, and the whole of the takhrij failure that stood red on
# d2c5828 before either parallel branch was written. MEASURED, both halves:
# guards/stored-deen-sub-suite.cjs's routeMutant() reads lib/route-classify.js RAW and mutates it
# by substring, and its seams contain \n. The file was stored LF and pinned NOWHERE, so a clone
# with core.autocrlf=true (the default state of this machine) checked it out with 320 CR, every
# seam missed, and routeMutant threw `route mutation seam moved: scan-old-turns` -- gate takhrij
# 84/87 FAIL. The guard was right to complain: a mutant that is never applied must never be
# reported as a mutant that was killed.
# And the pin cannot be replaced by flipping core.autocrlf, because guards/i18n-ui-guard.cjs cuts
# quest.html on \r\n and quest.html is unpinned too -- so that guard REQUIRES CRLF where this one
# requires LF. All four combinations were measured in round A: no single value of core.autocrlf
# satisfies both. Only a per-path pin does, and quest.html is a protected path this round.
lib/route-classify.js text eol=lf
lib/stored-deen.js text eol=lf
# The telemetry gate compares a record it BUILT against a record it read back out of a store
# double, and asserts byte-identical socket frames across two runs. Both comparisons are exact, so
# a CR introduced by core.autocrlf on one side and not the other would be a failure with nothing
# to do with telemetry.
guards/ledger-telemetry-guard.cjs text eol=lf
# The live-search gate compares the reader's stream against a fixed Arabic sentence and asserts it
# appears exactly once and at the very start. Both are exact string comparisons, so a CR introduced
# by core.autocrlf on one side and not the other would be a failure with nothing to do with the
# disclosure.
guards/live-search-disclosure-guard.cjs text eol=lf

# «شكلُ الجواب» (٨ أغسطس ٢٠٢٦) — the gate runs the prompt generator and matches Arabic sentences
# against its output, so a CRLF working tree on one machine would break the line-oriented preamble
# detector for reasons that have nothing to do with the answer's shape.
guards/answer-shape-guard.cjs text eol=lf

tools/soft-404.cjs text eol=lf
lib/domain-budget.js text eol=lf
lib/policy/takhrij-disclosure.js text eol=lf

# Gate 69 (identity-guard) and the two modules it drives. Same reason as every gate above: the
# guard reads them as text and asserts on their contents, and the whitelist is keyed on
# NORMALISED Arabic — a CR injected by core.autocrlf would sit inside a key and split it.
guards/identity-guard.cjs text eol=lf
lib/identity/index.js text eol=lf
lib/identity/whitelist.js text eol=lf
lib/identity/wikipedia.js text eol=lf
lib/identity/cache.js text eol=lf

# Gate 70 (transfer-mode-guard) and the four modules it drives. Same reason as every gate above:
# the guard reads them as text and asserts on their contents, and the flip-token list is matched
# on NORMALISED Arabic — a CR inside a token would split it and silence the veto.
guards/transfer-mode-guard.cjs text eol=lf
lib/transfer/index.js text eol=lf
lib/transfer/match.js text eol=lf
lib/transfer/extract.js text eol=lf
lib/transfer/trim.js text eol=lf

# Gate 71 (anchor-mode-guard) and the two modules it drives. Same reason as every gate above:
# the guard reads them as text, and the span test matches NORMALISED Arabic — a CR inside a span
# would break the containment check that decides whether a claim may be printed at all.
guards/anchor-mode-guard.cjs text eol=lf

# Gates 72-73 and the module they drive. searchbudgetp0 was ALREADY tracked and pinned; the
# full-fatwa contract guard and lib/full-fatwa.js are new. Same reason as every gate above,
# and sharper here: span verification is a LITERAL substring test over normalised Arabic, so a
# stray CR inside a carrying passage would silently fail a span that is genuinely in the source
# — and a dropped span is exactly the truncation this gate exists to prevent.
guards/full-fatwa-contract-guard.cjs text eol=lf
guards/retired-chat-endpoints-guard.cjs text eol=lf
guards/guard-honesty-guard.cjs text eol=lf
guards/honesty/_lib.cjs text eol=lf
guards/honesty/g01-recon.cjs text eol=lf
guards/fixtures/honesty/g01-recon.json text eol=lf
guards/honesty/g06-ledger-runtime.cjs text eol=lf
guards/fixtures/honesty/g06-ledger-runtime.json text eol=lf
guards/honesty/g07-rfc-runtime.cjs text eol=lf
guards/fixtures/honesty/g07-rfc-runtime.json text eol=lf
guards/honesty/g08-rfc-round3.cjs text eol=lf
guards/fixtures/honesty/g08-rfc-round3.json text eol=lf
guards/honesty/g09-rfc-mode.cjs text eol=lf
guards/fixtures/honesty/g09-rfc-mode.json text eol=lf
guards/honesty/g10-shipped-reality.cjs text eol=lf
guards/fixtures/honesty/g10-shipped-reality.json text eol=lf
guards/honesty/g11-dead-domains.cjs text eol=lf
guards/fixtures/honesty/g11-dead-domains.json text eol=lf
guards/honesty/g12-source-liveness.cjs text eol=lf
guards/fixtures/honesty/g12-source-liveness.json text eol=lf
guards/honesty/g15-live-search.cjs text eol=lf
guards/fixtures/honesty/g15-live-search.json text eol=lf
guards/honesty/g16-transfer-mode.cjs text eol=lf
guards/fixtures/honesty/g16-transfer-mode.json text eol=lf
guards/honesty/g17-anchor-mode.cjs text eol=lf
guards/fixtures/honesty/g17-anchor-mode.json text eol=lf
guards/fixtures/honesty/x021-fatwa-telemetry.json text eol=lf
guards/fixtures-full-fatwa.json text eol=lf
lib/full-fatwa.js text eol=lf
lib/anchor/flag.js text eol=lf
lib/anchor/units.js text eol=lf

# Gates 76-80: the five answer-path guards of parallel round A, listed as gates in the merge
# round because gates.json belonged to the other writer while they were being built. Same
# reason as every gate above, and it is not decorative for these five: each of them reads
# repository files RAW and seams on text containing \n — prompt-consistency derives the age
# bound from an expression in index.html, truncated-tag boots the shipped text/babel block and
# calls the real functions, and the other three seam on source literals to plant their mutants.
# A clone with core.autocrlf=true would hand them CRLF, the seams would miss, and a mutant that
# was never applied reports as a mutant that was killed — which is defect 40 at the head of this
# file arriving through the same door, and the exact shape of the takhrij failure below.
guards/prompt-consistency-par-a-guard.cjs text eol=lf
guards/truncated-tag-fallback-par-a-guard.cjs text eol=lf
guards/explicit-failure-par-a-guard.cjs text eol=lf
guards/scholar-separation-par-a-guard.cjs text eol=lf
guards/card-or-no-context-par-a-guard.cjs text eol=lf

# Gates 81-86: the FreeBrain-B output reviewer. The five mutation guards rewrite literal seams
# from lib/output-reviewer.js into OS-temp twins, so CR injection would make a non-applied mutant
# look killed. The matrix and its fixture are byte-stable evidence of the six required cases.
guards/output-reviewer-matrix-guard.cjs text eol=lf
guards/attribution-on-output-guard.cjs text eol=lf
guards/domain-contract-guard.cjs text eol=lf
guards/no-empty-answer-guard.cjs text eol=lf
guards/ladder-order-guard.cjs text eol=lf
guards/tag-honesty-guard.cjs text eol=lf
guards/standing-notice-band-guard.cjs text eol=lf
guards/telemetry-scan-lib.cjs text eol=lf
guards/telemetry-text-guard.cjs text eol=lf
guards/output-reviewer-mutant-lib.cjs text eol=lf
lib/output-reviewer.js text eol=lf
fixtures/output-reviewer-six-cases.json text eol=lf
guards/gemini13-route-guard.cjs text eol=lf
fixtures/gemini13-matrix.json text eol=lf

# The FreeBrain-A answer path. Same reason as lib/route-classify.js below and lib/output-reviewer.js
# above, and it is the same PAIRING: guards/no-empty-answer-guard.cjs is pinned on line 378 and it
# reads lib/free-brain/loop.js RAW and plants six mutants into it by literal substring and by
# `^…$` regex. A guard that is pinned while the file it mutates is not is the exact asymmetry that
# produced the takhrij failure (lib/route-classify.js, 320 CR) and the takhrij-lock failure
# (lib/finalized-sse-writer.js, 396 CR). MEASURED here before the pin: the file was stored LF
# (41,044 bytes) and checked out with 737 CR (41,781 bytes) on this machine.
# The seams themselves were already written CRLF-aware — `\r?\n` where they span lines, and `$`
# in multiline mode matches before a bare CR in JS — so all six applied in BOTH states and this
# pin revives no mutant that was blind. It closes the disagreement, not a failure.
lib/free-brain/loop.js text eol=lf

# ح٣: the frozen transfer fixtures. THESE ARE NOT OUR TEXT — they are the bytes eleven hosts
# actually served on 2026-08-08, and data/transfer-fixtures/manifest.json publishes a full SHA-256 of
# each one. Any end-of-line normalisation, in either direction, on any machine, changes those
# bytes and breaks the attestation — which is precisely defect 40 at the head of this file,
# arriving a second time by a different door. So they are marked BINARY, not pinned to an
# ending: there is no correct ending for somebody else's page, only the one it was served with.
data/transfer-fixtures/*.html -text
# ...and the manifest itself IS ours, and is read as text.
data/transfer-fixtures/manifest.json text eol=lf
data/transfer-fixtures/a3-cases.json text eol=lf

# lib/colon-preamble.js — the colon-preamble detector adopted in 30e0ad4, and the pin that was
# missing from the same commit. It is the third file in this repository to arrive as a NEW module
# that a guard reads and mutates as text while carrying no pin, and the first two both cost a
# green gate: lib/route-classify.js was stored LF, pinned nowhere, and checked out with 320 CR, so
# guards/stored-deen-sub-suite.cjs's routeMutant() missed every seam and takhrij stood at 84/87;
# lib/finalized-sse-writer.js was stored LF, pinned nowhere, and checked out with 396 CR, so the
# three-line template seam at takhrij-lock-guard.cjs:425 missed and a mutant that was never
# applied was reported as a mutant that was killed. Both were baseline debt found only when
# something else was fixed.
# MEASURED here before the pin, on this machine (core.autocrlf=true): the blob is stored LF
# (4,395 bytes, 83 lone LF, 0 CR) and a forced fresh checkout produced 4,478 bytes with 83 CR and
# 0 lone LF. guards/takhrij-lock-guard.cjs imports this module and lib/takhrij-lock.js calls it on
# every sealed answer, so the pin closes the disagreement before it can cost a third gate.
lib/colon-preamble.js text eol=lf

# tools/core-bytes.cjs (item 112) -- the tool that derives sw.js's CORE_BYTES from the CORE list
# and rewrites it. It PARSES sw.js as text and matches on `const CORE = [`, and sw.js itself is
# pinned LF above because its sha256 seal is cut on LF bytes. A CRLF checkout of the tool costs
# nothing on its own, but this repository has already paid for three unpinned modules that a
# guard reads as text (lib/route-classify.js, lib/finalized-sse-writer.js, lib/colon-preamble.js),
# and the pin is cheaper than the fourth.
tools/core-bytes.cjs text eol=lf

# tools/prebuild-jsx.cjs (item 32-a) -- reads the text/babel block out of index.html with the
# same regex pair the fourteen coupled guards use, and index.html is pinned CRLF above. Pinning
# the tool itself to LF keeps the reader and the read apart: the tool's own endings are nobody's
# measurement, and a CRLF checkout of a file that carries regexes is the shape that has already
# cost this repository three green gates.
tools/prebuild-jsx.cjs text eol=lf

# guards/vacuous-assertion-guard.cjs (item 116) -- the gate that refuses an assertion which is
# satisfied by an empty region. It parses every .cjs file in the tree, so a CRLF checkout would
# shift every line number it reports and every offset Babel hands it. Pinned for the same
# reason as every gate above it.
guards/vacuous-assertion-guard.cjs text eol=lf

# guards/boot-invariants-guard.cjs (item 113) -- it counts the scripts between <body> and the
# catcher and reads offsets out of index.html, which is pinned CRLF above. Its own endings are
# nobody's measurement, so it is pinned LF like every gate beside it.
guards/boot-invariants-guard.cjs text eol=lf

# tools/babel-block.cjs (item 32-b) -- the ONE place the shipped text/babel block is cut and the
# JSX runtime is settled. Fifteen files used to carry a copy of its two regexes; they all require
# it now, and index.html is pinned CRLF above. Pinned LF for the same reason as every reader of a
# CRLF file in this list: the tool's own endings are nobody's measurement.
tools/babel-block.cjs text eol=lf

# app.js + tools/build-app.cjs (item 32) -- the generated bundle and the tool that generates it.
# app.js is COMPARED BYTE FOR BYTE against a fresh build by `npm run verify:build` and by the
# gate `babel`, so a checkout that rewrote one ending would fail a gate on a machine that had
# changed nothing -- the defect 40 shape, from the generated-file end. It is built from an
# LF-normalised source and carries no CR at all; this pin is what keeps that true after a clone
# with core.autocrlf=true. The builder is pinned for the same reason every tool beside it is.
app.js                   text eol=lf
tools/build-app.cjs      text eol=lf

# vendor/*.js + app.jsx (item 32) -- MEASURED, and this is defect 40 in its most expensive form.
# index.html now loads vendor/react.umd.js and vendor/react-dom.umd.js with an SRI hash, and a
# hash is cut over BYTES. Both files are stored in git as LF (10751 and 131835 bytes) and both
# were checked out on this machine, with core.autocrlf=true and no pin, carrying 31 and 267
# injected CR (10782 and 132102 bytes). The two states hash differently:
#   vendor/react.umd.js       git  DGyLxAyjq0f9SPpVevD6IgztCFlnMF6oW/XQGmfe+IsZ8TqEiDrcHkMLKI6fiB/Z
#                             disk t63xaoqI4/tnZOZs58Xd/POzyY+r0ZlL3MZJsg5uEpKiDX6mJDAaS/KuesDjH55i
# Cut the hash on the working copy and the browser refuses to run the file that ships; cut it on
# the shipped bytes and the working copy fails every local check. '-text' is what removes the
# question: git stores and checks out these bytes and no ending is ever rewritten. (The git-side
# hashes above are the ones unpkg served for react 18.3.1, byte for byte -- which is how the two
# files got here, and why the integrity attributes in index.html do not change at all.)
#
# app.jsx is the SOURCE the bundle is built from and it is compared by rebuilding, so the same
# rule applies for the same reason; it is LF because the build normalises to LF before Babel.
vendor/*.js              -text
app.jsx                  text eol=lf

# guards/lessons-search-guard.cjs + its fixture + the two modules it drives (the lessons call,
# gate `lessonssearch`). The gate script itself is pinned because recon-audit item 14 FAILS any
# gate that is not; the other three are pinned for the reason every reader-of-text in this list
# is: the guard reads api/lessons-search.js and lib/lessons-source-card.js RAW and mutates them
# at LITERAL ANCHORS to prove its own assertions can fail, and an anchor written with LF endings
# matches nothing in a CRLF checkout -- which is the shape that has already cost this repository
# three green gates. The fixture is byte-compared through JSON.parse and carries Arabic display
# names, so it is pinned beside the guard that reads it.
guards/lessons-search-guard.cjs text eol=lf
guards/fixtures-lessons-search.json text eol=lf
api/lessons-search.js text eol=lf
lib/lessons-source-card.js text eol=lf

# EZIK-CC-LESSONS-CALL-REPORT-2026-08-23.md -- the round report, and the ONE report in this
# tree that seals itself: its last two lines carry PAYLOAD_BYTES and PAYLOAD_SHA256 over every
# byte before them, cut on LF. A clone with core.autocrlf=true would rewrite those bytes and the
# digest would be false of the file carrying it. The earlier EZIK-CC-* reports are unpinned
# because none of them makes a claim about its own bytes; this one does.
EZIK-CC-LESSONS-CALL-REPORT-2026-08-23.md text eol=lf

# fiqhindex is an offline gzip/JSON guard. Pin its source, its Arabic-bearing fixture and the
# self-sealed round report so checkout line-ending conversion cannot change their audit bytes.
guards/fiqh-index-guard.cjs text eol=lf
guards/fixtures-fiqh-index.json text eol=lf
EZIK-CX-B-FIQH-INDEX-144-REPORT-2026-08-23.md text eol=lf
