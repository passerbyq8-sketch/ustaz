# تقريرُ عزك — سطورُ نداءِ خدمةِ الدروس (بلا واجهة)

**الوكيل:** Claude Code · **الشجرة:** `C:\Users\passe\projects\ustaz` · **التاريخ:** ٢٣ أغسطس ٢٠٢٦

---

## ١ · الفرعُ ورؤوسُ الإيداعات

| البند | القيمة |
|---|---|
| نقطةُ البدء | `f0a94f42e6d26e4fa1406e9c3466be1063534e7e` (= `origin/main`) |
| الفرع | `feat/lessons-call-20260823` |
| الإيداع | `fa936c1` — `feat: the lessons search call, with no interface (gate lessonssearch)` |
| عددُ الإيداعات | إيداعٌ واحدٌ جامع |
| `PUSHES` | `0` |

**لماذا إيداعٌ واحدٌ لا ثلاثة.** الأمرُ ترك القرارَ للوكيل واشترط أن يكونَ **كلُّ إيداعٍ قابلًا للنقضِ وحدَه**. الملفّاتُ الثلاثةُ متشابكةٌ في اتّجاهٍ واحد: `api/lessons-search.js` يستوردُ `lib/lessons-source-card.js`، و`gates.json` يسمّي الحارس. فأيُّ قسمةٍ تُنتِجُ إيداعًا لا يُنقَضُ وحدَه — نقضُ إيداعِ الملفّاتِ يتركُ الروسترَ يسمّي حارسًا غائبًا. الإيداعُ الجامعُ يُنقَضُ بأمرٍ واحدٍ ويعودُ بالشجرةِ إلى `f0a94f4` بالضبط.

**حالةُ الشجرةِ عندَ البدء:** `DIRTY_LINES=1` لا `0` — الملفُّ غيرُ المتعقَّبِ `ORDER-EZIK-LESSONS-CALL.md` كان في الجذر. نُقِلَ إلى مجلّدِ العملِ المؤقّتِ خارجَ الشجرةِ قبلَ الإيداعِ وقبلَ تشغيلِ البوّابات، لأنّ `recon` يعُدُّ «working tree clean» بوّابةً بنفسِه ولأنّ `run-gates` يفحصُ اتّساخَ الشجرة. الملفُّ لم يُودَعْ (لا سابقةَ لإيداعِ ملفِّ أمرٍ في هذه الشجرة).

---

## ٢ · الملفّاتُ المكتوبة

| الملفّ | البايتات | البصمة `sha256` |
|---|---:|---|
| `api/lessons-search.js` | 10352 | `f9290d0a41debf53caa5d4d915f8e79cc3e4a1c3b177d76acd7238db8d960c5b` |
| `lib/lessons-source-card.js` | 4941 | `caec4c52419f853cb0f06e4bf9e1c3c7efe450508a1ef7496be55d2209bb05b6` |
| `guards/lessons-search-guard.cjs` | 31608 | `f097a771018ba74f42b0b06e55e33b064377c27939fa05920a655ca58623fca9` |
| `guards/fixtures-lessons-search.json` | 3210 | `71a9d21f5af458b7c980d941173f4f2d2482aa0f6cc0e5544a4a2b30b839e504` |

الفيكستشرُ ملفٌّ رابعٌ نصَّ عليه §٢-٣، وليسَ ملفًّا زائدًا على الثلاثة.

**مخرَجُ الحارس:** `=== 124/124 - PASS ===` · `EXIT=0` · بلا شبكةٍ وبلا توكنٍ حقيقيّ (يُستَبدَلُ `globalThis.fetch` ويُقادُ المُصدَّرُ الحقيقيُّ من الدالّة).

**الفروقُ عن `api/lib-search.js`، وهي الثلاثةُ المسموحةُ وحدَها:**

1. العنوان: `https://lib.ezik.app/lessons/search`.
2. التحقّقُ من عشرةِ حقولٍ في الإصابةِ لا سبعةَ عشر.
3. `snippet` يُنزَعُ من كلِّ إصابةٍ قبلَ إعادتِها.

وما عدا ذلك مَنقولٌ عن النموذج: التحقّقُ من الطريقةِ (405) · قراءةُ الجسمِ بسقفِ 400 حرفٍ على `q` وسقفِ 4 ميغابايت على الردِّ · التوكنُ من `SEARCH_API_TOKEN` (503 عندَ غيابِه، والسببُ في سجلِّ الخادمِ لا في جسمِ العميل) · `AbortSignal.timeout(12000)` · شكلُ الخطأِ (`{ok:false,error:{code,message}}`) · نداءٌ واحدٌ عندَ 401 بلا إعادةِ محاولة.

**ولا زيادةَ على العقد.** الدالّةُ لا تُلحِقُ بطاقةً بالإصابةِ ولا تُضيفُ جملةً إلى الجسم — بخلافِ `api/lib-search.js` التي تُضيفُ `source_card` وثلاثةَ نصوصٍ من البندِ ١٦-ب. الأمرُ حصرَ الفروقَ المسموحةَ في ثلاثةٍ، والزيادةُ ليسَتْ منها؛ فالردُّ هو العقدُ المقيسُ ناقصًا حقلًا واحدًا. البطاقةُ تُبنى في `lib/lessons-source-card.js` ويقودُها الحارسُ وحدَه.

**الأربعةُ التي تحملُها البطاقة:** `title` · `scholar_id` · `url` · `content_type`. **والستّةُ التي لا تحملُها:** `snippet` · `unit_id` · `tier` · `usage` · `citation_allowed` · `score` — وكلُّها مسمّاةٌ في المُصدَّرِ `CARD_FORBIDDEN_FIELDS` ليَعُدَّها الحارسُ من قائمةٍ لا من عادة.

---

## ٣ · جدولُ التأكيداتِ السبعة

| # | التأكيدُ كما نصَّ عليه الأمر | القسمُ في الحارس | عددُ الفحوص | الحال |
|---|---|---|---:|---|
| 1 | `api/lessons-search.js` لا يُعيدُ `snippet` ولو أرسلَتْه الخدمةُ مملوءًا | `1.` | 12 | PASS |
| 2 | البطاقةُ لا تحملُ إلّا الأربعةَ — تُعَدُّ مفاتيحُها وتُقارَنُ بالقائمةِ حرفًا | `2.` | 15 | PASS |
| 3 | البطاقةُ لا تخترعُ قيمةً حينَ يغيبُ حقل | `3.` | 12 | PASS |
| 4 | `content_type` خارجَ الإحدى عشرةَ يُعامَلُ كغياب | `4.` | 21 | PASS |
| 5 | صفرُ ذكرٍ لعنوانِ الخدمةِ أو للملفَّينِ الجديدَينِ داخلَ `index.html` | `5.` | 12 | PASS |
| 6 | صفرُ مساسٍ بملفّاتِ الشاملةِ الأربعة — بصماتٌ قبلَ وبعدُ وتُطبَع | `6.` | 9 | PASS |
| 7 | التوكنُ لا يُطبَعُ ولا يُسجَّلُ في أيِّ مسارٍ من مساراتِ الخطأ | `7.` | 13 | PASS |
| — | العقدُ المقيسُ (عشرةُ حقولٍ · أحدَ عشرَ صنفًا · تسعةٌ تعود) | `A.` | 8 | PASS |
| — | النداءُ على السلك (العنوان · الترويسة · الجسم · سقفُ العشرة) | `B.` | 14 | PASS |
| — | شكلُ الردِّ الواصلِ إلى العميل (تسعةٌ لكلِّ إصابةٍ، بلا زيادة) | `C.` | 8 | PASS |
| | **المجموع** | | **124** | **124/124** |

### تفصيلُ ما يُقاسُ في كلِّ تأكيدٍ من السبعة

**١ — المقطع.** الفيكستشرُ يحملُ ثلاثَ إصاباتٍ: واحدةٌ بمقطعٍ **مملوءٍ** (`SNIPPET-FIXTURE-FILLED-DO-NOT-DISPLAY`)، وواحدةٌ بمقطعٍ **فارغٍ** (وهي حالةُ ٩٧٫٦٪)، وثالثةٌ **بحقولٍ غائبة**. يُقادُ المُعالِجُ الحقيقيُّ عليها فيُؤكَّد: لا إصابةَ تحملُ مفتاحًا اسمُه `snippet`، ونصُّ المقطعِ ليسَ في جسمِ الردِّ ولا في أيِّ سطرِ سجلٍّ، وبقيّةُ الحقولِ التسعةِ عبرَتْ (نزعٌ لا بترٌ). وقبلَ ذلك يُؤكَّدُ أنّ الفيكستشرَ **فعلًا** يحملُ مقطعًا مملوءًا، فلا يقيسُ التأكيدُ عدمًا.
**والمُطفِّر:** يُستبدَلُ `HIT_FIELDS.filter((f) => f !== DROPPED_HIT_FIELD)` بـ`HIT_FIELDS.filter((f) => true)` في نسخةٍ في الذاكرة؛ يُتحقَّقُ أوّلًا أنّ الطفرةَ **غيّرَتِ المصدرَ فعلًا** (طفرةٌ فارغةٌ تُبلِّغُ PASS كاذبًا)، ثمّ يُبرهَنُ أنّ الدالّةَ المُطفَّرةَ تُعيدُ المقطعَ — أي أنّ التأكيدَ يعَضُّ — ثمّ يُعادُ قراءةُ الملفِّ من القرصِ ويُقارَنُ فيَظهرُ أنّه لم يُمَسّ.

**٢ — أربعةٌ لا خامسَ.** `Object.keys(card).sort()` يُقارَنُ بـ`CARD_FIELDS.sort()` حرفًا، ويُؤكَّدُ أنّ العددَ أربعةٌ. ثمّ لكلِّ اسمٍ من الستّةِ الممنوعةِ فحصانِ في تأكيدٍ واحد: أنّه غائبٌ عن البطاقةِ **وأنّ الإصابةَ كانت تحملُه** — فلا تمرُّ البطاقةُ لأنّ المصدرَ كان خاليًا.

**٣ — الغيابُ يبقى غيابًا.** إصابةٌ بلا `scholar_id` ولا `url` ولا `content_type`: يُؤكَّدُ أنّ الفيكستشرَ فعلًا يفتقدُها وفعلًا يحملُ `title`، ثمّ أنّ البطاقةَ لا تحملُ مفاتيحَها ألبتّة، وأنّ لا قيمةَ استُبدِلَتْ بها (لا نصٌّ فارغٌ ولا `null` ولا نائبٌ). و`url: null` غيابٌ أيضًا لا قيمة.
**والمُطفِّر:** `carry(card, hit, 'url')` يُستبدَلُ بإسنادٍ يضعُ نصًّا فارغًا عندَ الغياب، فيُبرهَنُ أنّ المُطفَّرَ يخترعُ `url` وأنّ التأكيدَ كان سيسقط.

**٤ — الصنفُ خارجَ الإحدى عشرة.** الأحدَ عشرَ صنفًا مقيسةٌ اسمًا اسمًا (أحدَ عشرَ فحصًا)، ثمّ صنفٌ مصطنعٌ خارجَها (`khutbah_series`) يُنتِجُ بطاقةً **بلا مفتاحِ `content_type`** واسمُه ليسَ في البطاقة، وبقيّةُ البطاقةِ تُبنى. وصنفٌ ليسَ نصًّا (عددٌ) غيابٌ كذلك. ويُؤكَّدُ أنّ صنفًا مقيسًا **يصلُ** إلى البطاقة — فالقاعدةُ مُرشِّحٌ لا حذف. ويُؤكَّدُ أنّ لا ترجمةَ عربيّةً عُلِّقَتْ بصنفٍ في أيٍّ من الملفَّين (§٤).
**والمُطفِّر:** يُستبدَلُ الشرطُ بـ`carry` مباشرةً، فيُبرهَنُ أنّ المُطفَّرَ يكرّرُ اسمَ صنفٍ لا تضمنُه هذه الشجرة.

**٥ — بلا واجهة.** `index.html` يُقرأُ ويُؤكَّدُ أوّلًا أنّه قُرِئَ فعلًا (`length > 100000`)، ثمّ أنّه لا يذكرُ `lessons/search` ولا `lessons-search` ولا `lessons-source-card` ولا `lib.ezik.app/lessons`. والقاعدةُ نفسُها مطبَّقةٌ على `app.jsx` و`app.js` و`quest.html` — لأنّ نصَّ الصفحةِ نُقِلَ من `index.html` في الجولةِ ٢٨، فحصرُ الفحصِ في ملفٍّ واحدٍ يجعلُه قولًا عن ملفٍّ لا عن الواجهة. و`sw.js` لا يذكرُ `lessons` أيضًا.

**٦ — ملفّاتُ الشاملةِ الأربعة.** تُقاسُ بصماتُها في أوّلِ سطرٍ من `main()` قبلَ استيرادِ أيِّ وحدةٍ، وتُقاسُ ثانيةً بعدَ الأقسامِ كلِّها ومنها الأقسامُ التي **تُطفِّرُ مصادرَ في الذاكرة**، وتُطبَعُ الاثنتانِ في مخرَجِ الحارس. ويُؤكَّدُ زيادةً أنّ أيًّا من الأربعةِ لا يذكرُ أسماءَ هذه الجولة، وأنّ الملفَّينِ الجديدَينِ لا يستوردانِ ولا يقرآنِ أيًّا منها.

**٧ — التوكن.** تُقادُ ثمانيةُ مساراتٍ: ردٌّ سليمٌ · توكنٌ غائبٌ · 401 · 500 · خدمةٌ لا تُبلَغُ · جسمٌ غيرُ مقروء · طريقةٌ غيرُ POST · بلا `q`. ويُؤكَّدُ أنّ التوكنَ ليسَ في جسمٍ ولا ترويسةٍ ولا سطرِ سجلٍّ في أيٍّ منها، وأنّ التوكنَ المزروعَ في حقلٍ زائدٍ قد ترسلُه الخدمةُ يومًا (`server_token_hint`) يُسقَطُ ولا يُمَرَّر، وأنّ لا جسمَ يسمّي `SEARCH_API_TOKEN` ولا `lib.ezik.app`.
**وفحصٌ بنيويٌّ فوقَ ذلك:** يُمسَحُ الملفُّ بماسحِ الشجرةِ نفسِه (`guards/telemetry-scan-lib.cjs`) فتُستخرَجُ نداءاتُ `console` كلُّها، ويُؤكَّدُ أنّ عددَها ≥ 5 وأنّها مغلقةٌ (شرطٌ موجَبٌ يمنعُ التأكيدَ من المرورِ لأنّه لم يقرأْ شيئًا)، ثمّ أنّ **لا نداءَ منها يقرأُ** `token` ولا `authorization` ولا `Bearer` ولا `AUTH_`. وهذا يغطّي مساراتٍ لا يبلغُها فيكستشر.

---

## ٤ · روسترُ البوّابات

| | قبل | بعد |
|---|---:|---:|
| عددُ المدخلاتِ في `gates.json` | 90 | 91 |
| `SUITE` | 90/90 | **91/91 · EXIT=0** |
| `GATES_EXIT` | — | **0** |
| `recon` | PASS=181 WARN=1 FAIL=0 | **PASS=183 WARN=1 FAIL=0** |
| اتّساخُ الشجرةِ بعدَ الجولة | — | `tree after: 0 dirty path(s)` |

**البوّابةُ المضافةُ:** `lessonssearch` → `guards/lessons-search-guard.cjs` (بلا وسائط). ولا بوّابةَ سقطَتْ ولا حُذِفَتْ ولا غُيِّرَ اسمُها.

**والبوّابةُ الواحدةُ كلّفَتْ ستّةَ ملفّاتٍ لا ملفًّا واحدًا** — قِيسَ كلُّ واحدٍ منها، وأربعةٌ منها لا يذكرُ أحدُها الآخر:

| # | الملفّ | التغيير | ما يحدثُ بدونِه |
|---|---|---|---|
| 1 | `gates.json` | المدخلُ نفسُه | البوّابةُ لا تُشغَّل |
| 2 | `recon-audit.cjs` | `GATES_EXPECTED` 90 ← 91 | `recon` يسقطُ: «expected exactly 90 — update GATES_EXPECTED deliberately» |
| 3 | `.gitattributes` | خمسةُ تثبيتاتِ `text eol=lf` | `recon` يسقطُ: «gate script not pinned» |
| 4 | — | `git add` للحارس | `recon` يسقطُ: «gate script not tracked in git» |
| 5 | `guards/stored-deen-sub-suite.cjs` | `names.length === 90` ← `91` وسطرا مُطفِّرَين | **`takhrij` يسقطُ EXIT=1** ولا شيءَ في مخرَجِه يذكرُ عددَ البوّابات (يُستدعى فرعيًّا) |
| 6 | `EZIK-RFC-V0.5-R2-IMPLEMENTATION-REPORT.md` | `All 91 gates` · سطرُ الروسترِ `lessonssearch 0` · `TOTAL_GATES 91/91` · `RECON PASS=182` | `recon` بندُ 16 يسقطُ: «gate roster omits lessonssearch; gate-count prose is stale; TOTAL_GATES is stale; RECON summary is stale» |

**عن `RECON PASS=182` في وثيقةِ التنفيذ:** بندُ 16 يقارنُ السطرَ بمجموعِ `recon` **قبلَ فحصِ البندِ نفسِه**، لا بالمجموعِ النهائيّ. المجموعُ النهائيُّ على شجرةٍ نظيفةٍ هو 183، ومنه واحدٌ هو نجاحُ البندِ 16 ذاتِه. والفرقُ عن 181 السابقةِ سببُه واحد: بندُ 14 يُصدِرُ PASS **لكلِّ بوّابةٍ** على حِدَة، فالبوّابةُ الحاديةُ والتسعونَ تزيدُ المجموعَ واحدًا.

**`.gitattributes` — أربعةُ تثبيتاتٍ لا واحد:** الحارسُ لأنّ `recon` يشترطُه، والفيكستشرُ والملفّانِ الجديدانِ لأنّ الحارسَ يقرأُ `api/lessons-search.js` و`lib/lessons-source-card.js` **نصًّا خامًا** ويطفّرُهما عندَ مراسٍ حرفيّةٍ (`carry(card, hit, 'url');` وأمثالِها)، ومرسًى مكتوبٌ بنهاياتِ LF لا يطابقُ شيئًا في نسخةٍ مسحوبةٍ بـCRLF. (ملفّاتُ البندِ ١٦-أ الأربعةُ غيرُ مثبَّتةٍ في `.gitattributes` لأنّ حارسَها ليسَ بوّابةً؛ قِيسَ ذلك ولم يُفترَض.) والتثبيتُ الخامسُ لهذا التقريرِ نفسِه: سطراه الأخيرانِ بصمةٌ على بايتاتِه قُطِعَتْ على LF، ونسخةٌ مسحوبةٌ بـCRLF تجعلُ البصمةَ كاذبةً على الملفِّ الحاملِ لها. وتقاريرُ `EZIK-CC-*` السابقةُ غيرُ مثبَّتةٍ لأنّ أيًّا منها لا يدّعي شيئًا عن بايتاتِ نفسِه.

**`guards/stored-deen-sub-suite.cjs` وحدَه بنهاياتِ CRLF** في شجرةٍ كلُّها LF (738 سطرًا، `LF == CRLF == 738` بعدَ التعديل). حُرِّرَ بـNode مع الحفاظِ على النهايات؛ تحريرُه بـLF يترُكُه مختلطًا فيُنذِرُ `recon` وينقصُ PASS واحدًا.

---

## ٥ · `CORE_BYTES`

| | قبل | بعد |
|---|---:|---:|
| المُعلَنُ في `sw.js` | 1781243 | 1781243 |
| المقيسُ على القرص | 1781243 | 1781243 |

**لا تغيير.** `CORE` في `sw.js` يسمّي عشرةَ مدخلاتٍ: `/` (= `index.html`) · `manifest.json` · أربعةُ أيقوناتٍ · `adhkar.json` · `app.js` · ملفّا `vendor`. ولا ملفَّ من ملفّاتِ هذه الجولةِ داخلٌ فيها: `api/` و`lib/` و`guards/` هي النصفُ الخادمُ ولا تدخلُ `CORE` أصلًا. قِيسَ بـ`node tools/core-bytes.cjs` قبلَ وبعدُ، وأجابَ `MATCH` في المرّتَين.

---

## ٦ · بصمةُ `index.html`

| | البصمة `sha256` | البايتات |
|---|---|---:|
| **قبل** | `0933a9cb9e48823dae6589e1d6720f606a261b55064520345a49abab959bf99a` | 120617 |
| **بعد** | `0933a9cb9e48823dae6589e1d6720f606a261b55064520345a49abab959bf99a` | 120617 |

`git diff --name-only origin/main HEAD -- index.html api/ask.js` ⟵ **فارغ**.

---

## ٧ · بصماتُ ملفّاتِ الشاملةِ الأربعة

| الملفّ | البصمةُ قبلَ = بعدَ | البايتات |
|---|---|---:|
| `api/lib-search.js` | `cd9d370aaeaa8ca84c0b06db64bf0227d5cfc1a3638268e84fa083aba9142bee` | 13452 |
| `lib/lib-source-card.js` | `44d5810eb8a6ab53ca1c2019ca578638357cdb502b40dff43d5f6e65b541dee1` | 7789 |
| `guards/lib-search-16a-guard.cjs` | `97d155bd1808028db1ad33d38c5ba9298c5f750c27cf828ec269620ed63bb57a` | 42057 |
| `guards/fixtures-lib-search-16a.json` | `68a54224a64c48e92cb091df2ddecd12dc92a51a984aea09039dbf26b9aa69d7` | 4384 |

البصمتانِ متطابقتانِ في كلِّ صفٍّ — قِيسَتَا قبلَ العملِ وبعدَ الإيداع، ويقيسُهما الحارسُ مرّةً ثالثةً في كلِّ تشغيلٍ ويطبعُهما. و`git diff --name-only origin/main HEAD` لا يذكرُ أيًّا منها.

---

## ٨ · ملاحظاتٌ نصَّ الأمرُ على كتابتِها لا على تنفيذِها

**`vercel.json`:** لا مدخلَ للدالّةِ الجديدة، تماثلًا مع `api/lib-search.js` التي لا مدخلَ لها اليوم. **وملاحظةٌ عن السقفِ الافتراضيّ:** مهلةُ الدالّةِ نفسِها مضبوطةٌ داخلَها بـ`AbortSignal.timeout(12000)` — اثنتا عشرةَ ثانية. والسقفُ الافتراضيُّ لمهلةِ دالّةِ Vercel على خطّةٍ مدفوعةٍ **لم يُقَسْ في هذه الجولة** (لا نداءَ حيًّا ولا قراءةَ لضبطِ المشروع)، فإن كان أقلَّ من اثنتَي عشرةَ ثانيةً فالمهلةُ الداخليّةُ لا تُبلَغُ أبدًا وتُقطَعُ الدالّةُ من فوق. لم يُعدَّلْ شيء.

**الترجمة:** لا ترجمةَ عربيّةً لأيٍّ من أصنافِ `content_type` الأحدَ عشرَ في أيِّ ملفٍّ من ملفّاتِ الجولة، ويؤكِّدُ الحارسُ ذلك بفحصٍ صريح.

**نصُّ الدرس:** لا نصَّ درسٍ في ملفٍّ ولا في هذا التقرير. كلُّ قيمةٍ في الفيكستشرِ مصطنعةٌ بيّنةُ الاصطناع: أسماءُ المشايخِ نوّابٌ ظاهرون (`شيخٌ مصطنعٌ ألف` … `واو`)، والعناوينُ مثلُها، والمقاطعُ سلسلةٌ لاتينيّةٌ صارخةٌ واحدة، والعناوينُ الشبكيّةُ كلُّها على `example.invalid`.

**الشبكة:** صفرُ نداءٍ حيٍّ إلى الخدمة. الحارسُ يستبدلُ `globalThis.fetch` — وهو السَّيمُ الوحيدُ الذي تمرُّ منه كلُّ نداءاتِ هذه الشجرةِ الخارجة — ويُحصي كلَّ نداءٍ وترويسةً وجسمًا. والتوكنُ في الاختباراتِ `tk-lsn-7`: ثمانيةُ محارفَ عمدًا، لأنّ ماسحَ الأسرارِ في `recon` يعُدُّ الحرفيَّ الشبيهَ بالتوكنِ من ستّةَ عشرَ محرفًا فأكثرَ اعتمادًا مسرَّبًا ويُحمِّرُ بوّابةً على فيكستشر.

**البيئة:** لم يُنشَأْ متغيّرُ بيئةٍ جديد. الدالّةُ تقرأُ `SEARCH_API_TOKEN` نفسَه الذي يقرأُه `api/lib-search.js` اليوم. ولم يُمَسَّ ضبطُ Vercel.

---

## ٩ · ما لم يُقَسْ

هذه أشياءُ لم تُقَسْ في هذه الجولةِ ولا يُبنى عليها:

1. **الخدمةُ نفسُها لم تُنادَ ولا مرّةً.** كلُّ ما في هذا التقريرِ عن العقدِ منقولٌ عن نصِّ الأمرِ لا عن ردٍّ حيّ. النسبتانِ ٩٧٫٦٪ و٢٫٤٪ لامتلاءِ `snippet` منقولتانِ عن الأمرِ ولم تُتحقَّقا هنا. وكذلك `MAX_LIMIT=10` في الخدمة.
2. **مِظرافُ الردِّ غيرُ مقيسٍ إلّا في حقلٍ واحد.** الأمرُ سمّى `hits` وحدَه على المستوى الأعلى، فـ`RESPONSE_FIELDS` في الدالّةِ اسمٌ واحدٌ لا أحدَ عشرَ كما في النموذج. أيُّ حقلٍ آخرَ ترسلُه الخدمةُ فوقَ `hits` — عددٌ إجماليٌّ، صفحةٌ، سببُ اقتضاب — **يُسقَطُ صامتًا اليوم**، وهذا هو حكمُ النموذجِ في المجهول. إن كان في المِظرافِ حقلٌ يجبُ أن يعبرَ فهو غيرُ معروفٍ هنا ويحتاجُ قياسًا.
3. **نصُّ خطأِ الاتّصالِ يُسجَّلُ حرفيًّا.** في مسارِ «الخدمةُ لا تُبلَغ» ومسارِ «جسمٌ غيرُ مقروء» يُكتَبُ `String(error.message)` في سجلِّ الخادمِ كما هو — وهو ما يفعلُه `api/lib-search.js` حرفًا. فلو حملَ نصُّ خطأٍ من الطبقةِ الأدنى قيمةَ التوكنِ يومًا لعبرَ إلى السجلّ. لم يُقَسْ أنّ ذلك يقعُ (رسائلُ `fetch` تحملُ العنوانَ لا الترويسات)، **ولم يُعالَجْ** لأنّ الأمرَ حصرَ الفروقَ عن النموذجِ في ثلاثةٍ وليسَ التنقيةُ منها. التأكيدُ السابعُ يُغطّي هذا بنيويًّا من الجهةِ الأخرى: لا نداءَ `console` في الدالّةِ يقرأُ التوكنَ أو ترويسةَ التوثيقِ أصلًا.
4. **لا شيءَ يستدعي الدالّةَ.** الجولةُ إضافيّةٌ محضة: لا مسارَ جوابٍ ولا واجهةَ تنادي `/api/lessons-search`. وهذا مؤكَّدٌ لا مفترَض (التأكيدُ الخامس).
5. **`vercel.json`** — سقفُ المهلةِ الافتراضيّ، كما في §٨.
6. **النشرُ لم يُجرَّب.** لا `push` ولا نشر، فلم يُقَسْ سلوكُ الدالّةِ على Vercel ولا حجمُ الحزمةِ بعدَ إضافتِها.
7. **`DIRTY_LINES=0` المذكورُ في الأمرِ لم يكنْ صحيحًا عندَ البدء** — كان `1` بملفِّ الأمرِ نفسِه. عولِجَ بنقلِ الملفِّ خارجَ الشجرة، ولم يُقَسْ ما إذا كان المالكُ يريدُ إيداعَه.

---

## ١٠ · الأوامرُ التي تُعيدُ إنتاجَ هذا

```
git checkout feat/lessons-call-20260823
node guards/lessons-search-guard.cjs      # 124/124 - PASS, EXIT=0
node tools/run-gates.cjs                  # SUITE: 91/91 EXIT=0, GATES_EXIT=0
node recon-audit.cjs                      # SUMMARY PASS=183 WARN=1 FAIL=0
node tools/core-bytes.cjs                 # MATCH, 1781243
git diff --name-only origin/main HEAD -- index.html api/ask.js api/lib-search.js \
  lib/lib-source-card.js guards/lib-search-16a-guard.cjs guards/fixtures-lib-search-16a.json
```

`PUSHES=0`

---
PAYLOAD_BYTES=24551
PAYLOAD_SHA256=667e38500578eeb688049b7cf6a3bb7a69da341791dae59ddd69c7bcc2b70fdb
