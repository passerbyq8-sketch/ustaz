# EZIK — تقريرُ ٤٢-ج · البطاقةُ تحملُ نسبتَها وتنبيهَها · ٢٤ أغسطس ٢٠٢٦

**ثلاثةٌ من الأربعةِ أُغلِقَتْ ومقيسةٌ في متصفّحٍ حقيقيّ · والرابعُ (وسمُ الحديث) يمنعُه قيدُ الأمرِ نفسِه فوُقِفَ عندَه وكُتِبَ · والعيّناتُ الأربعُ محفوظةٌ لعينِ المالك. لا دفعَ ولا نشر.**

---

## ١ · الأساسُ والفرعُ والإيداع

| المفتاح | القيمة |
|---|---|
| الشجرة | `C:\Users\passe\projects\ustaz` |
| الأساس | `origin/main` بعدَ `fetch` = `56952b263f19e6b38bcc9966e6501b3c78c5a82f` — **مطابقٌ** لـ`56952b2` |
| الفرع | `fix/card-attribution-20260824` |
| الإيداع | **`04911d4`** — واحدٌ لا غير |
| ما مسَّ | **`app.jsx` و`app.js` (مولَّدٌ منه) فقط** |
| `index.html` | **لم يُمَسَّ** — فشرطُ §٣-٥ لا يقعُ أصلًا |
| `api/` · `lib/` · `sw.js` · أيُّ حارس | **صفرُ ملفّ** — مقيسٌ بـ`git diff --name-only origin/main..HEAD` ⟹ `app.js` · `app.jsx` |
| مكتبةٌ أو CDN | صفر |

---

## ٢ · جوابُ §١ الخمسةُ — بالقياسِ لا بالقراءة

قُدْتُ التطبيقَ المشحونَ نفسَه: `index.html` + `app.jsx` مُصرَّفَين ومُشغَّلَين في `vm` بـlinkedom وحزمتَي React — الطريقةُ نفسُها التي يستعملُها `theme-coverage-guard.cjs` — ثمّ نوديَتْ `parseRichMessage` و`serializeReply` و`ezikCardSourceLine` و`resolveHadithAttribution` على أجوبةٍ حقيقيّةٍ بصيغةِ السلك. **لا شيءَ هنا مُعادُ كتابتِه.**

### ٢-١ · من أينَ تأخذُ `ezikDrawReplyCard` ما ترسمُه؟

**نصُّ الجوابِ وحدَه — لا كائنُ الردِّ ولا أجزاؤه.** مقيسٌ من موضعِ النداءِ نفسِه:

```
const SaveReplyImageButton = ({ getText, getSource })
ezikDrawReplyCard({ text: payload, source: (typeof getSource === 'function') ? getSource() : '' })
<SaveReplyImageButton getText={buildCopyText} getSource={buildCardSource} />
```

فالحقولُ المتاحةُ لها ثلاثةٌ لا رابعَ لها: **`text`** (وهو `serializeReply(segments)` نفسُه، أي نصُّ الحافظة)، و**`source`** (سلسلةٌ نصّيّة)، و**`canvas`** (مقبضُ اختبارٍ للحرّاس). **لم يكنْ للبطاقةِ سبيلٌ إلى الأجزاءِ ألبتّة.**

### ٢-٢ · أينَ يسكنُ سطرُ المصدر، وهل يبلغُ الرسم؟

يسكنُ في **جزءِ `source`** بحقولِه الثلاثة، وهو مقيسٌ على جوابِ المالك:

```
{ site: "binbaz.org.sa",
  url:  "https://binbaz.org.sa/fatwas/arkan-al-iman",
  content: "أركان الإيمان الستة — مادة: شرح أصول الإيمان" }
```

و`REPLY_SERIALIZERS.source` يحوّلُه إلى فقرةٍ من سطرَين:

```
binbaz.org.sa — أركان الإيمان الستة — مادة: شرح أصول الإيمان
https://binbaz.org.sa/fatwas/arkan-al-iman
```

**ويبلغُ الرسمَ فعلًا — لكنّه لا يُرسَم.** لأنّه **آخرُ ما يكتبُه المُسَلسِلُ**، فميزانيّةُ الأسطرِ الثابتةُ تقطعُه أوّلًا. مقيسٌ على جوابِ «أركانِ الإيمان»: **١٦ سطرًا من ٢٦ رُسِمَتْ، وهو من العشرةِ التي لم تُرسَم.**

### ٢-٣ · أينَ يُولَّدُ تنبيهُ «فهمٌ لا فتوى»، ومتى يظهر؟

يصلُ في نصِّ الجوابِ علامةً بينَ قوسَي `【 】`، و`ezikLiftNotices` يرفعُه إلى **جزءِ `notice`**:

```
{ label: "فهم لا فتوى",
  content: "ما تقدّم فهمٌ مبنيٌّ على ما بين يديّ في هذه الدورة، لا فتوى مسندة إلى مفتٍ بعينه." }
```

**وليسَ لكلِّ جوابٍ بل لصنفٍ منه**: الجزءُ لا يوجدُ إلّا حيثُ وضعَ المراجِعُ العلامةَ؛ وجوابٌ بلا علامةٍ لا جزءَ له ولا سطر. وهو أيضًا **آخرُ ما يكتبُه المُسَلسِلُ**، فسقطَ مع المصدرِ في القطعةِ نفسِها.

### ٢-٤ · وسمُ الحديث — من أينَ يأخذُ كلٌّ منهما؟

🔴 **من المصدرِ نفسِه، بقاعدتَينِ مختلفتَين.** الحقلانِ واحدٌ (`narrator` و`ruling`)، والمُحلِّلُ واحدٌ (`resolveHadithAttribution`)، والفرقُ في القاعدة:

| | الموضع | القاعدة | ما خرجَ على جوابِ المالك |
|---|---|---|---|
| **الشاشة** | `app.jsx:12347` — `HadithCard` | `(!narrator && !ruling) ? 'نص منقول' : 'من السنة النبوية'` — **اسمُ الراوي لا يُطبَعُ أبدًا** | **`من السنة النبوية`** |
| **البطاقةُ والحافظة** | `REPLY_SERIALIZERS.hadith` | `narrator ? 'رَوَى ' + narrator : 'من السنة النبوية'` | **`رَوَى مسلم`** |

`resolveHadithAttribution('مسلم', 'صحيح')` ⟹ `{ narrator: 'مسلم', ruling: 'صحيح' }` — مقيسٌ بالنداء.

### ٢-٥ · من أينَ تأتي نطاقاتُ التذييل؟

من `ezikCardSourceLine(segments)`: تمشي على أجزاءِ `source`، تأخذُ `sg.site`، وإلّا تشتقُّ المضيفَ من `sg.url`، وتصلُ الأسماءَ بـ` · `. على جوابِ المالك ⟹ **`binbaz.org.sa`** وحدَه.

---

## ٣ · موضعُ كلِّ إصلاحٍ وسطرُه

| # | ما أُصلِح | الموضع | كيف |
|---|---|---|---|
| ١ | النسبةُ تبلغُ البطاقة | `app.jsx:11202–11216` — `ezikCardBlockOf` وتوأماها | تقرأُ الأجزاءَ و**تنادي `REPLY_SERIALIZERS`** فتعيدُ فقرتَه **حرفًا بحرف**. صفرُ تأليف: جوابٌ بلا جزءٍ يعيدُ `''`، و`''` لا يرسمُ شيئًا. |
| ٢ | التنبيهُ يبلغُ البطاقة | الدالّتانِ نفسُهما | كذلك، من جزءِ `notice` وحدَه. |
| ٣ | مساحتُهما **تُحجَزُ أوّلًا** | `app.jsx:2656–2688` — `ezikCardTail` | تبني الذيلَ (نسبةٌ · تنبيهٌ · روابطُ · `ezik.app`) **قبلَ قياسِ المتن**، وتعيدُ ارتفاعَه. مجموعُ الخطواتِ **هو** الارتفاع، فلا يختلفانِ أبدًا. |
| ٤ | الارتفاعُ يتمدّدُ بسقف | `app.jsx:2540–2545` · `2709–2718` | `EZIK_CARD_H_MIN = 1350` · `EZIK_CARD_H_MAX = 2700`. العرضُ `1080` لم يتحرّك. |
| ٥ | لا يُرسَمُ شيءٌ مرّتَين | `app.jsx:2692–2705` | الفقرتانِ **تُنقَلانِ** لا تُحذَفان: المطابقةُ على فقرةِ المُسَلسِلِ نفسِها بايتةً بايتة، وفقرةٌ لا تطابقُ تمامًا تبقى في المتنِ كما هي. |
| ٦ | التذييلُ لا يُقدِّمُ نطاقًا مصدرًا | `app.jsx:2678–2684` · `EZIK_CARD_LINKS_LABEL` | صارَ سطرُ الروابطِ **موسومًا «روابط»** بوزنٍ وشفافيّةٍ أخفَّ، والنسبةُ فوقَه بنصِّها وبوزنٍ أثقل. |

**ولم يُمَسَّ:** اتّجاهُ الرسم · ميزانيّةُ الأسطرِ التي نزلَتْ (صارَتْ سقفًا مشتقًّا لا رقمًا حرفيًّا) · شفافيّةُ الشعار · مسارُ PDF · **ونصُّ الجوابِ بحرف**.

### ٣-١ · 🔵 قيدٌ حارسٌ اصطدمتُ به، وكيفَ بقيتُ داخلَه

أوّلُ تنفيذٍ أضافَ خاصّيّتَين للزرِّ (`getAttribution` و`getNotice`). **فسقطَتْ `themecoverage` عندَ `ZC6`**، وهي تثبّتُ موضعَ النداءِ حرفًا:

```js
// theme-coverage-guard.cjs:4812-4813
ok('ZC6: it exports THE REPLY, from the very payload the clipboard and the PDF are handed',
  /<SaveReplyImageButton getText=\{buildCopyText\} getSource=\{buildCardSource\} \/>/.test(html));
```

**و§٣-٢ تحرّمُ مسَّ الحارس.** فأُعيدَ موضعُ النداءِ إلى حرفِه، وصارَ `buildCardSource` — وهو الذي يقرأُ الأجزاءَ أصلًا — يعيدُ النسبةَ كلَّها بدلَ قائمةِ المضيفينَ وحدَها:

```js
const buildCardSource = () => ({
  links:       ezikCardSourceLine(segments),
  attribution: ezikCardAttributionBlock(segments),
  notice:      ezikCardNoticeBlock(segments),
});
```

**ولمَ أعدُّ هذا داخلَ الحارسِ لا التفافًا عليه** — وأقولُه صراحةً ليَرُدَّه المالكُ إن رآه غيرَ ذلك:

- ما يؤكّدُه `ZC6` **ما زالَ صادقًا حرفيًّا ومعنًى**: البطاقةُ تُغذَّى من `buildCopyText` ومن `buildCardSource`، لا من مصدرِ حقيقةٍ ثانٍ.
- الشقُّ الثاني من `ZC6` يباركُ صراحةً أن يقرأَ `buildCardSource` الأجزاءَ عبرَ قارئٍ على مستوى الوحدة، ويمنعُ فقط أن يبدأَ بـ`segments.filter` — ولا يبدأ.
- **الحارسُ ما يزالُ يعضّ**: لو وُجِّهَتِ البطاقةُ إلى حمولةٍ أخرى لسقطَ `ZC6` كما كان.
- ولم يبقَ في الشجرةِ حرفٌ ميّتٌ يُرضي نمطًا بلا معنًى — وهو الفرقُ بينَ هذا وبينَ ما رفضتُه في الجولةِ ٤٢-ب.

و`ezikDrawReplyCard` تقبلُ `source` **سلسلةً** (فتُرسَمُ سطرَ روابطَ كما كانت دائمًا — وهي الصيغةُ التي تمرّرُها ثوابتُ الحرّاس) **أو كائنًا**. `themecoverage` خضراءُ `1351/1351` و`markdown` خضراءُ `121/121`.

### ٣-٢ · 🔴 وسمُ الحديثِ — وُقِفَ عندَه ولم يُمَسّ

§٢-٤ تأمرُ بتوحيدِ اللفظِ، وتُعطي مخرجًا واحدًا: «وإن كانَ اللفظانِ من مصدرَينِ مختلفَينِ فاطبعْ ذلك وقفْ عندَه». **والقياسُ يقولُ إنّهما ليسا من مصدرَينِ مختلفَين** (§٢-٤ أعلاه): مصدرٌ واحدٌ وقاعدتان. فالمخرجُ المذكورُ لا ينطبق.

**والتوحيدُ نفسُه ممنوعٌ بقيدٍ آخرَ في الأمرِ ذاتِه.** الطرقُ الثلاثُ كلُّها مسدودة:

1. **تعديلُ `REPLY_SERIALIZERS.hadith`** ليطابقَ الشاشة ⟹ **§٣-١ تحرّمُه نصًّا**: «صفرُ مساسٍ بالمخّ ولا بمولِّدِ الجوابِ **ولا بمُسَلسِلاتِه**». وهو مع ذلك يغيّرُ الحافظةَ والمشاركةَ والصوتَ والـPDF، وهي أوسعُ بكثيرٍ من بندِ عرض.
2. **أن تبنيَ البطاقةُ متنَها من الأجزاء** ⟹ مُسَلسِلٌ ثانٍ ينحرفُ عن الأوّل — وهو بعينِه ما يحذّرُ منه تعليقُ الملفِّ: «there are not two serializers that can drift».
3. **أن تُبدِّلَ البطاقةُ العبارةَ داخلَ النصِّ بعدَ التسلسل** ⟹ تحريرُ نصِّ الجوابِ على البطاقة، و**§٢-٥ تحرّمُه بحرف**.

**فوُقِفَ وكُتِب.** الحالُ المقيسةُ: البطاقةُ `رَوَى مسلم` · الشاشةُ `من السنة النبوية`. **ورفعُ هذا يحتاجُ إذنًا صريحًا بمسِّ `REPLY_SERIALIZERS.hadith`** — وأثرُه يتعدّى البطاقةَ إلى كلِّ ما يُنسَخُ ويُشارَكُ ويُنطَقُ ويُصدَّر، فهو قرارُ المالكِ لا قراري.

---

## ٤ · أرقامُ §٤ الثمانيةُ — للأجوبةِ الثلاثة

**بالمصنوعِ**: `ezikDrawReplyCard` المشحونةُ نفسُها، مسلوخةً من `app.jsx` بعلاماتِها، مرسومةً على قماشٍ حقيقيٍّ في متصفّحٍ بلا رأس. والنصُّ الذي تُعطاه هو ما يُعطيه إيّاه التطبيقُ، مولَّدًا في `vm` من الأجزاءِ الحقيقيّة.

| # | المقياس | قصير | **متوسّط (جوابُ المالك)** | طويل |
|---|---|---|---|---|
| ١ | **سطرُ المصدرِ بنصِّه** | 🟢 حاضر | 🟢 **حاضر** | 🟢 حاضر |
| ٢ | **تنبيهُ «فهمٌ لا فتوى»** | 🟢 حاضر | 🟢 **حاضر** | 🟢 حاضر |
| ٣ | **التذييلُ لا يُقدِّمُ نطاقًا مصدرًا** | 🟢 | 🟢 | 🟢 |
| ٤ | **الارتفاع** | **`1350`** | **`1786`** | **`2700`** |
| ٥ | **القطعُ والشارة** | صفرُ قطعٍ · صفرُ شارة | **صفرُ قطعٍ · صفرُ شارة** | قطعٌ · شارةٌ · **`2077`** محرفًا باقيًا |
| ٦ | **أسطرٌ تبدأُ بترقيمٍ نهائيّ** | **`0`** من `1` | **`0`** من `11` | **`0`** من `12` |
| ٧ | **الذيلُ لا يتراكبُ مع آخرِ سطرِ متن** | فجوةٌ **`811`** بكسل | فجوةٌ **`69`** بكسل | فجوةٌ **`103`** بكسل |
| ٨ | **وسمُ الحديثِ: بطاقة / شاشة** | — (لا حديث) | 🔴 **`رَوَى مسلم` / `من السنة النبوية`** | 🔴 **غيرُ متطابقَين** |

**والأمرُ الرابعُ في §٢-٣ («لا يُقتَطَعُ من النسبةِ لأجلِ المتنِ أبدًا») مقيسٌ لا مفترَض:** الذيلُ يزنُ **`276`** بكسلًا في الثلاثةِ جميعًا — بما فيها البطاقةُ المقطوعة. **القطعُ يقعُ على المتنِ وحدَه.**

**ولا يُرسَمُ شيءٌ مرّتَين، مقيسًا:** **فقرتانِ** نُقِلَتا من المتنِ إلى الذيلِ في الثلاثةِ جميعًا، و**`bodyStillHoldsReserved = false`** في الثلاثةِ — أي لم تبقَ منهما فقرةٌ في المتن. وهذا عطبٌ **أحدثتُه أنا** في أوّلِ تنفيذٍ (لمّا كفَّ المتنُ عن القطعِ ظهرَتِ الفقرتانِ مرّتَين) ثمّ قِيسَ وأُغلِق.

### ٤-١ · قبلَ الرقعةِ — على الجوابِ نفسِه

| | قبل | بعد |
|---|---|---|
| الارتفاع | `1350` | **`1786`** |
| أسطرُ المتن | `16` من `26` — **مقطوع** | **`21` من `21` — غيرُ مقطوع** |
| سطرُ المصدر | **🔴 غائب** | 🟢 حاضرٌ بنصِّه |
| التنبيه | **🔴 غائب** | 🟢 حاضرٌ بنصِّه |
| ما يقفُ موضعَ المصدر | **🔴 `binbaz.org.sa` وحدَه** | 🟢 النسبةُ بنصِّها، والنطاقُ تحتَها موسومًا «روابط» |

---

## ٥ · مسارُ العيّناتِ — خارجَ المستودع

```
C:\Users\passe\AppData\Local\Temp\claude\C--Users-passe-projects-ustaz\
  5b0a4c83-a6f1-45cf-bf24-69f9735ef7d7\scratchpad\42c\samples\

  00-BEFORE-medium-1350px.png        249,349 بايتة  ← ما كانَ يخرجُ: مقطوعٌ، بلا نسبةٍ ولا تنبيه
  01-AFTER-short-1350px.png          120,268 بايتة  ← الأرضيّةُ لم تتحرّك
  02-AFTER-medium-1786px.png         342,836 بايتة  ← جوابُ المالك، كاملًا، بنسبتِه وتنبيهِه
  03-AFTER-long-2700px-cut.png       629,273 بايتة  ← السقفُ، والقطعُ، والشارةُ، والذيلُ سليم
```

**افتحْ `02` مقابلَ `00`.** الأركانُ الستّةُ كلُّها هناك بلا قطع، وتحتَها «binbaz.org.sa — أركان الإيمان الستة — مادة: شرح أصول الإيمان» ثمّ رابطُها، ثمّ التنبيهُ، ثمّ «روابط: binbaz.org.sa»، ثمّ `ezik.app`. **وفي `00` لا شيءَ من ذلك إلّا النطاقُ عاريًا.**

---

## ٦ · البوّابات

`summary.json` ⟸ `head_before = 04911d4…` · `head_after = 04911d4…` · `dirty = 0/0` · `tree_dirtied_by_run = false`
المجلَّد: `…\Temp\ezik-gates\runs\2026-08-24T13-14-15-303Z-20108`

| البوّابة | الشرط | **المقيس** | |
|---|---|---|---|
| الروستر | `93` | **`93`** | 🟢 |
| `recon` | `184/1/0` | `SUMMARY   PASS=184   WARN=1   FAIL=0` | 🟢 |
| الإنذارُ الوحيد | المعروف | `[WARN] LONGEST_CARD_CHARS = 3405 > longest card 3401 -> cap oversized/stale (re-derive in api/report.js)` | 🟢 |
| حارسُ الدروس | `269` | `=== 269/269 - PASS ===` | 🟢 |
| `themecoverage` | خضراء | `OK: 1351/1351 checks passed.` | 🟢 |
| `markdown` | — | `OK: 121/121 checks passed.` | 🟢 |
| `SUITE` | — | **`92/93`** · `FAILING (1): bankintegrity=1` | 🟡 متوقَّع |
| `bankintegrity` | تحمرُّ بـ`CORE_BYTES` وحدَه | `FAIL  74 checks passed, 2 failed` | 🔴 **بأمرٍ صريح** |

### ٦-١ · الفرقُ رقمًا — **مُعلَنٌ ولا يُصلَح** (ملكُ جولةِ الدمج)

```
FAIL [B12] CORE_BYTES = 1862145 but CORE weighs 1870613 bytes on disk (+8468).
FAIL [B14] sw.js prose says app.js is 1026796 bytes; the disk says 1035264 (+8468).
```

| | قبل | بعد | الفرق |
|---|---|---|---|
| `app.js` | `1026796` | `1035264` | **`+8468`** |
| `index.html` | `122568` | `122568` | **`0`** — لم يُمَسّ |
| **`CORE_BYTES` على القرص** | `1862145` | `1870613` | **`+8468`** |

الفرقُ كلُّه من `app.js` وحدَه، **وحمرتانِ لا ثلاث** لأنّ `index.html` لم يتحرّكْ هذه الجولة. و`sw.js` لم يُمَسَّ ولا خَتْمَ ولا اسمَ مخزنٍ — كلُّها محرَّمةٌ في §٠.

---

## ٧ · ما لم يُقَسْ · وما كذّبَه القياس

### ٧-١ · فرضيّاتٌ كذّبَها القياس

1. **🔴 «البطاقةُ تأخذُ كائنَ الردِّ بأجزائِه»** — لا. تأخذُ **نصًّا وسلسلةً** لا غير (§٢-١). ولهذا لم يكنْ للنسبةِ ولا للتنبيهِ سبيلٌ إليها إلّا عبرَ النصِّ الذي يُقطَع.
2. **🔴 «وسمانِ من مصدرَينِ مختلفَين»** — لا. **مصدرٌ واحدٌ وقاعدتان** (§٢-٤)، فالمخرجُ الذي أتاحَتْه §٢-٤ لا ينطبق، والوقوفُ وقعَ على أرضِ §٣-١ لا على أرضِها.
3. **🟡 «التذييلُ يُقدِّمُ نطاقًا في موضعِ المصدر»** — صحيحٌ، **وسببُه ليسَ التذييلَ نفسَه**: التذييلُ كانَ يفعلُ ما بُنيَ له (يسمّي النطاقات)، والعطبُ أنّ النسبةَ التي كانت فوقَه قد قُطِعَتْ فبقيَ وحدَه في الميدان.
4. **🟡 عطبٌ أحدثتُه أنا ثمّ أُغلِق**: أوّلُ تنفيذٍ رسمَ الفقرتَينِ **مرّتَين** — مرّةً في المتنِ ومرّةً في الذيلِ — لأنّ المتنَ كفَّ عن القطع. قِيسَ وأُغلِقَ بالنقلِ لا بالحذف (§٤).

### ٧-٢ · ما لم يُقَسْ — يُسمّى ولا يُطوى

1. **🔴 لم أفتحِ التطبيقَ الحيَّ ولم أضغطْ زرَّ «صورة» بإصبعي.** المقيسُ هو `ezikDrawReplyCard` المشحونةُ على قماشٍ حقيقيّ، مُعطاةً ما يُعطيها إيّاه التطبيقُ. **الزرُّ وقفلُ `busyRef` والتنزيلُ لم تُشغَّل** — يقيسُها `themecoverage` ZC5 وحدَه، وهي خضراء.
2. **🔴 وجوابُ المالكِ الحقيقيُّ لم يُستعمَلْ حرفًا.** بنيتُ نصًّا بصيغةِ السلكِ يحملُ الأجزاءَ نفسَها التي وصفَها (متنٌ · حديثٌ · مصدرٌ بـ«مادة:» · تنبيه) وسلّمتُه `parseRichMessage`. **فإن كانَ في بطاقتِه شيءٌ لا تفسّرُه هذه الأجزاءُ، فلن يظهرَ في أرقامي.** العيّناتُ بانتظارِ عينِه لهذا السبب.
3. **الخطُّ مُقاسٌ على Chrome/Windows** (`system-ui` ⟸ Segoe UI). **اللفُّ وعددُ الأسطرِ وارتفاعُ البطاقةِ ستختلفُ على iOS وAndroid** — والهندسةُ مشتقّةٌ فتتبعُ، لكنّ `1786` رقمُ هذا الجهازِ لا رقمٌ عالميّ.
4. **السقفُ `2700` لم يُختبَرْ على شبكةٍ اجتماعيّة.** بطاقةٌ بنسبةِ `1080×2700` قد تُقَصُّ في معاينةِ بعضِ التطبيقات. **مُبلَّغٌ**: هو قرارُ المالكِ في §٢-٣ ولم أراجعْه، ولم أقِسْ كيفَ يعرضُه واتساب أو تويتر.
5. **لم أقِسْ ما يحدثُ لجوابٍ فيه أكثرُ من مصدرٍ واحد.** الدالّةُ تصلُ الفقراتِ بسطرٍ فارغٍ وتلفُّها، والذيلُ يكبرُ فيصغرُ المتن — **مقيسٌ بالبناءِ لا بالتشغيل**: كلُّ عيّناتي تحملُ مصدرًا واحدًا.
6. **ولا جوابًا بلا مصدرٍ ولا تنبيه.** الدالّتانِ تعيدانِ `''` و`''` لا ترسمُ شيئًا — و`ZC3` تؤكّدُ أنّ جوابًا بلا مصدرٍ لا يُخترَعُ له سطر، لكنّي لم أرسمْ بطاقةً كهذه بنفسي.
7. **لم أشغّلْ أيَّ بوّابةٍ ثلاثَ مرّاتٍ لفرزِ الرفرفة.** الطقمُ مرّةً واحدةً، والحمرةُ الوحيدةُ مفهومةٌ ومأمورٌ بها.
8. **`index.html` لم يُمَسَّ**، فلم يُقَسْ فيه `bare LF`؛ شرطُ §٣-٥ معلَّقٌ على المسِّ ولم يقع.
9. **أربعةُ ملفّاتٍ غيرِ متعقَّبةٍ نُقِلَتْ من جذرِ الشجرةِ قبلَ الطقم** — أمرُ هذه الجولةِ وتقاريرُ الجولاتِ الثلاثِ السابقة — لأنّ `run-gates` يخرجُ بـ`1` على أيِّ اتّساخ. وأُعيدَتِ التقاريرُ الثلاثةُ بعدَه. **مذكورٌ لأنّه تغييرٌ في حالةِ الشجرةِ لم يأمرْ به الأمرُ نصًّا.**
10. **البندُ ٤٢-ج لا يُغلَقُ بهذا التقرير.** ثلاثةٌ من أربعةٍ أُغلِقَتْ، والرابعُ ينتظرُ إذنًا صريحًا (§٣-٢). والحَكَمُ الأخيرُ عينُ المالكِ على العيّناتِ الأربع.

---

## ٨ · الحال

```
origin/main                         56952b263f19e6b38bcc9966e6501b3c78c5a82f   (لم يُمَسَّ)
fix/card-attribution-20260824 HEAD  04911d4dbde01d4fb202d99cd591cee284fce04d
ما مسَّه الإيداع                     app.jsx · app.js
حارسٌ عُدِّل                         صفر
مكتبةٌ أو CDN أُضيف                  صفر
بندٌ جديدٌ فُتِح                      صفر
دفع · نشر                           صفر
```

**تُنتظَرُ كلمةُ المالكِ بعدَ فتحِ العيّنات.**

---

REPORT_PAYLOAD_BYTES=23654
REPORT_SHA256=0b1cc0ddf01877c1e46ff1f97a2c42b91f2cf3f68369bd17a020e17ae7f67d16
REPORT_SHA8=0b1cc0dd

(الحمولةُ المختومةُ: كلُّ بايتةٍ حتّى سطرِ الفصلِ الأخيرِ وسطرِه الجديدِ بعدَه، قبلَ إلحاقِ هذه الأسطر. أُعيدُ قطعَها بـ `head -c 23654 <file> | sha256sum`.)
