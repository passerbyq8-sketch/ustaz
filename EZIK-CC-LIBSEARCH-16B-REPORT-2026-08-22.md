# تقريرُ تنفيذٍ — عزك · الشاملةُ · البندُ **١٦-ب**: شاشةُ البحثِ في المكتبةِ ووصلُ بطاقةِ المصدر

**النافذة:** ن١ · وكيلُ Claude Code · `C:\Users\passe\projects\ustaz-cc-ui`
**التاريخ:** 2026-08-22
**الفرع:** `feat/lib-search-16b-20260822`
**الأساس:** `origin/main = 6cae89c92cc2dced8f9137ebd6142a2c00e41d1e` (دمجُ الجولةِ ٢٦)

> 🔴 **الحصيلة: `STOP_REASON=gate_babel_import_scan`.**
> العملُ كلُّه منجَزٌ ومودَعٌ وأخضرُ إلّا بوّابةً واحدةً: `babel` تحمرُّ على **سطرٍ واحدٍ** هو آليّةُ الاستيرادِ نفسُها التي أمرَ بها البندُ §٤-٢. وشرطُ §٥ لا يُجيزُ إلّا حمراءَ `bankintegrity`، فوقفتُ عندَها ولم أمسَّ البوّابة. التفصيلُ في §٧، والقرارُ لصاحبِ الأمر.

---

## ٠ · نقلُ الأمر

`ORDER-LIBSEARCH-16B.md` نُقِلَ خارجَ الشجرةِ إلى `C:\EZIK-STAGE\orders\` **قبلَ أيِّ فعلٍ آخر** ⟹ `ORDER_MOVED=True`.
لم تكنْ في الشجرةِ ملفّاتٌ شاردةٌ سواه، فلم يُنقَلْ ولم يُحذَفْ شيءٌ غيرُه.

---

## ١ · بوّاباتُ ما قبلَ العمل

| المفتاح | المطلوب | **المقيس** | الحال |
|---|---|---|---|
| `ORIGIN_MAIN` | ≠ `4c9531cb4687…` | `6cae89c92cc2dced8f9137ebd6142a2c00e41d1e` | ✅ نزلَ دمجُ ٢٦ |
| `DROPDOWN_FIX_PRESENT` | `True` | `app.jsx:4840` عندَ `origin/main`: `style={{ ...s.memAyahSelect, flex: 'none' }}` على منسدلةِ `dailyCat` — ومرّةً واحدةً في الملفِّ كلِّه | ✅ |
| `DIRTY_LINES` | `0` | `0` (بعدَ نقلِ الأمر) | ✅ |
| `LIBA_BRANCH_HEAD` | موجود | `41b7be06ee048355194ec62a82bc87612109df9d` | ✅ |

ثمّ: `git checkout -B feat/lib-search-16b-20260822 origin/main` ⟹ الرأسُ `6cae89c9`.

---

## ٢ · ضمُّ النصفِ الخادميِّ (٢٥-أ)

`git merge --no-ff feat/lib-search-16a-20260822`

| المفتاح | **المقيس** |
|---|---|
| `MERGE_EXIT` | `0` |
| `MERGE_CONFLICT_FILES` | **صفر** (`git diff --diff-filter=U` ⟹ لا شيء) |
| إيداعُ الدمج | `33b8d65a91fa1d9814c0ac37e329a58762bf1ee6` |

`git --no-pager diff --stat origin/main..HEAD` بعدَ الضمِّ مباشرةً — **إضافةٌ محضةٌ، صفرُ حذف**:

```
 EZIK-CC-LIBSEARCH-16A-REPORT-2026-08-22.md | 232 ++++++++++++
 api/lib-search.js                          | 189 ++++++++++
 guards/fixtures-lib-search-16a.json        | 142 ++++++++
 guards/lib-search-16a-guard.cjs            | 277 +++++++++++++
 lib/lib-source-card.js                     | 166 ++++++++
 5 files changed, 1006 insertions(+)
```

الملفّاتُ الأربعةُ وأحجامُها على القرص:

| الملف | البايتات |
|---|---|
| `api/lib-search.js` | 8575 |
| `lib/lib-source-card.js` | 7311 |
| `guards/lib-search-16a-guard.cjs` | 19486 |
| `guards/fixtures-lib-search-16a.json` | 4384 |

`node guards/lib-search-16a-guard.cjs` **بعدَ الضمِّ** ⟹ `73/73 - PASS`، خروجٌ `0`. بقيَ أخضرَ.

---

## ٣ · جردُ التغييرِ في `app.jsx`

`254` سطرًا مضافًا و`1` معدَّلًا (سطرُ `EZIST_SUB` الذي أُضيفَ إليه مفتاحُ المكتبة). ولا شيءَ حُذِف.

**المدخل — بلاطةٌ سابعةٌ على الرئيسة**، على وصفةِ بلاطةِ الصلاةِ (البند ١٠٨-أ) حرفًا بحرف:

| السطر | ما فيه |
|---|---|
| `3268` | `const EZH_LIBRARY = 'المكتبة';` |
| `3269` | `EZH_ICON_LIBRARY` — علامةٌ في صندوقِ 24×24 بنفسِ السَّمْكِ والأطرافِ المدوَّرةِ كالستِّ بجانبِها. لا ملفَّ رسمٍ جديدًا، ولا صورةً، ولا `data:` URI |
| `3292` | الواصفةُ في `ezHomeModules` — المكانُ الوحيدُ الذي تُبنى فيه القائمة |
| `3324-3325` | `EZIST_SUB_LIBRARY` وإدخالُها في خريطةِ السطورِ الوصفيّة |
| `3527 · 3555 · 3574` | حالةُ اللوحةِ ومقبضُها وفرعُ عرضِها في `Home` |

**الجسم — لوحةٌ لا شاشة** (`12290`–`12514`): ثوابتُ الشاشةِ · `ezLibViewState` · `ezLibSentence` · `ezLibSearchCall` · `ezLibCardModule` · `EzLibHit` · `LibrarySheet`.
و`15710`–`15716`: سبعةُ مفاتيحِ أنماطٍ جديدة.

**ولمَ لوحةٌ لا شاشة:** جردُ الشاشاتِ في `index.html` عقدٌ عابرٌ للملفّات — `theme-coverage-guard` يقرأُ `EZIK-THEME-33-HANDOFF.md` ويشترطُ أن يطابقَ جدولُه وعددُه العربيُّ مجموعةَ فروعِ `screen === '...'`. وذلك المستندُ ليس من نطاقِ هذه الدفعة، فمسارٌ جديدٌ كان سيُحمِرُ بوّابةً لسببٍ لا علاقةَ له بالمكتبة. وهي نفسُ العلّةِ التي جعلتْ `PrayerSheet` لوحةً، والأمرُ أجازَ «شاشةٌ **أو لوحةٌ**» صراحةً.

### كيف وصلَتِ البطاقةُ إلى المتصفِّح — وهي عقدةُ الجولة

`app.js` **سكربتٌ عاديٌّ**: `tools/babel-block.cjs` يثبِّتُ `sourceType: 'script'`، و`index.html` يحمِّلُه بـ`<script src="app.js">` لا بـ`type="module"`. فـ**تصريحُ** استيرادٍ لا يمكنُ أن يُحلَّلَ في هذا الملفِّ ألبتّة. أمّا `import()` الديناميُّ فتعبيرٌ لا تصريح: يُحلَّلُ، **ومقيسٌ** أنّه ينجو من التحويلِ كما هو:

```
DYNAMIC_IMPORT_SURVIVES_TRANSFORM=true
const f = () => import("/lib/lib-source-card.js");
```

وأنّ المتصفِّحَ يقدرُ عليه فعلًا — **مقيسٌ بمتصفِّحٍ حقيقيٍّ بلا رأسٍ فوقَ هذه الشجرةِ نفسِها** (خادمٌ محلّيٌّ على `127.0.0.1`، صفرُ إنترنت):

```
PROBE_200=/lib/lib-source-card.js type=application/javascript; charset=utf-8
PROBE_RESULT={"state":"ok",
  "keys":["DEGRADED_TEXT","HIT_FIELDS","REFUSED_TEXT","RESPONSE_FIELDS","TRUNCATED_TAG",
          "buildSourceCard","renderSourceCard"],
  "notCitable":"B · A · x ← y",
  "citable":"B · A · ج 2 · ص 7-9"}
```

السبعةُ كلُّها وصلَتْ، والفرعُ غيرُ القابلِ للإحالةِ لم يطبعْ صفحةً وإن حملَها المدخَل.
والمسارُ الساكنُ الذي يخدمُه هو نفسُه الذي يخدمُ `vendor/react.umd.js` للصفحةِ اليوم، و`lib/**` في قائمةِ **«لا يُضافُ أبدًا»** في `.vercelignore`. (وما لم يُقَسْ من هذا: §٨.)

---

## ٤ · جدولُ الحالاتِ الأربعِ الملزِمة

كلُّها مقيسةٌ بفيكستشراتٍ محلّيّةٍ — **صفرُ شبكةٍ في الفحص** — مرّتَينِ: على الدالّةِ الخالصةِ المقتطَعةِ من `app.jsx` نفسِه (قسم F)، وعلى **الشاشةِ المرسومةِ فعلًا** (قسم G).

| الحال | ما يقعُ على الشاشة | مصدرُ الجملة | إعادةُ محاولة | لونُ عطب |
|---|---|---|---|---|
| `refused: true` | «تعذّرَ توسيعُ البحث.» | `REFUSED_TEXT` **من الوحدةِ**، حرفًا بحرف | لا | **لا** |
| `degraded_reason` | النتائجُ **تُعرَض** + «النتائجُ مبتورةٌ عندَ سقفِ البحث.» | `DEGRADED_TEXT` **من الوحدةِ**، حرفًا بحرف | لا | **لا** |
| `503` (لا توكن) | «البحث في المكتبة غير متاح هنا.» | الشاشة | لا | **لا** |
| `502` / انقطاعُ شبكة | «تعذر الوصول إلى المكتبة الآن.» + «أعد المحاولة» | الشاشة | **نعم** | **لا** |

ثلاثُ حالاتٍ إضافيّةٍ قِيسَتْ معها: `200` بلا إصاباتٍ ⟹ ليس عطبًا · `200` بجسمٍ غيرِ مقروءٍ ⟹ يُعامَلُ كجوابٍ ميّتٍ لا كصفرِ نتائج · انقطاعُ الشبكةِ يهبطُ في حالِ `502` نفسِها.

**ولا يُذكَرُ اسمُ متغيّرِ بيئةٍ في أيٍّ منها** — مقيسٌ بأنّ نصَّ الشاشةِ لا يطابقُ `/[A-Z][A-Z0-9_]{6,}/` ولا يحوي `TOKEN`.

**وصفرُ نصِّ بطاقةٍ في `app.jsx`:** الجملُ الثلاثُ التي تملكُها الوحدةُ (`REFUSED_TEXT` · `DEGRADED_TEXT` · `TRUNCATED_TAG`) **غيرُ مكتوبةٍ في `app.jsx`** — مقيسٌ بالبحثِ عن بايتاتِها. الشاشةُ تحملُ **أسماءَ** التصديرِ لا نصَّه، والوحدةُ تُسأَلُ بالاسم.

---

## ٥ · تأكيداتُ شرطِ القبول

| المفتاح | **المقيس** |
|---|---|
| `LIB_HOST_IN_APPJSX` | **0** — وصفرٌ كذلك في `app.js` الذي يحمِّلُه المتصفِّح |
| `'/api/lib-search'` في `app.jsx` | **مرّةً واحدة**، ولا يمسُّها كودٌ خارجَ كتلةِ المكتبة |
| `SEARCH_API_TOKEN` في العميل | **غيرُ موجودٍ** في `app.jsx` ولا في `app.js` |
| `CALLS_ON_BOOT` | **0** — بشجرةِ البناءِ (موضعُ نداءٍ واحدٌ، داخلَ `LibrarySheet > run`) **وعلى لوحةٍ مرسومةٍ لم يُكتَبْ فيها شيء** |
| `PAGE_SHOWN_WHEN_NOT_CITABLE` | **0** — على البطاقةِ وعلى الشاشةِ المرسومة، **ببرهانِ طفرتَين** (أدناه) |
| الشاشةُ تقرأُ حقلَ صفحةٍ | **لا** — `page_start` · `page_end` · `volume` · `page_citable`: صفرُ ذكرٍ في كتلةِ العرض |
| `node tools/build-app.cjs --check` | `961149` بايتة · `397640062c235d4bb3c8871c3c8ed085ec676a116aa394212adab6c664deb298` — المبنيُّ = ما على القرص |
| `node C:\Users\passe\tdz-scan.cjs` | `TDZ_COUNT=0` · `SCOPES=1358` · `MODE=script` |
| الإيداعُ أوّلًا | نعم، ثمّ `run-gates` على شجرةٍ نظيفة (`0 dirty path(s)`) |
| الدفع | **صفر** |

> ⚠️ **فخٌّ وقعتُ فيه ثمّ خرجتُ منه، ويُسجَّلُ:** `tdz-scan.cjs` افتراضُه `C:/Users/passe/projects/ustaz` — **شجرةٌ أخرى**. أوّلُ جريانٍ طبعَ `SRC_CHARS=1006843` وهو حجمُ `app.jsx` **قبلَ** التعديل، أي أنّه قاسَ ملفًّا آخر. القيمةُ أعلاه من جريانٍ مُمرَّرٍ إليه مسارُ هذه الشجرةِ صراحةً (`SRC_CHARS=1021003`، مطابقٌ للقرص).

### الحارس: `73 → 139` فحصًا

وُسِّعَ `guards/lib-search-16a-guard.cjs` — حارسٌ قائمٌ، وهو **غيرُ مسجَّلٍ في `gates.json`** فلا يُغيِّرُ عددَ البوّابات (٩٠ كما هي) — بقسمَينِ جديدَين:

* **F (47 فحصًا)** يقرأُ `app.jsx` والحزمةَ المبنيّةَ منه، و**يقتطعُ الدالّةَ الخالصةَ من `app.jsx` ويُنفِّذُها كما هي مكتوبة** (لا إعادةَ كتابةٍ لها في الحارس)، ويسوقُها على `lib/lib-source-card.js` الحقيقيِّ وعلى فيكستشراتِ ٢٥-أ نفسِها.
* **G (19 فحصًا)** يُقلِعُ الحزمةَ المشحونةَ في `linkedom` بـReact المحلّيِّ، **يركِّبُ اللوحةَ فعلًا**، ويسوقُ الحالاتِ الأربعَ عبرَ مُعالِجاتِ المكوِّنِ نفسِها مع `fetch` مُوقَفٍ على الفيكستشرات.

`139/139 - PASS`، خروجٌ `0`.

### برهانُ الطفرة — ثلاثُ طفراتٍ، وكلُّها عضَّت

| الطفرة | ما فُعِل | النتيجة |
|---|---|---|
| **١. البطاقة** (داخلَ الحارس، في الذاكرةِ لا على القرص) | جُعِلَ الفرعُ غيرُ القابلِ للإحالةِ يحملُ `volume` و`page_start` و`page_end` | الحارسُ يُثبِتُ أنّ الطفرةَ **غيَّرتِ المصدرَ فعلًا** (لا طفرةَ عاطلة)، ثمّ يُثبِتُ أنّ المطفَّرَ **يُسرِّبُ** الصفحةَ ⟹ التوكيدُ يعضّ. ويُعادُ قراءةُ `lib/lib-source-card.js` بعدَها ليُثبَتَ أنّه **لم يُمَسّ** |
| **٢. العرضُ يقرأُ حقلَ صفحة** (على القرصِ فعلًا، ثمّ رُدّ) | حُقِنَ `{hit.page_start}` في `EzLibHit` | `118/120 - FAIL`، خروجٌ `1`. ثمّ رُدَّ الملفُّ و`RESTORED_IDENTICAL=true` |
| **٣. حذفُ البطاقةِ من الرسم** (على القرصِ فعلًا، ثمّ رُدّ) | استُبدِلَ عنصرُ البطاقةِ بـ`{null}` | `133/139 - FAIL`. أربعةُ فحوصِ **شاشةٍ** سقطتْ — طفرةٌ لا يراها الفحصُ الساكنُ ويراها قسمُ G. ثمّ رُدَّ وعادَ `139/139` وبنفسِ الـsha |

---

## ٦ · الأرضيّاتُ — قبلَ وبعد

| | **قبل** (`33b8d65`، شجرةٌ نظيفةٌ بعدَ الضمِّ) | **بعد** (`395b2e73`) |
|---|---|---|
| `run-gates` | **90/90** · خروج `0` | **88/90** |
| `recon` | `PASS=182 WARN=1 FAIL=0` | `PASS=182 WARN=1 FAIL=0` |
| الشجرةُ بعدَ الجريان | `0 dirty path(s)` | `0 dirty path(s)` |
| `lib-search-16a-guard` | `73/73` | `139/139` |

**لم تنخفضْ أرضيّةٌ واحدةٌ في `recon`، ولم يسقطْ فحصٌ كان أخضر.** البوّابتانِ الحمراوانِ حمراوانِ لسببَينِ مسمَّيَينِ أدناه، لا لانحدارٍ في شيءٍ قائم.

### ٦-١ · الحمراءُ المستثناةُ — `bankintegrity`، ونصُّها حرفًا

```
  FAIL [B12] CORE_BYTES = 1781136 but CORE weighs 1794547 bytes on disk (+13411).
        The quota pre-check is measured against a number that stopped being true.
        Repair it with `node tools/core-bytes.cjs --write` and re-cut the sw.js
        digest in the SAME commit -- never by hand from the comment table.
  FAIL [B14] sw.js prose says app.js is 947738 bytes; the disk says 961149 (+13411).
        A comment that states a size is what the next person re-cuts CORE_BYTES from,
        so it is wrong in exactly the way the constant used to be.
FAIL  74 checks passed, 2 failed.
```

رقمانِ اثنانِ في `sw.js` وحدَهما، وكلاهما `+13411` وهو **نموُّ `app.js` بعينِه** (`947738 → 961149`). لا ثالثَ لهما. وهذا ما نصَّ عليه الأمرُ §٠-٦: متوقَّعٌ ومقصودٌ، ويُصحَّحُ في جولةِ الدمجِ وحدَها. **صفرُ بايتةٍ في `sw.js`.**

### ٦-٢ · الحمراءُ **غيرُ** المستثناة — `babel`

```
=== IMPORT SCAN (block-relative + HTML line) ===
block L12404 / html L12404: if (!ezLibCardModulePromise) ezLibCardModulePromise = import(EZLIB_CARD_MODULE);
FAIL: 1 import-scan violation(s) in the babel block
```

**سطرٌ واحد.** وهذا سببُ الوقوف، وتفصيلُه §٧.

> جولةٌ أولى من `run-gates` أحمرَتْ ثلاثًا لا اثنتَين: `themecoverage` كذلك، على `N32`/`P17`
> (`expected /\bFIXTURE\b/i`). والسببُ كان **كلمةً في تعليقٍ** كتبتُها في `app.jsx` («with a fixture
> and no browser»)، لا سلوكًا. أُعيدَتْ صياغتُها، وأُعيدَتْ صياغةُ سطرَي تعليقٍ آخرَينِ كانا
> يحملانِ كلمةَ `import` مجرَّدةً — حتّى يبقى ما يسمّيه ماسحُ `babel` **سطرَ الآليّةِ وحدَه**، لا نثري.
> `themecoverage` أخضرُ الآن. ولم تُمَسَّ بوّابةٌ ولا حارسٌ لتحقيقِ ذلك.

---

## ٧ · لِمَ وقفتُ — `STOP_REASON=gate_babel_import_scan`

بوّابةُ `babel` (`babel-gate.cjs:51-65`) تمسحُ الكتلةَ سطرًا سطرًا بـ`/(^|[^.\w])import\b/` وتسقطُ على أوّلِ مطابقة. وتعليقُها يصفُها بأنّها بحثٌ عن **«import statements»**.

والمقيسُ أنّ الماسحَ **أعرضُ ممّا يصفُ نفسَه به**:

* التحويلُ الذي يسبقُه في الملفِّ نفسِه ينجحُ ويطبعُ `OK: Babel transform succeeded with sourceType=script (no error reproduced)` — أي أنّ الخاصّيّةَ التي يوجدُ الماسحُ ليحرسَها (**ألّا يدخلَ تصريحُ استيرادٍ كتلةً تُحلَّلُ كسكربت**) **مُثبَتةٌ فعلًا وقبلَه**.
* `import()` الديناميُّ **ليس تصريحًا** وهو مشروعٌ في سكربت. وقد قِيسَ أنّه ينجو من التحويلِ وأنّ متصفِّحًا حقيقيًّا يحمِّلُ به الوحدةَ (§٣).
* وممّا يدلُّ على عَرَضِه: ثلاثُ مطابقاتٍ في الجريانِ الأوّلِ، **اثنتانِ منها داخلَ تعليقَين**.
* وأثرٌ جانبيٌّ: الماسحُ يخرجُ بـ`process.exit(1)` **قبلَ** نصفِ البوّابةِ الثاني (توكيدا البندِ ٣٢: أنّ البناءَ قابلٌ لإعادةِ الإنتاجِ وأنّ `app.js` المودَعَ = بناءُ اليوم). فما دامتْ حمراءَ بهذا السببِ، **ذانِكَ التوكيدانِ لا يجريانِ أصلًا** — وقد قِستُهما يدويًّا وهما أخضرانِ (§٥).

**ولمَ لم أُصلِحْها:** §٣ تُجيزُ «حارسًا قائمًا **يُوسَّعُ** إن لزمَ **تأكيدٌ جديد**». وتضييقُ هذا الماسحِ ليس توسيعًا بتأكيدٍ جديدٍ، بل **إرخاءُ** توكيدٍ قائم — وهو بالضبطِ الشكلُ الذي تحرسُ منه ثقافةُ هذا المستودع. و§٥ تقولُ: أيُّ حمراءَ غيرِ `bankintegrity` ⟹ قفْ. فوقفتُ، **ولم أمسَّ `babel-gate.cjs` ولا بايتةً**.

**ولمَ لم أتحايلْ عليها:** كان يمكنُ بناءُ الكلمةِ في زمنِ التشغيلِ (`['im','port'].join('')` وما شابه) فتمرَّ البوّابةُ خضراءَ. وهذا **نزعُ سلاحٍ صامتٌ** لحارسٍ، لا حلّ. لم يُفعَلْ ولن يُقترَح.

### الطرقُ الثلاثُ أمامَ صاحبِ الأمر

| # | الطريق | الكلفة | ما تكسرُه |
|---|---|---|---|
| **١** | تضييقُ ماسحِ `babel-gate.cjs` من «الكلمةِ `import`» إلى «**تصريحِ** استيرادٍ» (سطرٌ واحد؛ مثلًا `/^\s*import\s+[^(]/`، أو توكيدٌ على الشجرةِ بدل السطور) | ملفٌّ **خارجَ** نطاقِ §٣ | لا شيء: الخاصّيّةُ المحروسةُ يُثبِتُها التحويلُ الذي يسبقُه. **وهي المرشَّحةُ عندي** |
| **٢** | تحميلُ `index.html` للوحدةِ ونشرُها في `window` | `index.html` — **محظورٌ** في §٣، وعليه ثلاثةُ حرّاس | — |
| **٣** | نسخُ نصوصِ الوحدةِ ومُصيِّرِها داخلَ `app.jsx` مع حارسِ تطابق | — | **يخالفُ §٤-٢ نصًّا**: «صفرُ نصِّ بطاقةٍ يُكتَبُ في `app.jsx`». وهي عينُ العلّةِ التي أنشأَ البندُ ليمنعَها |

الطريقُ ١ وحدَها تُبقي §٤-٢ قائمةً. والحكمُ لصاحبِ الأمر.

**ما وقعَ فعلًا:** `صفرُ دفع` · `صفرُ بايتةٍ في sw.js` · `صفرُ مساسٍ بـ index.html` · `صفرُ تعديلٍ لسطرٍ قائمٍ في lib/** أو api/**` · `صفرُ رفعةِ مخزنٍ` · `صفرُ بايتةِ فهرسٍ أو كتاب`.

---

## ٨ · ما لم يُقَسْ، بعلّتِه

1. **لم يُجرَّبْ نداءٌ حيٌّ لأنّ التوكنَ في الإنتاجِ وحدَه.** كلُّ ما في §٤ فيكستشراتٌ محلّيّة. أوّلُ نداءٍ حقيقيٍّ لـ`https://lib.ezik.app/search` من هذه الشاشةِ لم يقعْ بعدُ ولن يقعَ في معاينة: كلُّ معاينةٍ بلا `SEARCH_API_TOKEN` تُجيبُ `503`، وهي إحدى الحالاتِ الأربعِ المقيسةِ لهذا السبب.
2. **لم يُقَسْ أنّ Vercel يخدمُ `/lib/lib-source-card.js` عبرَ HTTP في الإنتاج.** المقيسُ ثلاثةٌ: أنّ متصفِّحًا حقيقيًّا يستوردُها من تخطيطِ هذه الشجرةِ (§٣)؛ وأنّ `lib/**` في قائمةِ «لا يُضافُ أبدًا» في `.vercelignore` فهي منشورة؛ وأنّ الصفحةَ **تحمِّلُ اليومَ** `vendor/react.umd.js` — ملفًّا في مجلَّدٍ فرعيٍّ بنفسِ الآليّةِ الساكنة، وحذفُ `vendor/` من `.vercelignore` وقعَ لهذا الغرضِ عينِه. الباقي — توجيهُ Vercel نفسُه — لا يُقاسُ إلّا بنشرٍ، ولم أنشر. **وإن لم تُخدَمْ، فالسلوكُ معروفٌ ومقصود:** لا تُعرَضُ إصابةٌ بلا بطاقتِها ألبتّة، وتقفُ الشاشةُ على الجملةِ المحايدة — لا اقتباسَ بلا نسبة.
3. **لم يُقَسْ عاملُ الخدمةِ على هذا المسار.** قراءةً في `sw.js:726` وحدَها: `/lib/...` يقعُ في ذراعِ «cache-first للأصولِ الساكنةِ من نفسِ الأصل» ⟹ إخفاقُ ذاكرةٍ ثمّ شبكةٌ ثمّ تخزين. لم يُشغَّلْ عاملُ خدمةٍ حقيقيٌّ للتحقُّق، لأنّ `sw.js` محظورةٌ في هذه الجولةِ فلم أُرِدْ أن أُشغِّلَ ما لا أملكُ إصلاحَه.
4. **`CALLS_ON_BOOT=0` مقيسٌ على لوحةٍ مركَّبةٍ وحدَها، لا على إقلاعِ التطبيقِ كاملًا.** المقيسُ: (أ) موضعُ نداءٍ واحدٌ في شجرةِ البناءِ كلِّها، سلسلتُه `LibrarySheet > run`؛ (ب) لوحةٌ مرسومةٌ لم يُكتَبْ فيها شيءٌ ⟹ صفرُ نداءٍ للمسار. ولم يُقَدْ إقلاعُ `App` كاملًا إلى الرئيسةِ ثمّ إلى البلاطة.
5. **لم تُقَسْ فروقُ حرفِ العلّةِ ولا اتّجاهُ النصِّ على شاشةٍ حقيقيّة.** `linkedom` بلا `getComputedStyle`، ولم تُلتقطْ صورة.
6. **`babel` لم تُقَسْ خضراءَ في أيِّ حالٍ**، لأنّ الماسحَ يوقفُها قبلَ نصفِها الثاني (§٧). توكيدا البندِ ٣٢ قِيسا يدويًّا (`--check` مطابق) لا عبرَ البوّابة.
7. **رسالتا `502`/انقطاعِ الشبكةِ نصُّهما من الشاشةِ لا من الخادم.** الأمرُ لم يُملِ نصًّا لهما، وجسمُ الخطأِ الذي يرسلُه `api/lib-search.js` لم يُعرَضْ للقارئِ — فما يقرؤُه القارئُ في هاتَينِ الحالتَينِ كُتِبَ هنا، وهو ليس نصَّ بطاقة.
8. **لم يُقَسْ زمنٌ ألبتّة** (§٠-٢). ولا رقمَ أداءٍ في هذا التقرير.

---

## ٩ · الرأسُ الأخير

| المفتاح | القيمة |
|---|---|
| `MERGE_HEAD` (ضمُّ ٢٥-أ) | `33b8d65a91fa1d9814c0ac37e329a58762bf1ee6` |
| إيداعُ العمل | `d72b27a92253e671a22d6f9c0b69eb0bf8301b24` |
| `FINAL_HEAD` | **`395b2e738b8db1e738d9088d78c337ec60fcfbe2`** — وعليه قِيسَتِ البوّاباتُ على شجرةٍ نظيفة |
| الدفع | **صفر** |

ويُودَعُ هذا التقريرُ فوقَه، وSHA إيداعِه مطبوعٌ على الطرفيّةِ بعدَ الختم.

<!-- PAYLOAD-END -->

---

## ١٠ · الختم

```
PAYLOAD_BYTES = 24333
SHA256        = a5b61dd6331d671709cc5a83d322722a09e0d1090de2724e27ab2d6363f24ef8
ALGO          = sha256 over every byte of this file up to and including the line <!-- PAYLOAD-END --> and its newline
```
