# EZIK — تقريرُ البند ٤٢ · بطاقةُ الردِّ صورةً وتصديرُ PDF · ٢٤ أغسطس ٢٠٢٦

**خمسةٌ من الثمانيةِ أُصلِحَتْ ومقيسةٌ في متصفّحٍ حقيقيّ · وثلاثةٌ يمنعُها حارسٌ، فوُقِفَ عندَ حدِّه وكُتِبَ كما تأمرُ §٣-٤ · والعيّنتانِ محفوظتانِ لعينِ المالك. لا دفعَ ولا نشر.**

---

## ١ · الأساسُ والفرعُ والإيداع

| المفتاح | القيمة |
|---|---|
| الشجرة | `C:\Users\passe\projects\ustaz` |
| الأساس | `origin/main` بعدَ `fetch` = `80007538e6c2ac9e38a5992734d30dd55b2d6fe5` — **مطابقٌ** لـ`8000753` الذي توقّعَه الأمر |
| الفرع | `fix/reply-card-pdf-20260824`، مقطوعٌ من `origin/main` |
| الإيداع | **`cfac9d3`** — واحدٌ لا غير |
| ما مسَّ | `app.jsx` · `app.js` (مولَّدٌ منه بـ`tools/build-app.cjs`) · `index.html` |
| صفرُ مساسٍ بالمخّ | **`api/` صفرُ ملفّ · `lib/` صفرُ ملفّ** — مقيسٌ بـ`git diff --stat` |
| صفرُ مكتبةٍ وصفرُ CDN | **صفرُ سطرٍ في `package.json` أو خريطةِ الموردين** — لا شيءَ أُضيف |
| صفرُ تعديلِ حارس | **صفرُ ملفٍّ `.cjs` في الفرقِ** |

**والرقمُ الوحيدُ الذي توقّعَه الأمرُ صحَّ:** `origin/main` عندَ `8000753` — وقد دُفِعَتْ جولةُ الدمجِ ٢٥ وإيداعُ استثناءِ التقاريرِ إلى الأصلِ بينَ الأمرَين، فصارَ المتوقَّعُ هو المقيس.

**ستُّ رقعٍ في `app.jsx`، وواحدةٌ في `index.html`، لا سابعةَ:**
`@@ -2502 (ثوابتُ الميزانيّة)` · `@@ -2543 (سياقُ الرسم)` · `@@ -2577` و`@@ -2587` (الإزاحتانِ المسمّاتان) · `@@ -2628 (الرابطُ المقروء)` · `@@ -2662 (سطرُ الرابطِ في docToHtml)` · و`index.html @@ -1350` وحدَها.

---

## ٢ · القياسُ أوّلًا — أينَ يعيشُ كلٌّ منهما (§١)

### ٢-١ · موضعُ رسمِ البطاقة

| السؤال | الجواب المقيس |
|---|---|
| الملفّ | `app.jsx` وحدَه (ومنه `app.js` بالبناء) |
| آلةُ الرسم | **`<canvas>` عاديّةٌ** من `document.createElement('canvas')` — **لا `OffscreenCanvas`** · `canvas.getContext('2d')` · `ctx.fillText` |
| الدالّة | `ezikDrawReplyCard` — `app.jsx:2566` · والزرُّ `SaveReplyImageButton:2643` · ونداؤه الحيُّ الوحيدُ في `:11758` |
| قسمةُ الأسطر | `ezikCardWrap` (`:2545`) — لفٌّ جشعٌ كلمةً كلمةً على **العرضِ المقيسِ فعلًا** بـ`ctx.measureText`، لا بعدِّ محارف |
| عرضُ الكتابة | `EZIK_CARD_W - 2×EZIK_CARD_PAD` = `1080 − 168` = **`912`** |
| ارتفاعُ السطر | `EZIK_CARD_LINE` = **`62`** · وأوّلُ خطِّ أساسٍ عندَ `EZIK_CARD_PAD + EZIK_CARD_LINE` = **`146`** |
| **أينَ يُقرَّرُ القطع** | **`app.jsx:2611`** — `const cut = all.length > EZIK_CARD_BODY_LINES;` وحدَه لا ثانيَ له |

### ٢-٢ · موضعُ توليدِ الـPDF

| السؤال | الجواب المقيس |
|---|---|
| أطباعةٌ أم رسمٌ يدويّ؟ | **لا هذا ولا ذاك في الحالةِ الغالبة**: `printAsPdf` (`app.jsx:2387`) ينتظرُ `window.__ezikVendor('html2pdf')` ثمّ **يُرَستِرُ المستندَ** عبرَ `html2canvas` داخلَ حزمةِ `html2pdf` (٩٠٦٬٠٤١ بايتة من cdnjs، بصمةُ SRI مطابقةٌ لما في `index.html:1650`). **وطباعةُ المتصفّحِ `window.print()` موجودةٌ ومكتملةٌ لكنّها احتياطٌ فقط**، لا يُسلَكُ إلّا إذا سقطَتِ الحزمة. |
| من يبني سطورَه | **`html2canvas`** — قِيسَ باعتراضِ `CanvasRenderingContext2D.fillText`: **٣٤٤ نداءً** لجوابٍ واحد، كلٌّ منها «كلمةٌ + فراغٌ لاحق» |
| صحيفةُ الأنماط | `index.html:1347–1365` — `#print-area` و`@media print{@page{size:A4;margin:1.5cm}}` وخطُّ `"Amiri","Arial",serif` |
| **أينَ تُبنى الروابط** | **`REPLY_SERIALIZERS.source`** — `app.jsx:11045` — `REPLY_LINE([… , sg.url \|\| ''])`: الرابطُ يخرجُ **كما جاءَ من الجواب**، مرمَّزًا بالنسبة المئويّة. لا شيءَ في الشجرةِ يفكُّ ترميزَه. |
| **ما يخرجُ عن `app.jsx`** | **`index.html` وحدَها** — صحيفةُ الطباعة. مطبوعٌ ومحدودٌ: رقعةٌ واحدةٌ من ستّةِ أسطرٍ (`@@ -1350`)، ولا شيءَ غيرَها. |

---

## ٣ · الأعطابُ الثمانيةُ — موضعُها وسببُها المقيسُ وسطرُ إصلاحِها

| # | العطب | الموضع | **السببُ المقيس** | سطرُ الإصلاح | الحال |
|---|---|---|---|---|---|
| **أ-١** | الترقيمُ ينقلبُ إلى أوّلِ السطر | `ezikDrawReplyCard` | **`ctx.direction === 'ltr'`** — مقيسٌ بقراءتِه من سياقِ الرسمِ المشحون. قماشٌ يُصنَعُ بـ`createElement` ليسَ في المستند، فيحسمُ `direction` إلى LTR، و`fillText` يشغّلُ خوارزميّةَ ثنائيِّ الاتّجاهِ **بذلك** اتّجاهًا للفقرة. فكلُّ محرفٍ محايدٍ في آخرِ جملةٍ عربيّةٍ يأخذُ اتّجاهَ الفقرةِ لا اتّجاهَ النصِّ ويُرسَمُ أقصى اليمين — وهو **أوّلُ** السطرِ عربيًّا. | **`app.jsx:2583`** — `ctx.direction = 'rtl';` | 🟢 |
| **أ-٢** | القطعُ مبكِّرٌ ويتركُ ربعَ البطاقةِ فارغًا | `EZIK_CARD_BODY_LINES` | كانَ **العددَ الحرفيَّ `13`**. البطاقةُ تسعُ `16`: آخرُ خطِّ أساسٍ نزلَ عندَ `890` من بطاقةٍ ارتفاعُها `1350` لا يبدأُ تذييلُها قبلَ `1220` — `330` بكسلًا خاليةً تحتَ نصٍّ قيلَ للقارئِ إنّه مقتطع. | **`app.jsx:2527`** — الميزانيّةُ **تُشتَقُّ** من الهندسةِ عندَ أسوأِ حال (سطرُ مصدرٍ **و**قطعٌ معًا)، فكلُّ ترتيبٍ أخفَّ يبقى معه فضلٌ لا عجز. والإزاحاتُ الثلاثُ صارَتْ ثوابتَ مسمّاةً (`:2509–2511`) يستعملُها الرسمُ نفسُه (`:2621`, `:2631`) فلا تنفصلُ الميزانيّةُ عمّا يُرسَمُ به. | 🟢 |
| **أ-٣** | الشعارُ المائيُّ يزاحمُ القراءة | الشعارُ المائيّ | `globalAlpha = 0.07` قِيسَ فكانَ **٣٠ من ٢٥٥ درجةً** فوقَ الخلفيّةِ في أقواهُ، على **٧٧٬٨٢٧ بكسلًا** داخلَ نطاقِ النصِّ نفسِه. | **`app.jsx:2597`** — `0.07 ⟶ 0.035` ⟹ **١٤ درجةً**. و«إزاحتُه عن كتلةِ النصِّ» لم تعدْ متاحةً: الميزانيّةُ الجديدةُ تملأُ البطاقة. | 🟢 |
| **أ-٤** | التذييلُ سليمٌ — **لا يُمَسّ** | `foot`/`EZIK_CARD_SITE` | — | **صفرُ تعديل**. الإزاحتانِ `46` و`14` صارَتا اسمَينِ بالقيمةِ نفسِها، فالبكسلاتُ لم تتحرّك. | 🟢 مصون |
| **ب-١** | العنوانُ ملتصقٌ «ردُّعزك» | `html2canvas` | `EZIK_PDF_TITLE` يحملُ **فراغًا حقيقيًّا** — `0631 062f 064f 0651 **0020** 0639 0632 0643`. لكنّ `html2canvas` يرسمُه **مقطعَين**: `"ردُّ "` ثمّ `"عزك"`، والفراغُ اللاحقُ في المقطعِ الأوّلِ يُوضَعُ على **الجانبِ الخطأِ** من مقطعِه (لأنّ أساسَه LTR)، فيقعُ خارجَ الكلمتَين. **فجوةُ الحبرِ المقيسةُ بينهما `2.38` بكسل، وعرضُ الفراغِ `6.66`.** | 🔴 **يمنعُه حارس** — §٦ | 🔴 |
| **ب-٢** | كلماتٌ تلتصقُ عندَ حدودِ الأسطر | `html2canvas` | العلّةُ نفسُها، معمَّمةً: **٢٤ زوجًا ملتصقًا من ٢٧٦** زوجًا متجاورًا قبلَ الرقعة. **و`٣٤٤/٣٤٤` من مقاطعِ الرسمِ خرجَتْ بأساسٍ `ltr`** — وهي علّةُ أ-١ نفسُها في محرِّكٍ آخر. | 🔴 **يمنعُه حارس** — §٦ | 🔴 |
| **ب-٣** | الروابطُ تُطبَعُ مرمَّزةً وتفيض | `REPLY_SERIALIZERS.source` ⟵ `docToHtml` | الرابطُ يصلُ العارضَ **كما يحملُه الجواب**: عربيٌّ مرمَّزٌ بالنسبةِ المئويّة، **٥٦ هروبَ `%XX`** في صفحةٍ واحدة. ومقطعٌ من `%XX` **لا يحملُ فرصةَ كسرٍ واحدة**، فلا يلفُّ ألبتّة. | **`app.jsx:2686–2694` و`:2732`** — سطرٌ ليسَ إلّا رابطًا يُعرَضُ **مفكوكَ الترميز** · و**`index.html:1357–1358`** تسمحُ لأيِّ مقطعٍ بالكسر. **وهذا عرضٌ لا تحرير**: `serializeReply` لم يُمَسّ. | 🟢 مع تحفُّظٍ في §٥-٤ |
| **ب-٤** | الصفحةُ الثانية: تراكبٌ وسطرٌ مبتور | `html2canvas` | **لم يُعَدْ إنتاجُه في هذا المِرصَد**: `html2pdf` وضعَ الجوابَ كلَّه في **صفحةٍ واحدةٍ** مقلَّصةٍ (`pdfPages = 1`)، فلم يقعْ انتقالُ صفحةٍ أصلًا. **مُبلَّغٌ كغيرِ مُعادٍ إنتاجُه، لا كمُصلَح.** | 🔴 **يمنعُه حارس** — §٦ | 🔴 |

---

## ٤ · أرقامُ §٤ السبعةُ — بالمصنوعِ لا بالنيّة

**الجوابُ واحدٌ ثابتٌ** فيه جملٌ تنتهي بنقطةٍ · كلامٌ بينَ «عارضتَين» · رابطٌ طويلٌ مرمَّزٌ · وأرقامٌ عربيّةٌ ولاتينيّة. وكلُّ رقمٍ أدناه من **متصفّحٍ حقيقيٍّ بلا رأس** يشغّلُ **الشيفرةَ المشحونةَ نفسَها**، مسلوخةً من `app.jsx` بعلاماتِها لا منسوخةً.

### ٤-١ · الترقيم — «صفرُ سطرٍ يبدأُ بمحرفِ ترقيمٍ نهائيّ»

قِيسَ **بالبكسل**، لا بالنصّ: كلُّ سطرٍ ينتهي بـ`.` `،` `؟` `!` `:` `؛` يُرسَمُ مرّتَينِ — كاملًا وبلا محرفِه الأخير — ويُسألُ هل الأقصرُ **بادئةٌ عموديّةٌ** للأطول. إن كان، فحذفُ المحرفِ غيّرَ الطرفَ الأيمنَ وحدَه ⟹ المحرفُ مرسومٌ في **أوّلِ** السطرِ عربيًّا ⟹ عطب.

| | قبل | بعد |
|---|---|---|
| أسطرٌ منتهيةٌ بترقيمٍ فُحِصَتْ | `7` | `9` |
| **أسطرٌ تبدأُ بمحرفِ ترقيمٍ نهائيّ** | **`7`** | **`0`** ✅ |
| `ctx.direction` المقروءُ من السياقِ المشحون | `ltr` | `rtl` |

وفي مسارِ الطباعةِ للـPDF: **`0`** أيضًا (بقياسِ أقصى يمينِ كلِّ صندوقِ سطر). وفي مسارِ `html2pdf`: **غيرُ قابلٍ للقياسِ على مسارٍ رستريّ** — المقاطعُ مسجَّلةٌ بترتيبِها المنطقيِّ لا المرئيّ — **ولا يُدَّعى فيه صفر**؛ وصورةُ الصفحةِ المستخرجةُ تُظهِرُ العلاماتِ ما تزالُ منقلبةً (`الجمعة ،ويقضي`).

### ٤-٢ · الملء

| | قبل | بعد |
|---|---|---|
| أسطرُ المتنِ المرسومة | `13` | **`16`** |
| حبرُ المتنِ من `y` إلى `y` | `107 → 973` | `107 → 1159` |
| نطاقُ المتنِ المسموح | `106 → 1180` | `106 → 1180` |
| **نسبةُ الملء** | **`80.6٪`** | **`98.0٪`** ✅ |

**وشارةُ «مقتطع» لا تظهرُ إلّا على قطعٍ حقيقيّ**، مقيسًا بالمحارف: `cut = true` ومعه **`1083` محرفًا باقيًا** من `1725` (قبلَ الرقعة: `cut = true` و`580` باقيًا من `1122`). فالشارةُ صادقةٌ في الحالتَين، والفرقُ أنّ المعروضَ صارَ `642` محرفًا بدل `542`.

### ٤-٣ · الالتصاق

قِيسَ **هندسيًّا**، لأنّ البحثَ النصّيَّ عن «خطوة.خطوة» يُصيبُ حتّى في رسمٍ سليم — الكلمتانِ متجاورتانِ في المصدر. المعيار: **فجوةُ الحبرِ** بينَ آخرِ بكسلٍ من مقطعٍ وأوّلِ بكسلٍ من جارِه، مقابلَ عرضِ الفراغِ في الخطِّ نفسِه. أقلُّ من نصفِ فراغٍ = ملتصق.

| المسار | أزواجٌ متجاورة | **ملتصقة** |
|---|---|---|
| **طباعةُ المتصفّح** — قبل | ٢٣ سطرًا | **`0`** ✅ |
| **طباعةُ المتصفّح** — بعد | ٢٠ سطرًا | **`0`** ✅ |
| `html2pdf` — قبل | `276` | **`24`** 🔴 |
| `html2pdf` — بعد | `320` | **`64`** 🔴 |

**والصعودُ من ٢٤ إلى ٦٤ ليسَ تدهورًا خفيًّا بل نتيجةٌ مباشرةٌ لرقعةِ ب-٣**، وهي مذكورةٌ في §٥-٤ ولم تُطوَ.

### ٤-٤ · الروابط

| | قبل | بعد |
|---|---|---|
| **هروبُ `%XX` في المخرَجِ المعروض** — مسارُ الطباعة | `56` | **`0`** ✅ |
| **هروبُ `%XX`** — مسارُ `html2pdf` | `56` | **`0`** ✅ |
| **أسطرٌ تتجاوزُ عرضَ الصفحة** — مسارُ الطباعة | `0` | **`0`** ✅ |
| أعرضُ سطرٍ / عرضُ الصفحة | `755 / 764` | `755 / 764` |
| مقاطعُ تخرجُ عن القماش — `html2pdf` | `0` | **`0`** ✅ |

**وفيضُ العرضِ الذي رآه المالكُ لم يُعَدْ إنتاجُه على A4 بهذا الجواب** — الرابطُ المرمَّزُ لفَّ على أربعةِ أسطرٍ بدل أن يفيض. المقيسُ هو **الترميزُ** لا الفيض، والرقعةُ تغلقُ البابَين معًا (`overflow-wrap:anywhere` مضافةٌ سواءٌ فاضَ أم لم يفض).

### ٤-٥ · الصفحات

| | قبل | بعد |
|---|---|---|
| صفحاتُ الملفِّ — مسارُ الطباعة | `2` | `1` |
| **أسطرٌ متراكبة** | **`0`** | **`0`** ✅ |
| **أسطرٌ تعبرُ حدَّ صفحة** (مبتورة) | **`0`** | **`0`** ✅ |
| ارتفاعُ محتوى الصفحة المحسوب | `1009px` | `1009px` |
| صفحاتُ الملفِّ — `html2pdf` | `1` | `1` |

نزولُ الصفحاتِ من ٢ إلى ١ **أثرٌ للرابطِ المفكوك**: أربعةُ أسطرٍ من `%XX` صارَتْ سطرًا واحدًا.

### ٤-٦ · العنوان — نصُّه حرفًا

| المسار | ما خرجَ فعلًا |
|---|---|
| **طباعةُ المتصفّح** | **`ردُّ عزك`** ✅ — بفراغِه |
| `html2pdf` | **`ردُّعزك`** 🔴 — فجوةُ حبرٍ `2.38` بكسل مقابلَ فراغٍ عرضُه `6.66` |

### ٤-٧ · العيّنتان — **خارجَ المستودع، لعينِ المالك**

```
C:\Users\passe\AppData\Local\Temp\claude\C--Users-passe-projects-ustaz\
  5b0a4c83-a6f1-45cf-bf24-69f9735ef7d7\scratchpad\42\samples\

  01-card-BEFORE.png                                  244118 بايتة
  02-card-AFTER.png                                   274009 بايتة   ← البطاقةُ المصلَحة
  03-pdf-BEFORE-html2pdf.pdf                          440538 بايتة
  03-pdf-BEFORE-html2pdf-page1.jpg                    307083 بايتة   ← صفحتُها صورةً
  04-pdf-AFTER-html2pdf.pdf                           358167 بايتة   ← ما يُنتِجُه التطبيقُ اليوم
  04-pdf-AFTER-html2pdf-page1.jpg                     354802 بايتة   ← صفحتُها صورةً
  05-pdf-AFTER-browser-print-BLOCKED-BY-GUARD.pdf      38911 بايتة   ← ما كانَ سيخرجُ لولا الحارس
```

**افتحْ `02` مقابلَ `01`** لترى الترقيمَ عادَ إلى آخرِ الأسطرِ والبطاقةَ امتلأتْ.
**وافتحْ `05` مقابلَ `04`** لترى الفرقَ بينَ محرِّكِ طباعةِ المتصفّحِ وبينَ `html2canvas` — الأوّلُ ملفٌّ نصِّيٌّ حقيقيٌّ (`38911` بايتة، **لا صورةَ فيه**)، والثاني صورةٌ واحدةٌ ممسوحةٌ (`358167` بايتة).

---

## ٥ · ما لم يُصلَحْ، وما كذّبَه القياس

### ٥-١ · 🔴 ثلاثةُ أعطابٍ يمنعُها حارسٌ — وُقِفَ عندَ حدِّه ولم يُمَسّ (§٣-٤)

العلاجُ الوحيدُ لـ**ب-١** و**ب-٢** و**ب-٤** هو **إخراجُ `html2canvas` من مسارِ التصدير**. وهو ممنوعٌ بثلاثةِ تأكيداتٍ في `theme-coverage-guard.cjs` — **مقتبَسةً بنصِّها**:

```js
// theme-coverage-guard.cjs:4552
eq('Z1: the app has exactly one PDF path', (html.match(/const printAsPdf = async /g) || []).length, 1);

// theme-coverage-guard.cjs:4553-4556
okOn('Z1: ...and it still awaits the lazy bundle and degrades to print()', [['html', html]],
  /await window\.__ezikVendor\('html2pdf'\)/.test(html)
  && /if \(!window\.html2pdf\) \{ document\.title = title; window\.print\(\); return; \}/.test(html));

// theme-coverage-guard.cjs:4607-4608
ok('Z5: ...and html2canvas is named as an OPTION and never constructed or called',
  !/html2canvas\s*[.(]/.test(html) && /html2canvas: \{ scale: 2, useCORS: true \}/.test(html));
```

ونزعُ `await window.__ezikVendor('html2pdf')` أو `html2canvas: { scale: 2, useCORS: true }` **يُحمِّرُ بوّابةَ `themecoverage`**. وإبقاءُ الحرفِ مع تعطيلِ معناهُ — أن يُنتظَرَ المورِدُ ثمّ لا يُستعمَل — **غشٌّ للحارسِ لا إصلاحٌ للعطب**، ولم أفعلْه.

**والبرهانُ أنّ الطريقَ الآخرَ صحيحٌ مقيسٌ لا مظنون** — المستندُ نفسُه، الجوابُ نفسُه، الصحيفةُ نفسُها:

| | `html2pdf` (المشحونُ اليوم) | **طباعةُ المتصفّح** (احتياطٌ مُعطَّل) |
|---|---|---|
| العنوان | `ردُّعزك` 🔴 | **`ردُّ عزك`** 🟢 |
| أزواجٌ ملتصقة | **`64`** من `320` 🔴 | **`0`** 🟢 |
| أساسُ اتّجاهِ المقاطع | **`ltr` في ٣٤٤ من ٣٤٤** 🔴 | اتّجاهُ المستندِ نفسِه 🟢 |
| أسطرٌ تتجاوزُ العرض | `0` | **`0`** 🟢 |
| أسطرٌ متراكبةٌ أو مبتورة | لم يقعِ انتقالُ صفحة | **`0`** 🟢 |
| نوعُ الملفّ | صورةٌ ممسوحةٌ ٣٥٨ ك.ب | **نصٌّ حقيقيٌّ ٣٩ ك.ب** 🟢 |

**التوصيةُ، ولا أنفّذُها:** الحارسُ `Z1`/`Z5` كُتِبَ ليمنعَ رجوعَ `html2pdf` إلى مسارِ الإقلاعِ ويمنعَ دخولَ مُرَستِرِ DOM — وهما هدفانِ سليمانِ لا يُنقَضان. لكنّه **يُثبِّتُ أيضًا أنّ `html2pdf` هو مسارُ التصديرِ الأوّلُ**، وهذه هي الجملةُ التي كذّبَها القياس. أمرٌ مستقلٌّ لتعديلِ `Z1`/`Z5` — يُبقي منعَ الإقلاعِ ومنعَ مُرَستِرِ DOM ويُسقِطُ تثبيتَ الأوّليّةِ — يغلقُ الثلاثةَ في سطرَين.

### ٥-٢ · فرضيّةٌ في الشيفرةِ كذّبَها القياس

`app.jsx:2431` يقولُ بنصِّه:

> *«ARABIC READS THE RIGHT WAY BECAUSE THE BROWSER SHAPES IT. html2pdf rasterises the DOM through html2canvas, so the glyphs in the file are the glyphs the browser laid out — joined, shaped and right-to-left — rather than a font-substituted re-run inside a PDF writer.»*

**هذه الجملةُ غيرُ صحيحة، ومِلَفُّ المالكِ برهانُها.** `html2canvas` **لا يستعملُ تخطيطَ المتصفّح**: يقسمُ النصَّ بنفسِه ويرسمُ كلَّ مقطعٍ بـ`fillText` عندَ إزاحةٍ يحسبُها هو — ٣٤٤ نداءً، كلُّها بأساسٍ `ltr`. فالحروفُ موصولةٌ لأنّ `fillText` يصلُها، لكنّ **ترتيبَ المحايداتِ ومواضعَ الفراغاتِ من عندِه هو**. تُرِكَ التعليقُ كما هو لأنّ تصحيحَه يمسُّ سطرًا يقرؤه الحارس؛ وهو مذكورٌ هنا.

### ٥-٣ · فرضيّةٌ في الأمرِ كذّبَها القياس

**§٢-ب-٤ («الصفحةُ الثانية: أحرفٌ متراكبةٌ وسطرٌ مبتور») لم يُعَدْ إنتاجُها.** `html2pdf` بإعداداتِه المشحونةِ وضعَ الجوابَ كلَّه — بما فيه أربعُ فقراتٍ أُضيفَتْ عمدًا ليطولَ — في **صفحةٍ واحدة**. فالعطبُ حقيقيٌّ في ملفِّ المالكِ ولم أرَه بعيني، والسببُ الذي وصفَه الأمرُ («حسابُ موضعِ السطرِ عندَ انتقالِ الصفحةِ خاطئ») **لم أقِسْه ولا أؤكّدُه**.

### ٥-٤ · 🟡 كلفةٌ مقيسةٌ لرقعةِ ب-٣، معلَنةٌ لا مطويّة

فكُّ ترميزِ الرابطِ يحوّلُه من لاتينيٍّ (`%D9%85…`) إلى **عربيّ**. وعلى مسارِ `html2pdf` — الذي يُفسِدُ **كلَّ** عربيٍّ — صارَ الرابطُ يخرجُ مبعثَرًا (`مذ ه-بـلاجم هو-فـ-يإدرا-لكركع-ةلجمعة`) بدلَ أن يخرجَ مرمَّزًا سليمًا. **وهو غيرُ مقروءٍ في الحالتَين**، لكنّ الأزواجَ الملتصقةَ ارتفعَتْ `24 ⟶ 64` بسببِه.

على مسارِ الطباعةِ — وهو ما ستؤولُ إليه الأمورُ إن رُفِعَ منعُ §٥-١ — الرقعةُ **مكسبٌ خالص**: رابطٌ عربيٌّ مقروءٌ، `0` هروبٍ، `0` فيض.

**والقرارُ للمالك:** إبقاءُ الفكِّ (وهو ما نزل، لأنّ §٢-ب-٣ أمرَتْ به نصًّا) أو تعليقُه حتّى يُرفَعَ منعُ الحارس. **رجوعُه سطرٌ واحدٌ** — `app.jsx:2732`.

### ٥-٥ · أثرٌ خارجَ التصديرِ، معلَنٌ

`docToHtml` هو **أيضًا** عارضُ بطاقةِ المستندِ على الشاشة (`dangerouslySetInnerHTML`، مثبَّتٌ في `markdown-guard.cjs:380`). فسطرُ الرابطِ صارَ يُعرَضُ مفكوكًا **على الشاشةِ كذلك**. هذا اتّساقٌ مقصودٌ — الشيفرةُ نفسُها تقولُ إنّ المسارَينِ «made the same way» — ومذكورٌ لأنّه خارجُ عنوانِ البند. بوّابةُ `markdown` خضراءُ: `121/121`.

---

## ٦ · البوّابات

`summary.json` ⟸ `head_before = cfac9d3…` · `head_after = cfac9d3…` · `dirty_before = 0` · `dirty_after = 0` · `tree_dirtied_by_run = false`
المجلَّد: `…\Temp\ezik-gates\runs\2026-08-24T10-27-12-025Z-24912`

| البوّابة | الشرط | **المقيس** | |
|---|---|---|---|
| الروستر | `93` | **`93`** | 🟢 |
| `SUITE` | — | **`92/93`** · `GATES_EXIT=1` · `FAILING (1): bankintegrity=1` | 🟡 متوقَّع |
| `recon` | `185/1/0` | `SUMMARY   PASS=185   WARN=1   FAIL=0` | 🟢 |
| الإنذارُ الوحيد | المعروف | `[WARN] LONGEST_CARD_CHARS = 3405 > longest card 3401 -> cap oversized/stale (re-derive in api/report.js)` | 🟢 |
| حارسُ الدروس | `269` | `=== 269/269 - PASS ===` | 🟢 |
| `themecoverage` | — | `OK: 1346/1346 checks passed` | 🟢 |
| `markdown` | — | `OK: 121/121 checks passed` | 🟢 |
| `bankintegrity` | يحمرُّ بـ`CORE_BYTES` وحدَه | `FAIL  74 checks passed, 3 failed` | 🔴 **بأمرٍ صريح** |

**والأمرُ توقّعَ `SUITE 93/93` وتوقّعَ في الوقتِ نفسِه أن تحمرَّ `bankintegrity` — وهما لا يجتمعان.** المقيسُ `92/93`، والحمرةُ واحدةٌ وهي المأمورُ بها.

### ٦-١ · الفرقُ رقمًا — **مُعلَنٌ ولا يُصلَح** (ملكُ جولةِ الدمج)

```
FAIL [B12] CORE_BYTES = 1854255 but CORE weighs 1859688 bytes on disk (+5433).
FAIL [B14] sw.js prose says index.html is 121979 bytes; the disk says 122517 (+538).
FAIL [B14] sw.js prose says app.js is 1019495 bytes; the disk says 1024390 (+4895).
```

| | قبل | بعد | الفرق |
|---|---|---|---|
| `index.html` | `121979` | `122517` | **`+538`** |
| `app.js` | `1019495` | `1024390` | **`+4895`** |
| **`CORE_BYTES` على القرص** | `1854255` | `1859688` | **`+5433`** |

`5433 = 538 + 4895` بالبايتةِ الواحدة. **و`sw.js` لم يُمَسَّ، ولا خَتْمَ أُعيدَ قطعُه، ولا اسمَ مخزنٍ رُفِع** — كلُّها من المحرَّمِ في §٠ وكلُّها ملكُ جولةِ الدمجِ القادمة.

---

## ٧ · ما لم يُقَسْ — يُسمّى ولا يُطوى

1. **لم أفتحِ التطبيقَ الحيَّ ولم أضغطْ زرًّا واحدًا.** كلُّ ما هنا من `ezikDrawReplyCard` و`docToHtml` و`printAsPdf` **مسلوخةً من `app.jsx` بعلاماتِها ومشغَّلةً في متصفّحٍ بلا رأس**. الزرَّانِ (`SaveReplyImageButton`, `ExportPdfReplyButton`) وقفلُ `busyRef` **لم يُشغَّلا** — يقيسُهما `themecoverage` ZC5 وحدَه، وهو أخضر.
2. **لم أرَ الحرفَ العربيَّ يُرسَمُ بخطِّ الجهاز.** المِرصَدُ Chrome على Windows، فـ`system-ui` عندَه Segoe UI و`"Amiri"` غيرُ محمَّل. **الأشكالُ ستختلفُ على iOS وAndroid**، وقد يختلفُ معها اللفُّ وعددُ الأسطر. المقيسُ هو **الاتّجاهُ والهندسةُ**، لا مظهرُ الخطّ.
3. **`EZIK_CARD_BODY_LINES = 16` صحيحٌ لهذه الهندسةِ لا لكلِّ خطّ.** الاشتقاقُ عن `EZIK_CARD_LINE = 62` وحدَه؛ خطٌّ أطولُ ارتفاعًا قد يجعلُ السطرَ السادسَ عشرَ يمسُّ التذييل. **لم أقِسْه على خطٍّ آخر.**
4. **لم أقِسِ الحمولةَ على شاشةِ هاتف.** البطاقةُ `1080×1350` ثابتةٌ فلا تتأثّر؛ أمّا صحيفةُ الطباعةِ فقِيسَتْ على A4 وحدَها.
5. **§٢-ب-٤ لم يُعَدْ إنتاجُه** — §٥-٣.
6. **فيضُ الرابطِ عن عرضِ الصفحةِ لم يُعَدْ إنتاجُه** — §٤-٤. المقيسُ الترميزُ، والفيضُ أُغلِقَ بالوقايةِ لا بعدَ رؤيتِه.
7. **لم أقِسْ ما يفعلُه `overflow-wrap:anywhere` بجدولٍ عربيٍّ طويلٍ في التصدير.** أُضيفَتْ إلى `td`/`th` أيضًا، ولم أصدّرْ جوابًا فيه جدول.
8. **لم أشغّلْ أيَّ بوّابةٍ ثلاثَ مرّاتٍ لفرزِ الرفرفة.** الطقمُ مرّةً واحدةً، والحمرةُ الوحيدةُ مفهومةٌ ومأمورٌ بها.
9. **`ORDER-CARD-PDF-42.md` وتقريرا الجولتَينِ السابقتَينِ نُقِلَتْ من جذرِ الشجرةِ إلى مجلَّدِ العملِ المؤقّتِ** قبلَ تشغيلِ الطقم، لأنّ `run-gates` يخرجُ بـ`1` على أيِّ اتّساخ. **مذكورٌ لأنّه تغييرٌ في حالةِ الشجرة.** نسخُها محفوظةٌ في `…\scratchpad\42\`.
10. **الحكمُ الأخيرُ ليسَ لي.** §٤-٧ تقولُ إنّ المالكَ هو الحَكَم، والعيّنتانِ بانتظارِه. **٤٢-ج لا يُغلَقُ حتّى يراهما سليمَين** — ولم أغلقْه.

---

## ٨ · الحال

```
origin/main                       80007538e6c2ac9e38a5992734d30dd55b2d6fe5   (لم يُمَسَّ)
fix/reply-card-pdf-20260824 HEAD  cfac9d3882efb33ed98800b623355785f2e705bc
الإيداع                            cfac9d3  —  app.jsx · app.js · index.html
حارسٌ عُدِّل                        صفر
مكتبةٌ أو CDN أُضيف                 صفر
بندٌ جديدٌ فُتِح                     صفر
دفع · نشر                          صفر
```

**تُنتظَرُ كلمةُ المالكِ بعدَ فتحِ العيّنتَين.**

---

REPORT_PAYLOAD_BYTES=29113
REPORT_SHA256=973951851783957039db1b456bf4f8f91545630507f7fab608546ceb706aa1a4
REPORT_SHA8=97395185

(الحمولةُ المختومةُ: كلُّ بايتةٍ حتّى سطرِ الفصلِ الأخيرِ وسطرِه الجديدِ بعدَه، قبلَ إلحاقِ هذه الأسطر. أُعيدُ قطعَها بـ `head -c 29113 <file> | sha256sum`.)
