# EZIK — تقريرُ الوِردِ والختمة · مسارُ ١

**٢٤ أغسطس ٢٠٢٦ · `feat/wird-khatmah-20260824` · صفرُ دفعٍ وصفرُ نشر**

---

## ١ · الأساسُ والفرعُ والإيداعات

```
origin/main (بعدَ fetch)          8b45a390d8cd008cf119cd73403282c645c31bce   ← الأساسُ المتوقَّعُ، ومُقاسٌ مطابقًا
الفرع                             feat/wird-khatmah-20260824
الإيداعُ الوحيدُ فوقَ الأساس        4e26fff  feat(khatmah): a completion tracker the reader marks by hand
ما مسَّه                           app.jsx  (+426 −1)  ·  app.js  (+130 −10، وهو المبنيُّ لا المكتوب)
ملفٌّ آخرُ مُسَّ                     صفر
حارسٌ عُدِّل                         صفر
بوّابةٌ أُضيفت                       صفر  (الروستر باقٍ ٩٣)
`sw.js` · `api/**` · `lib/**`     لم تُمَسّ
`index.html`                      لم يُمَسّ — فلا حاجةَ إلى قياسِ `bare LF`
`CORE_BYTES` · ختمٌ · رفعةُ مخزن    لم يُقطَعْ ولم يُختَمْ ولم تُرفَعْ (وهو ما تأمرُ به §٠)
مكتبةٌ أو CDN                      صفر — لا سطرَ `import` ولا وسمَ `script` جديدًا
دفع · نشر                          صفر
```

`app.js` مبنيٌّ بـ `node tools/build-app.cjs`، و`--check` يُعيدُ بناءَه في الذاكرةِ ويطابقُه بايتةً بايتة:

```
built    1057624 bytes  sha256 35fe4ce0d1a8d0e532f9ed0e0c607b4b96ae862049bc5ebd533b08e94c44d07e
on disk  1057624 bytes  sha256 35fe4ce0d1a8d0e532f9ed0e0c607b4b96ae862049bc5ebd533b08e94c44d07e
CR في المولَّد = 0
```

---

## ٢ · جوابُ §١ السبعة — بنصِّه، ومقيسًا

### ٢-١ · سطحُ المصحفِ اليوم

المصحفُ اليومَ ثلاثُ طبقاتٍ في `app.jsx`، ولا واحدةَ منها في `index.html`:

| الطبقة | الموضع | ما هي |
|---|---|---|
| `MushafScreen` | `app.jsx:16802` | **الفهرس**: صدرٌ (صفُّ «تابِع القراءة» + صفُّ العلامةِ اليدويّة)، ثمّ شبكةُ الأجزاءِ الثلاثين، ثمّ شبكةُ السورِ الـ١١٤. ويفتحُ حيثُ تُرِك مرّةً واحدةً لكلِّ تركيب. |
| `PagedMushaf` | `app.jsx:16138` | **القارئ**: صفحةٌ لكلِّ شاشة، جرٌّ وقلب، شريطٌ علويٌّ وقاعدةٌ سفليّةٌ ينسحبانِ بعدَ أوّلِ قلبة، وشريطُ الوِردِ ولوحتُه. |
| `MushafSheet` / `MushafPage` | `app.jsx:15362` / `15425` | **الورقةُ نفسُها**. مع `MADINA_IMG_ON` هي صورةُ صفحةِ المدينةِ المطبوعةِ واحدةً، وبدونِه نصٌّ يُصَفُّ من التخطيط. |

ونصُّ الصفحةِ وتخطيطُها يأتيانِ من ملفَّين ولا ثالثَ لهما: `__quranData` من `/quran-uthmani.json` عبرَ `loadQuran()`، و`__layoutData` من `/mushaf-layout.json` عبرَ `loadLayout()` (`app.jsx:1341`)، ويُبنى منهما فهرسٌ واحدٌ بـ `buildLayoutIndex()`. **ولم يُمَسَّ من هذا كلِّه حرفٌ في هذه الجولة**: لا نصُّ القرآنِ ولا تخطيطُه ولا مصدرُه ولا حجمُ الخطِّ ولا خوارزميّةُ ملاءمةِ السطر.

### ٢-٢ · حفظُ آخرِ موضع — نازلٌ فعلًا

**نازلٌ، والمفتاحُ حرفًا: `mushaf_last_page_v1`.**

- الشكل: `{"p": صفحة ١..٦٠٤, "s": سورة ١..١١٤}` — لا ثالثَ لهما.
- القارئُ والكاتب: `readMushafLastPage()` و`writeMushafLastPage(pg, sr)` (`app.jsx:15620` وما بعدَه). القارئُ يلفُّ `getItem` و`JSON.parse` في `try` **منفصلَين**، والكاتبُ يتحقّقُ **قبلَ** الكتابةِ لا بعدَها.
- **متى يُكتَب**: أثرٌ في `PagedMushaf` على `[state, page, startSurah]` — أي عندَ كلِّ هبوطِ صفحةٍ مهما كان سببُه: جرٌّ، أو زرُّ تالٍ/سابق، أو قفزةٌ برقمٍ. وهو **غيرُ مقرونٍ بمؤقّتِ المكوث** عمدًا، والتعليقُ فوقَه يقولُ ذلك.
- **متى يُقرَأ**: ثلاثُ مرّاتٍ في `MushafScreen` — قيمةً ابتدائيّةً عندَ التركيب، وأثرًا كلَّما عاد `selected` إلى `null`، وأثرًا يفتحُ عليه مرّةً واحدةً لكلِّ تركيب (`autoResumedRef`).

### ٢-٣ · البندُ ٤٧-أ — **نزلَ في المصحف**

نزلَ، وهو حيٌّ. المفتاحُ `ezik_daily_wird_v1` (`app.jsx:15666`)، والسجلُّ:

```
{ mushaf: { mode: '' | 'pages' | 'surah', surah: 0..114 },
  adhkar: { cat, title },
  memorize: { surah } }
```

والقائمةُ المنسدلةُ نفسُها في لوحةِ «وردُ اليوم» داخلَ القارئِ، بـ `aria-label={DW_MUSHAF_LABEL}`. **وعددُ الصفحاتِ في اليومِ لم يُنقَلْ إليه**: هو باقٍ في مفتاحِه القديمِ `mushaf_wird_target_v1`.

**وعليه بنَتِ الختمةُ ولم تبنِ بجانبِه**: سطرُ «كم بقي بلغةِ الأيّام» يُشتَقُّ من `mushaf_wird_target_v1` عينِه ولا يسألُ القارئَ رقمًا ثانيًا، ولوحةُ الختمةِ تسكنُ لوحةَ الوِردِ نفسَها ولا تفتحُ لوحةً ثانية. ومن اختارَ سورةً بدلَ الصفحاتِ، أو لم يختَرْ شيئًا، **لا يُختلَقُ له عددُ أيّام**.

### ٢-٤ · شارةُ الوِردِ وحارسُها

الشارةُ حبّةٌ في شريطٍ مطلقٍ فوقَ القاعدة، مشروطةٌ بـ `MADINA_IMG_ON && chromeOn`، وضغطُها يفتحُ لوحةَ «وردُ اليوم». وحارسُها `tools/wird-guard.cjs`، وهو بوّابةُ `wird`.

**أرضيّتُه مقيسةً: `passed: 1122  ·  failed: 0` — عندَ الأساسِ وبعدَ الإيداعِ سواءً بسواء.** ومسحُه الداخليُّ للنصِّ المرئيِّ ارتفعَ من `1032` سلسلةً عربيّةً إلى `1056`، و«مطابقاتُ الكلماتِ الممنوعة» **صفرٌ** في الحالَين.

وما يحرسُه بالضبطِ اليومَ: الصفحةَ الأخيرةَ وقارئَها وكاتبَها، وهدفَ الوِردِ، وسجلَّ اليومِ ومؤقّتَ مكوثِه، وحدَّ اليومِ المحلّيَّ (لا `toISOString`)، وأنَّ `resetAll` يمسحُ ثلاثةَ مفاتيحَ بأسمائِها، **وأنَّ لا رمزٍ من رموزِ الوِردِ يشاركُ سطرًا مع رمزِ طلبٍ شبكيّ**، وأنَّ البندَ ٤٧-أ لا يعِدُ بما لا يفي (أربعُ كلماتٍ ممنوعةٌ في كلِّ نصٍّ مرئيّ)، وأنَّ في العميلِ صفرَ حقلِ وقت.

### ٢-٥ · جردُ مفاتيحِ المصحفِ — كلُّها بأسمائِها

| المفتاح | ما فيه | من يكتبُه |
|---|---|---|
| `mushaf_svg_v1` | راية جهاز: `'0'` تُطفئُ قارئَ الـSVG | معاملُ عنوانٍ `?mushafsvg=` |
| `madina_img_v1` | راية جهاز: `'0'` تُطفئُ قارئَ صورةِ المدينة | معاملُ عنوانٍ `?madinaimg=` |
| `mushaf_bookmark_v1` | `{p,s}` — العلامةُ اليدويّةُ الواحدة | زرُّ العلامةِ وحدَه |
| `mushaf_last_page_v1` | `{p,s}` — آخرُ موضعٍ، تلقائيًّا | أثرُ الصفحةِ في القارئ |
| `ezik_daily_wird_v1` | اختيارُ البندِ ٤٧-أ للوحداتِ الثلاث | القوائمُ المنسدلةُ الثلاث |
| `mushaf_wird_target_v1` | عددٌ عشريٌّ ١..٦٠٤ — صفحاتُ اليوم | مُنتقي الوِرد |
| `mushaf_wird_day_v1` | `{d:'YYYY-MM-DD', pages:[…]}` — صفحاتُ اليومِ بالمكوث | `markWirdPageRead` وحدَه |

وثامنٌ **ميّتٌ**: `mushaf_pos_v1` — لا يُقرَأُ ولا يُكتَب، ووجودُه الوحيدُ سطرُ مسحٍ داخلَ `resetAll`، وحارسُ الوِردِ يثبّتُه عندَ ظهورٍ واحدٍ لا غير.

**والمضافُ في هذه الجولةِ واحدٌ، باسمِه: `ezik_khatmah_v1`.** ولا مفتاحَ قائمٌ رُفِعَ إصدارُه، ولا واحدٌ أُعيدَت تسميتُه، ولا واحدٌ هُجِّرَت بياناتُه.

### ٢-٦ · معرِّفُ الملفّ — **موجودٌ، وهذا تكذيبُ ما في الشجرة**

**يوجدُ معرِّفٌ داخليٌّ يصلحُ مفتاحًا، واسمُه `pid`.** يُسَكُّ بـ `ezikMintId()` (`app.jsx:6735`، ثمانيةُ بايتاتٍ عشوائيّةٍ ست‑عشريّةً)، ويُقرَأُ بـ `ezikProfileKey(p)` (`app.jsx:6752`) الذي يردُّ `pid` أو `'anon'`. وهو يُسَكُّ عندَ إنشاءِ الملفِّ ويُهاجَرُ إليه عندَ الإقلاعِ لكلِّ ملفٍّ قديم.

**والتعليقُ فوقَ `MUSHAF_BOOKMARK_KEY` يقولُ حرفيًّا إنَّ «الملفَّ لا يحملُ معرِّفًا ثابتًا، وإضافتُه خارجَ نطاقِ هذه الميزة» — وهذا كلامٌ ماتَ منذُ الجلسةِ ٩٢ ولم يُصحَّحْ في مكانِه.** لم أمسسْه لأنّه ليس من عملِ هذه الجولة، وأُسمّيه هنا كما أمرَتْ §٤-٦.

**ولا اسمَ طفلٍ في مفتاحٍ ولا في قيمةٍ ألبتّة**، ومقيسٌ: الاسمُ المزروعُ `"Sami"` لا يظهرُ في بايتاتِ المخزَّنِ، ولا يظهرُ فيها حرفٌ عربيٌّ واحد.

### ٢-٧ · الـ٦٠٤ — من أينَ تُعرَف

من `mushaf-layout.json` وحدَه. الملفُّ يعلنُ عن نفسِه: `pages: 604`، و`p` مصفوفةٌ طولُها `604`، و`words: 77429`، والصفحةُ الأولى `n=1` والأخيرةُ `n=604`. وهو **لا يحملُ لافتةَ سورةٍ واحدة**: أرقامُ صفحاتٍ وأسطرٍ ومواقعُ كلماتٍ فقط.

**والشجرةُ تثبّتُ `604` في الكودِ تسعًا وثلاثينَ مرّةً** — في مدياتِ القارئِ والكاتبِ، وفي حدودِ القفزِ، وفي لافتةِ «صفحة N من ٦٠٤». لم أمسسْ واحدةً منها؛ **وكودُ هذه الجولةِ لا يحملُ منها ولا واحدة**: `khatmahTotalPages()` تقرأُ `__layoutData.p.length`، وقبلَ وصولِ التخطيطِ تردُّ `0`، وكلُّ نداءٍ يقرأُ الصفرَ بمعنى «غيرُ معلومٍ بعد» لا «لا صفحاتِ» ولا رخصةً في التخمين. ومقيسٌ في المِرقب: `khatmahTotalPages() === 604`، وكتلةُ الختمةِ كلُّها خاليةٌ من السلسلةِ `604`.

---

## ٣ · قرارُ التصميمِ الذي قُطِع، ولماذا

### ٣-١ · شكلُ الفعلِ الصريحِ الذي يعُدُّ الصفحةَ مقروءة

**حبّةٌ واحدةٌ على صفِّ القارئِ نفسِه، بجانبِ حبّةِ الوِرد: «علِّمْ هذه الصفحة» / «صفحةٌ معلَّمة — اضغطْ للتراجع».** ضغطةٌ تعُدُّ، وضغطةٌ ثانيةٌ تتراجع، و`aria-pressed` تحملُ الحالَ لمن لا يرى الامتلاء.

ولماذا هذا الشكلُ بالذات:

- **لماذا فعلٌ لا مؤقّت.** فوقَ هذه الكتلةِ مباشرةً مؤقّتُ مكوثٍ حقيقيٌّ (`WIRD_DWELL_MS = 8000`) يعتمدُ الصفحةَ للوِردِ اليوميّ. وهو صوابٌ **هناك**: ثماني ثوانٍ متّصلةٍ على ورقةٍ قراءةٌ، ووِردُ اليومِ دعوى صغيرة. أمّا الختمةُ فدعوى على الكتابِ كلِّه، ودعوى تُجمَّعُ من مؤقّتٍ دعوى لم يقُلْها القارئُ قطّ. وختمةٌ كاذبةٌ أسوأُ من لا ختمة.
- **لماذا على الشريطِ لا داخلَ اللوحة.** لو سكنَ الزرُّ اللوحةَ وحدَها لصارتِ الختمةُ ذاتُ الستِّ مئةِ صفحةٍ ألفًا ومئتَي ضغطة. وهو على الشريطِ، فيغيبُ مع الأثاثِ كلِّه في وضعِ القراءةِ ويعودُ بلمسة — كما تغيبُ حبّةُ الوِردِ وتعودُ اليوم.
- **لماذا التراجعُ بالضغطةِ نفسِها.** من علّمَ سهوًا لا بدَّ أن يقدرَ على قولِ ذلك، وإلّا فالعدُّ يكذبُ في الاتّجاهِ الآخر.

### ٣-٢ · اسمُ العلامةِ معرِّفٌ لا نصٌّ يُكتَب

§٢-٢ تقولُ «ويسمّي كلًّا منهما إن شاء»، و§٢-٣ تقولُ «المخزَّنُ أرقامُ صفحاتٍ ومعرِّفاتٌ فقط · ولا بياناتٍ شخصيّةً جديدة». **وحقلُ نصٍّ حرٍّ هو البابُ الوحيدُ الذي تمشي منه بيانةٌ شخصيّةٌ إلى السجلّ.** فالتسميةُ اختيارٌ من أربعةٍ ثابتة — `wird` · `hifz` · `tadabbur` · `muraja` — والمخزَّنُ هو المعرِّفُ لا الكلمة.

وللقرارِ سببٌ ثانٍ يساويه: **الواجهةُ بلغتَين من يومِ ميلادِها**. المعرِّفُ يُرسَمُ بلغةِ من يقرأ؛ وكلمةٌ عربيّةٌ يكتبُها الطفلُ لا يمكنُ أن تُعادَ إليه إلّا بالعربيّة. فالنصُّ الحرُّ كان سيكسرُ الشرطَينِ معًا.

### ٣-٣ · المفتاحُ على النمطِ القائمِ لا على نمطٍ جديد

`ezik_khatmah_v1`: **مفتاحٌ واحدٌ ذو إصدارٍ يحملُ خريطةً من مفتاحِ الملفِّ إلى سجلّ** — وهو حرفًا شكلُ `ezik_reading_prefs_v1` الذي يشحنُ اليوم. ومفتاحُ الملفِّ هو `ezikProfileKey`، وارتدادُه `'anon'` **هو بعينِه** السجلُّ العامُّ للجهازِ الذي تطلبُه §٢-٣ — فلا مفتاحَ ثانٍ ولا مسلكَ كودٍ ثانٍ له.

والسجلّ: `{ p: [أرقامُ صفحاتٍ متمايزة], c: عددُ الختماتِ التامّة, m: [علامةٌ|null, علامةٌ|null] }`، والعلامةُ `{p, s, l}`. **كلُّ قيمةٍ فيه عددٌ صحيحٌ أو أحدُ المعرِّفاتِ الأربعة.**

وقُرِئَ الملفُّ من المخزنِ لا من `props`، **لأنَّ حارسَ الوِردِ يثبّتُ عنصرَ `<PagedMushaf …/>` بخصائصِه الثلاثِ وحدَها** بتعبيرٍ نمطيٍّ ينتهي عندَ `onExit`؛ فتوسيعُ خصائصِه كان سيُحمِرُّ حارسًا، وتعديلُ الحارسِ محرَّم.

### ٣-٤ · ما لم يُمَسَّ عمدًا

العلامةُ اليدويّةُ القائمةُ وحفظُ آخرِ موضعٍ **لم يُمَسّا**: مفتاحاهما وقارئاهما وكاتباهما وصفّاهما على الفهرسِ هي عينُها. والعلامتانِ الجديدتانِ سجلٌّ آخرُ في مفتاحٍ آخرَ وقسمٌ آخرُ على الفهرس، **تحتَ الصدرِ لا داخلَه** — لأنَّ الصدرَ شريطُ `flex` من صفٍّ واحدٍ يثبّتُ حارسُ الثيمِ كونَه كذلك، وصفٌّ ثالثٌ ورابعٌ ليسا من شأنِه.

و`resetAll` يأخذُ المفتاحَ الجديدَ مع الباقي: «امحُ بياناتي كلَّها» لا يجوزُ أن يتركَ سجلَّ قراءةٍ لمن يستلمُ الجهازَ بعدُ.

---

## ٤ · أرقامُ §٣ السبعة

القياسُ كلُّه على **المكوِّنِ المشحونِ نفسِه**: `PagedMushaf` و`MushafScreen` يُركَّبانِ في نافذةِ `linkedom` بجذرِ React حقيقيّ، من الشيفرةِ التي يقرؤها المتصفّحُ عبرَ `tools/babel-block.cjs` عينِه الذي تقرأُ به البوّابات، والملفّانِ اللذانِ يجلبُهما المصحفُ يُخدَمانِ من القرص. **٩٩ تأكيدًا، صفرُ إخفاق.**

| # | المطلوب | المقيس |
|---|---|---|
| ١ | **الحفظُ يعيش** | ثلاثُ ضغطاتٍ على ثلاثِ صفحاتٍ ⟹ `p = [1,2,3]`. ثمّ **إعادةُ تحميلٍ حقيقيّة** (هدمُ الجذرِ، نافذةٌ جديدةٌ فوقَ البايتاتِ نفسِها) ⟹ `[1,2,3]` عينُها، والعلامتانِ في موضعِهما، والمسمّاةُ ما زالت مسمّاة، والقارئُ الواقفُ على الصفحةِ ١ يُرسَمُ معلَّمًا. |
| ٢ | **لا عدَّ بالمرور** | **اثنتا عشرةَ سحبةً حقيقيّةً** (لمسٌ ← حركةٌ تتجاوزُ عتبةَ ٥٥px ← رفع) نقلتِ القارئَ من الصفحةِ **١ إلى ١٣**، ١٢ من ١٢ حرّكتِ الشاشةَ فعلًا. **العدُّ صفرٌ**، والمفتاحُ **لم يُكتَبْ أصلًا**. وفي المسيرِ نفسِه انتظرتُ ٩٫٢ ثانيةً ساكنًا على الصفحةِ التي هبطتُ عليها: **`mushaf_wird_day_v1` كُتِبَ** — فالمؤقّتُ كان يعملُ، والختمةُ تجاهلَتْه. |
| ٣ | **التلفُ لا يُسقِط** | ستُّ قيمٍ تالفةٍ (نصٌّ ليسَ JSON · مصفوفة · `null` · سجلٌّ ليسَ كائنًا · حقولٌ من أنواعٍ خاطئة · صفحاتٌ سالبةٌ وكسريّةٌ ومكرَّرةٌ وخارجَ المدى مع `c = 1e30` وعلامةٍ نصفِ مبنيّة) ⟹ في الستِّ: حالٌ ابتدائيّةٌ صالحة، **صفرُ لوحةِ حدِّ خطأ**، والقارئُ يرسمُ أدواتِه، والبايتاتُ التالفةُ تبقى كما هي لا تُعادُ كتابتُها صامتةً. وفي المخلوطةِ: الصفحةُ الحقيقيّةُ الوحيدةُ `[7]` نجَتْ منزوعةَ التكرار، والعددُ المعقولُ `3` معها، والعلامتانِ عادتا فارغتَينِ لا نصفَ مبنيّتَين. |
| ٤ | **الإتمام** | ٦٠٤ صفحةً معلَّمةً ⟹ `khatmahComplete` صحيحٌ، والإقرارُ «تمّتِ الختمة. تقبّلَ اللهُ منك.» على الشاشة، و«ابدأْ ختمةً جديدة» **زرٌّ**. وقبلَ ضغطِه: الصفحاتُ ٦٠٤ كما هي والعددُ `2` كما هو. وبعدَه: الصفحاتُ `0`، والعددُ `3` — **زيادةُ واحدٍ بالضبط** — والإقرارُ يغيبُ معها، والعلامتانِ لم تُمَسّا. |
| ٥ | **صفرُ شبكة** | عبرَ كلِّ ما سبق، الطلباتُ الخارجةُ الوحيدةُ هي `/mushaf-layout.json` و`/quran-uthmani.json` — **الملفّانِ اللذانِ كان القارئُ يجلبُهما قبلَ هذه الجولة**. صفرُ طلبٍ سواهما، وصفرُ `XMLHttpRequest` و`WebSocket` و`EventSource` و`sendBeacon`. وكتلةُ الختمةِ في المصدرِ خاليةٌ من `fetch(` ومن `capHeaders` ومن كلِّ رمزِ طلب. |
| ٦ | **جردُ المفاتيح** | **قبل**: `["child_profile","ezik_ui_lang_v1","mushaf_last_page_v1"]`. **بعد**: هي عينُها **زائدَ `ezik_khatmah_v1` ولا شيءَ غيرَه**. صفرُ مفتاحٍ حُذِف. و`mushaf_bookmark_v1` و`mushaf_wird_target_v1` **لم يُكتَبا ولا مرّةً** في المسيرِ كلِّه. والمخزَّنُ حرفيًّا: `{"abcdef0123456789":{"p":[1,2,3],"c":0,"m":[{"p":3,"s":1,"l":"wird"},{"p":3,"s":1,"l":""}]}}` — صفرُ حرفٍ عربيّ، وصفرُ اسم. |
| ٧ | **الخطّافات** | `KhatmahPanel` (جديد): **صفرُ خطّاف**، ورجوعُه الوحيدُ عندَ المحرفِ ٣٤٠ — فلا ترتيبَ خطّافاتٍ فيه ليختلّ. `PagedMushaf`: ٣٤ ← **٣٨** خطّافًا، آخرُها عندَ المحرفِ ١٥٢٨٨ وأوّلُ رجوعٍ خاصٍّ به عندَ ٣٠٧٠٦. `MushafScreen`: ١٤ ← **١٧**، آخرُ خطّافٍ عندَ ٥٩٢١ وأوّلُ رجوعٍ عندَ ٦٢٩٠. **في الثلاثةِ: الرجوعُ بعدَ آخرِ خطّاف.** |

وثلاثةُ قياساتٍ زائدةٍ عن السبعةِ لأنّها شروطُ الأمرِ نفسِها:

- **فشلُ الكتابةِ لا يُبتلَع**: مخزنٌ يرفضُ كلَّ كتابةٍ ⟹ الشاشةُ **لا** تدّعي أنَّ الصفحةَ عُلِّمت، واللوحةُ تقولُ «لم يُحفَظْ هذا على الجهاز، وهو قائمٌ في هذه الجلسة وحدَها»، ولا شيءَ انهار.
- **لكلِّ ملفٍّ على حدة**: جهازٌ بلا ملفٍّ ⟹ السجلُّ تحتَ `anon`. ملفٌّ يحملُ `pid` ⟹ يرى صفحاتِه هو `[9,10]`، وسجلُّ الجهازِ يحتفظُ بـ`[5]`، وملفٌّ ثالثٌ مجهولٌ يبدأُ فارغًا. **وملفٌّ بلا `pid` أصلًا يسقطُ إلى سجلِّ الجهازِ ولا يسقطُ إلى اسمٍ أبدًا.**
- **العودةُ بضغطةٍ من الفهرس**: قسمُ «علامتاك» يظهرُ على الفهرسِ باسمِ العلامةِ وصفحتِها، **وصفُّ «تابِع القراءة» وصفُّ العلامةِ اليدويّةِ باقيانِ في مكانِهما**، والقسمُ الجديدُ **خارجَ شريطِ الصدر**؛ وضغطةٌ واحدةٌ تُنزِلُ القارئَ على صفحةِ العلامة (`mushaf_last_page_v1` صارَ `{"p":42,…}`).

### البوّابات — بعدَ الإيداعِ وعلى شجرةٍ نظيفة

شُغِّلَت `npm run gates` بعدَ الإيداعِ على شجرةٍ نظيفةٍ تمامًا. وللوصولِ إلى النظافةِ نقلتُ سبعةَ تقاريرَ **غيرَ متتبَّعةٍ من جولاتٍ سابقة** إلى خارجِ المستودعِ أثناءَ التشغيلِ ثمّ أعدتُها؛ لم يُحذَفْ منها شيءٌ ولم يُعدَّلْ فيها حرف.

```
HEAD                4e26fffcaa00275753311f4a6c300d88ca6924b9
tree                0 dirty path(s)          ← قبلَ التشغيلِ وبعدَه
الروستر              93 بوّابةً من gates.json
=== SUITE: 92/93 EXIT=0 ===
FAILING (1)         bankintegrity = 1
recon               PASS=184  WARN=1  FAIL=0
```

`recon 184/1/0` كما أمرَتْ §٣ بالضبط. و`WARN=1` هو التحذيرُ القائمُ منذُ قبلِ هذه الجولة
(`LONGEST_CARD_CHARS = 3405 > longest card 3401`)، لا علاقةَ له بالختمة.

**والأرضيّاتُ الثلاثُ التي تأمرُ §٣ ألّا تنقصَ — لم تنقصْ ولا واحدةٌ منها:**

| البوّابة | عندَ الأساسِ `8b45a39` | بعدَ الإيداع | الفرق |
|---|---|---|---|
| `wird` | `passed: 1122 · failed: 0` | `passed: 1122 · failed: 0` | **صفر** |
| `themecoverage` | `1351/1351` | `1351/1351` | **صفر** |
| `questux` | `61/61` | `61/61` | **صفر** |
| `lessonssearch` (حارسُ الدروس) | `269/269` | `269/269` | **صفر** — والعددُ الذي تسمّيه §٣ |
| `i18nui` | `277/277` | `277/277` | **صفر** |
| `a11y` | — | `138/138` | — |
| `chatux` | — | `439/439` | — |
| `lessonsbrowse` | `81/81` | `81/81` | **صفر** |

**والحمراءُ الوحيدةُ `bankintegrity`، ولم تُصلَحْ عمدًا:**

```
FAIL [B12] CORE_BYTES = 1870613 but CORE weighs 1892973 bytes on disk (+22360).
FAIL [B14] sw.js prose says app.js is 1035264 bytes; the disk says 1057624 (+22360).
FAIL  74 checks passed, 2 failed.
```

**الفرقُ رقمًا: `+22360` بايتة**، وهو رقمٌ واحدٌ يظهرُ مرّتَين — `app.js` نمَتْ من `1035264`
إلى `1057624`، وCORE نمَتْ بالمقدارِ عينِه. وموضعُ الرقمَينِ كليهما `sw.js`، وقطعُ `CORE_BYTES`
والختمُ ومسُّ `sw.js` محرَّمٌ في §٠ — **فلم يُقطَعْ ولم يُختَمْ ولم يُمَسّ**.

---

## ٥ · ما تراهُ عينُ المالكِ بعدَ النشر

يفتحُ المصحفَ فيجدُ تحتَ حبّةِ الوِردِ حبّةً ثانيةً: «علِّمْ هذه الصفحة». يضغطُها حينَ يفرغُ من الصفحةِ فعلًا، فتحمرُّ وتقولُ «صفحةٌ معلَّمة». وفي لوحةِ «وردُ اليوم» صارَ تحتَ منتقي الوِردِ شريطُ ختمةٍ يقولُ كم قرأَ من ستِّ مئةٍ وأربعٍ وكم بقي، وكم يومًا يبقى على وردِه اليوميِّ الذي اختارَه بنفسِه.

وفيها علامتانِ يضعُهما حيثُ شاءَ ويسمّي كلًّا منهما إن شاء، ويعودُ إليهما بضغطةٍ من اللوحةِ أو من فهرسِ السورِ — **والعلامةُ القديمةُ ومتابعةُ القراءةِ حيثُ تركَهما، لم يتغيّرْ فيهما شيء**.

وحينَ تكتملُ الستُّ مئةُ وأربع، يظهرُ سطرٌ واحدٌ: «تمّتِ الختمة. تقبّلَ اللهُ منك.» — ولا تبدأُ ختمةٌ جديدةٌ حتّى يضغطَ هو، ويبقى عددُ ما ختمَ.

---

## ٦ · ما لم يُقَسْ، وما كذّبَه القياس

### ٦-١ · ما لم يُقَسْ — مسمًّى ولا يُطوى

1. **لم أفتحِ التطبيقَ في متصفّحٍ حقيقيّ.** كلُّ قياسٍ في §٤ جرى في `linkedom` بجذرِ React حقيقيّ. فالسلوكُ والحالُ والتخزينُ مقيسةٌ على المكوِّنِ المشحون؛ **أمّا المظهرُ فلا**: لم أقِسْ عرضًا عندَ ٣٢٠px، ولا تباينَ لونٍ، ولا التفافَ الحبّتَينِ على سطرَين. (أعطيتُ `wirdWrap` الآنَ `flexWrap` و`gap`، وأعطيتُ زرَّ التعليمِ `minHeight: 44`، وكلُّ لونٍ رمزُ ثيمٍ لا لونٌ صريح — لكنَّ هذا احتياطٌ لا قياس.)
2. **مسلكُ الرجوعِ (`?madinaimg=0`) لا يحملُ الميزة.** حبّةُ التعليمِ ولوحةُ الختمةِ مشروطتانِ بـ`MADINA_IMG_ON`، **وهو عينُ شرطِ شريطِ الوِردِ ولوحتِه اليوم** — فتبعتُ السابقةَ ولم أوسّعْها. ومن أطفأَ قارئَ صورةِ المدينةِ عمدًا لا يرى الختمةَ ولا الوِرد. مقيسٌ في المصدرِ لا في متصفّح.
3. **لا هجرةَ من `anon` إلى `pid`.** من علّمَ صفحاتٍ قبلَ إنشاءِ ملفٍّ، ثمّ أنشأَ ملفًّا، يبدأُ من جديد. **وهذا سلوكُ `ezik_reading_prefs_v1` عينُه اليوم**، وقد تبعتُه بدلَ أن أخترعَ هجرةً لم تُطلَبْ منّي.
4. **لم أقِسْ صحّةَ الترجمةِ الإنجليزيّةِ عندَ ناطقٍ بها**، ولا الصياغةَ العربيّةَ عندَ عالمٍ. الأربعُ وعشرونَ مفتاحًا متطابقةُ الأسماءِ والحُواشي على النصفَين، والحارسُ يشهدُ بذلك — لكنَّ المطابقةَ ليست الجودة.
5. **لم أقِسِ الأداء.** لم أقِسْ أثرَ الحبّةِ الثانيةِ ولا اللوحةِ على زمنِ أوّلِ رسمٍ ولا على قلبِ الصفحة.
6. **`ezik_khatmah_v1` غيرُ محروسٍ ببوّابة.** أرقامُ §٤ كلُّها من مِرقبٍ **خارجَ المستودعِ**، لأنَّ إضافةَ بوّابةٍ تكلّفُ أربعةَ ملفّاتٍ وتنقلُ الروسترَ عن ٩٣، والأمرُ يثبّتُ ٩٣ ويحرّمُ مسَّ حارس. **فما تُثبِّتُه هذه الجولةُ في الشجرةِ صفرٌ، وأيُّ إعادةِ صياغةٍ لاحقةٍ لن تُحمِرَّ لها بوّابة.** وأوصي بأن تُفتَحَ لها بوّابةٌ في جولةٍ تملكُ الإذنَ بتكلفتِها.

### ٦-٢ · فرضيّاتُ الأمرِ التي كذّبَها القياس

1. **«معرِّفُ الملفِّ؟ أيوجدُ؟»** — يوجدُ، وهو `pid` منذُ الجلسةِ ٩٢. **والذي كذّبَه القياسُ هو الشجرةُ نفسُها**: تعليقُ `MUSHAF_BOOKMARK_KEY` ما زالَ يقولُ إنَّ الملفَّ «لا يحملُ معرِّفًا ثابتًا». لم أصحّحْه — ليس من عملِ هذه الجولة — وأسمّيه.
2. **«`bankintegrity` تحمرُّ بـ`CORE_BYTES` وحدَه»** — تحمرُّ **بتأكيدَينِ لا بواحد**، وهما رقمٌ واحدٌ بعينِه:
   - `[B12] CORE_BYTES = 1870613` بينما CORE على القرصِ `1892973` — **الفرقُ `+22360`**.
   - `[B14]` نثرُ `sw.js` يقولُ إنَّ `app.js` تزنُ `1035264` والقرصُ يقولُ `1057624` — **الفرقُ `+22360` عينُه**.

   وكلاهما في `sw.js`، و`sw.js` محرَّمة. **فلم يُصلَحْ شيءٌ، والفرقُ مُعلَنٌ رقمًا: `+22360` بايتة.** وسائرُ تأكيداتِ الحارسِ: `74 checks passed, 2 failed`.
3. **«`recon 184/1/0`»** — صحيحةٌ، **لكن بشرطٍ لا يذكرُه الأمر**: التأكيدُ الثالثُ والثمانونَ بعدَ المئةِ هو «شجرةُ العملِ نظيفة»، والتأكيدُ الرابعُ والثمانونَ هو تطابقُ سطرِ `RECON` في تقريرِ التنفيذِ مع ما عدَّه recon قبلَ فحصِ نفسِه. **وعندَ الأساسِ، وقبلَ أن ألمسَ شيئًا، كان recon يقولُ `182/1/1`** — لا لعطبٍ، بل لأنَّ في جذرِ الشجرةِ سبعةَ تقاريرَ غيرَ متتبَّعةٍ من جولاتٍ سابقة. نقلتُها إلى خارجِ المستودعِ للقياسِ ثمّ أعدتُها، **ولم أعدِّلْ سطرَ `RECON` في تقريرِ التنفيذِ ولا حرفًا**: كان `PASS=183 WARN=1 FAIL=0` وهو الصوابُ على شجرةٍ نظيفة، وقد طابقَ.
4. **«لا سطرَ ولا اسمَ دالّةٍ في هذا الأمرِ حجّةٌ»** — عملتُ بها. ولم يُذكَرْ في الأمرِ أنَّ الوِردَ اليوميَّ **يُعَدُّ بمؤقّتِ مكوثٍ بالفعل** (`WIRD_DWELL_MS = 8000`)، وهو أهمُّ ما وجدتُه: الأمرُ يحرّمُ العدَّ بالمرورِ للختمة، والشجرةُ تعُدُّ بالمكوثِ للوِرد. **لم أمسسِ الثانيةَ ولا حرفًا**، وبنيتُ الأولى عدّادًا مستقلًّا لا يشاركُها كاتبًا ولا مفتاحًا. ومقيسٌ في §٤-٢ أنَّ الاثنَينِ يعملانِ في المسيرِ نفسِه ولا يتلوّثُ أحدُهما بالآخر.

### ٦-٣ · حارسٌ رأيتُه يحتاجُ نظرًا — ووقفتُ ولم أمسّ

`tools/wird-guard.cjs` يثبّتُ عنصرَ القارئِ بتعبيرٍ نمطيٍّ ينتهي عندَ `onExit=\{\w+\} \/>;`، أي **إنّه يمنعُ إضافةَ أيِّ خاصّيّةٍ إلى `PagedMushaf` إلى الأبد**، وهو أوسعُ ممّا يقصدُه نصُّه («القارئُ يفتحُ من `selected` وحدَها»). دُرتُ حولَه بقراءةِ الملفِّ من المخزنِ، وهو حلٌّ صحيحٌ لا حيلة — لكنَّ الحارسَ يقيّدُ أكثرَ ممّا يحرس. **لم أمسسْه، وأُبلِّغُ.**

---

## ٧ · الحدُّ

```
git --no-pager status --porcelain   صفرُ سطرٍ عندَ تشغيلِ البوّابات
دفع                                  صفر
نشر                                  صفر
حارسٌ عُدِّل                            صفر
`CORE_BYTES`                          لم تُقطَعْ — والفرقُ +22360 مُعلَنٌ أعلاه
رفعةُ اسمِ مخزنٍ · ختمٌ                  صفر
```

**تُنتظَرُ كلمةُ المالك.**

---

REPORT_PAYLOAD_BYTES=32973
REPORT_SHA256=45e229b66a24614e69624640e6c725fe5990fa175ef0dab53e37c881d8e09207
REPORT_SHA8=45e229b6

(الحمولةُ المختومةُ: كلُّ بايتةٍ حتّى سطرِ الفصلِ الأخيرِ وسطرِه الجديدِ بعدَه، قبلَ إلحاقِ هذه الأسطر. أُعيدُ قطعُها بـ `head -c 32973 <file> | sha256sum`.)
