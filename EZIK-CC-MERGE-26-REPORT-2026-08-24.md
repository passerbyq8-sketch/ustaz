# EZIK — تقريرُ الدمج · الجولةُ ٢٦ · ٢٤ أغسطس ٢٠٢٦

**فرعٌ واحدٌ دُمِجَ بصفرِ تعارضٍ، والقطعُ والختمُ والرفعةُ وتصحيحُ نثرِ `sw.js` وقعَتْ في إيداعٍ واحد. البوّاباتُ الثلاثُ والتسعونَ خضراءُ بخروجٍ صفرًا. لا دفعَ ولا نشر.**

---

## ١ · الأساسُ · رأسُ الفرعِ · رأسُ الدمجِ · الإيداعات

| المفتاح | القيمة | مقابلَ الأمر |
|---|---|---|
| الشجرة | `C:\Users\passe\projects\ustaz` | — |
| الأساس | `origin/main` = `80007538e6c2ac9e38a5992734d30dd55b2d6fe5` | **مطابقٌ** لـ`8000753` |
| فرعُ الدمج | `merge/round-26-20260824`، مقطوعٌ من `origin/main` | مطابق |
| المدموج | `fix/reply-card-pdf-20260824` = `9242a594b0052ebd4d0454ba4e361c7cecb72b13` | **مطابقٌ** لـ`9242a59` |
| عددُ إيداعاتِه فوقَ الأساس | **٣** — `cfac9d3` · `a049492` · `9242a59` | **مطابقٌ**، والثلاثةُ بأعيانِها |
| رأسُ الدمج | **`56952b263f19e6b38bcc9966e6501b3c78c5a82f`** | — |

🔵 **وكلُّ رقمٍ في §٠ من الأمرِ صحَّ بالقياس** — وهذه أوّلُ جولةٍ من ثلاثٍ لا يكذّبُ فيها القياسُ رقمًا واحدًا من أرقامِ الهويّة.

| # | الإيداع | ما مسَّ |
|---|---|---|
| ١ | `cfac9d3` | بِدْيُ البطاقةِ وميزانيّةُ أسطرِها والرابطُ المقروء — `app.jsx` · `app.js` · `index.html` |
| ٢ | `a049492` | التصديرُ صارَ محرِّكَ طباعةِ المتصفّح، وخروجُ الحزمةِ، وتعديلُ الحارسِ بأمرِ المالك — `app.jsx` · `app.js` · `index.html` · `theme-coverage-guard.cjs` |
| ٣ | `9242a59` | عاقبتانِ كشفَهما الطقم — `theme-coverage-guard.cjs` · `EZIK-RFC-V0.5-R2-IMPLEMENTATION-REPORT.md` |
| **د** | **`71df665`** | **دمجُ `--no-ff`** — ٥ ملفّاتٍ · `402` إضافةً · `110` حذفًا |
| **ت** | **`56952b2`** | **إيداعُ التوفيقِ الواحد** — `sw.js` · `quest-bank-integrity-guard.cjs` فقط · `46` إضافةً · `17` حذفًا |

**تحقُّقُ الرأسِ بـ`merge-base --is-ancestor` لا بالمساواة** (فخّ §٣-٣): `fix/reply-card-pdf-20260824` **سلفٌ لـ`HEAD`** — مطبوعٌ ومقيس.

---

## ٢ · التعارضات

```
Merge made by the 'ort' strategy.
 EZIK-RFC-V0.5-R2-IMPLEMENTATION-REPORT.md |   2 +-
 app.js                                    | 149 +++++++++++++++++-----
 app.jsx                                   | 197 +++++++++++++++++++++++-------
 index.html                                |  40 +++---
 theme-coverage-guard.cjs                  | 124 ++++++++++++++++---
 5 files changed, 402 insertions(+), 110 deletions(-)
```

**عددُ التعارضات: صفر.** ولا شيءَ حُسِم، لأنّه لا شيءَ تنازعَ: فرعٌ واحدٌ يُدمَجُ في أساسٍ لم يتحرّكْ منذُ قُطِعَ منه.

---

## ٣ · `CORE_BYTES` · مواضعُ الأحجامِ · نصُّ الملحوظات

### ٣-١ · `CORE_BYTES` قبلَ وبعدُ

| | عندَ `origin/main` | بعدَ الدمج | الفرق |
|---|---|---|---|
| `index.html` (على القرص، CRLF) | `121979` | `122568` | **`+589`** — ستّةُ أسطرِ صحيفةِ الطباعة |
| `app.js` | `1019495` | `1026796` | **`+7301`** — البطاقةُ والتصديرُ معًا |
| الثمانيةُ الباقيةُ في `CORE` | لم تُمَسَّ | لم تُمَسَّ | `0` |
| **`CORE_BYTES`** | **`1854255`** | **`1862145`** | **`+7890`** |

`7890 = 589 + 7301` **بالبايتةِ الواحدة**. أُعيدَ قطعُه بـ`node tools/core-bytes.cjs --write` لا باليد.

**ولا دورَ في الأرقامِ** (تحذيرُ §٢ الأزرق): `sw.js` **ليسَ في `CORE`** — العشرةُ هي `/` و`manifest.json` وأربعُ أيقوناتٍ و`adhkar.json` و`app.js` وحزمتا React. فتحريرُ `sw.js` لا يغيّرُ `CORE_BYTES` ولا يُبطِلُ ما كُتِبَ فيه. قِيسَ مرّةً وكُتِبَ مرّةً، ثمّ خُتِمَ.

### ٣-٢ · كم موضعًا وُجِدَ لبياناتِ الأحجامِ — فعلًا

الأمرُ قالَ «بياناتُ الأحجامِ في `sw.js`». **المقيسُ ثلاثةٌ حيّةٌ في `sw.js`، ومعها مرآتانِ في الحارسِ لا تصحُّ إحداهما بغيرِ الأخرى، وموضعانِ تاريخيّانِ تُرِكا.**

| # | الموضع | النوع | ما جرى |
|---|---|---|---|
| ١ | `sw.js:57` — «نسخةٌ ثانيةٌ من `121979` بايتةً» | **حيّ** (حجمُ `index.html`) | `⟶ 122568` |
| ٢ | `sw.js:106` — جدولُ البايتاتِ فوقَ `CORE_BYTES`، شقُّ `index.html` | **حيّ** | `⟶ 122568` |
| ٣ | `sw.js:106` — الجدولُ نفسُه، شقُّ `app.js` | **حيّ** | `⟶ 1026796` |
| ٤ | `quest-bank-integrity-guard.cjs:1342` — `SW_PROSE` | **مرآةٌ ملزِمة** | `{ n: 121979 } ⟶ { n: 122568 }` |
| ٥ | `quest-bank-integrity-guard.cjs:1345` — `SW_PROSE` | **مرآةٌ ملزِمة** | `{ n: 1019495 } ⟶ { n: 1026796 }` |
| ٦ | `quest-bank-integrity-guard.cjs:122–123` — سجلُّ إعادةِ القطعِ لجولةِ ٢٥ | **تاريخيّ** | تُرِكَ كما هو |

**والمرآتانِ ليستا اختيارًا:** فحصُ `B14` يوجبُ أن يذكرَ نثرُ `sw.js` كلَّ عددٍ في `SW_PROSE` **وأن يطابقَ القرص**، ويوجبُ في الاتّجاهِ الآخرِ ألّا يبقى في النثرِ عددٌ ≥ 500 غيرُ مسجَّل. فتحريكُ ثلاثةٍ دونَ اثنتَينِ يقلبُ حمرةً واحدةً إلى حمرتَين.

**والعددُ الذي أَحصيتُه: `121979` في أربعةِ مواضعَ (ثلاثةٌ حيّةٌ وواحدٌ تاريخيّ) · و`1019495` في ثلاثةٍ (اثنانِ حيّانِ وواحدٌ تاريخيّ).**

### ٣-٣ · الملحوظاتُ — نصُّها قبلَ وبعدُ

#### 🔴 الملحوظةُ الأولى (`sw.js:21–25`) — كانت كاذبةً، فصُحِّحَتْ

**قبل:**
```
//   ignored     : every other origin (everyayah.com recitation audio, the cdnjs bundles that
//                 html2pdf and mammoth are fetched from on first use) -- left entirely to the
//                 network; the SW never intercepts them.
```
**بعد:**
```
//   ignored     : every other origin (everyayah.com recitation audio, the one cdnjs bundle that
//                 mammoth is fetched from when a reader attaches a .docx) -- left entirely to
//                 the network; the SW never intercepts them. ... ITEM 42-B took the PDF bundle
//                 off cdnjs as well -- the export is the browser's own print engine now and
//                 fetches nothing -- so ONE bundle is named here where two used to be.
```

#### 🔴 الملحوظةُ الثانية (`sw.js:34–36`) — كانت كاذبةً في شقَّين، فصُحِّحَتْ

**قبل:**
```
//    TWO CDN SCRIPTS REMAIN AND NEITHER BLOCKS A BOOT: html2pdf and mammoth are fetched from
//    cdnjs the first time a reader exports a PDF or attaches a .docx, and never otherwise. An
//    offline reader loses those two features and nothing else -- the app itself renders.
```
**بعد:**
```
//    ONE CDN SCRIPT REMAINS AND IT DOES NOT BLOCK A BOOT: mammoth is fetched from cdnjs the
//    first time a reader attaches a .docx, and never otherwise. An offline reader loses that
//    one feature and nothing else -- the app itself renders.
//    IT USED TO BE TWO. Item 42-B measured the PDF bundle drawing every run of Arabic with a
//    left-to-right base direction -- words glued together, the title glued to itself, and a
//    flattened image of the page instead of text -- and took it out of the tree entirely. The
//    export is window.print() now: it composes the document with the browser's own print engine
//    under @media print, fetches NOTHING, and therefore works with no network at all. ...
//    The line above this one still quotes the note as it read before item 32; that quotation is
//    a record and is left exactly as it was.
```

#### 🟡 الملحوظةُ الثالثةُ (`sw.js:30`) — **ليست كاذبةً، وتُرِكَتْ عمدًا**

```
// OFFLINE BOOT IS POSSIBLE NOW, and this note is what it replaced. It used to read: "OFFLINE
//    BOOT IS NOT POSSIBLE while React/Babel/html2pdf/mammoth load from unpkg + cdnjs
//    (cross-origin <script> tags in index.html) ..."
```

**هذا اقتباسٌ، لا دعوى.** الجملةُ المحيطةُ تقولُ صراحةً `It used to read: "…"` وتُتبِعُها بـ`Item 32 did it`. فالنصُّ **سجلٌّ صادقٌ لدعوى قديمةٍ كاذبة**، وتصحيحُ اقتباسٍ يجعلُ السجلَّ كاذبًا لا صادقًا. **تُرِكَ حرفًا**، وأُضيفَ تحتَه سطرٌ يقولُ إنّه اقتباسٌ متروكٌ عمدًا فلا يظنُّه قارئٌ بعدَ سنةٍ سهوًا.

### ٣-٤ · 🔴 فرضيّةٌ كذّبَها القياس

الأمرُ يقولُ **«ثلاثُ ملحوظاتٍ في `sw.js` صارَتْ كاذبةً (تقولُ إنّ حزمةَ الـPDF تُجلَبُ عندَ أوّلِ تصدير)»**، وهو ناقلٌ عن تقريرِ الجولةِ السابقةِ §٨-٣ الذي سمّى السطورَ `22` و`28` و`34`.

**المقيسُ: كاذبتانِ لا ثلاث.** السطرُ `28` (`30` اليوم) لا يقولُ إنّ الحزمةَ تُجلَبُ عندَ التصدير — إنّه **داخلَ اقتباسٍ** لنصٍّ سابقٍ عن وسومِ `<script>` عندَ الإقلاع، والاقتباسُ صادقٌ بوصفِه اقتباسًا. **الخطأُ خطأُ تقريري أنا، لا خطأُ الأمر**، وهو مصحَّحٌ هنا وفي سجلِّ الحارس.

### ٣-٥ · رفعةُ اسمِ المخزنِ والختم

| # | الموضع | قبل | بعد |
|---|---|---|---|
| ١ | `sw.js:54` — `const CACHE` | `'ezik-v24'` | **`'ezik-v25'`** |
| ٢ | `quest-bank-integrity-guard.cjs:259` — `const SW_CACHE` | `'ezik-v24'` | **`'ezik-v25'`** |
| ٣ | `quest-bank-integrity-guard.cjs:124` — سجلٌّ تاريخيّ | `CACHE moved v23 -> ezik-v24` | `CACHE moved v23 -> v24` |

الموضعُ ٣ **سجلٌّ لم يُكذَّبْ بحرف**: قُصِّرَ الاسمُ المتقاعدُ وحدَه، وهو عرفُ الملفِّ نفسِه — `v21 -> ezik-v22` و`ezik-v22 -> v23` قبلَه.

**و`ezik-mushaf-pages-v1` لم يُمَسَّ ألبتّة**، والبوّابةُ تشهدُ بسطرَيها:
`PASS a version bump sweeps the superseded store and LEAVES "ezik-mushaf-pages-v1" intact`
`PASS the page store still carries its own unversioned name (ezik-mushaf-pages-v1)`

**والختمُ** أُعيدَ قطعُه **بإبدالِ سطرٍ واحدٍ في مكانِه** — لا بتوليدِ الخريطةِ جملةً — والسكربتُ **يرفضُ** إن لم يجدْ وردًا واحدًا بالضبطِ، **ويرفضُ** إن حملَ `sw.js` بايتةَ `CR` واحدةً كما تشترطُ ملحوظةُ `D09`:

```
SW_BYTES     = 43912        (كانَ 42995)
SW_CR_BYTES  = 0
sha256 قبل   = 752e79fe7a4266c020d28ec2c3df081a6b25c3335b46095477cd7f7c46ff27fb
sha256 بعد   = c61fb85b9440f87778e9396e54d718a622a16ec1d9de53b7f8a9d91fd14df0e1
```

`sw.js` نما `+917` بايتة، كلُّها نثرٌ: الملحوظتانِ المصحَّحتانِ والقياسُ الذي أنهى الأولى.

---

## ٤ · بوّاباتُ §٤ السبعُ — بنصِّها

**والجريانُ مُعرَّفٌ بـHEAD المطبوعِ في رأسِه** (فخّ §٣-٥):
`head_before = 56952b26…` · `head_after = 56952b26…` · `dirty = 0/0` · `tree_dirtied_by_run = false`.
المجلَّد: `…\Temp\ezik-gates\runs\2026-08-24T12-11-12-645Z-2916`

| # | البوّابة | الشرط | **المقيس** | |
|---|---|---|---|---|
| ١ | الروستر | `93` | **`93`** | 🟢 |
| ٢ | `SUITE` | `93/93` · خروجٌ صفرًا | `=== SUITE: 93/93 EXIT=0 ===` · `GATES_EXIT=0` · `failed=[]` | 🟢 |
| ٣ | `recon` | `PASS=184 WARN=1 FAIL=0` | `SUMMARY   PASS=184   WARN=1   FAIL=0` | 🟢 |
| ٤ | حارسُ الدروس | `269` | `=== 269/269 - PASS ===` | 🟢 |
| ٥ | `themecoverage` | `1351/1351` | `OK: 1351/1351 checks passed.` | 🟢 |
| ٦ | `bankintegrity` | **خضراءُ بعدَ إيداعِ التوفيق** | `PASS  76 checks passed, 0 failed.` | 🟢 |
| ٧ | اسمُ المخزن | `ezik-v24` صفرُ مواضع | `grep -rlE "ezik-v24([^0-9]|$)"` ⟹ **`0` ملفّاتٍ في الشجرةِ كلِّها** | 🟢 |

والإنذارُ الوحيدُ هو المعروفُ ولم يتغيّر:
`[WARN] LONGEST_CARD_CHARS = 3405 > longest card 3401 -> cap oversized/stale (re-derive in api/report.js)`

### ٤-١ · `recon 184` — الأمرُ كانَ محقًّا وقد تحقّقتُ منه لا بالظنّ

أمرَ §٤ بطباعةِ المقيسِ لا بتصحيحِ الأمرِ بالظنّ. **المقيسُ `184` كما قالَ الأمرُ بالضبط.** والسببُ مقيسٌ بسطرِه: `recon-audit.cjs:426` يُصدِرُ `pass('pinned: ' + u)` لكلِّ رابطِ CDN مثبَّتٍ في `index.html`، وقد خرجَ رابطُ حزمةِ الـPDF في الجولةِ السابقة. **والأرضيّةُ عندَ `origin/main` قِيسَتْ `185/1/0` قبلَ الدمج** — فالفرقُ سببُه الفرعُ المدموجُ نفسُه، لا هذه الجولة.

### ٤-٢ · فخُّ §٣-٢ وقعَ كما وُصِف

`bankintegrity` **حمراءُ بينَ الدمجِ وإيداعِ التوفيق**، مقيسةً لا مفترَضة: بعدَ الدمجِ مباشرةً قالَ `tools/core-bytes.cjs` ⟸ `DRIFT — CORE_BYTES is 1854255 and CORE weighs 1862145 (+7890)` وخرجَ بـ`1`. وإيداعُ الشحنةِ هو الذي أصلحَها: `76/0` بعدَه.

### ٤-٣ · الأرضيّةُ مقابلَ ما بعدَ الإيداع

| المقياس | `origin/main` (`8000753`) | بعدَ الإيداع (`56952b2`) |
|---|---|---|
| الروستر | `93` | `93` |
| `SUITE` | `93/93 EXIT=0` | `93/93 EXIT=0` |
| `recon` | `185/1/0` | `184/1/0` (§٤-١) |
| حارسُ الدروس | `269/269` | `269/269` |
| `themecoverage` | `1346/1346` | **`1351/1351`** |
| `bankintegrity` | `76/0` | `76/0` |
| `CORE_BYTES` | `1854255` مطابقٌ للقرص | `1862145` مطابقٌ للقرص |
| `git status` | صفرُ سطر | صفرُ سطر |

**ولا مقياسَ انخفضَ عن أرضيّتِه إلّا `recon`، وعلّتُه مسمّاةٌ بسطرِها؛ و`themecoverage` ارتفعَ خمسةَ تأكيدات.**

---

## ٥ · ما تراهُ عينُ المالكِ بعدَ النشر

### ٥-١ · على الشاشة: بطاقةُ الردِّ تُقرأُ أخيرًا كما تُكتَب

يفتحُ المالكُ ردًّا ويضغطُ **«صورة»**. البطاقةُ التي تنزلُ الآنَ:

- **النقاطُ والفواصلُ في أواخرِ الأسطرِ لا أوائلِها.** قبلَ هذه الجولةِ كانت النقطةُ تقفزُ إلى **بدايةِ** السطرِ التالي — `.الفتوى المسندة فيها` — في سبعةِ أسطرٍ من سبعةٍ تنتهي بترقيم. والآنَ صفرٌ من تسعة. والأرقامُ `١.` `٢.` `٣.` تقفُ في أوّلِ سطورِها لا في آخرِها.
- **البطاقةُ امتلأتْ.** كانت تقطعُ النصَّ وتتركُ قرابةَ ربعِها فارغًا تحتَ عبارةِ «النصُّ مقتطع» — الآنَ ستّةَ عشرَ سطرًا بدل ثلاثةَ عشرَ، وامتلاءُ منطقةِ النصِّ `98٪` بدل `80.6٪`.
- **الشعارُ المائيُّ خلفَ الكلامِ خفَّ إلى النصف**، فصارَ يُرى ولا يُزاحِم.
- **وشارةُ «مقتطع» ما تزالُ تظهرُ متى قُطِعَ نصٌّ حقًّا** — لم تُنزَعْ ولم تُخفَّفْ.

### ٥-٢ · على الشاشة: زرُّ **«PDF»** يفتحُ ورقةَ الطباعةِ بدلَ أن يُنزِلَ ملفًّا

**هذا أظهرُ تغييرٍ في هذه الجولةِ ويستحقُّ عينَ المالكِ قبلَ النشر.** كانَ الضغطُ على «PDF» يُنزِلُ ملفًّا إلى مجلَّدِ التنزيلاتِ مباشرة. صارَ يفتحُ **ورقةَ الطباعةِ التي يعرفُها الجهاز**: على الحاسوبِ وجهتُها «حفظ كـPDF»، وعلى الآيفون هي ورقةُ المشاركةِ ومنها «حفظ في الملفّات». واسمُ الملفِّ المقترَحُ ما يزالُ اسمَ الردِّ نفسِه.

**ولمَ استحقَّ ذلك:** الملفُّ الذي كانَ ينزلُ **صورةٌ مسطَّحةٌ للصفحة**، فيها العنوانُ ملتصقٌ «ردُّعزك»، و`64` زوجًا من `320` من الكلماتِ ملتصقةٌ بعضُها ببعض، والفواصلُ منقلبةٌ — ولا يُظلَّلُ فيها حرفٌ ولا يُنسَخ. والذي ينزلُ الآنَ **نصٌّ حقيقيّ**: العنوانُ بفراغِه، وصفرُ زوجٍ ملتصقٍ من `272`، وصفرُ سطرٍ يبدأُ بعلامةِ ترقيم، ويُظلَّلُ ويُنسَخُ ويقرؤه قارئُ الشاشة. وحجمُه `38,923` بايتةً بدل `358,167` — **أقلُّ بتسعةٍ وثمانينَ بالمئة**.

**وجوابٌ طويلٌ صارَ يخرجُ في ثلاثِ صفحاتٍ سليمة**: صفرُ حرفٍ فوقَ حرف، وصفرُ سطرٍ مبتورٍ عندَ انتقالِ الصفحة.

### ٥-٣ · ما تحتَ السطحِ ولا يظهرُ في الشاشة

- **حزمةُ ٩٠٦ كيلوبايتٍ خرجَتْ من الشجرةِ خروجًا تامًّا.** كانَ أوّلُ تصديرٍ يضغطُه القارئُ يُنزِلُها من مضيفٍ خارجيّ. صارَ التصديرُ **لا يجلبُ شيئًا ألبتّة** — فيعملُ **بلا شبكةٍ** لأوّلِ مرّة. وتبعيّةُ سلسلةِ التوريدِ نقصَتْ واحدةً: خريطةُ الموردينِ صارَتْ مدخلًا واحدًا (قارئُ `.docx`).
- **مخزنُ الشحنةِ صارَ `ezik-v25`.** أوّلَ فتحةٍ بعدَ النشرِ ينزلُ عاملٌ جديدٌ، يُخلي كلَّ مخزنٍ لا يطابقُ الاسمَ، ويسلّمُ التحكّمَ فورًا. **و٦٠٤ مسحةِ صفحةٍ من المصحفِ لا تُمحى** — مخزنُها مستثنًى بالاسم؛ لولا ذلك لضاعَ ستّةٌ وستّونَ ميغابايتًا من جهازِ كلِّ قارئٍ عندَ كلِّ شحنة.
- **الروابطُ في المستندِ المصدَّرِ تُقرَأُ.** كانَ رابطُ الفتوى يُطبَعُ `%D9%85%D8%B0…` في ستّةٍ وخمسينَ هروبًا على أربعةِ أسطر. صارَ عربيًّا مقروءًا، وصفرَ هروب، ولا يفيضُ عن عرضِ الورقة.
- **ونصُّ الجوابِ نفسُه لم يتغيّرْ بحرف.** فكُّ الترميزِ **عرضٌ لا تحرير**: الحافظةُ والمشاركةُ والصوتُ والبطاقةُ تحملُ الرابطَ كما كتبَه الجوابُ بايتةً بايتة.
- **وثلاثُ ملحوظاتٍ في عاملِ الخدمةِ كانت تصفُ آلةً لم تعدْ موجودةً** — صُحِّحَتْ ثنتانِ، وتُرِكَتِ الثالثةُ لأنّها اقتباسٌ (§٣-٣).

### ٥-٤ · ما لم ينزلْ — ولماذا

| ما لم ينزل | العلّة |
|---|---|
| أيُّ تعديلِ حارسٍ في **هذه** الجولة | §٠ تحرّمُه. الحارسُ الذي عُدِّلَ عُدِّلَ في `a049492` **بأمرِ المالكِ الصريحِ** في جولةٍ سابقة، ونزلَ هنا بالدمجِ لا بالتحرير. وما مُسَّ في `quest-bank-integrity-guard.cjs` اليومَ هو **ما أمرَتْ به §٢ حرفًا**: اسمُ المخزنِ، ومرآتا `SW_PROSE`، والختمُ، وسطرُ السجلِّ الذي يشرحُها. **صفرُ تأكيدٍ نُزِع · صفرُ أرضيّةٍ خُفِضَتْ · صفرُ استثناءٍ وُسِّع.** |
| أيُّ بندٍ جديد | §٠ تحرّمُه. |
| إصلاحُ إنذارِ `LONGEST_CARD_CHARS` | إنذارٌ معروفٌ قديمٌ في `api/report.js`، وفتحُه بندٌ جديد. |
| الدفعُ والنشر | 🔴 المحرَّمُ في §٠. `origin/main` ما يزالُ `8000753`، لم يُلمَسْ ولم يُقدَّمْ. |

---

## ٦ · ما لم يُقَسْ · وما كذّبَه القياس

### ٦-١ · فرضيّاتُ هذا الأمرِ مقابلَ القياس

1. **🟢 كلُّ أرقامِ §٠ صحَّتْ** — الأساسُ `8000753`، ورأسُ الفرعِ `9242a59`، وثلاثةُ إيداعاتٍ بأعيانِها. لم يُكذَّبْ رقمُ هويّةٍ واحد.
2. **🟢 `recon 184` صحَّ** — والأمرُ نبَّهَ إليه صراحةً ونهى عن تصحيحِه بالظنّ. قِيسَ فكانَ `184`.
3. **🔴 «ثلاثُ ملحوظاتٍ كاذبة» ⟵ المقيسُ: ثنتانِ كاذبتانِ وواحدةٌ اقتباس** — §٣-٤. **والخطأُ خطأُ تقريري السابقِ الذي نقلَ عنه الأمر.**
4. **🟡 «بياناتُ الأحجامِ في `sw.js`» ثلاثةٌ** — لكنّها **لا تصحُّ وحدَها**: مرآتانِ في الحارسِ تلزمانِ معها، فالمواضعُ العاملةُ خمسةٌ في ملفَّين (§٣-٢).

### ٦-٢ · ما لم يُقَسْ — يُسمّى ولا يُطوى

1. **🔴 لم أفتحْ متصفّحًا في هذه الجولةِ ولم أرَ بطاقةً ولا ملفَّ PDF.** كلُّ ما في §٥-١ و§٥-٢ **منقولٌ عن قياسِ الجولتَينِ السابقتَينِ** (متصفّحٌ حقيقيٌّ بلا رأس، وعيّناتٌ محفوظة)، لا مُعادُ قياسِه هنا. **العلّة:** هذه جولةُ دمجٍ لا جولةُ تنفيذ، والشيفرةُ نزلَتْ بالدمجِ بلا تغييرِ حرفٍ فيها.
2. **🔴 ولم يُختبَرْ ما يراهُ القارئُ على iOS.** أنّ ورقةَ الطباعةِ تفتحُ «حفظ في الملفّات» **مقروءٌ من التوثيقِ لا مقيسٌ على جهاز**. وهو أظهرُ تغييرٍ يلمسُه القارئ، **فيستحقُّ عينَ المالكِ قبلَ النشرِ أكثرَ من أيِّ شيءٍ آخرَ هنا**.
3. **لم أتحقّقْ أنّ `ezik-v25` يُخلي `ezik-v24` فعليًّا على جهازِ قارئ.** المقيسُ حارسُ `bankintegrity` وحدَه، يشغّلُ العاملَ في `vm` بـ`caches` و`fetch` مُدجَّنَين. وكذلك بقاءُ `ezik-mushaf-pages-v1`. **العلّة:** لا جهازَ ولا متصفّحَ في هذا المسار.
4. **لم أقِسْ وزنَ ما يُشحَنُ كلَّه** كما فعلَتْ جولةُ ٢٥. المقيسُ `CORE_BYTES` وحدَه، وهو **ليسَ وزنَ الشحنة**: العشرةُ المسبوقةُ بالتخزينِ جزءٌ من خمسينَ ممّا يُرفَع. **العلّة:** §٥ لم تطلبْه، ولم أفتحْ بندًا.
5. **`sw.js` نما `+917` بايتةً ولم أقِسْ أثرَ ذلك على زمنِ تحليلِ العامل.** الزيادةُ كلُّها تعليقاتٌ، والمتصفّحُ يحلّلُها ولا ينفّذُها — **وهذا استنتاجٌ لا قياس**.
6. **لم أشغّلْ أيَّ بوّابةٍ ثلاثَ مرّاتٍ لفرزِ الرفرفة.** الطقمُ مرّتَين — عندَ الأرضيّةِ وبعدَ الإيداعِ — وكلتاهما خضراءُ من أوّلِ مرّة. **لا حمرةَ تستدعي فرزًا.**
7. **لم أمسحِ الشجرةَ كلَّها بحثًا عن انقسامِ CRLF/LF.** قِستُ ما مسَّتْه هذه الجولةُ: `sw.js` (`CR=0`) و`index.html` (`bare LF = 0` · `bare CR = 0`، بأداةٍ تقرأُ البايتاتِ لا بأدواتِ git-bash).
8. **ثلاثةُ ملفّاتٍ غيرِ متعقَّبةٍ نُقِلَتْ من جذرِ الشجرةِ قبلَ الطقمِ** — أمرُ هذه الجولةِ وتقريرا الجولتَينِ السابقتَين — لأنّ `run-gates` يخرجُ بـ`1` على أيِّ اتّساخ. **وأُعيدَ التقريرانِ إلى الجذرِ بعدَ الطقمِ وأُعيدَ قطعُ ختمَيهما للتحقّق**: `97395185` و`c8bd5e48` كما كانا. **مذكورٌ لأنّه تغييرٌ في حالةِ الشجرةِ لم يأمرْ به الأمرُ نصًّا.**
9. **البندُ ٤٢-ج ما يزالُ مفتوحًا.** لم يُغلَقْ في جولتِه ولا في هذه: أمرُه علَّقَ الإغلاقَ على أن يرى المالكُ البطاقةَ والملفَّ سليمَينِ بعينِه. **العيّنتانِ بانتظارِه** في `…\scratchpad\42\samples42b\`.

---

## ٧ · الحال

```
origin/main                      80007538e6c2ac9e38a5992734d30dd55b2d6fe5   (لم يُمَسَّ)
merge/round-26-20260824  HEAD    56952b263f19e6b38bcc9966e6501b3c78c5a82f
الإيداعاتُ فوقَ الأساس            cfac9d3 · a049492 · 9242a59 · 71df665 (دمج) · 56952b2 (توفيق)
تعارضات                          صفر
حارسٌ عُدِّلَ في هذه الجولة        صفر  (وما مُسَّ في حارسِ البنكِ هو ما أمرَتْ به §٢)
بندٌ جديدٌ فُتِح                   صفر
git status --porcelain           صفرُ سطرٍ قبلَ كتابةِ هذا التقرير
دفع · نشر                        صفر
```

**تُنتظَرُ كلمةُ المالكِ بالنشر.**

---

REPORT_PAYLOAD_BYTES=26628
REPORT_SHA256=f2db6fbf346e199281bcde4c7f8e9bf009930239cfdf28eb2688ae5a0827aed2
REPORT_SHA8=f2db6fbf

(الحمولةُ المختومةُ: كلُّ بايتةٍ حتّى سطرِ الفصلِ الأخيرِ وسطرِه الجديدِ بعدَه، قبلَ إلحاقِ هذه الأسطر. أُعيدُ قطعَها بـ `head -c 26628 <file> | sha256sum`.)
