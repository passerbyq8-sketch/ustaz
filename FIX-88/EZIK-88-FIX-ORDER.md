# EZIK — أمرُ الإصلاحِ الطويل · البندُ `٨٨` · رحلةٌ واحدة

> **هذا الملفُّ أمرٌ واحدٌ قارٌّ.** نفِّذْه من أوّلِه إلى آخرِه ولا تسألِ المالكَ في أثنائِه.
> **ثلاثةُ أفعالٍ بهذا الترتيبِ لا غيرِه: تُصلِحُ ⟶ تجرّبُ بنفسِك ⟶ تكتبُ التقرير.**
> ومصدرُ الأعطابِ تقريرُ التشييكِ في `C:/Users/passe/projects/ustaz-check88/check88/EZIK-CHECK-REPORT.md` — اقرأْه أوّلًا كلَّه.

---

## ٠ · الأرضُ والحدود

| المفتاح | القيمة |
|---|---|
| شجرةُ عملِك | `C:\Users\passe\projects\ustaz-fix88` — أُنشِئَتْ لك، لا تخرجْ منها كاتبًا |
| فرعُك | `fix/item88-defects-20260914` مبنيٌّ على `origin/main` |
| مجلَّدُ أوراقِك | `C:\Users\passe\projects\ustaz-fix88\FIX-88` |
| شجرةُ الأداةِ (اقرأْ منها وشغّلْها، ولا تُصلِحْ فيها إلّا ما يقولُه §٥) | `C:\Users\passe\projects\ustaz-check88\check88` |
| شجرةُ `main` | `C:\Users\passe\projects\ustaz` — **لا تدخلْها كاتبًا ألبتّة** |
| المحرّكُ الوحيدُ العامل | `chromium.launch({ headless: true, channel: 'msedge' })` — كروميومُ المرفقُ ميّتٌ بـ`spawn UNKNOWN` |

### الممنوعُ نصًّا

| # | الممنوع |
|---|---|
| ١ | **لا** `git add .` — أسماءُ الملفّاتِ صراحةً في كلِّ إيداع |
| ٢ | **لا دمجَ في** `main` **ألبتّة** · ولا دفعَ لغيرِ فرعِك |
| ٣ | **الملفّاتُ الثلاثةُ لا تُمَسّ:** `guards/attribution-truth-guard.cjs` · `guards/tag-honesty-guard.cjs` · `api/ask.js` |
| ٤ | **لا تحرِّرْ** `app.js` **بيدِك** — هو حصيلةُ بناء. تُحرَّرُ `app.jsx` ثمّ يُعادُ البناءُ بسكربتِ المشروعِ نفسِه |
| ٥ | **متغيّرٌ واحدٌ في كلِّ إيداع** — عطبٌ واحدٌ = إيداعٌ واحدٌ بعنوانٍ يسمّيه |
| ٦ | **لا عربيَّ يُطبَعُ في الطرفيّة** — مخرَجُك `ASCII` والعربيُّ في الملفّات |
| ٧ | **لا رقمَ سرّيَّ ولا رمزَ مؤسّسٍ** في ملفٍّ ولا سجلٍّ ولا طرفيّة |
| ٨ | **لا تُصلِحْ ما لم يُقَسْ** — كلُّ مشكوكٍ يُقاسُ أوّلًا، فإن لم يثبتْ كُتِبَ «لم يثبتْ» وتُرِكَ |

---

## ١ · ما أضافَه المالكُ بعينِه اليوم — يُقاسُ ثمّ يُصلَحُ

| # | لفظُه | ما يعنيه للإصلاح |
|---|---|---|
| ١ | **«قسمُ المرأةِ والمقالاتِ ما زالا فارغَين»** | 🔴 القسمانِ **بلا محتوًى أصلًا**. فرسالةُ «انقطعَتِ الشبكةُ» فيهما قد لا يكونَ سببُها الشبكةَ ولا الخنقَ بل **الفراغ**. قِسْ أوّلًا ماذا يردُّ `/api/articles-list` فعلًا: رمزُ الحالةِ وشكلُ الجسمِ وعددُ العناصر |
| ٢ | **«الفتاوى والدروسُ دائمًا فيهما تأخيرٌ عندَ الفتح»** | 🔴 بندٌ جديدٌ لم تقسْه الأداةُ من قبل: **زمنُ ظهورِ المتنِ بعدَ الفتح**، وهل يظهرُ للقارئِ مؤشّرُ تحميلٍ في أثنائِه أم شاشةٌ ساكنة |

**وهذانِ يدخلانِ الرحلةَ بمرحلتَيهما المستقلّتَينِ أدناه.**

---

## ٢ · المرحلةُ صفر — القياسُ قبلَ أيِّ سطر

1. سجّلْ: `origin/main` · `HEAD` شجرتِك · الشجرةُ نظيفةٌ أم لا.
2. شغّلْ في شجرتِك: `npm run gates` و`node recon-audit.cjs` واكتبِ النتيجتَين.
   **خطُّ الأساسِ المقيسُ اليوم:** الطقمُ `112/113` والساقطُ `liveness` وحدَه · و`recon` خروجٌ `0` بـ`PASS=203 WARN=1 FAIL=0`.
   **هذا الخطُّ عقدٌ:** إن خرجَ إصلاحٌ منك يُنقِصُه فالإصلاحُ يُردُّ لا الحارس.
3. **جِدْ سكربتَ البناءِ والختمِ في** `package.json` **وسمِّه في تقريرِك** — هو الذي يبني `app.js` من `app.jsx` ويرفعُ ختمَ المخزن. لا تخترعْ بناءً من عندِك.
4. **اقرأْ عقدَ رسائلِ الخطأ:** اجمعْ من `app.jsx` كلَّ مفاتيحِ القاموسِ التي تصفُ حالًا سيّئًا (`*.error` · `common.retry` · `feedback.*` · ما شابهَها) واكتبْ جدولَها. **لا تخترعْ مفتاحًا ولا نصًّا عربيًّا من رأسِك** — أيُّ نصٍّ جديدٍ يلزمُه مفتاحٌ في القاموسِ على هيئةِ أخواتِه.

---

## ٣ · مِنصّةُ التجربةِ — تُبنى قبلَ أوّلِ إصلاح

🔑 **لن تجرّبَ إصلاحَك على الموقعِ الحيِّ — الحيُّ يحملُ الشفرةَ القديمة.**

| # | الواجب |
|---|---|
| ١ | قدِّمْ شجرةَ عملِك من خادمٍ محلّيٍّ ساكنٍ على منفذٍ حرٍّ، وسجّلِ الأمرَ والمنفذ |
| ٢ | افتحْه ببلايرايت بالمحرّكِ المذكور |
| ٣ | **اعترضْ نداءاتِ** `/api/**` **وزوِّرْ ردودَها** — هنا الاعتراضُ **مأذونٌ ومطلوبٌ** لأنّ المقصودَ اختبارُ ما يرسمُه العميلُ عندَ كلِّ ردّ. (منعُ الاعتراضِ في الأمرِ السابقِ كانَ خاصًّا بالبطاريّةِ المدفوعةِ وحدَها) |
| ٤ | **الحالاتُ الأربعُ التي تُزوَّرُ لكلِّ قسمٍ يجلبُ من الشبكة:** `429` خنقٌ · `503` أو سقوطُ الاتّصالِ · `200` بقائمةٍ **فارغة** · `200` بعناصرَ حقيقيّة |
| ٥ | **قِسْ قبلَ وبعدَ:** شغّلِ المِنصّةَ نفسَها على شجرةٍ من `origin/main` غيرِ مصلَحةٍ أوّلًا، فيكونَ عندَك عمودانِ في التقرير: «قبل» و«بعد» — بالنصِّ الذي ظهرَ في كلِّ حال |

---

## ٤ · الإصلاحاتُ — بهذا الترتيب

### ٤-أ · الدفعةُ (أ) — الرسائلُ التي تكذبُ أو تصمتُ

**١ · `٥`+`٦` المقالاتُ وركنُ المرأة.**
المقيسُ: الرسالةُ `articles.error` + `common.retry` ظهرَتْ **والشبكةُ موصولة** · وطولُ المتنِ `93` مقطوعًا و`93` موصولًا · و`48` ردَّ `429` على `/api/articles-list` في أثناءِ المسح · **والمالكُ يقولُ إنّ القسمَينِ فارغانِ أصلًا**.
**الواجب:** يُفصَلُ مآلُ الجلبِ إلى **أربعِ حالاتٍ لكلٍّ منها سطرُها**: خنقٌ (`429`) ⟵ سطرٌ يقولُ «تجاوزتَ الحدَّ، أعِدْ بعدَ قليل» · انقطاعٌ حقيقيٌّ ⟵ سطرُ الانقطاعِ الحاليُّ · **فارغٌ بلا محتوًى ⟵ سطرُ حالةٍ فارغةٍ جديدٌ** لا سطرُ خطأٍ · وعناصرُ ⟵ تُرسَم.
**ولا يُخلَطُ رمزُ حالةٍ بآخر.** والمفاتيحُ الجديدةُ تُضافُ إلى القاموسِ على هيئةِ أخواتِها.

**٢ · `٧` الدروسُ مقطوعَ الشبكةِ: بياضٌ صامت.**
المقيسُ: `63` حرفًا مقطوعًا مقابلَ `1057` موصولًا · سقطَ `1` من `2` من نداءاتِه · **ولم تظهرْ رسالةٌ ألبتّة**.
**الواجب:** كلُّ مسارٍ يسقطُ في هذا القسمِ **ينطقُ** بسطرٍ من عقدِ الرسائل. والصمتُ ممنوعٌ حالًا نهائيّةً.

**٣ · `٤` بابُ الاقتراحات: البلاغُ يضيعُ صامتًا.**
المقيسُ: `76` حرفًا في الحقلِ وضُغِطَ «إرسال» ⟵ انتقلَتِ الشاشةُ إلى الإعدادات · **ولم يظهرْ أيٌّ من الستّةِ** `feedback.thanks` · `feedback.signInRequired` · `feedback.capped` · `feedback.failed` · `feedback.empty` · `feedback.noChat` · ولا زرُّ `feedback.signInGo`.
**الواجب:** لكلِّ مآلٍ سطرُه على الشاشةِ **قبلَ أيِّ انتقال**، ولا انتقالَ صامتٌ ألبتّة. وإن كانَ الرفضُ لعدمِ تسجيلِ الدخولِ فليُقَلْ ذلك بصريحِ سطرِه.

### ٤-ب · المرحلةُ الجديدةُ — تأخيرُ الفتحِ في «الفتاوى» و«الدروس»

**١ · قِسْ أوّلًا:** من لحظةِ النقرِ إلى ظهورِ أوّلِ متنٍ حقيقيٍّ — في القسمَين، على المنصّةِ المحلّيّةِ وعلى الحيِّ كليهما، ثلاثَ مرّاتٍ وخذِ الوسيط. واكتبِ الرقمَ.
**٢ ·** سجّلْ: **أيظهرُ مؤشّرُ تحميلٍ في أثناءِ الانتظارِ أم شاشةٌ ساكنة؟**
**٣ · الواجب:** إن لم يكنْ ثَمَّ مؤشّرٌ فأضِفْه — هيكلٌ أو مؤشّرٌ من جنسِ ما يستعملُه التطبيقُ في مواضعَ أخرى، لا شيءٌ مخترَع.
**٤ ·** وإن تبيَّنَ أنّ التأخيرَ من الخادمِ لا من العميلِ **فلا تُصلِحْ خادمًا في هذه الرحلة** — اكتبِ الرقمَ المقيسَ وسمِّ المسارَ البطيءَ واتركْه للمالك.

### ٤-ج · الدفعةُ (ب) — الرخيصانِ

**١ · `١٣` تحيّةُ الرئيسةِ تنتهي بأداةِ نداءٍ بلا اسم.** المقيسُ: نصُّ `home.hello` والذيلُ فارغٌ عندَ ضيفٍ بلا اسمٍ محفوظ.
**الواجب:** إن لم يكنْ ثَمَّ اسمٌ فلا تُعرَضُ أداةُ النداءِ عاريةً — تحيّةٌ تامّةٌ بلا نداءٍ، بمفتاحٍ في القاموس.

**٢ · `٨` زرُّ `fatwa.source` لا يجيب.** المقيسُ: نُقِرَ فلم يتغيّرْ نصٌّ ولا عددُ عناصرَ ولا مسارٌ بعدَ `1.3` ثانيةٍ، في المقاسَين.
**الواجب:** اقرأْ ما كانَ يُفترَضُ أن يفعلَه من الشفرة، ثمّ إمّا أن يعملَ أو يختفيَ. **وزرٌّ لا يفعلُ شيئًا لا يبقى على الشاشة.**

### ٤-د · المشكوكاتُ — تُقاسُ أوّلًا ولا تُصلَحُ إلّا إن ثبتَتْ

| # | المشكوك | كيف يُحسَم |
|---|---|---|
| `٩`+`١٠` | «المصحف» و«رحلة الكنوز» بلا زرِّ رجوعٍ في مستواهما الأعلى | الأداةُ بحثَتْ عن الاسمِ المعجميِّ `common.back` وحدَه. **ابحثْ عن أيِّ طريقِ رجوعٍ** بأيِّ اسمٍ أو رمزٍ. فإن وُجِدَ فالعطبُ في الأداةِ لا في التطبيقِ ⟵ أصلِحِ الأداةَ. وإن لم يوجدْ فأضِفْ طريقَ رجوعٍ كالذي في بقيّةِ الأقسام |
| `١` | `4` أزواجٍ متراكبةٍ في «رحلة الكنوز» | استخرجْ أسماءَ العناصرِ المتراكبةِ بأعيانِها ومقاساتِها. فإن كانَ التراكبُ يمنعُ إصابةَ أحدِها بالإصبعِ فهو عطبٌ يُصلَح، وإن كانا زرَّينِ متجاورَينِ لا يحجبُ أحدُهما الآخرَ فاكتبْ «لم يثبتْ» واتركْه |
| `١٢` | صدرُ «الأذكار» يقولُ «أذكار المساء» | قِسْ: أيفتحُ القسمُ على مجموعةٍ بعينِها بلا عنوانٍ جامعٍ فوقَها؟ فإن كانَ كذلك فأضِفْ عنوانَ القسمِ فوقَ المجموعة. **ولا تغيّرِ المجموعةَ الافتراضيّةَ ولا ترتيبَها** |

### ٤-ه · `١٤` التحديثُ في منتصفِ القسمِ يرمي إلى المحادثة

المقيسُ: `16` من `24` قياسًا — وأصابَ البندَ `٨٧` أيضًا فأضاعَ الطورَ المفتوح.
**حكمُ المالك: عطبٌ يُصلَح.** يُحفَظُ القسمُ الحاليُّ ويُستعادُ بعدَ التحديث. **والمحادثةُ تبقى بدايةَ التطبيقِ عندَ الفتحِ الأوّل** — لا تُغيّرْ ذلك، فالمقصودُ استعادةُ الموضعِ عندَ التحديثِ وحدَه.

### ٤-و · ما لا يُصلَحُ في هذه الرحلةِ — ويُكتَبُ سببُه

**`٢`+`٣` مداخلُ الرفِّ: صندوقٌ أطولُ من حبرِه بـ`4.96` في الجوّالِ و`11.13` في اللوحيِّ، وصفرُ رسمٍ في اثنَي عشرَ مدخلًا.**
**لا تخترعْ أيقوناتٍ ولا تغيّرْ هويّةً بصريّة.** ما تفعلُه: تسمّي في تقريرِك **موضعَ الرفِّ في الشفرةِ بالسطرِ**، وتعدُّ المداخلَ الاثنَي عشرَ بأسمائِها، وتكتبُ المقاسَ الحاليَّ لكلِّ صندوق — ليكونَ عملُ التصميمِ بعدَها خطوةً واحدةً لا بحثًا.

---

## ٥ · إصلاحُ الأداةِ نفسِها

**`١١` مدخلُ «المحفّظ»** حُكِمَ عليه بأنّ صدرَ القسمِ لا يحملُ اسمَه — **وصدرُ القسمِ يبدأُ بالاسمِ نفسِه**، والفرقُ تشكيلٌ لا لفظ.
**الواجب:** في `ustaz-check88/check88` اجعلْ مقارنةَ الأسماءِ **تتجاهلُ التشكيلَ والمدَّ والتطويل** قبلَ المقارنة، ثمّ أعِدْ `npm run check:items` وأثبتْ زوالَ الحكمِ الكاذب.
**وأضِفْ إلى الأداةِ:** متغيّرَ بيئةٍ يجعلُ وجهتَها قابلةً للضبطِ (الحيُّ أو المحلّيُّ)، **وقياسَ زمنِ ظهورِ المتنِ بعدَ فتحِ القسمِ ومؤشّرِ التحميل** — فيصيرُ بندُ `٤-ب` مقيسًا آليًّا في كلِّ إعادة.

---

## ٦ · التجربةُ بنفسِك — بعدَ كلِّ إصلاحٍ لا في آخرِ الرحلة

| # | الواجب |
|---|---|
| ١ | بعدَ كلِّ إصلاحٍ: أعِدْ بناءَ `app.js` بسكربتِ المشروعِ، ثمّ شغّلْ حالاتِ §٣-٤ على المنصّةِ المحلّيّة |
| ٢ | سجّلْ للحالِ الواحدةِ: النصُّ الذي ظهرَ **قبل**، والنصُّ الذي ظهرَ **بعد**، ولقطةً لكلٍّ منهما |
| ٣ | بعدَ كلِّ إصلاحٍ: `npm run gates` و`node recon-audit.cjs`. **أيُّ نقصٍ عن `112/113` أو أيُّ `FAIL` في `recon` ⟵ رُدَّ إصلاحَك ولا تُضعِفِ الحارس** |
| ٤ | في آخرِ الرحلةِ: شغّلْ من مجلَّدِ الأداةِ `npm run check` و`npm run check:offline` **على المنصّةِ المحلّيّةِ المصلَحة** — وضعْ حصيلتَها في وجهِ حصيلةِ اليوم: `14` عطبًا · `10` شكوكٍ · رسالتانِ كاذبتان |
| ٥ | **ولا تشغّلِ البطاريّةَ المدفوعةَ ألبتّة** — لا نداءَ واحدًا في هذه الرحلة |

---

## ٧ · الإيداعُ والدفع

1. **إيداعٌ لكلِّ عطبٍ على حدة**، وعنوانُه يسمّي العطبَ بلغةٍ يفهمُها قارئٌ لا شفرةٌ.
2. أسماءُ الملفّاتِ صراحةً — **لا** `git add .`.
3. في آخرِ الرحلةِ **ادفعْ فرعَك وحدَه** إلى `origin`، واطبعْ في الطرفيّةِ **رابطَ المقارنةِ** ليفتحَه المالكُ للمراجعة.
4. **ولا دمجَ في `main`** — الدمجُ بيدِ المالكِ وحدَه بعدَ مراجعتِه.

---

## ٨ · التقريرُ الذي تسلّمُه

اكتبْه في `C:\Users\passe\projects\ustaz-fix88\FIX-88\EZIK-88-FIX-REPORT.md`:

| القسم | المحتوى |
|---|---|
| ١ · الرأس | الإيداعُ الذي بدأتَ منه · وآخرُ إيداعٍ لك · الفرعُ · رابطُ المقارنةِ · سكربتُ البناءِ الذي استعملتَه |
| ٢ · جدولُ الإصلاحِ | صفٌّ لكلِّ عطبٍ من الأربعةَ عشرَ: رقمُه · ماذا كانَ · ماذا صارَ · الملفُّ والسطرُ · رقمُ الإيداع |
| ٣ · **قبل / بعد** | لكلِّ حالٍ من حالاتِ §٣-٤: النصُّ الذي كانَ يظهرُ والنصُّ الذي صارَ يظهرُ، ولقطتاهما |
| ٤ · زمنُ الفتح | «الفتاوى» و«الدروس»: الرقمُ قبلَ وبعدَ · ومؤشّرُ التحميلِ: أكانَ ثمَّ وصارَ ثمَّ |
| ٥ · ما لم يثبتْ | كلُّ مشكوكٍ قِسْتَه فلم يثبتْ — ولماذا لم يُصلَحْ |
| ٦ · ما لم يُصلَحْ وسببُه | ومنه مداخلُ الرفِّ ومعها موضعُها في الشفرةِ ومقاساتُها |
| ٧ · الطقمُ والأداة | `gates` و`recon` قبلَ وبعدَ · وحصيلةُ إعادةِ الأداةِ في وجهِ حصيلةِ اليوم |

**وفي آخرِ عملِك اطبعْ بالـ`ASCII` وحدَه:** عددَ الأعطابِ المصلَحةِ من `14` · وعددَ ما لم يثبتْ · وعددَ ما لم يُصلَحْ · و`gates` و`recon` بعدَ الإصلاحِ · واسمَ الفرعِ · ورابطَ المقارنةِ · ومسارَ التقرير.
