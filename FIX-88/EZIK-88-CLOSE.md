# EZIK — ملفُّ إنهاءِ الجلسةِ الجانبيّة · البندُ `٨٨`

> **يُرفَعُ في أوّلِ الجلسةِ التنظيميّةِ ولا يُطلَبُ معه شيءٌ آخر.** فيه الأرضُ المقيسةُ بعدَ النزول، وما أُغلِقَ، وما تحرَّرَ للجدولة، وما ينتظرُ لفظَ المالك.
> **تاريخُه:** ١٥ سبتمبر ٢٠٢٦ · **وكُتِبَ بعدَ النزولِ للناسِ وبعدَ حكمِ المالكِ بإصبعِه.**

---

## ٠ · ماذا كانَتْ هذه الجلسةُ

جلسةٌ جانبيّةٌ واحدةٌ على البندِ `٨٨` — **التشييكُ الكامل**: تُبنى أداةٌ آليّةٌ تمرُّ على التطبيقِ كلِّه وتشيّكُه ثمّ تُسلِّمُ تقريرًا (**الأداةُ تكتشفُ ولا تصلح**)، ثمّ رحلةُ إصلاحٍ على ما وجدَتْ: **يصلحُ ⟶ يجرّبُ بنفسِه ⟶ يسلّمُ تقريرًا ⟶ يُدفَعُ للمراجعة**. ثمّ دفعةٌ ثانيةٌ (ب) بثلاثةِ بنود، ثمّ الدمجُ والنشر.

**وقد أُغلِقَتْ كلُّها.** لا يبقى منها عملٌ مفتوح.

---

## ١ · الأرضُ المقيسةُ — الحالُ الآنَ بعدَ النزول

| المفتاح | القيمة |
|---|---|
| `origin/main` | **`8faf3d8`** — دمجُ البندِ `٨٨` · `ahead_by=13` · `behind_by=0` |
| رسالةُ الدمج | `item 88: the full check, the fourteen defects, batch B, and the store-name bump` |
| فرعُ العمل | `fix/item88-defects-20260914` عند `31baa44` — **مدفوعٌ ومدموج** |
| شجرةُ الإصلاح | `C:\Users\passe\projects\ustaz-fix88` — **نظيفةٌ `dirty=0`** |
| شجرةُ الأداة | `C:\Users\passe\projects\ustaz-check88\check88` — إصلاحاتُها كلُّها قائمة، **وشفرتُها غيرُ متعقَّبةٍ في `git`** |

**أرقامُ الحيِّ المقيسةُ على `https://ezik.app` بعدَ النشر:**

| الملف | القيمة |
|---|---|
| `sw.js` | `48510` بايت · **اسمُ المخزنِ `ezik-v35`** |
| `index.html` | `153974` بايت |
| `app.js` | `1730899` بايت |
| `app.jsx` (على القرص) | `1937983` بايت |

🔴 **و`index.html` في بصمةِ `git` = `151903` وليسَ نقصًا** — الفرقُ نهاياتُ أسطرٍ (`CRLF` على القرصِ · `LF` في الفهرس) لا نقصُ متن. مسجَّلٌ من قبلُ ولا يُعادُ اكتشافُه.

**عقدُ الطقمِ — قِيسَ بعدَ كلِّ شيءٍ وصمد:**

`npm run gates` ⟵ **`112/113`** والساقطُ `liveness` وحدَه · و`node recon-audit.cjs` ⟵ خروجٌ `0` بـ**`PASS=204 WARN=1 FAIL=0`**.

🟡 **و`PASS` ارتفعَ `203 ⟶ 204`** بإيداعِ `8b8b658` في الدفعةِ (ب): تعدادُ `recon` صارَ يسجّلُ رقمَ الشجرةِ النظيفة، وهو ما تقرؤُه شجرةٌ مُودَعة.

---

## ٢ · ما نزلَ للناس — سجلُّ التغيير

### ما يراهُ القارئُ بعينِه

| # | البند | ما صارَ |
|---|---|---|
| `٤` | بابُ الاقتراحاتِ والشكاوى | الشكوى المرفوضةُ تقولُ سببَها **والقارئُ أمامَ كلامِه** — كانَتْ تقفزُ به إلى الإعداداتِ صامتةً، ونصُّه محفوظٌ في الحالَين |
| `٥`+`٦` | المقالاتُ والمرأة | حينَ يخنقُ الخادمُ الطلبَ (`429`) يُقالُ **«انتظرْ قليلًا»** — كانَ يُقالُ «تحقَّقْ من الاتّصال» والاتّصالُ سليم |
| `١٣` | صدرُ الرئيسيّة | **«مرحبًا بك»** لمن لا اسمَ محفوظًا له — كانَتْ «مرحبًا يا» ثمّ فراغ |
| `١٢` | صدرُ الأذكار | **اسمُ القسمِ فوقَ اسمِ المجموعة** — والمجموعةُ الافتراضيّةُ وترتيبُها لم يتغيّرا |
| `١٤` | التحديثُ داخلَ قسم | يعودُ **إلى القسمِ نفسِه** — كانَ يرمي إلى المحادثة · **وأوّلُ فتحٍ للتطبيقِ ما زالَ على المحادثة** |
| `ب-٢` | بابُ الأذكار | يفتحُ على **جميعِ الأذكار** — كانَ يدخلُ «أذكار المساء» رأسًا |
| `ب-٣` | المحادثةُ القديمة | **تُفتَحُ من الرئيسيّة** كما تُفتَحُ من داخلِ المحادثة |
| `ب-١` | تحديثُ المصحف | يعودُ **إلى الفهرس** لا إلى صفحةِ القراءة — فالخروجُ ضغطةٌ واحدة |

### ما تحتَ السطح

| # | البند |
|---|---|
| `١` | **اسمُ المخزنِ `ezik-v34 ⟶ ezik-v35`** في `sw.js:54` ومرآتُه `SW_CACHE` في `quest-bank-integrity-guard.cjs` — العاملُ الجديدُ يكنسُ القديمَ فيصلُ الإصلاحُ لمن يحملُ نسخةً محفوظة |
| `٢` | **أداةُ تشييكٍ آليّةٌ بُنيَتْ من الصفر**: تمرُّ على `١٢` قسمًا في مقاسَين (`390×844` و`820×1180`) + جولةَ قطعِ شبكةٍ + بطاريّةً مدفوعةً لا تُشغَّلُ بلا إذن |
| `٣` | **إحدى عشرةَ كذبةً في الأداةِ نفسِها** اكتُشِفَتْ وأُصلِحَتْ — أشهرُها ضمّةٌ واحدةٌ خدعَتْ مقارنةَ بايتٍ ببايت، وقائمةُ مفاتيحِ انقطاعٍ مكتوبةٌ باليدِ ينقصُها مفتاح |
| `٤` | حارسانِ شُدِّدا: `theme-coverage-guard` كسبَ حالتَين، و`wird-guard` صارَ يثبّتُ استئنافَ المصحفِ بمنطقِه الجديدِ (والسطرُ الذي نقضَه أمرُ المالكِ حُذِفَ صراحةً لا صمتًا) |
| `٥` | «احذفْ كلَّ بياناتي» صارَ يكنسُ مفتاحَ الموضعِ الجديدَ كذلك، ودخلَ روسترَ `delete-truth-measure.cjs` |

### حصيلةُ الأداةِ مقيسةً

| المقياس | الجولةُ الأولى (على الحيّ) | بعدَ الإصلاح |
|---|---|---|
| **الأعطابُ الحقيقيّة** | `١٤` | **`٣`** |
| الشكوكُ لا أعطاب | `١٠` | `٧` |
| **الرسائلُ الكاذبة** | `٢` | **`٠`** |
| الرسائلُ الصادقة | `٢` | `٥` |
| التغطية | — | `١٢` من `١٢` قسمًا |

**والثلاثةُ الباقيةُ حكمُك أو لا ضررَ فيها ⟹ صفرُ أعطابٍ غيرِ مقصودة.**

---

## ٣ · ما لم ينزلْ ولماذا

| # | البند | السبب |
|---|---|---|
| `٢`+`٣` | مداخلُ الرفِّ العاريةُ (لا علامةَ · مربّعة) | 🔴 **حكمُ المالكِ نفسِه** مكتوبٌ في `app.jsx:7272` — البندُ `٠٥-E1`: «احذفِ العلامةَ من كلِّ قسم، أبقِ الاسمَ وحدَه، واجعلْها مربّعة». والأداةُ قاسَتْ قرارَ تصميمٍ وسمَّتْه تشويهًا |
| `١` | أربعةُ أزواجٍ متراكبةٍ في «رحلة الكنوز» | شريطُ تنقّلٍ سفليٌّ ثابتٌ يعلو آخرَ صفٍّ من البطاقات — **لا عنصرَ مدفونٌ ولا متعذّرُ اللمس**. الأداةُ تقيسُ التقاطعَ ولا تقيسُ من يصيبُه الإصبع |

---

## ٤ · ما ينتظرُ لفظَ المالك — مرتَّبًا من الأسهل

| # | البند | شكلُه | توصيتي |
|---|---|---|---|
| `٦٦` | موضعُ مدخلِ البوصلة | **قرارٌ بلا شفرة** | يُحسَمُ في الجلسةِ التنظيميّةِ نفسِها |
| `٨٤` | الاعتراضاتُ السبعُ على الواجهة | **فرزٌ بلا وكيل** | يسردُها المالكُ فتُفرَزُ ثلاثًا: ينزلُ الآن · يُؤجَّل · ليسَ عطبًا |
| `١٦` | `‏/api/v1/fatwas/browse` بطيءٌ وثقيل | **بندٌ جديدٌ مقترَح** | `949` مِلّي و`121520` بايت مقيسان · يصلحُ لوكيلٍ بأمرٍ واحد |
| `١٥` | **ميزانُ الخنقِ على أبوابِ القراءة** | **بندٌ خادميٌّ مستقلّ** | 🔴 **أثقلُ ما لم يُفتَحْ · شُخِّصَ كاملًا · أقربُ ما يُفتَح** |

### تفصيلُ `١٥` — الشفرةُ مقيسةٌ وجاهزة

`api/articles-list.js:47` و`api/articles-admin.js:117` ⟵ `checkAuthLimit(clientAddress(req))` ثمّ `429` بجسمٍ `{"ok":false,"error":"articles-rate-limited"}`.
**وعشرةُ مساراتٍ تشربُ من الميزانِ نفسِه:** `articles-admin` · `articles-get` · `articles-list` · `auth-delete` · `auth-exchange` · `auth-native` · `auth-return` · `auth-start` · `inbox` · `roles-admin`.
والحدودُ `AUTH_PER_IP_MIN = 20` و`AUTH_PER_IP_DAY = 200` في `lib/ratelimit.js:368-369`، و🔴 **`AUTH_FAIL_OPEN = false`** عندَ `367` ⟹ تعثُّرُ المخزنِ لحظةً يُغلِقُ العشرةَ كلَّها.
**والعلاجُ المقترَحُ ثلاثةٌ بمتغيّرٍ واحدٍ لكلٍّ:** بابا القراءةِ لهما ميزانُهما · ويفشلانِ **مفتوحَين** · ويُميَّزُ ردُّ الخنقِ عن ردِّ العطبِ في الجسم.
**وتسجيلٌ على نفسي:** ظننتُ أوّلًا أنّ الخنقَ صنيعةُ الأداةِ وحدَها، فثبتَ أنّه يضربُ من أوّلِ نداءٍ في صندوقٍ نظيفٍ بعدَ ساعات. **العطبُ حقيقيٌّ وإن كانَتِ الأداةُ عجّلَتْ به.**

---

## ٥ · نظافةٌ معلَّقةٌ تنتظرُ لفظَك — لا تعطّلُ شيئًا

| # | الأمر | الحال |
|---|---|---|
| ١ | **ستُّ إحالاتٍ في تقريرِ (أ)** إلى `bench/…` بأسماءِ ملفّاتٍ صارَتْ فصولًا داخلَ `BENCH-SOURCES.md` | المتنُ محفوظٌ كاملًا (`161983` بايت · `59` دالّة) — **سطرُ الإحالةِ وحدَه يُصحَّح** |
| ٢ | **`_probe/`** في شجرةِ `main` (`C:\Users\passe\projects\ustaz`) | أربعةُ ملفّاتٍ غيرُ متعقَّبةٍ، اثنانِ منها مؤرَّخانِ **١٢ سبتمبر** ⟹ أقدمُ من هذه الرحلة · `main_tracked_dirty = 0` و`HEAD` سليم ⟹ **ضررٌ صفر** · يُترَكُ أو يُرفَعُ بأمرِك |

---

## ٦ · قواعدُ تشغيلٍ تعلّمناها هنا — لا تُكتَشَفْ مرّتَين

| # | القاعدة |
|---|---|
| ١ | **الوكيلُ يقفُ إن أنهى دورَه لينتظرَ صدَفةً في الخلفيّة** — مخرَجُ الخلفيّةِ لا يصلُه إلّا بدورٍ جديد. الأمرُ الصحيح: **يُحبَسُ داخلَ أمرٍ واحدٍ حتّى تنتهي** ولا ينهي دورَه. **صارَتْ مرّتَينِ في هذه الجلسة** |
| ٢ | 🔴 **الريستارتُ يقتلُ الجلسةَ لا العمل** — ما أُودِعَ ودُفِعَ نجا كلُّه، والجلسةُ وحدَها ماتَتْ. **فالإيداعُ بعدَ كلِّ بندٍ هو ما ردَّ الخسارةَ إلى صفر** |
| ٣ | **لا استبدالَ أعمى لسلسلةِ إصدار** — `quest-bank-integrity-guard.cjs` يحملُ `١٤` اسمَ مخزنٍ قديمًا في سجلِّه النثريّ، واستبدالٌ شاملٌ كانَ سيزوّرُها كلَّها. **يُقرأُ الموضعانِ أوّلًا: شفرةٌ حيّةٌ تُرفَع، ونثرُ تاريخٍ لا يُمَسّ** |
| ٤ | **معاينةُ فيرسل خلفَ دخول** (`302` إلى `sso-api`) ⟹ النافذةُ الخاصّةُ تُرَدُّ إلى صفحةِ تسجيل. **تُجرَّبُ في المتصفّحِ الداخلِ على فيرسل، وبالرابطِ المثبَّتِ للنشرِ بعينِه** لا برابطِ الفرعِ الثابتِ (الأخيرُ قد يخدمُ عاملَ خدمةٍ قديمًا) |
| ٥ | **الأمرُ لا يُبنى على أرضٍ متحرّكة** — ماتَتْ أرضٌ تحتَ ملفِّ أمرٍ كاملٍ فبطلَ ورُدَّ وأُعيد |
| ٦ | **الوكيلُ يُصلِحُ أداتَه لا التطبيقَ حينَ تكذبُ الأداة** — فعلَها إحدى عشرةَ مرّةً وكانَ محقًّا في كلِّها |
| ٧ | **بوّابةُ الشروطِ قبلَ أيِّ مساس**: الصندوقُ يقيسُ الشجرةَ ويعدُّ المراسيَ، وإن خالفَ عددٌ واحدٌ **توقَّفَ ولم يكتبْ شيئًا**. منعَتْ خطأً حقيقيًّا في هذه الجلسة |

---

## ٧ · أينَ الأوراقُ على القرص

**كلُّها مُودَعةٌ في `git` ومدموجةٌ في `main` — لا شيءَ منها في محادثةٍ ولا في التنزيلات.**

| المسار | ما فيه |
|---|---|
| `FIX-88/EZIK-88-FIX-REPORT.md` | تقريرُ الدفعةِ (أ) كاملًا · `66168` بايت |
| `FIX-88/EZIK-88-FIX-REPORT-B.md` | تقريرُ الدفعةِ (ب) · `26530` بايت |
| `FIX-88/evidence/` | شواهدُ (أ) · `219` ملفًا: قياساتُ قبل/بعد · لقطاتٌ · سجلّاتُ بوّاباتٍ بأسمائها `g1`…`g5` |
| `FIX-88/evidence-b/` | شواهدُ (ب) · سجلّاتُ `gb1`…`gb7` · لقطاتٌ · **وإعادةُ تشغيلِ الأداةِ في `tool-rerun/`** |
| `FIX-88/bench/BENCH-SOURCES.md` | منصّةُ القياسِ كاملةً متنًا · `161983` بايت |
| `FIX-88/bench-b/BENCH-B-SOURCES.md` | منصّةُ (ب) |
| `FIX-88/EZIK-88-FIX-ORDER.md` · `EZIK-88-FIX-ORDER-B.md` | أمرا الدفعتَين |

---

## ٨ · ما ليسَ من شأنِ الجلسةِ التنظيميّة

- **البندُ `٤٥` الكاستنغُ والودجة** — جلسةٌ جانبيّةٌ أخرى تعملُ عليه، ولها بروموتُ حدودٍ يمنعُها من لمسِ `app.jsx` و`app.js` و`sw.js` و`quest-bank-integrity-guard.cjs`.
- **ملفُّ الانتقالِ** — موقوفٌ بالكلّيّةِ بأمرِ المالكِ (٢٥ أغسطس) حتّى يُسكَّرَ شغلُ المتجرِ ويُرفَعَ لآبل وجوجل. **لا يُكتَب.**
- **نسبةُ الإنتاج** — لا تُصدَرُ من هذا الملف: جدولُ الطابورِ الساري لم يُرفَعْ في هذه الجلسة، **ولا يُبنى رقمٌ على غيرِ قياس**.

---

## ٩ · أوّلُ ما يُفعَلُ في الجلسةِ التنظيميّة

1. تُعرَضُ البنودُ الباقيةُ في جداولَ **مرتَّبةً من الأسهل**، ويختارُ المالكُ ما يُسلَّم.
2. `٦٦` و`٨٤` **يُحسَمانِ في الجلسةِ نفسِها** — قرارٌ وفرزٌ بلا وكيل.
3. `١٥` ثمّ `١٦` **يُسلَّمانِ وكيلًا بملفِّ أمرٍ واحدٍ لكلٍّ** — كلاهما مشخَّصٌ بالسطرِ والملفّ.
4. ويُسأَلُ المالكُ في النظافةِ المعلَّقةِ (§٥): الإحالاتُ السّتُّ و`_probe/`.
