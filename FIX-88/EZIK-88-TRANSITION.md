# EZIK — ملفُّ انتقالِ الجلسةِ الجانبيّة · البندُ `٨٨`

> **يُرفَعُ أوّلَ محادثةٍ جديدةٍ ولا يُطلَبُ معه شيءٌ آخر.** فيه كلُّ ما يحتاجُه من يُكمِلُ: الأرضُ المقيسةُ، وما أُغلِقَ، وما يمشي الآن، وما ينتظرُ لفظَ المالك.
> **تاريخُه:** ١٤ سبتمبر ٢٠٢٦ · وكُتِبَ والوكيلُ ما زالَ يعملُ.

---

## ٠ · ما كانَتْ هذه الجلسةُ ولماذا

جلسةٌ جانبيّةٌ واحدةٌ فتحَها المالكُ على البندِ `٨٨` — **التشييكُ الكامل**: تُبنى أداةٌ آليّةٌ تمرُّ على التطبيقِ كلِّه وتشيّكُه، ثمّ تُشغَّل، ثمّ تُسلَّمُ تقريرًا. **الأداةُ تكتشفُ ولا تصلح.**
ثمّ أمرَ المالكُ برحلةِ إصلاحٍ ثانيةٍ في الجلسةِ نفسِها: **يصلحُ ⟶ يجرّبُ بنفسِه ⟶ يسلّمُ تقريرًا ⟶ يُدفَعُ للمراجعة.**

---

## ١ · الأرضُ المقيسةُ — لا تُعِدْ قياسَها ولا تخالفْها

| المفتاح | القيمة |
|---|---|
| `origin/main` | `4e8b47c` — **لم تتحرّكْ منذُ نزولِ البندِ `٨٧`** |
| فرعُ الإصلاح | `fix/item88-defects-20260914` · **مدفوعٌ عندَ `7ad780d`** (خمسةُ إيداعات) |
| شجرةُ الإصلاح | `C:\Users\passe\projects\ustaz-fix88` |
| أوراقُ الإصلاح | `C:\Users\passe\projects\ustaz-fix88\FIX-88` |
| فرعُ التشييك | `check/item88-20260914` |
| شجرةُ الأداة | `C:\Users\passe\projects\ustaz-check88\check88` |
| شجرةُ `main` | `C:\Users\passe\projects\ustaz` — **لا تُمَسُّ كتابةً ألبتّة** |
| رابطُ المقارنة | `https://github.com/passerbyq8-sketch/ustaz/compare/main...fix/item88-defects-20260914` |
| المعاينة | `https://ustaz-4c6wah94q-musaed-s-projects1.vercel.app` — **خلفَ دخولِ فيرسل** (`302` إلى `sso-api`)، تُفتَحُ بحسابِ المالكِ مرّةً |

**أرقامُ الحيِّ على `4e8b47c`:** `index.html` = `153974` (وفيه `2071` نهايةَ سطرٍ من نوعِ `CR`) · `app.js` = `1713640` · `sw.js` = `48510` باسمِ مخزنٍ `ezik-v34` · و`app.jsx` على القرصِ `1918006`.
🔴 **ورقمُ `153180` في ملفِّ الأعطابِ خطأُ قياسٍ — لا يُبنى عليه شيء.** بصمةُ `git` للفهرسِ `151903` والفرقُ نهاياتُ أسطرٍ لا نقصُ متن.

**عقدُ الطقمِ — أيُّ إصلاحٍ يُنقِصُه يُردُّ ولا يُضعَفُ حارس:**

`npm run gates` ⟵ `112/113` والساقطُ `liveness` وحدَه · و`node recon-audit.cjs` ⟵ خروجٌ `0` بـ`PASS=203 WARN=1 FAIL=0` · وتحذيرٌ واحدٌ يتيمٌ `LONGEST_CARD_CHARS = 3405 > 3401`.

---

## ٢ · الأداةُ — ما هي وكيف تُعاد

مبنيّةٌ في `ustaz-check88\check88`، وشفرتُها **غيرُ متعقَّبةٍ في `git`** فليسَتْ في أيِّ إيداع.

| الأمر | ماذا يفعل |
|---|---|
| `npm run check` | الجولةُ الميكانيكيّةُ كاملةً في مقاسَين + **جولةُ قطعِ الشبكةِ في داخلِها** |
| `npm run check:offline` | جولةُ الانقطاعِ وحدَها |
| `npm run check:items` | البنودُ المسمّاةُ وحدَها |
| `npm run check:battery` | البطاريّةُ — **مدفوعةٌ، لا تُشغَّلُ بلا إذنٍ صريح** |

| المفتاح | القيمة |
|---|---|
| المحرّكُ الوحيدُ العامل | `chromium.launch({ headless: true, channel: 'msedge' })` |
| المحرّكُ الميّت | كروميومُ المرفقُ بنوعَيه — `spawn UNKNOWN` · **لا تُضيِّعْ وقتًا في إحيائِه** |
| ضبطُ الوجهةِ والشجرة | `EZIK_CHECK_TARGET` و`EZIK_CHECK_TREE` |
| المقاسان | `390×844` و`820×1180` |

🔑 **وخطأٌ في أمري أنا يجبُ ألّا يُعاد:** `npm run check` يشغّلُ جولةَ الانقطاعِ في داخلِه، فأمري بتشغيلِ `check` **و**`check:offline` صرفَ عشرَ دقائقَ مرّتَين.

---

## ٣ · حصيلةُ التشييكِ الأوّل

| القياس | النتيجة |
|---|---|
| التغطية | `12` من `12` قسمًا |
| الأعطاب | `14` · الشكوك `10` · ما لم يُشَيَّكْ `9` |
| رسائلُ انقطاعٍ كاذبة | `2` |
| البطاريّةُ | `18` نداءً · ثلاثةُ أطوارٍ · كلُّها اكتملَتْ |
| رايةُ المخِّ الحرّ | 🔴 **`لم يُقَسْ` في الأطوارِ الثلاثة** — `14` جسمًا من `18` قُرِئَتْ خامًا ولا حقلَ فيها يسمّي الطريق |

**ثلاثةٌ من البطاريّةِ تستحقُّ عينَ المالك:**
«مفصّل» جاءَ **أقصرَ** من «موجز» في `5` من `6` (`127` كلمةً مقابلَ `289`) · وزمنُ أوّلِ كلمةٍ `4.2` ثانيةً ثابتًا والجوابُ ينزلُ كتلًا · و`x-murabbi-remaining` **على السلكِ** (`39 ⟶ 22`) ولا يظهرُ على الشاشة.

**والبندانِ المقيسان:** `١٧` الأطوارُ الثلاثةُ **حيّةٌ** بلا قفل · و`٨٧` مرَّ في المقاسَينِ بأطوارِه الثمانيةِ وطيِّ الحديثِ وزرَّي الرجوع.

---

## ٤ · الأربعةَ عشرَ — حكمُها النهائيُّ بعدَ الإصلاح

| الحكم | العدد | أيُّها |
|---|---|---|
| **أُصلِحَ ونزلَ بإيداعِه** | `٦` | `٤` · `٥` · `٦` · `١٢` · `١٣` · `١٤` |
| **لم يثبتْ — عطبٌ في الأداةِ لا في التطبيق** | `٦` | `١` · `٧` · `٨` · `٩` · `١٠` · `١١` |
| **لم يُصلَحْ — حكمُ المالكِ نفسِه** | `٢` | `٢` · `٣` |

**الإيداعاتُ الخمسةُ بترتيبِها:**

`15c5efd` ⟵ `٥`+`٦` · `45f7e5b` ⟵ `٤` · `d9436e4` ⟵ `١٣` · `729ce34` ⟵ `١٢` · `7ad780d` ⟵ `١٤`

🔴 **`٢`+`٣` مداخلُ الرفِّ العاريةُ ليسَتْ عطبًا — هي أمرُ المالكِ مكتوبٌ في `app.jsx:7272` (البندُ `٠٥-E1`):** «احذفِ العلامةَ من كلِّ قسم، أبقِ الاسمَ وحدَه، واجعلْها مربّعة». والأداةُ قاسَتْ قرارَ تصميمٍ وسمَّتْه تشويهًا. وموضعُ البطاقةِ `app.jsx:7279` ويثبّتُه `theme-coverage-guard.cjs`.

**وكذبَتِ الأداةُ سبعَ مرّاتٍ وأُصلِحَتْ في كلِّها:** مقارنةُ الأسماءِ بايتًا ببايتٍ فخدعَتْها ضمّةٌ (`١١` ثمّ تكرَّرَتْ في اختبارِ التحديث) · وقائمةُ مفاتيحِ الانقطاعِ ثلاثةَ عشرَ مفتاحًا مكتوبةً باليدِ ينقصُها `lessons.browseError` (`٧`) · والبحثُ عن الرجوعِ بالاسمِ `common.back` وحدَه (`٩` و`١٠`) · وعدُّ الوصلةِ إلى لسانٍ آخرَ «ضابطًا لا يجيب» (`٨`) · وعدُّ زرٍّ داخلَ بطاقةٍ تراكبًا (`١`) · واختبارُ التحيّةِ يسقطُ على «السلامُ عليكم» وهي تحيّةٌ تامّة · واختبارُ التحديثِ يعمى عن شاشةٍ داخليّةٍ لا تحملُ اسمَ قسمِها.

**وحصيلةُ الإعادةِ بعدَ الإصلاح:** `DEFECTS 14 ⟶ 5` · `SUSPICIONS 10 ⟶ 7` · `FALSE LINES 2 ⟶ 0` — **والخمسةُ فيها `٢` و`٣`**، وتنقصُ بعدَ آخرِ تصحيحَينِ للأداة.

---

## ٥ · ما جرّبَه المالكُ بإصبعِه على المعاينة

| البند | حكمُه |
|---|---|
| `١٣` التحيّة · `٤` بابُ الاقتراحات · `٥`+`٦` المقالات · `١٤` التحديث | ✅ **تمام** |
| النشرُ للناس | ✅ **وصلَتِ المقالةُ الجديدةُ للناسِ فعلًا** |
| الأذكار | 🔴 تفتحُ على «أذكار المساء» — **وأمرَ أن تفتحَ على جميعِ الأذكار** |
| المحادثةُ القديمةُ من القائمةِ وأنتَ على الرئيسيّة | 🔴 **لا تُفتَح** — وأقرَّ المالكُ أنّه **عطبٌ قديمٌ ليسَ من الإصلاح** |

---

## ٦ · الدفعةُ (ب) — تمشي الآن

**أمرُها ملفٌّ نازلٌ مقرَّه:** `C:\Users\passe\projects\ustaz-fix88\FIX-88\EZIK-88-FIX-ORDER-B.md` (`12194` بايت · بصمةٌ `f2edc879…`).

| # | البند | الموضعُ المسمّى |
|---|---|---|
| ١ | مصيدةُ الرجوعِ في المصحفِ — **من صنعِ إصلاحِ `١٤`** | `app.jsx:28753` — الاستعادةُ التلقائيّة |
| ٢ | الأذكارُ ⟵ جميعُ الأذكار | `adhkarTimeDoor` · **مستدعٍ واحدٌ** عندَ `app.jsx:12790` |
| ٣ | `١٧` المحادثةُ القديمة | `openSavedChat` عندَ `app.jsx:16130` — **لا تستدعي `setScreen` ألبتّة** |

**والمقيسُ في المصحف:** صفحةُ القراءةِ تحملُ ضابطَينِ اثنَينِ فقط (**وضع العلامة** · **السور**) **ولا رجوعَ فيها**، و«السور» يذهبُ إلى الفهرسِ لا إلى الرفِّ ⟹ **الخروجُ ضغطتانِ لا ضغطة**.
**وتعقيدٌ معروفٌ سلفًا في البندِ الثالث:** `openFavoriteChat` تستدعي `openSavedChat` ثمّ `goEzikBack()`، و`theme-coverage-guard` يثبّتُ جسمَها بايتًا ببايت.

---

## ٧ · ما ينتظرُ لفظَ المالك

| # | البند | حالُه وتوصيتي |
|---|---|---|
| `١٥` | **ميزانُ الخنقِ على أبوابِ القراءة** | 🔴 شُخِّصَ كاملًا · بندٌ **خادميٌّ مستقلٌّ بعدَ الدمج** |
| `١٦` | **`/api/v1/fatwas/browse` بطيءٌ وثقيل** | 🔴 `949` مِلّي و`121520` بايت · بندٌ جديدٌ مقترَح |
| `٨٤` | الاعتراضاتُ السبعُ على الواجهة | **فرزٌ بلا وكيل** — يسردُها المالكُ فتُفرَزُ ثلاثًا |
| `٦٦` | موضعُ مدخلِ البوصلة | **قرارٌ بلا شفرة** |
| — | **الدمجُ في `main`** | 🔒 **بيدِ المالكِ وحدَه** بعدَ الدفعةِ (ب) |

### تفصيلُ `١٥` — لأنّه أثقلُ ما لم يُفتَحْ بعد

`api/articles-list.js:47` و`api/articles-admin.js:117` ⟵ `checkAuthLimit(clientAddress(req))` ثمّ `429` بجسمٍ `{"ok":false,"error":"articles-rate-limited"}`.
**وعشرةُ مساراتٍ تشربُ من الميزانِ نفسِه:** `articles-admin` · `articles-get` · `articles-list` · `auth-delete` · `auth-exchange` · `auth-native` · `auth-return` · `auth-start` · `inbox` · `roles-admin`.
والحدودُ `AUTH_PER_IP_MIN = 20` و`AUTH_PER_IP_DAY = 200` في `lib/ratelimit.js:368-369`، و🔴 **`AUTH_FAIL_OPEN = false`** عندَ `367` ⟹ تعثُّرُ المخزنِ لحظةً يُغلِقُ العشرةَ كلَّها.
**والعلاجُ المقترَحُ ثلاثةٌ بمتغيّرٍ واحدٍ لكلٍّ:** بابا القراءةِ لهما ميزانُهما · ويفشلانِ **مفتوحَين** · ويُميَّزُ ردُّ الخنقِ عن ردِّ العطبِ في الجسم.

**وخطأٌ في تشخيصي أُسجِّلُه:** ظننتُ أوّلًا أنّ الخنقَ صنيعةُ الأداةِ وحدَها، فثبتَ أنّه يضربُ من أوّلِ نداءٍ في صندوقٍ نظيفٍ بعدَ ساعات. **والعطبُ حقيقيٌّ وإن كانَتِ الأداةُ عجّلَتْ به.**

---

## ٨ · قواعدُ تشغيلٍ تعلّمناها هنا — لا تُكتَشَفْ مرّتَين

| # | القاعدة |
|---|---|
| ١ | **كلود كودُ يقفُ إن أنهى دورَه لينتظرَ صدَفةً في الخلفيّة** — مخرَجُ الخلفيّةِ لا يصلُه إلّا بدورٍ جديد. الأمرُ الصحيح: **يُحبَسُ داخلَ أمرٍ واحدٍ حتّى تنتهي** ولا ينهي دورَه |
| ٢ | **معاينةُ فيرسل خلفَ دخول** — `302` إلى `sso-api`. فقياسُ بايتاتِها من صندوقٍ بلا جلسةٍ يعطي صفحةَ تحويلٍ لا الملفّ |
| ٣ | **لا صندوقَ ببصمةٍ فارغةٍ ولا `PLACEHOLDER`** — المالكُ ينفّذُ ما في الصندوقِ لا ما في النثر. وقد خالفتُها مرّةً في هذه الجلسةِ ولم يقعْ ضرر |
| ٤ | **الأمرُ لا يُبنى على أرضٍ متحرّكة** — ماتَتْ أرضُ `eece010` تحتَ ملفِّ أمرٍ كاملٍ فبطلَ ورُدَّ وأُعيدَ على `4e8b47c` |
| ٥ | **الوكيلُ يُصلِحُ أداتَه لا التطبيقَ حينَ تكذبُ الأداة** — وقد فعلَها سبعَ مرّاتٍ وكانَ محقًّا |

---

## ٩ · أوّلُ ما يُفعَلُ في الجلسةِ الجديدة

1. اسألِ المالكَ: أفرغَ الوكيلُ من الدفعةِ (ب)؟ فإن فرغَ فاطلبْ آخرَ عشرةِ أسطرٍ من شاشتِه ومعها `EZIK-88-FIX-REPORT-B.md`.
2. اكتبْ له التقريرَ القصيرَ بلغتِه: ما أُصلِحَ · ما لم يثبتْ · ما بقيَ · وخطواتُ ما يجرّبُه بإصبعِه.
3. ثمّ **استأذنْ في الدمجِ** — ولا يُدمَجُ بغيرِ لفظِه.
4. وبعدَ الدمجِ يُفتَحُ `١٥` بندًا مستقلًّا، ثمّ `١٦`.
5. و`٨٤` و`٦٦` يمشيانِ متى شاءَ بلا وكيل.

**وما ليسَ من شأنِ هذه الجلسةِ:** البندُ `٤٥` الكاستنغُ والودجة — **جلسةٌ جانبيّةٌ أخرى تعملُ عليه الآن**، ولها بروموتُ حدودٍ يمنعُها من لمسِ `app.jsx` و`app.js` و`sw.js` و`quest-bank-integrity-guard.cjs` ومن دخولِ الشجراتِ الثلاث.
