# تقريرُ كودكس — البندُ ١٠٨-ب: توقّفٌ عندَ بوّابةِ ما قبلَ العمل

**الشجرة:** `C:\Users\passe\projects\ustaz-wt-qibla-20260827`
**الفرع:** `feat/qibla-heading-20260827`
**الرأسُ عندَ القياس:** `8b52ad7df140936c9bc0550a9dc176390319ec97`
**التاريخ:** ٢٧ أغسطس ٢٠٢٦ (Asia/Kuwait)
**الحكم:** **توقّفٌ بلا تنفيذ**؛ خالفَ قياسُ ما قبلَ العملِ الأرقامَ الملزِمةَ في أمرِ العمل.

## ١ · سببُ التوقّف

بدأتِ الشجرةُ نظيفةً، وكانَ `HEAD` مساويًا للأصلِ المطلوبِ `8b52ad7`، وكانَ
`gates.json` يحوي ٩٩ بوّابةً. لكنَّ التشغيلَ الكاملَ الإلزاميَّ أعاد:

```text
SUITE_MEASURED=36/99
RUNNER_PROCESS_EXIT=1
RECON=PASS=189 WARN=2 FAIL=1
DIRTY_BEFORE=0
DIRTY_AFTER=0
```

والمطلوبُ في الأمر:

```text
SUITE_REQUIRED=99/99
RUNNER_PROCESS_EXIT_REQUIRED=0
RECON_REQUIRED=PASS=190 WARN=1 FAIL=0
```

يطبعُ `tools/run-gates.cjs` سطرًا نصّيًّا فيه `EXIT=0` حتى في هذه التشغيلة، لكنَّ
خروجَ عمليّةِ `npm.cmd run gates` الفعليَّ كانَ **١**؛ لذلك لا يُستعملُ ذلك النصُّ
المطبوعُ لإخفاءِ الحمرة.

دليلُ التشغيلةِ الذي أنشأه العَدّاء خارجَ شجرةِ العمل:

```text
C:\Users\passe\AppData\Local\Temp\ezik-gates\runs\2026-08-27T07-59-21-219Z-11932
```

أظهرَ الدليلُ مانعَينِ سابقَين لأيِّ لمسٍ لهذا البند:

1. `node_modules` غائبة. سجّلَ `recon` إنذارَ `node_modules absent`، وسجّلتْ
   بوّاباتٌ فاشلةٌ أمثلةً صريحةً منها غيابُ `@babel/core` و`@babel/parser`
   و`@upstash/ratelimit`.
2. فشلَ `recon` أيضًا في فحصِ اتّساقِ التقريرِ بقوله:
   `implementation report drift: RECON summary is stale (current 189/2/0)`.

بناءً على §٠ و§٥ من أمرِ العمل: لم تُثبَّتِ الحزم، ولم تُصلَحْ بوّابةٌ أو تقريرٌ
قائمٌ ليمرَّ، ولم يبدأ تنفيذُ استهلاكِ الاتّجاه.

## ٢ · عقدُ رسالةِ الاتّجاهِ من الغلاف

قُرئَ العقدُ من لقطةِ شيفرةِ الغلافِ في الإيداع
`3404149476ca24163d2e9f50f40f6fe77e4efb54`، ومن تقريرِها في الإيداع
`6623697522b801e1a821c32362cab002d7db9dbd`؛ لا من أمرِ العمل.

### ٢٫١ أسماءُ الرسائلِ الصريحة

| الاتّجاه | اسمُ الرسالة | الحقولُ التي يستهلكُها الطرفُ المستقبل | المصدرُ الأوّليُّ | شاهدُ التقرير |
|---|---|---|---|---|
| الويب ⟶ الغلاف | `ezik:heading:start` | `type` وحدَه، بقيمةِ الاسمِ نفسِه | `3404149:src/location.js:179,198-209` | `6623697:EZIK-CODEX-HEADING-SCHED-REPORT-2026-08-26.md:75` |
| الويب ⟶ الغلاف | `ezik:heading:stop` | `type` وحدَه، بقيمةِ الاسمِ نفسِه | `3404149:src/location.js:180,198-209` | `6623697:EZIK-CODEX-HEADING-SCHED-REPORT-2026-08-26.md:76` |
| الغلاف ⟶ الويب | `ezik:heading:result` | `type`, `ok`, `status`, `magHeading`, `trueHeading`, `accuracy`, `error` | `3404149:src/location.js:181,230-238,244-252` | `6623697:EZIK-CODEX-HEADING-SCHED-REPORT-2026-08-26.md:77,79-85` |

محلّلُ أمرَي البدءِ والإيقاف لا يقرأُ حقلًا غيرَ `type`؛ وأيُّ حقولٍ زائدةٍ ليست
جزءًا مطلوبًا ولا مستهلَكًا من العقد (`src/location.js:198-209` في لقطةِ
`3404149`).

### ٢٫٢ كلُّ حقلٍ في `ezik:heading:result`

| الحقلُ بحرفِه | قيمتُه/حكمُه | مصدرُ النجاح | مصدرُ الفشل |
|---|---|---|---|
| `type` | دائمًا `ezik:heading:result` | `3404149:src/location.js:181,231` | `3404149:src/location.js:181,245` |
| `ok` | `true` فقط حينَ `status === 'ready'`؛ وإلّا `false` | `3404149:src/location.js:229-233` | `3404149:src/location.js:246` |
| `status` | واحدٌ من الحالاتِ الخمسِ أدناه | `3404149:src/location.js:186-192,229-233` | `3404149:src/location.js:242-247` |
| `magHeading` | رقمٌ محدودٌ كما وصلَ من SDK في القراءةِ الصالحة | `3404149:src/location.js:217-225,234` | `null` في `3404149:src/location.js:248` |
| `trueHeading` | رقمٌ محدودٌ كما وصلَ من SDK في القراءةِ الصالحة | `3404149:src/location.js:217-225,235` | `null` في `3404149:src/location.js:249` |
| `accuracy` | رقمٌ محدودٌ من ٠ إلى ٣ | `3404149:src/location.js:217-225,236` | `null` في `3404149:src/location.js:250` |
| `error` | `null` في القراءةِ العدديّةِ الصالحة | `3404149:src/location.js:237` | اسمُ `status` في `3404149:src/location.js:251` |

الحالاتُ التي سمّتْها الشيفرةُ حرفيًّا هي:

- `ready`
- `calibration-needed`
- `permission-denied`
- `sensor-unavailable`
- `heading-error`

مصدرُ القائمةِ هو `3404149:src/location.js:186-192`. وتفصلُ الشيفرةُ المعايرةَ
هكذا: `accuracy` صفرٌ أو ١ ⟹ `calibration-needed`، و٢ أو ٣ ⟹ `ready`
(`3404149:src/location.js:212-229`). ويؤكّدُ تقريرُ الغلافِ الأحكامَ نفسَها في
`6623697:EZIK-CODEX-HEADING-SCHED-REPORT-2026-08-26.md:87-93`.

يبثُّ الغلافُ الحمولةَ إلى الصفحةِ بوصفِها `CustomEvent` اسمُه
`ezik:heading:result` وحمولتُه في `event.detail`
(`3404149:src/location.js:255-263`). وتصلُ حمولةُ الغلافِ إلى ذلك الحقنِ من
`3404149:src/SiteScreen.js:55-65`، بينما تُسلَّم أوامرُ الويبِ إلى الجسرِ في
`3404149:src/SiteScreen.js:167-173`.

## ٣ · الملفاتُ الممسوسةُ وأسطرُها

- `EZIK-QIBLA-108B-REPORT-2026-08-27.md:1-150` — هذا التقريرُ وحدَه.
- صفرُ سطرٍ في `app.jsx` أو `app.js` أو أيِّ ملفِ إنتاجٍ أو بوّابة.
- صفرُ كتابةٍ في `C:\Users\passe\projects\murabbi-shell`؛ كانتْ قراءتُه بـ`git show` فقط.

## ٤ · البوّاباتُ قبلَ العملِ وبعدَه

| القياس | قبلَ العملِ | بعدَ العملِ |
|---|---:|---:|
| عددُ المسجَّلِ في `gates.json` | 99 | لم يُعَدْ تشغيلُه |
| `SUITE` | `36/99` | لم يُعَدْ تشغيلُه |
| خروجُ عمليّةِ العَدّاء | `1` | لم يُعَدْ تشغيلُه |
| `recon` | `PASS=189 WARN=2 FAIL=1` | لم يُعَدْ تشغيلُه |
| اتّساخُ الشجرةِ الذي أحدثتْه التشغيلة | `0` | لا ينطبق |

لا يوجدُ «بعدَ تنفيذ» لأنَّ مخالفةَ أرقامِ ما قبلَ العملِ هي التي تمنعُ التنفيذَ
نفسَه. وإعادةُ البوّاباتِ بعدَ إنشاءِ تقريرِ التوقّفِ ليستْ قياسًا بعدَ تغييرٍ
وظيفيٍّ، كما أنها تخالفُ أمرَ الوقوفِ وعدمِ تنفيذِ ما بعدَ الشرط.

## ٥ · الحالاتُ الأربعُ وما يراهُ القارئُ الآن

لا تحوي نسخةُ الأساسِ أيَّ اسمٍ من أسماءِ `ezik:heading:*` في `app.jsx` أو
`app.js`، ولذلك لا يمكنُ نسبةُ ظاهرٍ جديدٍ إليها. النصوصُ الآتيةُ وصفٌ دقيقٌ
**للحالِ السابقةِ التي لم تتغيّر**، لا ادّعاءَ تنفيذٍ للبندِ ١٠٨-ب:

| الحالةُ المطلوبة | ما يحدثُ في نسخةِ الأساس | ما يراهُ القارئُ بنصِّه |
|---|---|---|
| اتّجاهٌ حيٌّ من الغلاف (`ready`) | الحدثُ غيرُ مستهلَك؛ لا يدورُ السهمُ بسببه | قبلَ ضغطِ الزر: لا نصَّ في موضعِ حالِ البوصلة، ويظهرُ زرُّ `شغِّلِ البوصلة` |
| معايرةٌ ناقصة (`calibration-needed`) | الحدثُ غيرُ مستهلَك، فلا ظاهرَ مخصّصًا للمعايرة | قبلَ ضغطِ الزر: لا نصَّ في موضعِ الحال، ويظهرُ زرُّ `شغِّلِ البوصلة` |
| لا حسّاس (`sensor-unavailable`) | الحدثُ غيرُ مستهلَك، فلا ظاهرَ مخصّصًا لغيابِ الحسّاس | قبلَ ضغطِ الزر: لا نصَّ في موضعِ الحال، ويظهرُ زرُّ `شغِّلِ البوصلة` |
| لا جسر | المسارُ القديمُ يحاولُ حدثَي المتصفّحِ بعدَ الضغط، بدلَ أن يعلنَ غيابَ اتّجاهِ الجهازِ هنا فورًا | `بانتظارِ قراءةٍ من حسّاسِ الاتّجاه…`، ثمَّ بعدَ ٤٠٠٠ ms بلا قراءةٍ صالحة: `لا تدورُ البوصلةُ على هذا الجهاز: لم تصلْ قراءةٌ صالحةٌ من حسّاسِ الاتّجاه، فالدرجةُ وحدَها هي المعروضة.` |

مصدرُ النصوصِ الحاليةِ هو `app.jsx:15951-15954`، ومصدرُ آلةِ الحالةِ القديمةِ
هو `app.jsx:15961-15973,16031-16069`، ومصدرُ إظهارِ السهمِ والنصِّ والزرِّ هو
`app.jsx:16071-16100`. أمّا اختبارُ الجسرِ الموجودُ أصلًا فيشتقُّه حصرًا من
`window.ReactNativeWebView` في `app.jsx:15043-15047`، لكنه مستعملٌ للموقعِ لا
لاتّجاهِ البوصلةِ في نسخةِ الأساس.

هذه الحالاتُ الأربعُ إذن **غيرُ منفَّذةٍ للبثِّ الجديد** بسببِ شرطِ التوقّف، ولا
يُقدَّمُ ظاهرُ الأساسِ أعلاه على أنه يحقّقُ المطلوب.

## ٦ · ما لم يُثبَتْ صراحةً

- لم يُنفَّذ استهلاكُ `ezik:heading:result`، ولم تُرسَلْ رسالتا البدءِ والإيقافِ من الويب.
- لم يُثبَت دورانُ سهمِ القبلةِ من بثِّ الغلاف، ولا ظواهرُ المعايرةِ الناقصةِ أو غيابِ الحسّاسِ أو غيابِ الجسرِ المطلوبةُ في الأمر.
- لم يُوسَّعْ حارسُ `wird` القائمُ؛ لم تُنشَأْ بوّابةٌ جديدةٌ ولم تُمسَّ بوّابةٌ قديمةٌ.
- لم يُضَفْ مفتاحُ تخزينٍ، ولم يُمسَّ مفتاحٌ قائمٌ.
- لم يُبنَ `app.js`، ولم يُنفَّذ أيُّ بناءٍ أو نشرٍ أو رفعٍ.
- لم تُقَسِ البوصلةُ على جهازٍ حقيقيٍّ، ولم تُستبدَلْ تلكَ الحقيقةُ بمحاكاة.
- لم يقعْ `git push`.
