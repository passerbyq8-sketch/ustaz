# تقريرُ كودكس — البندُ ١٠٨-ب: بثُّ اتّجاهِ القبلةِ من الغلاف

**الشجرة:** `C:\Users\passe\projects\ustaz-wt-qibla-20260827`
**الفرع:** `feat/qibla-heading-20260827`
**أساسُ التقريرِ المحفوظ:** `cc8fab4668630e18331ef51bc5909d30e43a5269`
**رأسُ التنفيذِ والقياس:** `1eb8f0ebff4474376c5deabc7ba291a7f1f7f786`
**التاريخ:** ٢٧ أغسطس ٢٠٢٦ (Asia/Kuwait)
**الحكم:** **نُفِّذَ البندُ محليًّا، واجتازتِ البطّاريّةُ الملزِمةُ ٩٩/٩٩ بخروجِ عمليّةٍ فعليٍّ صفر.**

لم يقعْ دفعٌ ولا دمجٌ. وبقيَ الإيداعُ `cc8fab4` كما هو؛ أُضيفَ فوقَه إيداعُ
التنفيذِ ثمّ إيداعُ القياسِ، وسيكونُ تغييرُ هذا التقريرِ وحدَه إيداعًا توثيقيًّا
مستقلًّا.

## ١ · ما نُفِّذ

- عندَ وجودِ جسرِ الغلافِ، تلتقطُ لوحةُ القبلةِ الكاشفَ القائمَ
  `ezikShellBridge()` مرّةً لعمرِها، وتُرسلُ `ezik:heading:start` عندَ دخولِها،
  وتفكُّ المستمعَ وتُرسلُ `ezik:heading:stop` عندَ خروجِها.
- لا يُرسَلُ البدءُ بلا جسر، ولا يوجدُ اشتراكٌ دائمٌ خارجَ عمرِ لوحةِ القبلة.
- يستهلكُ الويبُ `ezik:heading:result` من `event.detail`، ويثقُ بحقلِ `status`؛
  لا يُعيدُ تصنيفَ المعايرةِ من `accuracy` ولا يدمجُ حالتين.
- عندَ وجودِ الجسرِ يكونُ الغلافُ مصدرَ الاتّجاهِ وحدَه. وعندَ غيابِه يبقى
  مسارُ `DeviceOrientationEvent` السابقُ وزرُّه ومهلتُه ونصوصُه كما كانت.
- يفضِّلُ السهمُ `trueHeading` الصالحةَ لأنَّ حسابَ القبلةِ مرجِعُه الشمالُ
  الحقيقيُّ، ويستعملُ `magHeading` بديلًا إذا كانتِ الأولى غيرَ صالحةٍ، ومنها
  قيمةُ Expo المسمّاة `-1`.
- وُسِّعَ الحارسانِ القائمانِ `tools/location-bridge-measure.cjs` و
  `tools/wird-guard.cjs`. لم تُنشَأْ بوّابةٌ جديدةٌ ولم يُضَفْ مفتاحُ تخزين.

## ٢ · العقدُ المقاسُ من مصدرِ الغلاف

قُرئَ العقدُ قراءةً فقط من `murabbi-shell` في لقطةِ الشيفرةِ
`3404149476ca24163d2e9f50f40f6fe77e4efb54`، ومن تقريرِها في الإيداع
`6623697522b801e1a821c32362cab002d7db9dbd`. لم تُكتَبْ بايتةٌ في مستودعِ
الغلاف.

### ٢٫١ الرسائلُ الثلاث

| الاتّجاه | الاسمُ بحرفِه | الحقولُ | المصدرُ الأوّليُّ | شاهدُ التقرير |
|---|---|---|---|---|
| الويب ⟶ الغلاف | `ezik:heading:start` | `type` وحدَه | `3404149:src/location.js:179,198-209` | `6623697:EZIK-CODEX-HEADING-SCHED-REPORT-2026-08-26.md:75` |
| الويب ⟶ الغلاف | `ezik:heading:stop` | `type` وحدَه | `3404149:src/location.js:180,198-209` | `6623697:EZIK-CODEX-HEADING-SCHED-REPORT-2026-08-26.md:76` |
| الغلاف ⟶ الويب | `ezik:heading:result` | `type`, `ok`, `status`, `magHeading`, `trueHeading`, `accuracy`, `error` | `3404149:src/location.js:181,230-252` | `6623697:EZIK-CODEX-HEADING-SCHED-REPORT-2026-08-26.md:77,79-93` |

محلّلُ الأمرِ لا يقرأُ إلا `type` (`src/location.js:198-209`). أمّا النتيجةُ
فتُحقَنُ `CustomEvent` باسمِ `ezik:heading:result` وتوضعُ الحمولةُ في
`event.detail` (`src/location.js:255-263`). يمرِّرُ `SiteScreen` الحقنَ إلى
الويب في `3404149:src/SiteScreen.js:55-65`، ويتلقّى أمرَي الويبِ في
`3404149:src/SiteScreen.js:167-173`.

### ٢٫٢ الحقولُ السبعةُ بحروفِها

| الحقل | حكمُه في العقد | موضعُ تعريفِه |
|---|---|---|
| `type` | دائمًا `ezik:heading:result` | النجاح `src/location.js:231`؛ الفشل `:245` |
| `ok` | `true` في `ready` فقط؛ `false` في المعايرةِ وسائرِ الفشل | `src/location.js:229-233,246` |
| `status` | واحدةٌ من الحالاتِ الخمسِ المسماةِ أدناه | `src/location.js:186-192,229-233,242-247` |
| `magHeading` | رقمٌ محدودٌ كما أعادَه SDK في القراءةِ العدديّةِ، وإلّا `null` | `src/location.js:217-225,234,248` |
| `trueHeading` | رقمٌ محدودٌ كما أعادَه SDK، وقد يكونُ `-1`؛ وإلّا `null` | `src/location.js:217-225,235,249`، والتقرير `:61-65` |
| `accuracy` | رقمٌ محدودٌ من ٠ إلى ٣، وإلّا `null` | `src/location.js:217-225,236,250` |
| `error` | `null` مع القراءةِ العدديّة، واسمُ `status` مع الفشل | `src/location.js:237,251` |

الحالاتُ الخمسُ في المصدرِ هي `ready` و`calibration-needed` و
`permission-denied` و`sensor-unavailable` و`heading-error`
(`src/location.js:186-192`). يصنِّفُ الغلافُ `accuracy` صفرًا أو ١ معايرةً
ناقصةً، و٢ أو ٣ جاهزيةً (`src/location.js:217-229`)؛ أمّا الويبُ فلا يقرأُ
`accuracy` لاتخاذِ هذا الحكم.

### ٢٫٣ ما خالفَ الأمرَ عندَ التحقّق

نسخةُ الأمرِ (ب) توافقُ أسماءَ الرسائلِ والحقولِ والحالاتِ في المصدر. بقيَ
اختلافٌ توثيقيٌّ واحدٌ يجبُ تسميتُه: تقريرُ الغلافِ في السطرِ ٩٠ يقولُ عن
`calibration-needed` إنَّ `ok:false` هو «كي لا يُعرضَ سهمٌ موثوق»، بينما أمرُ
(ب) يوجبُ أن يدورَ السهمُ مع تحذيرِ المعايرة. الشيفرةُ المصدريةُ لا تمنعُ
العرض؛ بل تبثُّ `magHeading` و`trueHeading` في هذه الحال (`src/location.js:229-237`).
لذلك نُفِّذَ حكمُ الأمرِ الأحدث: يدورُ السهمُ، وتبقى المعايرةُ ظاهرةً صراحةً،
ولا يُستعملُ `ok` لطمسِ الحالِ أو دمجِها بـ`ready`.

كما أثبتَ تقريرُ الغلافِ أنَّ `trueHeading` قد تكونُ `-1` (`:64`)؛ وهذا ليس
تناقضًا بعدَ قرارِ البديلِ في §٤، بل سببُ وجودِه.

## ٣ · المصافحةُ وعمرُ الاشتراك

أسماءُ الرسائلِ وتعليمةُ الإرسالِ ذاتُ الحقلِ الواحدِ معرّفةٌ في
`app.jsx:15085-15110`. وتفصيلُ العمرِ في `app.jsx:15997-16044`:

1. تلتقطُ اللوحةُ نتيجةَ `ezikShellBridge()` في `headingBridgeRef` مرّةً
   (`app.jsx:16003-16005`)؛ لم يُكتَبْ كاشفٌ ثانٍ.
2. إنْ كانَ الجسرُ حاضرًا، يُركَّبُ مستمعُ `ezik:heading:result` ثمّ تُرسَلُ
   الحمولةُ المطابقةُ حرفيًّا `{"type":"ezik:heading:start"}`
   (`app.jsx:16012-16035`).
3. عندَ فكِّ اللوحةِ يُزالُ المستمعُ ثمّ تُرسَلُ
   `{"type":"ezik:heading:stop"}` (`app.jsx:16031-16043`).
4. إنْ غابَ الجسرُ، لا يدخلُ الفرعُ أصلًا ولا تُرسَلُ رسالةٌ؛ تبدأُ حالةُ
   المتصفّحِ القديمةُ `off` (`app.jsx:16003-16014`).
5. `heading-error` يعرضُ زرَّ إعادةِ المحاولة؛ ضغطُه يعيدُ الحالَ إلى الانتظارِ
   ويرسلُ `start` من جديد (`app.jsx:16142-16148,16184-16186`). والغلافُ يفكُّ
   اشتراكَه قبلَ بثِّ عطبِ القراءةِ، كما يبيِّن
   `3404149:src/location.js:322-325,367-374`، فتكونُ المحاولةُ اشتراكًا جديدًا.

لا يبدأُ بثٌ بلا جسر، ولا يبقى مستمعٌ أو اشتراكٌ بعدَ خروجِ لوحةِ القبلة.

## ٤ · الشمالُ الحقيقيُّ والبديلُ المغناطيسيُّ

الدليلُ من الويبِ سابقٌ للقرار: `qiblaBearing` يصرّحُ أنَّ ناتجَه درجاتٌ مع
عقاربِ الساعةِ من **الشمالِ الحقيقيِّ**، ثمّ يحسبُ bearing الدائرةِ العظمى
(`app.jsx:14961-14973`). ويحوِّلُ `qiblaNeedleAngle` ذلكَ إلى فرقِ
`bearing - heading` (`app.jsx:15001-15005`).

لذلك يختارُ `shellHeadingOf` في `app.jsx:15090-15104` هكذا:

1. `trueHeading` إذا كانت رقمًا محدودًا بين ٠ و٣٦٠؛
2. وإلّا `magHeading` بالشروطِ نفسِها؛
3. وإلّا لا اتّجاهَ ولا سهمَ صالحًا، وتتحوّلُ قراءةُ `ready` أو
   `calibration-needed` الناقصةُ عدديًّا إلى `heading-error`.

هذا يحفظُ مرجعَ الحسابِ الصحيحَ متى وفّرَه SDK، ويجعلُ البديلَ المغناطيسيَّ
صريحًا ومحدودًا بدلَ أن تُعامَلَ `trueHeading === -1` كزاويةٍ حقيقية. لا
يدخلُ `accuracy` في الاختيار.

## ٥ · الحالاتُ الستُّ وما يراهُ القارئُ بحرفِه

النصوصُ معرّفةٌ في `app.jsx:15982-15990`، وفروعُ العرضِ في
`app.jsx:16161-16186`.

| # | الحال | السهم | النصُّ الظاهرُ بحرفِه |
|---|---|---|---|
| ١ | `ready` | يدورُ باستعمالِ الاتّجاهِ الحقيقيِّ الصالح، أو المغناطيسيِّ بديلًا | `البوصلةُ تدورُ مع الجهاز.` |
| ٢ | `calibration-needed` | **يبقى دائرًا** ولا يُدمَجُ بـ`ready` | `السهمُ يدورُ، لكنَّ معايرةَ حسّاسِ الاتّجاهِ ناقصة. حرِّكِ الجهازَ ببطءٍ على شكلِ الرقم ٨ حتّى تتحسّنَ المعايرة.` |
| ٣ | `sensor-unavailable` | لا سهمَ حيًّا | `لا يحتوي هذا الجهازُ على حسّاسِ اتّجاهٍ متاح؛ تبقى درجةُ القبلةِ المعروضةُ أعلاه.` |
| ٤ | `permission-denied` | لا سهمَ حيًّا | `إذنُ الموقعِ مرفوض. افتحْ إعداداتِ الجهازِ الخاصّةَ بتطبيقِ عزك وامنحْ إذنَ الموقع، ثم عُدْ إلى هذه الشاشة.` |
| ٥ | `heading-error` | لا سهمَ حيًّا؛ يظهرُ زرُّ `أعِدِ المحاولة` | `تعذّرتْ قراءةُ اتّجاهِ الجهازِ بسببِ عطبٍ طارئ. أعِدِ المحاولة.` |
| ٦ | لا جسر | لا حكمَ من الغلافِ؛ يبقى مسارُ المتصفّحِ السابقُ | **لا يظهرُ نصٌّ لمجرّدِ غيابِ الجسر.** قبلَ الضغطِ يظهرُ زرُّ `شغِّلِ البوصلة`. بعدَه يظهرُ `بانتظارِ قراءةٍ من حسّاسِ الاتّجاه…`. إذا نجحَ المسارُ يظهرُ `البوصلةُ تدورُ مع الجهاز.`، وإذا فشلَ وحدَه بعدَ ٤٠٠٠ ms يظهرُ `لا تدورُ البوصلةُ على هذا الجهاز: لم تصلْ قراءةٌ صالحةٌ من حسّاسِ الاتّجاه، فالدرجةُ وحدَها هي المعروضة.` ويعودُ زرُّ التشغيل. |

فرعُ المتصفّحِ نفسه ما زالَ في `app.jsx:16102-16140`: يستأذنُ من
`DeviceOrientationEvent.requestPermission()` حيث توجدُ، ويركبُ مستمعَي
`deviceorientationabsolute` و`deviceorientation` من ضغطِ القارئ، ويفكُّهما
عندَ الخروج. ومع الجسرِ لا يظهرُ زرُّ هذا الفرعِ في أيٍّ من حالاتِ الغلافِ
الخمس، فيبقى المصدرُ shell-only كما يطلبُ الأمر.

## ٦ · القياسُ والحارسانِ القائمان

وُسِّعَ `tools/location-bridge-measure.cjs` بدلَ إنشاءِ بوابةٍ جديدة. يشغِّلُ
الدوالَّ والمعالجَ الحقيقيَّينِ المرفوعَينِ من `app.jsx` ويثبتُ:

- حمولتَي البدءِ والإيقافِ ذواتَي الحقلِ الواحدِ، وصفرَ إرسالٍ بلا جسر؛
- أولويةَ `trueHeading`، وبديلَ `magHeading` عندَ `-1`، وتطبيعَ ٣٦٠ إلى صفر؛
- أنَّ `accuracy` لا تُقرأُ للتصنيف، وأنَّ الحالاتِ الخمسَ تبقى خمسًا ولو
  تعارضَ رقمُ الدقّةِ مع اسمِ الحال؛
- بدءًا واحدًا في التركيب، وإيقافًا وفكًّا في الإزالة، وإعادةَ محاولةٍ قابلةً
  للوصول؛
- بقاءَ مسارِ `DeviceOrientationEvent` كما هو عندَ غيابِ الجسر.

نتيجةُ تشغيلِه المستقلّ: `21/21 cases hold`. ووسّعَ
`tools/wird-guard.cjs` قسمَ القبلةِ القائمَ ليجري اختيارَ الشمالِ والمصافحةَ
ويفحصَ الفروعَ الخمسةَ ومسارَ المتصفّح؛ نتيجتُه المستقلّة:
`passed: 1159`, `failed: 0`, `result: PASS`.

## ٧ · وقفتا خطِّ الأساسِ وما حلَّهما

### ٧٫١ الوقفةُ الأولى — الحزمُ غائبة

بدأتِ الشجرةُ الجديدةُ نظيفةً عندَ `8b52ad7`. أعادَتِ البطّاريّةُ في دليلِ
`2026-08-27T07-59-21-219Z-11932`:

```text
SUITE=36/99
RUNNER_PROCESS_EXIT=1
RECON=PASS=189 WARN=2 FAIL=1
DIRTY=0 -> 0
```

كانَ السببُ الجذريُّ واحدًا: `node_modules` غائبةٌ في worktree جديد، فتعذّرَ
تحميلُ الحزمِ وقرأَ recon أثرَ تقريرٍ قديم. لم يكنْ ذلكَ حكمًا على شيفرةِ
البند. شغّلَ المالكُ `npm ci`: ٧١ حزمةً، خروجٌ صفر، وصفرُ ملفِّ مصدرٍ ممسوس.
بعدَه أعادَ دليلُ `2026-08-27T08-11-56-561Z-6180` قياسًا نظيفًا
`99/99` و`RECON PASS=190 WARN=1 FAIL=0`.

### ٧٫٢ الوقفةُ الثانية — انقطاعُ متصفّحِ `questux`

في إعادةِ التثبيتِ عندَ `cc8fab4`، أعادَ دليلُ
`2026-08-27T08-21-31-588Z-4636`:

```text
SUITE=98/99
RUNNER_PROCESS_EXIT=1
RECON=PASS=190 WARN=1 FAIL=0
FAILED=questux
DIRTY=0 -> 0
```

اجتازَ `questux` سبعةَ فحوصٍ ثابتةً بصفرِ إخفاقِ assertion، ثم انقطعَ اتصالُ
المتصفّحِ في `Page.enable` بـ`ECONNRESET`. قَبِلَ المالكُ recon المثبَتَ وأمرَ
بالوقوفِ حتى يفرغَ الوكيلُ الآخرُ من المتصفّحات، ثم أعطى الإذنَ الصريحَ
بالبطّاريّةِ بعدَ فراغِه.

عندَ القياسِ النهائيِّ بقيَ الانقطاعُ داخلَ sandbox حتى بلا متصفّحِ وكيلٍ
آخر. فحصٌ تشخيصيٌّ مؤقّتٌ غيرُ مودَعٍ التقطَ سببَ Chrome نفسه: عمليةُ GPU
خرجتْ بـWindows `STATUS_ACCESS_DENIED` ثم قالَ Chrome
`GPU process isn't usable. Goodbye.` حُذِفَ الفحصُ المؤقّتُ، ولم يدخلْ أيَّ
إيداع. تشغيلُ البوابةِ نفسِها خارجَ عزلِ العملياتِ — وهو الحدُّ المطلوبُ
لتشغيلِ Chrome — أعادَ `61/61`, خروجًا صفر؛ ثم اجتازتِ البطّاريّةُ كاملةً.

### ٧٫٣ البطّاريّةُ الملزِمةُ بعدَ التنفيذ

التشغيلةُ الحاكمةُ هي:

```text
HEAD=1eb8f0ebff4474376c5deabc7ba291a7f1f7f786
SUITE=99/99
RUNNER_PROCESS_EXIT=0
RECON=PASS=190 WARN=1 FAIL=0
DIRTY_BEFORE=0
DIRTY_AFTER=0
TREE_DIRTIED_BY_RUN=false
DURATION_MS=360139
```

دليلُها خارجَ الشجرة:

```text
C:\Users\passe\AppData\Local\Temp\ezik-gates\runs\2026-08-27T09-25-33-408Z-23248
```

الإنذارُ الواحدُ وحدَه هو الموروثُ المطلوبُ:

```text
LONGEST_CARD_CHARS = 3405 > longest card 3401
```

وموضعُ الثابتِ الذي يسمّيه recon هو `api/report.js`. لا إنذارَ ثانيًا ولا
فشلًا. ومن القياساتِ المساندة:

| القياس | النتيجة |
|---|---|
| `npm.cmd run verify:build` | `app.js` مطابقٌ للمصدر، ١٢٠٥٢٨٤ بايتًا، SHA-256 `2bcb0c34767b607e5beed0f401a44c5e9d042e12ea4cbf990ceca79302352357` |
| `node tools/core-bytes.cjs` | المعلنُ والمقاسُ `2048131`، مطابقان |
| `quest-bank-integrity-guard.cjs` | ٧٦ ناجحًا، صفرُ فشل |
| `tools/location-bridge-measure.cjs` | ٢١/٢١ |
| `tools/wird-guard.cjs` | ١١٥٩ ناجحًا، صفرُ فشل |
| `quest-ux-guard.cjs quest.html` خارجَ sandbox | ٦١/٦١، خروجٌ صفر |

## ٨ · الملفاتُ والإيداعاتُ

### `a96fe7dce36e23386b107c904f04277869944643` — التنفيذُ والتغليفُ المتلازمان

- `app.jsx` — العقدُ، اختيارُ الاتّجاهِ، المصافحةُ، الحالاتُ الخمسُ، وبقاءُ
  مسارِ المتصفّح.
- `app.js` — الناتجُ الحتميُّ المبنيُّ من `app.jsx`.
- `sw.js` — تحديثُ وزنِ CORE ووصفِ حجمِ `app.js` فقط؛ لم يُرفَعْ إصدارُ cache.
- `quest-bank-integrity-guard.cjs` — مرآةُ حجمِ `app.js` وختمُ `sw.js`
  الملازمانِ لناتجِ البناء.

ملخّصُ الإيداع: ٤ ملفات، ١٢٠ إضافةً، ١٧ حذفًا.

### `1eb8f0ebff4474376c5deabc7ba291a7f1f7f786` — القياسُ القائم

- `tools/location-bridge-measure.cjs`
- `tools/wird-guard.cjs`

ملخّصُ الإيداع: ملفّان، ٢٤٥ إضافةً، ١٠ حذوفات. استُعملَ `git add` بأسماءِ
الملفاتِ الصريحةِ في الإيداعين. لا بوابةَ جديدةً ولا ملفَّ golden جديدًا.

أسماءُ ملفاتِ التنفيذِ الستّةِ وحدَها بينَ `cc8fab4` ورأسِ التنفيذ:

```text
app.jsx
app.js
sw.js
quest-bank-integrity-guard.cjs
tools/location-bridge-measure.cjs
tools/wird-guard.cjs
```

لم تُمسَّ بياناتُ الأسئلةِ أو القرآنِ أو أذكارُ الإنتاج، ولم يُضَفْ endpoint
ولا مفتاحُ تخزين. بقيَ `murabbi-shell` للقراءةِ فقط؛ وحالُه النهائيُّ يحوي
ملفًا غيرَ متتبَّعٍ يخصُّ عملًا آخرَ هو `ORDER-SHELL-AUTHPROOF-CODEX.md`، ولم
يُنشئْه هذا العملُ ولم يلمسْه.

## ٩ · ما لم يُثبَتْ صراحةً

- لم تُجرَّبِ البوصلةُ على هاتفٍ حقيقيٍّ، ولم يُدَّعَ دورانٌ فيزيائيٌّ أو
  دقّةُ معايرةٍ أو تصحيحُ انحرافٍ مغناطيسيٍّ على جهاز.
- لم تُشغَّلْ نسخةُ murabbi-shell الأصليةُ مع هذا الويبِ end-to-end على
  Android أو iOS. القياسُ يرفعُ معالجَ الويبِ الحقيقيَّ ويحقنُ شكلَ العقدِ
  نفسه، لكنه ليس جهازًا ولا WebView أصليةً.
- لم يُبنَ تطبيقٌ أصليٌّ ولم يُعدَّلْ إذنُ manifest أو شيفرةُ الغلاف.
- لم يُقَسْ استهلاكُ البطّارية. المثبَتُ برمجيًّا هو start عندَ تركيبِ اللوحةِ
  وstop عندَ فكِّها، لا قياسُ طاقةٍ حقيقي.
- حينَ يغيبُ `trueHeading` الصالحُ ويُستعملُ `magHeading`، قد يبقى فرقُ
  الانحرافِ المغناطيسيِّ قائمًا. هذا بديلٌ مسمًّى لا ادّعاءُ شمالٍ حقيقيٍّ.
- لم يُختبَرْ انتقالُ القارئِ يدويًّا إلى إعداداتِ النظامِ ومنحُ الإذنِ ثم
  عودتُه؛ المثبَتُ هو النصُّ وفرعُ `permission-denied` والعمرُ البرمجيُّ.
- لم يقعْ نشرٌ أو دفعٌ أو دمجٌ، ولم تُنشَأْ مطالبةُ سحب.
