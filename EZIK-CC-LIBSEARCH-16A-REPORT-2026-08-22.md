# تقريرُ تنفيذٍ — عزك · الشاملةُ · البندُ **١٦-أ**: دالّةُ النداءِ وبنّاءُ بطاقةِ المصدر

**النافذة:** ن٢ · وكيلُ Claude Code · `C:\Users\passe\projects\ustaz-cx-chatux`
**التاريخ:** 2026-08-22
**الفرع:** `feat/lib-search-16a-20260822`
**الأساس:** `origin/main = 4c9531cb4687ed9f576fe047aa21b130f692b3ff`

---

## ٠ · نقلُ الأمر

`ORDER-LIBSEARCH-16A.md` نُقِلَ خارجَ الشجرةِ إلى `C:\EZIK-STAGE\orders\` قبلَ أيِّ فعلٍ آخر ⟹ `ORDER_MOVED=True`.

وُجِدَتْ في الشجرةِ خمسةُ ملفّاتٍ غيرِ متتبَّعةٍ من جولاتٍ سابقةٍ في هذا المجلَّدِ نفسِه
(`EZIK-CX-B-ROUND25-REPORT-2026-08-22.md` · `ORDER-MERGE-23.md` · `ORDER-ROUND25-B.md` · `ORDER-SITE-SEO.md` · `SITE-SEO-REPORT.md`).
وشرطُ `DIRTY_LINES = 0` يقتضي خلوَّ الشجرة، فنُقِلَتْ — **ولم تُحذَفْ** — إلى
`C:\EZIK-STAGE\stray-untracked-cx-chatux-20260822\` وهي هناكَ بأكملِها.
وعملُ الجولةِ ٢٥-ب الذي أنتجَها مدموجٌ في `main` أصلًا (`git merge-base --is-ancestor 599851e origin/main` ⟹ صحيح)، فلا عملَ جاريًا فُقِد.

---

## ١ · بوّاباتُ ما قبلَ العمل

| المفتاح | المطلوب | **المقيس** | الحال |
|---|---|---|---|
| `HEAD` | `4c9531cb4687…` | `4c9531cb4687ed9f576fe047aa21b130f692b3ff` | ✅ |
| `DIRTY_LINES` | `0` | `0` | ✅ |
| `API_DIR_EXISTS` | `True` | `True` | ✅ |
| `LIB_DIR_EXISTS` | `True` | `True` | ✅ |
| الفرع | `feat/lib-search-16a-20260822` | أُنشئَ من `4c9531cb` | ✅ |

---

## ٢ · 🔴 البوّابةُ الحاسمة — ترويسةُ المصادقةِ **مقيسةٌ** لا مخمَّنة

قُرِئَ `C:\EZIK-LIB\service\src\server.mjs` **قراءةً فقط** (497 سطرًا · 16982 بايتة · مطبوعٌ بالإنجليزيّةِ على الطرفيّة). وهذه كلُّ الأسطرِ التي تذكرُ `401` أو `token` أو `authorization` أو `bearer` أو `headers.get` أو `x-`:

```
 29: import { retrieve, tokenize } from './retrieve.mjs';
 77:   const token = overrides.token ?? environment.SEARCH_API_TOKEN;
 78:   if (typeof token !== 'string' || token.length === 0) {
 79:     throw new ConfigurationError('SEARCH_API_TOKEN is required.');
 91:     token,
160: function hashToken(value) {
164: function authorized(header, expectedToken) {
165:   if (typeof header !== 'string' || !header.startsWith('Bearer ')) return false;
166:   const supplied = header.slice('Bearer '.length);
167:   return timingSafeEqual(hashToken(supplied), hashToken(expectedToken));
176:     'X-Content-Type-Options': 'nosniff',
274:   const terms = tokenize(normalized);
365:         if (!authorized(request.headers.authorization, config.token)) throw new UnauthorizedError();
411:       if (error instanceof UnauthorizedError) headers['WWW-Authenticate'] = 'Bearer';
```

وسياقُ المسارِ نفسِه (`server.mjs:364-366`):

```
if (pathname === '/search') {
  if (request.method !== 'POST') throw new MethodNotAllowedError('POST');
  if (!authorized(request.headers.authorization, config.token)) throw new UnauthorizedError();
```

**النصُّ المقيسُ المستخرَج:**

| المفتاح | القيمة | سطرُها المقيس |
|---|---|---|
| `AUTH_HEADER_NAME` | `authorization` | `server.mjs:365` — `authorized(request.headers.authorization, config.token)` |
| `AUTH_VALUE_SHAPE` | **مسبوقةٌ بكلمة**: `Bearer <token>` — بادئةٌ حرفيّةٌ `Bearer ` بمسافةٍ واحدةٍ، ثمّ التوكنُ خامًا | `server.mjs:165` — `!header.startsWith('Bearer ')` · `server.mjs:166` — `header.slice('Bearer '.length)` |

القيمةُ **ليستْ خامًا**. والمقارنةُ في `:167` عبرَ `timingSafeEqual` على `sha256` للطرفَين، فأيُّ بادئةٍ زائدةٍ أو ناقصةٍ تعني `401` صامتًا.
واسمُ متغيّرِ البيئةِ مؤكَّدٌ من مصدرِ الخدمةِ ذاتِه في `:77` و`:79`: **`SEARCH_API_TOKEN`** — وهو الاسمُ الذي تقرأُه الدالّةُ الجديدة.

**لم تُخترَعْ ترويسة.** ولم يُطلَبِ الوقوفُ بـ`auth_header_unmeasured` لأنّ القياسَ اتّضح.

---

## ٣ · جردُ الملفّاتِ المضافة

**إضافةٌ محضةٌ — صفرُ تعديلٍ لسطرٍ قائمٍ في المستودعِ كلِّه.** (لا `app.jsx` · لا `app.js` · لا `sw.js` · لا `index.html` · لا مخزنَ شحنةٍ · لا بايتةَ فهرسٍ أو كتابٍ أو ذرّةٍ دخلَتِ الشجرة.)

| الملفّ | البايتات | الأسطر | الدور |
|---|---|---|---|
| `api/lib-search.js` | 8386 | 189 | دالّةُ الخادمِ — النداءُ الوحيدُ للخدمة |
| `lib/lib-source-card.js` | 7145 | 166 | `buildSourceCard` — دالّةٌ خالصة |
| `guards/lib-search-16a-guard.cjs` | 19209 | 277 | الحارسُ — الحالاتُ السبعُ بلا شبكة |
| `guards/fixtures-lib-search-16a.json` | 4242 | 142 | فيكستشراتٌ محلّيّةٌ على شكلِ §٣ |

**الإيداعُ كانَ بأسماءِ الملفّاتِ صراحةً** (`git add` بأربعةِ أسماءٍ · لا `git add .`).

**صفرُ بوّابةٍ جديدةٍ:** الحارسُ **غيرُ مسجَّلٍ في `gates.json`** عمدًا، فعددُ البوّاباتِ باقٍ 90 كما كان، ويُشغَّلُ الحارسُ باسمِه وحدَه.

### قراراتُ التصميمِ التي تخصُّ صاحبَ الأمرِ لا الوكيل

1. **شكلُ جسمِ الخطأ.** حقولُ §٣ الأحدَ عشرَ تصفُ جوابَ `/search` الناجح. أمّا أجسامُ الخطأِ (`400` · `405` · `502` · `503`) فيوجبُها §٤-١ («نصٌّ محايد») ولا أصلَ لها في §٣، فاتُّبِعَ فيها عرفُ المستودعِ القائمُ في `api/fatwa-proxy.js`: `{ ok:false, error:{ code, message } }`. لا حقلَ من §٣ في أيِّ جسمِ خطأ، ولا حقلَ مخترَعٌ في أيِّ جسمِ نجاح.
2. **`page_citable` غيرُ موجودٍ أو ليسَ منطقيًّا.** §٤-٢ يسمّي `true` و`false` وحدَهما. الفرعُ المكتوبُ هو `=== true` للصفحةِ، وكلُّ ما عداهُ — بما فيه الغياب — يأخذُ فرعَ «لا صفحةَ ألبتّة». اتّجاهُ المجهولِ هو الصمتُ عنِ الصفحةِ لا التخمينُ بها.
3. **العرضُ مقابلَ البناء.** `buildSourceCard` تُرجعُ سجلًّا مفاتيحُه أسماءُ حقولِ §٣ حرفًا. ووسمُ النقصِ «ظاهرٌ» يقتضي نصًّا، فأُضيفَتْ `renderSourceCard(card)` الخالصةُ تقرأُ **البطاقةَ** (وقدْ نُزِعَتْ منها الصفحةُ قبلًا) فلا تستطيعُ طبعَ صفحةٍ لا تحملُها. ولا يناديها شيءٌ في هذه الجولةِ؛ هي للبندِ ١٦-ب.
4. **نصّا §٤-٣** (`REFUSED_TEXT` و`DEGRADED_TEXT`) صُدِّرا من `lib/lib-source-card.js` لأنّه الملفُّ الوحيدُ المسموحُ في `lib/`، ولئلّا تكتبَ الواجهةُ نصًّا من عندِها في ١٦-ب.

---

## ٤ · جدولُ الحالاتِ السبعِ الملزِمة

`node guards/lib-search-16a-guard.cjs` ⟹ **`73/73 PASS` · `EXIT=0`** · **صفرُ شبكةٍ** (`globalThis.fetch` مستبدَلٌ) · **صفرُ توكنٍ حقيقيّ**.

| # | الحالة | ما قِيس | الحال |
|---|---|---|---|
| ١ | إصابةٌ **بصفحة** (`page_citable: true`) | البطاقةُ: `book_title` · `author` · `volume=9407` · `page_start=7301` · `page_end=7302`؛ والسطرُ المعروضُ يظهرُ فيه الرقمان؛ و`hadith_no: null` **لم يُحمَل** | ✅ |
| ٢ | إصابةٌ **بلا صفحة** (`page_citable: false`) | البطاقةُ: `book_title` · `author` · `heading_path`؛ و**لا `page_start` ولا `page_end` ولا `volume`** — والإصابةُ في الفيكستشرِ تحملُ `7301/7302/9407` فعلًا، فالنزعُ حقيقيٌّ لا فراغٌ؛ ولا يظهرُ أيٌّ من الثلاثةِ في السطرِ المعروض | ✅ |
| ٣ | `truncated === true` | `card.truncated === true`، والسطرُ يحملُ وسمَ النقصِ الظاهرَ `(النصُّ مبتور)`؛ وإصابةٌ غيرُ مبتورةٍ لا تحملُ الوسمَ (ضبطٌ مضادّ) | ✅ |
| ٤ | `refused` + `estimated_postings_exceed_ceiling` | `200` لا شاشةَ خطأٍ · الحقلانِ يمرّانِ كما هما · **`logs.length === 0`** أي صفرُ سجلِّ عطبٍ · و`REFUSED_TEXT` موجودٌ نصًّا بديلًا | ✅ |
| ٥ | `degraded_reason: over_budget` | `200` · الحقلُ يمرُّ · **الإصابتانِ المبتورتانِ تُسلَّمانِ** لا تُطرحان · `hits_dropped=37` يمرُّ · و`DEGRADED_TEXT` موجودٌ لذكرِ النقص | ✅ |
| ٦ | غيابُ `SEARCH_API_TOKEN` | `503` · **الخدمةُ لم تُنادَ ألبتّة** (`calls=0`) · الجسمُ لا يذكرُ اسمَ المتغيّرِ ولا `lib.ezik.app` · والسببُ `search_api_token_missing` في **سجلِّ الخادمِ وحدَه** | ✅ |
| ٧ | `401` من الخدمة | `502` للعميلِ · **نداءٌ واحدٌ فقط** (`calls=1`) فلا إعادةَ بلا توكنٍ · والنداءُ الوحيدُ حملَ التوكنَ · والجسمُ لا يحملُ `401` ولا `unauthorized` ولا اسمَ المضيف | ✅ |

### التأكيدُ الذي وُجِدَتِ الجولةُ لأجلِه — صفرُ ظهورٍ للتوكن

| ما فُحِص | الحال |
|---|---|
| قيمةُ التوكنِ في أيِّ جسمِ جوابٍ من الأجوبةِ الخمسةِ المقاسة | ✅ صفر |
| قيمةُ التوكنِ في أيِّ ترويسةِ جوابٍ | ✅ صفر |
| قيمةُ التوكنِ في أيِّ سطرِ سجلٍّ | ✅ صفر |
| توكنٌ زرعَتْه الخدمةُ في حقلٍ زائدٍ (`server_token_hint`) | ✅ أُسقِطَ ولم يُمرَّرْ |
| مخرَجاتُ بنّاءِ البطاقةِ (ثلاثُ بطاقاتٍ وثلاثةُ أسطرٍ معروضة) | ✅ صفر |
| نصٌّ حرفيٌّ لتوكنٍ داخلَ `api/lib-search.js` أو `lib/lib-source-card.js` | ✅ صفر |

> توكنُ الفيكستشرِ `tk-fix-9` — **ثمانيةُ محارفَ عمدًا**. ماسحُ الأسرارِ في `recon` يقرأُ أيَّ نصٍّ بشكلِ توكنٍ طولُه 16 محرفًا فأكثرَ كاعتمادٍ مسرَّبٍ حقيقيٍّ ويُحمِّرُ البوّابة.

### فحوصٌ زائدةٌ على السبعِ (من الـ73)

- **العقدُ**: قائمةُ الجوابِ **أحدَ عشرَ** حقلًا · قائمةُ الإصابةِ **سبعةَ عشرَ** · **`book_id` ليسَ في أيِّ قائمةٍ** واسمُ الكتابِ `book_title`.
- **الترويسةُ على السلك**: المسارُ `https://lib.ezik.app/search` · `POST` · الاسمُ `authorization` · القيمةُ `Bearer ` + التوكنِ حرفًا · `content-type: application/json` · وجسمُ الطلبِ يحملُ `q` و`limit` **وحدَهما**.
- **الغربلةُ**: كلُّ مفتاحٍ عُلويٍّ من الأحدَ عشرَ · حقلٌ زائدٌ (`shard_host`) أُسقِطَ صامتًا · كلُّ مفتاحِ إصابةٍ من السبعةَ عشرَ · **`book_id` أُسقِطَ** · `internal_offset` أُسقِطَ · و`hadith_no` الذي أغفلَتْه الخدمةُ **بقيَ غائبًا ولم يُملأْ**.
- **بوّابةُ الطلبِ**: غيرُ `POST` ⟹ `405` ولا يبلغُ الخدمةَ · بلا `q` ⟹ `400` · `q` فارغٌ بعدَ التشذيبِ ⟹ `400` · سقفُ `limit` عشرةٌ · الافتراضُ عشرةٌ · و`limit` صحيحٌ (`4.9 ⟹ 4`) · وخدمةٌ لا تُبلَغُ ⟹ `502` ونصُّ العطبِ (`ECONNREFUSED` واسمُ المضيف) **لا يصلُ العميل**.
- **الحارسُ يعضُّ**: طُفِّرَ `lib/lib-source-card.js` ليحملَ `page_start` في فرعِ غيرِ القابلِ للصفحةِ. تأكَّدَ أنّ الملفَّ **تغيّرَ فعلًا على القرصِ** (`MUTATED=true`) ثمّ حُمِّرَ الفحصُ «card carries NO page_start»، ثمّ أُعيدَ الأصلُ فعادَ `73/73`. فليسَ الأخضرُ أخضرَ الفراغ.

---

## ٥ · حصيلةُ البوّابات

`node tools/run-gates.cjs` على **شجرةٍ نظيفةٍ بعدَ الإيداع** (`tree after: 0 dirty path(s)`):

```
=== SUITE: 89/90 EXIT=0 ===
recon:    SUMMARY   PASS=182   WARN=1   FAIL=0
FAILING (1): bankintegrity=1
```

- `recon FAIL=0` ✅ (`PASS=182` · `WARN=1`)
- `89/90` — **شرطُ القبولِ `90/90` غيرُ محقَّقٍ**، والحمراءُ واحدةٌ: `bankintegrity`.

### 🔴 `bankintegrity` — حمراءُ **سابقةٌ لهذه الجولةِ**، وقياسُها

فحوصُها الثلاثةُ الساقطةُ (74 ناجحًا · 3 ساقطة):

```
FAIL [B12] CORE_BYTES = 1781119 but CORE weighs 1781417 bytes on disk (+298).
FAIL [B14] sw.js prose says vendor/react-dom.umd.js is 131835 bytes; the disk says 132102 (+267).
FAIL [B14] sw.js prose says vendor/react.umd.js is 10751 bytes; the disk says 10782 (+31).
```

**البرهانُ أنّها ليسَتْ من عملِ هذه الجولةِ — قياسانِ لا استدلال:**

1. **شُغِّلَ الحارسُ والشجرةُ خاليةٌ من ملفّاتِ الجولةِ الأربعة.** نُقِلَتِ الأربعةُ خارجَ الشجرةِ مؤقّتًا حتّى صارَ `git status --porcelain` صفرًا عندَ `4c9531cb` بلا زيادةٍ ولا نقصان، فخرجَ `quest-bank-integrity-guard.cjs` بـ`EXIT=1` كما هو. والفحوصُ الثلاثةُ في الجريانِ النهائيِّ **مطابقةٌ بايتةً ببايتةٍ** لما ظهرَ قبلَ كتابةِ أيِّ ملفّ.
2. **السببُ مقيسٌ: انحرافُ نهاياتِ السطورِ في هذه الشجرةِ، لا شيفرةٌ.**

| الملفّ | على القرص | في الـblob | الفرق | أسطرٌ فيها `CR` |
|---|---|---|---|---|
| `vendor/react.umd.js` | 10782 | 10751 | **+31** | **31** |
| `vendor/react-dom.umd.js` | 132102 | 131835 | **+267** | **267** |

الفرقُ **يساوي عددَ أسطرِ `CRLF` بالضبطِ** في كلٍّ منهما، و`298 = 267 + 31` أي إنّ فرقَ `CORE_BYTES` كلَّه هما هذانِ الملفّانِ ولا شيءَ سواهما.
و`git config core.autocrlf = true` في هذه الشجرة، و`.gitattributes` يثبِّتُ `eol=lf` لملفّاتِ الذهبِ (`worship-golden.json` وأخواتِها) **ولا يثبِّتُه لـ`vendor/*.umd.js`**. فالبوّابةُ تزنُ القرصَ والختمُ يزنُ الـblob.

**ولم يُمَسَّ شيءٌ من ذلك:** إصلاحُه يقتضي تعديلَ `.gitattributes` أو إعادةَ تسويةِ `vendor/**` أو نصَّ `sw.js` — وثلاثتُها خارجُ نطاقِ §٢ («إضافةُ ملفّاتٍ جديدةٍ فقط»)، و`sw.js` ممنوعٌ نصًّا. فالقرارُ لصاحبِ الأمر.

> وبمقتضى §٧ فإنّ «بوّابةً حمراءَ بعدَ الإيداعِ على شجرةٍ نظيفةٍ» موقفٌ ⟹ **`STOP_REASON=preexisting_red_gate_bankintegrity`**. وقفَ الوكيلُ عندَها: **صفرُ دفعٍ**، وصفرُ محاولةٍ لتخضيرِها بتعديلٍ خارجَ النطاق. عملُ البندِ ١٦-أ نفسِه تامٌّ وأخضرُ بكاملِه.

### أثرُ الجولةِ على بوّابةٍ قائمةٍ — وقعَ وأُصلِح

عندَ أوّلِ جريانٍ حمَّرَتْ `telemetrytext` بسببِ هذه الجولة:

```
FAIL every printed field name has been reviewed onto the allow-list
     [{"file":"api/lib-search.js","line":166,"rule":"B","detail":"unreviewed field `status`"},
      {"file":"api/lib-search.js","line":170,"rule":"B","detail":"unreviewed field `status`"}]
```

البوّابةُ تكنسُ مسارَ التسليمِ وتشترطُ أن يكونَ **اسمُ كلِّ حقلٍ يُطبَعُ** مراجَعًا على قائمةِ سماحِ `guards/telemetry-text-guard.cjs`، و`status` ليسَ عليها. وملفٌّ جديدٌ في `api/` ينضمُّ إلى الكنسِ تلقائيًّا.
**الإصلاحُ كانَ في ملفِّ الجولةِ لا في الحارسِ**: استُبدِلَ `status` بأسماءٍ مراجَعةٍ أصلًا (`outcome` و`reason`) — إذ إنّ توسيعَ قائمةِ السماحِ تعديلٌ لملفٍّ قائمٍ، وهو خارجُ §٢، وهو أيضًا نقضٌ لغرضِ القاعدةِ نفسِها. ⟹ `SUMMARY telemetry-text PASS=21 FAIL=0`.

---

## ٦ · ما لم يُقَسْ، بعلّتِه

1. **النداءُ الحيُّ لم يُجرَّبْ ألبتّة — لأنّ التوكنَ ليسَ عندَ الوكيل.** لا `SEARCH_API_TOKEN` في هذه الشجرةِ ولا في بيئةِ الوكيل. فكلُّ ما في §٤ مقيسٌ على `globalThis.fetch` مستبدَلٍ وفيكستشراتٍ محلّيّةٍ مبنيّةٍ على شكلِ §٣. **لم يُثبَتْ عمليًّا أنّ `Bearer <token>` يُرجِعُ `200` من `lib.ezik.app`** — أُثبِتَ فقط أنّ الدالّةَ ترسلُ ما يشترطُه `server.mjs:165` حرفًا. أوّلُ نداءٍ حيٍّ يقعُ في الإنتاجِ أو في يدِ مَن يملكُ التوكن.
2. **قيمُ الحقولِ الأحدَ عشرَ والسبعةَ عشرَ لم تُرَ من الخدمةِ نفسِها.** الأسماءُ منسوخةٌ حرفًا من §٣ (وهي مقيسةٌ ثمَّ)، لكنّ **أنواعَ** القيمِ في الفيكستشرِ (أعددٌ · نصوصٌ · `heading_path` مصفوفةً) اجتهادٌ من الوكيل. لذلك تعاملُ `renderSourceCard` مع `heading_path` نصًّا **أو** مصفوفةً، ولا تفترضُ أحدَهما.
3. **صفرُ قياسِ زمنٍ** — بأمرِ §٠-٢. أزمنةُ البوّاباتِ المطبوعةُ أعلاهُ نقلٌ حرفيٌّ من مخرَجِ العدّةِ، لا قياسٌ ولا يُستدَلُّ بها.
4. **`/health` و`/ready` و`404` لغيرِ المسارَينِ لم تُلمَسْ.** البندُ ١٦-أ لا يطلبُ إلّا `/search`، فلا دالّةَ لها ولا فحصَ.
5. **البطاقةُ لم تُوصَلْ بواجهةٍ، ولم تُرَ على شاشة.** هذا هو البندُ ١٦-ب بعدَ نزولِ إصلاحِ ن١ — `app.jsx` لم يُفتَحْ ولم تدخلْه بايتةٌ واحدة.
6. **نسبةُ ٧٨٫٢٪ / ٢١٫٨٪ لم يُعَدْ قياسُها**؛ نُقِلَتْ منَ الأمرِ بوصفِها مقيسةً سابقًا، وهي علّةُ التصميمِ لا نتيجةَ قياسٍ في هذا الجريان.
7. **`recon WARN=1` لم يُفتَّشْ عن سببِه** — `FAIL=0` هو الشرطُ المنصوصُ، والتحذيرُ سابقٌ للجولةِ وظهرَ في الجريانِ الأساسِ كذلك.

---

## ٧ · الحصيلة

| المفتاح | القيمة |
|---|---|
| `ORDER_MOVED` | `True` |
| `AUTH_HEADER_NAME` | `authorization` |
| `AUTH_VALUE_SHAPE` | `Bearer <token>` (مسبوقةٌ بكلمة) |
| الحالاتُ السبعُ | `7/7` خضراء ضمنَ `73/73 PASS` |
| `book_id` في المخرَج | صفر |
| ظهورُ التوكنِ في المخرَج | صفر |
| بايتاتٌ في `app.jsx` / `sw.js` / `index.html` | صفر |
| بايتاتُ فهرسٍ أو كتابٍ دخلَتِ الشجرة | صفر |
| رفعةُ مخزنِ شحنة | صفر |
| `git push` | **صفر** |
| عددُ البوّابات | 90 (بلا زيادة) |
| البوّابات | `89/90` · `recon FAIL=0` · حمراءُ واحدةٌ سابقةٌ: `bankintegrity` |
| `STOP_REASON` | `preexisting_red_gate_bankintegrity` (§٧ · لا علاقةَ لها بشيفرةِ الجولة) |
| `FINAL_HEAD` | `837c54f` — إيداعُ الملفّاتِ الأربعةِ، وعليه قِيسَتِ البوّاباتُ على شجرةٍ نظيفة. ويُودَعُ هذا التقريرُ فوقَه، وSHA إيداعِه مطبوعٌ على الطرفيّةِ بعدَ الختم. |

<!-- PAYLOAD-END -->

## ٨ · الختم

```
PAYLOAD_BYTES = 21000
SHA256        = d16c009894cae692ae8c7764afefc6c46b55f9670976b8808b84a8fe335ee854
ALGO          = sha256 over every byte of this file up to and including the line <!-- PAYLOAD-END --> and its newline
```
