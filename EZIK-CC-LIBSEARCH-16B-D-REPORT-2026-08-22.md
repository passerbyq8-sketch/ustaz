# تقريرُ تنفيذٍ — عزك · الشاملةُ · **٢٥-ب · تصحيحُ الأسبقيّة**: الغيابُ لا يُدَّعى ببحثٍ مبتور

**النافذة:** ن١ · وكيلُ Claude Code · `C:\Users\passe\projects\ustaz-cc-ui`
**التاريخ:** 2026-08-22
**الفرع:** `feat/lib-search-16b-20260822`
**استُؤنِفَ من:** `dfbf2a10` — ولم يُعَدْ شيءٌ ممّا تمّ.

> ✅ **الحصيلة: منجَزٌ ومودَعٌ.** الحارسُ `198 → 210`. الحمراءُ الوحيدةُ `bankintegrity` برقمَي `sw.js` وحدَهما. `noemptyanswer` خضراء. **صفرُ دفع.**

---

## ٠ · نقلُ الأمر

`ORDER-LIBSEARCH-16B-D.md` نُقِلَ إلى `C:\EZIK-STAGE\orders\` **قبلَ أيِّ فعلٍ آخر** ⟹ `ORDER_MOVED=True`.

---

## ١ · بوّاباتُ ما قبلَ العمل

| المفتاح | المطلوب | **المقيس** | الحال |
|---|---|---|---|
| `CURRENT_BRANCH` | `feat/lib-search-16b-20260822` | `feat/lib-search-16b-20260822` | ✅ |
| `HEAD` | `dfbf2a10` أو سليلُه | `dfbf2a106473ed86aafdaddeb051487018379466` (هو نفسُه) | ✅ |
| `DIRTY_LINES` | `0` | `0` (بعدَ نقلِ الأمر) | ✅ |
| `GUARD_ASSERTIONS_BEFORE` | `198` مقيسًا | **`198/198 - PASS`** | ✅ |

---

## ٢ · التغييرُ — شرطٌ واحدٌ نُقِلَ، ومرآتُه

`api/lib-search.js` (`+19/-12`) · `app.jsx` (`+13/-10`) · `app.js` مولَّدًا · والحارسُ توسيعًا.
**و`lib/lib-source-card.js` لم يُمَسّ** — ليس في مجموعةِ التغييرِ أصلًا، و`sha256` قبلَ وبعدُ: `44d5810eb8a6ab53ca1c2019ca578638357cdb502b40dff43d5f6e65b541dee1` ⟹ `LIB_MODULE_UNTOUCHED=true`. النصوصُ الثلاثةُ بايتةً بايتة.

### جدولُ الأسبقيّةِ النهائيُّ — مقيسًا صفًّا صفًّا ضدَّ `shapeSearchResponse` الحقيقيّة

| # | الحال | المطلوب | **المُرسَلُ مقيسًا** |
|---:|---|---|---|
| ١ | `refused === true` | `refused_text` وحدَه | `refused_text` ✅ |
| ١-حدّ | `refused` **مع** إصابات | (مُقَرٌّ في §الحكم) `refused_text` | `refused_text` ✅ |
| ٢ | `degraded_reason` **بإصابات** | `degraded_text` وحدَه | `degraded_text` ✅ |
| ٢-حدّ | `degraded_reason` **بصفرِ إصابات** | `degraded_text` وحدَه | `degraded_text` ✅ |
| ٣ | صفرُ إصاباتٍ · غيرُ مرفوضٍ · **وغيرُ مبتور** | `empty_text` وحدَه | `empty_text` ✅ |
| ٤ | إصاباتٌ · غيرُ مرفوضٍ · غيرُ مبتور | لا شيءَ من الثلاثة | `(none)` ✅ |

**الصفُّ ٢-حدّ هو الذي انقلب.** كانَ في الجولةِ السابقةِ `empty_text` على القراءةِ الحرفيّةِ لجدولٍ صُحِّحَ بعدَها، وصارَ `degraded_text`.

**والعلّةُ مكتوبةٌ في الكودِ لا في هذا التقريرِ وحدَه:** «لا نتيجةَ لهذا البحث» **دعوى غياب**، وبحثٌ بُتِرَ عندَ سقفِ الميزانيّةِ لم يقرأْ من الفهرسِ ما يكفي لإثباتِها — توقّفَ مبكِّرًا وأخبرَ بما عندَه، وهو **لا شيءَ بعدُ**. فادّعاءُ الغيابِ هناكَ نفيٌ بلا دليلٍ يُعرَضُ على سائلٍ في دينِه. **و`empty_text` لا يُرسَلُ إلّا إن اكتملَ البحث.**

**وشرطُ الجملةِ الواحدةِ باقٍ:** لا يحملُ جسمٌ أكثرَ من نصٍّ واحد — مقيسٌ على عشرةِ أجسامٍ في هذه الجولة.

### مرآةُ العارض

فرعُ `degraded` نُقِلَ **فوقَ** فرعِ صفرِ الإصاباتِ في `ezLibViewState` بالترتيبِ نفسِه حرفًا. وليس ذلك تجميلًا: الخادمُ يرسلُ **واحدةً** بقاعدةٍ مرتَّبة، فعارضٌ يصنِّفُ الجسمَ تصنيفًا مخالفًا يطلبُ حقلًا لم يُرسَلْ **فلا يرسمُ شيئًا** — وهو الفراغُ الصامتُ عينُه الذي فُتِحَ له هذا البندُ كلُّه.

---

## ٣ · مصفوفةُ الاتّفاق — أربعةُ صفوفٍ وحدَّاهما

على كلِّ شكلٍ سؤالانِ: هل يطلبُ العارضُ **الحقلَ الذي أرسلَه الخادمُ فعلًا**، وهل يرسمُه؟

| الشكل | الخادمُ أرسلَ | العارضُ طلبَ | يرسمُه | الحال |
|---|---|---|---|---|
| صفّ ١ — مرفوض | `refused_text` | `refused_text` | ✅ | ✅ |
| صفّ ١ حدّ — مرفوضٌ مع إصابات | `refused_text` | `refused_text` | ✅ | ✅ |
| صفّ ٢ — مبتورٌ بإصابات | `degraded_text` | `degraded_text` | ✅ | ✅ |
| **صفّ ٢ حدّ — مبتورٌ بصفرِ إصابات** | **`degraded_text`** | **`degraded_text`** | ✅ | ✅ |
| صفّ ٣ — فارغ | `empty_text` | `empty_text` | ✅ | ✅ |
| صفّ ٤ — إصاباتٌ فقط | `(none)` | `(none)` | يرسمُ فراغًا | ✅ |

---

## ٤ · الحارسُ — `198 → 210`

| | قبل | **بعد** |
|---|---|---|
| عددُ التأكيدات | `198` | **`210`** |
| الحصيلة | `198/198 - PASS` | **`210/210 - PASS`** |

**ولم يُخفَّفْ تأكيدٌ واحد ولم يُضيَّقْ حارس.** توكيدٌ واحدٌ أُعيدَ توجيهُه — «مبتورٌ بصفرِ إصاباتٍ يقولُ لا نتيجة» ⟹ **«يقولُ مبتورٌ، ولا يقولُ لا نتيجةَ أبدًا»** — لأنّ الحقيقةَ التي كانَ يصفُها نُقِضَتْ بالحكم، لا لأنّه أزعج.

**الجديدُ الملزِمُ في §٣ من الأمر، وقد قِيسَ:**

| # | التوكيدُ الملزِم | الحال |
|---|---|---|
| ١ | `degraded` بصفرِ إصاباتٍ ⟹ `degraded_text` حاضرٌ و`empty_text` غائبٌ | ✅ (وتوكيدٌ ثالثٌ يُثبِتُ أنّ الجسمَ حقًّا بصفرِ إصاباتٍ، فالحالُ حقيقيّةٌ لا مُدَّعاة) |
| ٢ | `degraded` بإصاباتٍ ⟹ كما هو | ✅ |
| ٣ | **`empty_text` لا يُرسَلُ ألبتّة مع `degraded_reason` حاضر** — تأكيدٌ مستقلٌّ مسمًّى | ✅ |
| ٤ | مصفوفةُ الاتّفاقِ مُوسَّعةٌ إلى الصفوفِ الأربعة | ✅ |

والتوكيدُ الثالثُ لم يُقَسْ على شكلٍ واحدٍ بل على **أربعةِ أشكالٍ** من جسمٍ مبتور: بصفرِ إصابات · بإصابةٍ · بإصابتَين · **وبلا مفتاحِ `hits` أصلًا** — لأنّ جوابًا يُغفِلُ المفتاحَ يُعامَلُ كصفرِ إصاباتٍ في الكود، وهو المدخلُ الذي كانَ سيُفلِتُ من فحصٍ يقيسُ الشكلَ الواحد.

### طفرةُ العضّ — المطلوبةُ في §٣، على القرصِ ثمّ رُدَّتْ

| الخطوة | العدد | الخروج |
|---|---|---|
| قبلَ الطفرة | **`210/210 - PASS`** | `0` |
| أُعيدَ فرعُ صفرِ الإصاباتِ **فوقَ** فرعِ `degraded` في الخادم | **`202/210 - FAIL`** — سقطَ ثمانيةُ توكيدات، منها صفُّ المصفوفةِ «مبتورٌ بصفرِ إصابات» | `1` |
| رُدَّ الأصل | `RESTORED_IDENTICAL=true` ⟹ **`210/210 - PASS`** | `0` |

وفي الحارسِ طفراتٌ داخليّةٌ دائمةٌ من الجولاتِ السابقةِ (بطاقةٌ مطفَّرةٌ · خادمٌ يُسرِّبُ الصفحةَ · خادمٌ يُرسِلُ `empty_text` مع إصاباتٍ · عرضٌ يقرأُ حقلَ صفحةٍ)، وكلٌّ منها يتحقّقُ أوّلًا من أنّ الطفرةَ **ليست عاطلة**.

---

## ٥ · شرطُ القبول

| المفتاح | **المقيس** |
|---|---|
| الصفوفُ الأربعةُ ضدَّ الدالّةِ الحقيقيّة | ✅ (§٢) · والطفرةُ عضَّتْ |
| `CARD_TEXT_IN_APPJSX` | **0** |
| `DYNAMIC_IMPORT_SITES_AFTER` | **0** |
| نصوصُ `lib/lib-source-card.js` | **لم تتغيّرْ بايتة** — `sha256` واحدٌ قبلَ وبعد |
| `build-app.cjs --check` | `960680` بايتة · `da153a702832c55527b1cd83d061a493c7ca7442dae1cd61904769bc9c7300a9` — مطابق |
| `TDZ_COUNT` | **0** · `SCOPES=1355` · `MODE=script` · **بمسارٍ صريحٍ مطبوع:** `C:/Users/passe/projects/ustaz-cc-ui/app.jsx` (`SRC_CHARS=1020527`) |
| الإيداعُ أوّلًا | نعم، ثمّ السلسلةُ على شجرةٍ نظيفة (`0 dirty path(s)`) |
| الدفع | **صفر** |

### الأرضيّاتُ — قبلَ وبعد

| | قبل (`dfbf2a10`) | **بعد** (`4242a0c4`) |
|---|---|---|
| `run-gates` | 89/90 | **89/90** |
| `recon` | `PASS=182 WARN=1 FAIL=0` | `PASS=182 WARN=1 FAIL=0` |
| `babel` | `EXIT=0` | ✅ `EXIT=0` |
| `themecoverage` | `EXIT=0` | ✅ `EXIT=0` |
| `wird` | `EXIT=0` | ✅ `EXIT=0` |
| `i18nui` | `EXIT=0` | ✅ `EXIT=0` |
| **`noemptyanswer`** | `EXIT=0` | ✅ **`EXIT=0`** |
| حارسُ ١٦-أ | `198/198` | ✅ **`210/210`** |

**لم تنخفضْ أرضيّةٌ ولم يسقطْ فحصٌ كان أخضر.**

### الحمراءُ الوحيدةُ — `bankintegrity`، نصُّها حرفًا

```
  FAIL [B12] CORE_BYTES = 1781136 but CORE weighs 1794078 bytes on disk (+12942).
        The quota pre-check is measured against a number that stopped being true.
        Repair it with `node tools/core-bytes.cjs --write` and re-cut the sw.js
        digest in the SAME commit -- never by hand from the comment table.
  FAIL [B14] sw.js prose says app.js is 947738 bytes; the disk says 960680 (+12942).
        A comment that states a size is what the next person re-cuts CORE_BYTES from,
        so it is wrong in exactly the way the constant used to be.
FAIL  74 checks passed, 2 failed.
```

رقمانِ في `sw.js` وحدَهما، وكلاهما `+12942` وهو نموُّ `app.js` بعينِه (`947738 → 960680`). **صفرُ بايتةٍ في `sw.js`.**

---

## ٦ · ما لم يُقَسْ، بعلّتِه

1. **لم يُجرَّبْ نداءٌ حيٌّ — التوكنُ في الإنتاجِ وحدَه.** كلُّ جسمٍ هنا أنتجَتْه الدالّةُ الحقيقيّةُ من فيكستشراتٍ محلّيّة.
2. **لم يُقَسْ متى تُرسِلُ خدمةُ المكتبةِ `degraded_reason` فعلًا ولا كم يقعُ ذلك.** الفيكستشرُ يحملُ `degraded_reason: 'over_budget'`، وهو المذكورُ في تعليقِ الوحدةِ؛ أمّا قيمةٌ ثانيةٌ لهذا الحقلِ فلم تُشاهَدْ، والكودُ يعاملُ **أيَّ** قيمةٍ صادقةٍ معاملةَ البتْر — وهو الاتّجاهُ الآمنُ هنا: يُؤخَّرُ ادّعاءُ الغيابِ لا يُقدَّم.
3. **لم يُقَسْ أثرُ الترتيبِ الجديدِ على قارئٍ.** بحثٌ مبتورٌ بصفرِ إصاباتٍ يقرأُ الآن «النتائجُ مبتورةٌ عندَ سقفِ البحث» ولا يرى نتيجةً — وهو صادقٌ، لكنّه قد يبدو للقارئِ جوابًا ناقصًا. **وهذا مقصودٌ بالحكم:** نقصٌ معلَنٌ خيرٌ من نفيٍ بلا دليل.
4. **حالُ `refused` مع إصاباتٍ لم تُشاهَدْ من الخدمة.** مقرَّرةٌ بالحكمِ ومقيسةٌ ومثبَّتةٌ بتوكيد، لكنّ الخدمةَ لم تُرَ ترسلُ إصاباتٍ مع رفض.
5. **لم تُقَسْ فروقُ الخطِّ ولا اتّجاهُ النصِّ على شاشةٍ حقيقيّة.** `linkedom` بلا `getComputedStyle`، ولم تُلتقطْ صورة.
6. **لم يُقَسْ زمنٌ ألبتّة.**

---

## ٧ · الرأسُ الأخير

| المفتاح | القيمة |
|---|---|
| الرأسُ المُستأنَفُ منه | `dfbf2a106473ed86aafdaddeb051487018379466` |
| إيداعُ التصحيح | **`4242a0c491c9a3682bacfce3dc491e1e149d9fb7`** |
| `FINAL_HEAD` | **`4242a0c491c9a3682bacfce3dc491e1e149d9fb7`** — وعليه قِيسَتِ السلسلةُ على شجرةٍ نظيفة |
| الدفع | **صفر** |

ويُودَعُ هذا التقريرُ فوقَه، وSHA إيداعِه مطبوعٌ على الطرفيّةِ بعدَ الختم.

---

## ٨ · في الختمِ وسطورِه

🔴 **الختمُ محسوبٌ على بايتاتِ LF**، و`core.autocrlf` **مفعَّلٌ** في هذه الشجرة، فسحبٌ جديدٌ على وِندوز يكتبُ هذا الملفَّ بـCRLF ولن يُعيدَ الرقم. **فيُعادُ إنتاجُه من كائنِ git:**

```
git show <commit>:EZIK-CC-LIBSEARCH-16B-D-REPORT-2026-08-22.md
```

وقد أُعيدَ إنتاجُه في هذا الجريانِ **من القرصِ ومن كائنِ git كلَيهما**، وأعطيا الرقمَ نفسَه.

<!-- PAYLOAD-END -->

```
PAYLOAD_BYTES = 13194
SHA256        = 439ff0293280436d21978f97f1dff167372245a5346cf6dd7932ebf20689d290
ALGO          = sha256 over every byte of this file up to and including the line <!-- PAYLOAD-END --> and its newline
ENCODING      = LF. core.autocrlf is on in this tree: reproduce from the git object, not the working copy.
```
