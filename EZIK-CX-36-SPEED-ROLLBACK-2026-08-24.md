# EZIK — قياس البند 36: رايات رجعة السرعة

**تاريخ القياس:** 24 أغسطس 2026  
**طبيعة الجولة:** قراءة فقط؛ لا تعديل لملف متعقّب، ولا إيداع، ولا تشغيل طقم أو بوابة.

## 0 · الشجرة التي قِيست

نُفّذت أوامر الهوية والحالة المطلوبة قبل أي إنشاء للتقرير، وكانت النتيجة:

| الحقل | القيمة المقيسة |
|---|---|
| الفرع | `feat/wird-khatmah-20260824` |
| الرأس المختصر | `4e26fff` |
| ملف التقرير قبل الجولة | غير موجود |

كانت `git status --porcelain` الأولية كما يلي حرفيًا:

```text
?? EZIK-CC-42-CARD-PDF-REPORT-2026-08-24.md
?? EZIK-CC-42B-PDF-PATH-REPORT-2026-08-24.md
?? EZIK-CC-42C-CARD-ATTRIB-REPORT-2026-08-24.md
?? EZIK-CC-42D-HADITH-LABEL-REPORT-2026-08-24.md
?? EZIK-CC-MERGE-26-REPORT-2026-08-24.md
?? EZIK-CC-MERGE-27-REPORT-2026-08-24.md
?? EZIK-CC-WIRD-KHATMAH-REPORT-2026-08-24.md
?? ORDER-CX-36-MEASURE.md
?? ORDER-WIRD-KHATMAH.md
```

لم تُظهر الحالة تعديلًا متعقّبًا. طبع Git أيضًا تحذير منع وصول إلى
`C:\Users\passe\.config\git\ignore`؛ لم أغيّر إعدادًا لتجاوزه. كل ما يلي منسوب إلى الرأس
والفرع أعلاه، لا إلى `main`.

## 1 · حدّ الجرد والنتيجة التاريخية

دلّ سجل Git الموجود على القرص، وبالتحديد وصف الكائن `35de8a5`، مع تعليق المصدر الحالي
في `app.jsx:5521-5527`، على أن عبارة «ثلاث رايات رجعة حيّة» كانت تسمّي عائلة واحدة
من مفاتيح المتصفح المحلية ذات الهيئة نفسها:

- `adhkarui` / `adhkar_ui_v2` / `ADHKAR_UI_V2_ON`؛
- `mushafsvg` / `mushaf_svg_v1` / `MUSHAF_SVG_ON`؛
- `madinaimg` / `madina_img_v1` / `MADINA_IMG_ON`.

القياس الحالي يكذّب العبارة التاريخية: رايتا المصحف ما زالتا قابلتين للقلب من الجهاز،
أما قارئ راية الأذكار فلا يستدعيه شيء، والقيمة المستهلكة مثبتة في الكود على `true`.
إذن الموجود اليوم **رايتا رجعة حيّتان، ومدخل رجعة متقاعد، وثابت حيّ الاستهلاك**؛ وليس
ثلاث رايات قابلة للقلب.

مفاتيح التشغيل الخادمية والعائلية الأخرى، ومنها `FREE_BRAIN_V1` و`STREAM_V1` ومفاتيح
ledger وanchor، ليست إحالات السطر التاريخي ولا من هيئة URL + مفتاح جهاز المقيسة هنا؛
لم تُصدر لها هذه الجولة أحكام نزع.

## 2 · جرد الرايات

| الراية | موضع التعريف والاستهلاك | الافتراضي ومصدر القراءة | زمن القراءة | أثر القلب | مستهلك حي؟ |
|---|---|---|---|---|---|
| الأذكار: `adhkarui`، المفتاح `adhkar_ui_v2`، والثابت `ADHKAR_UI_V2_ON` | المفتاح والقارئ `app.jsx:5498-5508`؛ القيمة الفعلية `app.jsx:5528`؛ المصرف `app.jsx:6597-6599` | `true` مثبتة في الكود. لا قراءة بيئة. قارئ URL/`localStorage` باقٍ نصًا لكنه غير مستدعى | الثابت يُنشأ عند تحميل الوحدة. URL والمخزون لا يُقرآن الآن | لا قلب تشغيليًا. قلب الثابت في مصدر جديد كان سيختار `AdhkarScreenV1` بدل `AdhkarScreenV2`، أي يحتاج بناءً ونشرة | نعم للثابت في مبدّل الشاشتين؛ لا لقارئ الرجعة الخارجي |
| SVG: `mushafsvg`، المفتاح `mushaf_svg_v1`، والثابت `MUSHAF_SVG_ON` | المفتاح `app.jsx:14908`؛ القارئ والثابت `app.jsx:15019-15026`؛ المصارف `app.jsx:15300,15317,15396,16174,16546,16567,16573` | افتراضي `true` من الكود؛ وحده المخزون المطابق لـ`'0'` يعيد `false`. URL يحاول الكتابة إلى `localStorage` ثم يعيد القراءة. لا قراءة بيئة | مرة عند تحميل الوحدة، لا لكل طلب؛ التغيير يظهر مع تحميل الصفحة | إطفاؤها يمنع مسار SVG ويجعل الرجعة بعد WebP نصًا؛ ومع إطفاء صور المدينة أيضًا يصبح النص هو المسار الأول | نعم |
| صور المدينة: `madinaimg`، المفتاح `madina_img_v1`، والثابت `MADINA_IMG_ON` | المفتاح `app.jsx:15054`؛ القارئ والثابت `app.jsx:15179-15189`؛ مصارف مباشرة في `app.jsx:15191,15268,15300,16174,16391,16496,16546,16565,16571,16582,16587,16593,16664,16703,16725,16747` | افتراضي `true` من الكود؛ URL الصريح `'1'` أو `'0'` يجيب مباشرة ويحاول الحفظ؛ دون URL لا يعيد `false` إلا مخزون `'0'`. لا قراءة بيئة | مرة عند تحميل الوحدة، لا لكل طلب؛ التغيير يظهر مع تحميل الصفحة | إطفاؤها يمنع WebP ويختار SVG إن بقيت رايته مشغلة، وإلا النص؛ كما يعيد الكروم القديم داخل التدفق ويغلق الشريط والمنتقي الموصولين بهذه الراية | نعم |

فارق مهم بين القارئين الحيّين: `madinaimg` يعيد جواب URL الصريح حتى إذا تعذّر التخزين؛
أما `mushafsvg` فيكتب داخل `try` ثم يعيد قراءة المخزون في `try` أخرى. لذلك إذا كان التخزين
غير صالح فإن `mushafsvg=0` لا يضمن الإطفاء في الزيارة نفسها، بل يرجع القارئ إلى `true`.

مصفوفة القرار المقيسة في `MushafSheet` (`app.jsx:15387-15422`) هي:

| `MADINA_IMG_ON` | `MUSHAF_SVG_ON` | المسار المرئي وترتيب الرجعة |
|---|---|---|
| on | on | WebP، ثم SVG عند خطأ الصورة، ثم النص عند خطأ SVG |
| on | off | WebP، ثم النص عند خطأ الصورة |
| off | on | SVG، ثم النص عند خطأ SVG |
| off | off | النص مباشرة |

وعليه فإن إطفاء `mushafsvg` وحدها في الوضع الافتراضي لا يبدّل الصورة الناجحة؛ إنه يبدّل
مسار الفشل. لكنه يظل رجعة نافعة إذا كانت الصورة تُحمّل بنجاح ثم تُعرض على نحو غير صالح،
وهي حالة لا يلتقطها `onError` تلقائيًا.

## 3 · هل المسارات القديمة تدخل الشحنة؟

نعم. القياس على القرص أعطى:

| الملف | الحجم الخام | الدليل على الشحن |
|---|---:|---|
| `app.jsx` | 1,175,679 بايت | مصدر بناء؛ مستبعد صراحة في `.vercelignore:47-50` |
| `app.js` | 1,057,624 بايت | محمّل في `index.html:1697` ومسبق التخزين في `sw.js:83` |
| `index.html` | 122,568 بايت | غلاف التطبيق المشحون، وفيه CSS تابع للمسار القديم |

البناء في `tools/build-app.cjs:63-79` و`tools/babel-block.cjs:206-224` يستعمل تحويل JSX
بـpreset React؛ لا توجد في المسار المقيس مرحلة minify أو tree-shaking أو إزالة شيفرة ميتة.
كما أن المقاطع القديمة نفسها موجودة في `app.js`. لذلك لم أفترض إسقاطها: البايتات الخام
المبيّنة أدناه موجودة فعلًا في الشحنة الحالية.

طريقة الوزن: عدد الأسطر شامل لطرفي كل مدى في المصدر، والبايتات هي UTF-8 للمقاطع نفسها
كما على القرص. «المشحون» هو مجموع المقاطع المناظرة في `app.js`، ومع `index.html` حيث ذُكر؛
وليس تقدير gzip أو Brotli.

### 3.1 · مسار الأذكار القديم

| الجزء | المصدر | أسطر المصدر | بايتات المصدر | بايتات المناظر المشحون |
|---|---|---:|---:|---:|
| `AdhkarScreenV1` | `app.jsx:5935-5999` | 65 | 2,873 | 2,765 |
| مفاتيح النمط الخاصة به | `app.jsx:17802-17811` | 10 | 1,540 | 1,360 |
| **المسار القديم مباشرة** | — | **75** | **4,413** | **4,125** |
| جهاز الراية والمبدّل | `app.jsx:5498-5508,5528,6597-6599` | 15 | 715 | 637 |

الجرد الخام المباشر للمسار القديم مع جهاز الراية في الشحنة هو **4,762 بايت**. هذا المسار
لم يعد قابلًا للوصول من URL أو المخزون؛ وجوده الآن ثمن رجعة مصدرية فقط، لا رجعة تشغيلية.

### 3.2 · مسار النص الذي تحرسه راية SVG

| الجزء | المصدر | أسطر المصدر | بايتات المصدر | بايتات المناظر في `app.js` |
|---|---|---:|---:|---:|
| المحرك والثوابت والمساعدات | `app.jsx:14762-14887` | 126 | 7,544 | 6,756 |
| `MushafPage` و`PageDecor` | `app.jsx:15425-15585` | 161 | 10,732 | 9,732 |
| مفاتيح النمط الخاصة | `app.jsx:18292,18294,18332,18335-18339` | 8 | 1,492 | 1,326 |
| **المسار النصي المباشر** | — | **295** | **19,768** | **17,814** |

جهاز `mushafsvg` نفسه في `app.jsx:14908,15019-15026` يزن 9 أسطر و433 بايت مصدر،
ومناظره المباشر 328 بايت في `app.js`. لكن **17,814 بايت ليست مكسب نزع الراية**: النص
هو أيضًا رجعة تلقائية لأخطاء SVG، ولذلك يبقى مطلوبًا حتى لو لم يبق خيار الجهاز اليدوي.

### 3.3 · المسار الواقع خلف إطفاء صور المدينة

| الجزء | المصدر | أسطر المصدر | بايتات المصدر | بايتات مشحونة مباشرة |
|---|---|---:|---:|---:|
| مسار SVG ومساعداته وفرعه وأنماطه | `app.jsx:14907-14987,15009,15343-15360,15398-15422,18311,18315,18317,18321,18331` | 130 | 8,141 | 7,985 |
| الكروم القديم داخل التدفق | `app.jsx:16674-16685,16711-16718,18359-18364` و`index.html:860-869` | 36 | 3,864 | 3,866 |
| المسار النصي المبين أعلاه | — | 295 | 19,768 | 17,814 |
| **الجرد الإجمالي للمكدس القديم** | — | **461** | **31,773** | **29,665** |

جهاز `madinaimg` في `app.jsx:15054,15179-15189` يزن 12 سطرًا و588 بايت مصدر،
ومناظره 461 بايت في `app.js`. لا يصح عد **29,665 بايت** كلها مكسبًا محتملًا: SVG والنص
هما سلسلتا نجاة تلقائيتان عند فشل WebP. الجزء المنسوب مباشرة إلى اختيار الجهاز مع الكروم
القديم هو جرد خام مقداره **4,327 بايت** (3,866 + 461)، وليس فرق بناء مثبتًا.

## 4 · جرد الحرّاس

بحث الاسم البرمجي، واسم URL، ومفتاح الجهاز في ملفات الحرّاس أعطى الجرد الآتي. لم تُشغّل
أي بوابة؛ هذه قراءة لتأكيداتها على القرص.

### `adhkarui`

لا حارس يذكر `adhkarui` أو `adhkar_ui_v2` أو `ADHKAR_UI_V2_ON`. الثابت له مصرف في
التطبيق، لكنه غير مثبت بحارس اسمي.

### `mushafsvg`

- `tools/madina-hafs-guard.cjs:541-550` يتطلب وجود الاسم تحت تأكيد
  `index.html carries the mushafsvg flag, untouched`.
- `tools/madina-hafs-guard.cjs:682-694` يثبت ترتيب WebP ثم SVG ثم النص ومرصدي الخطأ،
  و`700-703` يثبت نصًا:
  `mushafsvg is preserved exactly: same parameter, same key, still default-on and off only for 0`.
- `tools/madina-hafs-guard.cjs:717-719` يثبت أن prefetch يتبع `MUSHAF_SVG_ON`.
- `theme-coverage-guard.cjs:2393-2395` يثبت شرط الانطواء المشترك، و`2501-2503` يثبت
  `the WebP -> SVG -> text fallback chain is intact` مع فرع `MUSHAF_SVG_ON`.
- `tools/wird-guard.cjs:621` يثبت `first-turn collapse untouched` ويذكر الرايتين في الشرط.

### `madinaimg`

- `tools/madina-hafs-guard.cjs:541-550` يتطلب الاسم تحت تأكيد
  `index.html carries the madinaimg flag name`.
- `tools/madina-hafs-guard.cjs:555-615` يرفع القارئ ويقوده في 12 حالة عادية، ثم 3 حالات
  تخزين رامٍ، ثم حالة نافذة بلا location؛ أي 16 مدخلًا منفذًا في نص الحارس. ويثبت في
  `:614` النص `MADINA_IMG_ON is that switch, read once at startup`.
- `tools/madina-hafs-guard.cjs:674-679` يثبت هندسة التراكب والحاوية، و`682-694` ترتيب
  الرجعة ومرصدي الخطأ، و`717-719` prefetch الخاص بالمصيّر الظاهر.
- `theme-coverage-guard.cjs:2390-2395` يثبت بوابتي التراكب وشرط الانطواء؛
  `2433-2435` يثبت بوابة الشريط؛ `2479-2482` يثبت المكتب والفتحة؛ و`2501-2503`
  يثبت سلسلة الرجعة.
- `tools/wird-guard.cjs:511,519` يثبت هندسة الشريط والحاوية؛ `549-550` يثبت حرفيًا
  `strip is gated by MADINA_IMG_ON, and it is the FIRST term`؛ `581` يثبت بوابة المنتقي؛
  `583-585` يثبت القارئ والمعامل والثابت؛ و`621` يثبت شرط الانطواء المشترك.

لهذا يحتاج نزع أي من رايتي المصحف الحيّتين إذن المالك الصريح: حرّاسهما يثبتون وجودهما
وسلوكهما، لا مجرد نتيجة عرض عابرة. لا يوجد القيد نفسه على راية الأذكار.

## 5 · صدق النثر وتعارضاته

| الموضع | الحكم المقيس |
|---|---|
| `app.jsx:5509-5519` | صادق: يعلن أن رجعة الأذكار أغلقت وأن القارئ والمسار القديم تُركا مؤقتًا |
| `app.jsx:5521-5527` | جوهر «الرايتين الأخريين ما زالتا حيّتين» صادق؛ لكن قوله إن حارس المدينة يقود عشر حالات قديم: نص الحارس الحالي يقود 16 مدخلًا |
| `app.jsx:6595-6598` | غير صادق: يزعم أن URL الأذكار يغيّر جواب المبدّل، بينما الثابت عند `5528` يمنعه |
| `app.jsx:17812-17813` | غير صادق في نسبة إمكان إظهار المسار القديم إلى URL نفسه |
| `index.html:225-229` | غير صادق في افتراض أن URL الأذكار ما زال يوصل إلى الشاشة القديمة؛ قواعد CSS نفسها لا تثبت قابلية الوصول |
| `app.jsx:15011-15018` | الافتراضي وقراءة التحميل موصوفان بدقة، لكن أثر URL مبالغ فيه عند تعذر التخزين لأن القارئ لا يعيد جواب URL مباشرة |
| `app.jsx:15327-15333` | غير صادق في قول إن إطفاء SVG يعني عدم إنشاء صورة: مع صور المدينة مشغلة تُنشأ WebP أولًا، والإطفاء يغيّر رجعتها فقط |
| `app.jsx:15032-15042,15173-15189` | صادق لراية صور المدينة: URL الصريح يسبق المخزون ويجيب حتى عند فشل الكتابة |
| `app.jsx:16720-16747` | صادق: إطفاء صور المدينة يزيل أيضًا الشريط والمنتقي المربوطين بها |

أما سطر الأمر «ثلاث رايات رجعة حيّة» فقد كذبه المصدر الحالي كما سبق: قابلية القلب الحية
موجودة في رايتين فقط. القياس هو المعتمد.

## 6 · الحكم وترتيب النزع ومكسب المستخدم

| الترتيب من الأسهل إلى الأخطر | الراية | الحكم | التعليل | المكسب المقيس لو أُنجز الحكم لاحقًا |
|---:|---|---|---|---|
| 1 | الأذكار | **تُنزع** | V2 مثبتة ومستهلكة، أما URL والمخزون فميتان تشغيليًا؛ المسار الجديد هو الواقع الحالي ولا حارس اسمي يثبت الرجعة | التغير المرئي الإضافي للمستخدم **صفر** لأن الثابت سبق أن أغلق الرجعة. الجرد الخام المباشر القابل للمراجعة 4,762 بايت مشحون، لكن فرق الحزمة النهائي والضغط والزمن غير مقيسة. المكسب المؤكد بنيويًا: زوال مبدّل ومسار قديم ونثر متناقض |
| 2 | SVG | **تبقى** | رجعة حية ومستمرة على الجهاز، وتختار النص مع إطفاء صور المدينة أو عند فشل WebP؛ كما تفيد في خلل عرض لا يطلق `onError`، وتحرسها تأكيدات اسمية | مكسب العرض والزمن للمسار الافتراضي من نزعها **صفر مقيس**. جهازها الخام 328 بايت فقط؛ فرق البناء النهائي غير مقيس. النزع يسلب المستخدم اختيار النص اليدوي |
| 3 | صور المدينة | **تبقى** | رجعة حية ومستدعاة في المصيّر والهندسة وواجهات مرتبطة، ولها أوسع سطح حراسة؛ تقلب WebP إلى SVG/نص وتبقى فعالة مع تخزين معطل | مكسب العرض والزمن للمسار الافتراضي من نزعها **صفر مقيس**. الجرد الخام المباشر للراية والكروم القديم 4,327 بايت؛ الفرق النهائي غير مقيس. أجهزة الرفض المخزّن ستتغير تجربتها وتفقد الرجعة |

هذا ترتيب قرار، لا اقتراح رقعة. الحكم التنفيذي الوحيد الذي تدعمه الجولة بلا تغيير عقد مالك
هو نزع بقايا راية الأذكار في جولة مستقلة؛ الرايتان الأخريان تبقيان. ولم يُنفذ أي نزع هنا.

## 7 · ما لم يُقَس

- لم تُشغّل اختبارات أو طقم أو بوابة أو بناء، التزامًا بالأمر؛ لذلك لا توجد نتيجة نجاح
  ديناميكية لهذه الجولة.
- لم يُقَس فرق حزمة ناتج عن رقعة فعلية، ولا gzip أو Brotli، ولا زمن parse/compile/execute،
  ولا مللي ثانية شبكة أو رسم. أرقام البايت أعلاه جرد خام موجود، لا وعد بتوفير نهائي.
- لم تُقَس نسبة الأجهزة التي تحمل `'0'`، ولا استعمال معامِلات URL في الإنتاج، ولا معدلات
  فشل أو سوء عرض WebP وSVG.
- لم تُقَس أصول بعيدة أثناء التشغيل أو حالات متصفح حقيقية؛ تحليل القلب مأخوذ من فروع
  المصدر المشحون فقط.
- لم يُصدر هذا التقرير حكمًا على مفاتيح التشغيل الخادمية خارج عائلة الرايات التاريخية
  المحددة في القسم 1.
- لم يُقَس `main`؛ القياس كله للرأس `4e26fff` على الفرع المسجل في القسم 0.

## 8 · نقطة التوقف

هذا هو مخرج الجولة الوحيد. لا تعديل شيفرة، ولا إيداع، ولا بوابة. تعريف payload في الختم:
كل بايت UTF-8 من أول هذا الملف حتى نهاية هذا السطر، بما في ذلك محرف LF الذي يليه، وقبل
الفاصل النهائي وحقلي الختم.

---
PAYLOAD_BYTES=20025
sha256=e3d60ab114301e9d842741f9a9c48a845c346566ed038161a145444b3cbbcf73
