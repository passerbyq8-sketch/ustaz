# EZIK — تقريرُ جولةِ حذفِ الحسابِ من داخلِ التطبيق · ٢٦ أغسطس ٢٠٢٦

**نُفِّذَ كاملًا.** المنفذُ والمحوُّ الثلاثيُّ والزرُّ ذو الخطوتَينِ وتأكيداتُ البوّابة — كلُّها في الشجرة. الطقمُ **`99/99 EXIT=0`** و`recon` **بلا `FAIL`** على شجرةٍ نظيفةٍ بعدَ الإيداع.

> وثلاثةُ أمورٍ تخصُّ المالكَ وحدَه مذكورةٌ في **§٨** — لم يُصلَحْ منها شيءٌ من عندي، لأنّها ليست لي.

---

## ١ · الأساسُ المقيسُ — عندَ البدءِ وعندَ الختم

| | عندَ البدء | عندَ الختم |
|---|---|---|
| الرأس | **`598fef0`** | **`10b2946`** |
| الفرع | `main` ⟹ قُطِعَ منه `feat/account-delete-20260826` | `feat/account-delete-20260826` |
| المساراتُ المتّسخة | **٠** | **٠** |
| الطقم | `=== SUITE: 99/99 EXIT=0 ===` | `=== SUITE: 99/99 EXIT=0 ===` |
| `recon` | `SUMMARY PASS=190 WARN=1 FAIL=0` | `SUMMARY PASS=190 WARN=1 FAIL=0` |

**الأساسُ طابقَ ما في §٠ حرفًا بحرف** — فلم يكنْ ثمّةَ ما يُوقَفُ عندَه.

وادّعاءُ §١ قِيسَ قبلَ أن يُكتَبَ سطر، في `api/**` و`lib/**` و`index.html`:

| اللفظ | قبل | بعد |
|---|---|---|
| `delete-account` | **٠** | ١ (اسمُ المسارِ في المنفذِ الجديد) |
| `deleteAccount` | **٠** | ٤ (المساعدُ ونداؤه) |
| `signOut` | **٠** | **٠** — لم يُمَسَّ الخروجُ ألبتّة |

---

## ٢ · كلُّ ملفٍّ مُسَّ — بسطرِه وسببِه

### ٢-أ · داخلَ قائمةِ الملكيّة (§٢)

| # | الملفّ | السطر | ما جرى ولماذا |
|---|---|---|---|
| ١ | `api/auth-delete.js` | **جديد**، ١١٠ سطرًا | §٢-١. `POST /api/auth-delete` — عائلةُ `auth-` نفسُها. الهويّةُ من الجلسةِ وحدَها. |
| ٢ | `lib/auth/account.js` | **٢٣١** `deleteAccount()` · **١٥–١٩** رأسُ الملفّ | §٢-٢: «مساعدٌ واحدٌ للحذفِ الجامع». والرأسُ كانَ يقولُ «لا حذفَ في هذا الملفِّ ألبتّة» — فصارَ يقولُ الصدقَ: لا حذفَ **في مسارِ الدخول**، والحذفُ الوحيدُ في أسفلِ الملفِّ ولا يبلغُه دخول. |
| ٣ | `lib/auth/store.js` | **لم يُمَسّ** | §٢-٣ أجازَ مساعدَ حذفٍ *إن لزم* — **ولم يلزم**: `deleteKey()` قائمٌ في السطرِ **١٥٤** منذُ جولةِ الدخول. |
| ٤ | `index.html` | **لم يُمَسّ** | انظرْ §٢-ب-١: `SettingsSheet` ليست فيه. |
| ٥ | `tools/delete-truth-measure.cjs` | **٦٩٠** ضبطُ الزرّ · **٧٩٧** النصفُ الخادميّ | §٢-٥ + §٣-١٠: التأكيداتُ الجديدةُ تسكنُ `deletetruth` ولا تُخلَقُ بوّابة. |
| ٦ | `EZIK-CC-ACCOUNT-DELETE-REPORT-2026-08-26.md` | **جديد** | §٢-٦ + §٦. |

### ٢-ب · خارجَ قائمةِ الملكيّة — بموجبِ استثناءِ §٢، ومعلَنٌ هنا بسطرِه وسببِه

> §٢ آخِرًا: «ملفٌّ خارجَ القائمةِ يُقبَلُ مسُّه **فقط** إذا فرضَه **تناقضٌ في هذا الأمرِ نفسِه** أو **دفترُ بايتاتٍ تابعٌ لإعادةِ بناء** — ويُعلَنُ في التقريرِ بسطرِه وسببِه.»

| # | الملفّ | السطر | العلّةُ الموجِبة |
|---|---|---|---|
| ١ | **`app.jsx`** | **١٤٧٧٧** مسارُ المنفذ · **١٤٩٤٥** `ezikAuthDelete` · **١٥٧٧٠** `armed` · **١٥٨٢٩** `askDelete` · **١٥٨٣٦** `confirmDelete` · **٤٤٦ / ٧٦٤** النصوص | **تناقضٌ في الأمرِ نفسِه.** §٢-٤ تقول «`index.html` — الزرُّ داخلَ `SettingsSheet`»، و**`SettingsSheet` ليست في `index.html`**: قِيسَ أنّ `index.html:1701` سطرُ `<script src="app.js"></script>` وحدَه، وأنّ المكوِّنَ يسكنُ `app.jsx` ويُصرَّفُ إلى `app.js`. فالزرُّ لا يمكنُ أن يُوضَعَ حيثُ أُمِرَ إلّا هنا. |
| ٢ | **`app.js`** | نتاجُ `node tools/build-app.cjs` | **دفترُ بايتاتٍ تابعٌ لإعادةِ بناء.** مبنيٌّ لا مكتوبٌ بيد: `1143467 → 1147830` (**+٤٣٦٣**). |
| ٣ | **`sw.js`** ⛔ | **١٠٦** جدولُ البايتات · **١٢٦** `CORE_BYTES` | **دفترُ بايتاتٍ تابعٌ لإعادةِ بناء** — وهو الاستثناءُ الذي سمّاه الأمرُ بعينِه. `sw.js` مذكورٌ في المنعِ، ومع ذلك **بوّابةُ `bankintegrity` تحمرُّ حتمًا** بعدَ أيِّ إعادةِ بناءٍ (B12 و B14). و`CORE_BYTES` أُعيدَ قطعُه بأداةِ المستودعِ نفسِها `node tools/core-bytes.cjs --write` لا باليد. **رقمانِ فقط تحرّكا: الملفُّ ٤٤٢٦٦ بايتةً قبلَ وبعد.** و`CACHE` لم يُمَسّ. |
| ٤ | **`quest-bank-integrity-guard.cjs`** | **١٦١٧** مرآةُ `SW_PROSE` · **٥٠٧** ختمُ `sw.js` · **١١٦** قيدُ الدفتر | تتمّةُ الدفترِ نفسِه. الحارسُ يأمرُ بذلك بنصِّه: «Re-cut the sw.js digest in quest-bank-integrity-guard.cjs in THIS commit». والختمُ قُطِعَ **آخِرًا**، بعدَ أن استقرَّ `sw.js`، كما يُملي قيدُ الجولةِ السابقة. |
| ٥ | **`tools/auth-bridge-measure.cjs`** | **١٢٤** رفعُ `ezikAuthDelete` · **٧٢٧** الختمُ الكلّيّ | **تناقضٌ في الأمرِ نفسِه.** البوّابةُ كانت تثبّتُ **كلَّ** ما يرسمُه الصفُّ تثبيتًا كلّيًّا: `eq(drawn, email + signOut)`. و§٣-٩ تأمرُ برسمِ الزرِّ في ذلك الصفِّ بعينِه، و§٥ تشترطُ طقمًا أخضر — ولا يجتمعانِ إلّا بتحريكِ هذا التثبيت. **ولم يُضعَّفْ**: بقيَ كلّيًّا (`email + signOut + delete`)، وزادَ عليه نفيٌّ جديد: أنّ زرَّ التنفيذِ **ليس** على الشاشةِ قبلَ التسليح. |

### ٢-ج · ما لم يُمَسَّ ألبتّة

`privacy.html` · `delete.html` · `api/parent-code.js` · كلُّ مفاتيحِ `pc:` · `api/ask.js` · `index.html` · `lib/auth/store.js` · `vercel.json` · `gates.json`.

**البوّابةُ لم تُخلَقْ ولم تُسجَّلْ**: `gates.json` باقٍ على **٩٩** مدخلًا.

---

## ٣ · الأرقامُ المقيسةُ — قبلَ وبعد

| المقياس | قبل | بعد |
|---|---|---|
| `gates.json` | ٩٩ مدخلًا | **٩٩** (لم تُسجَّلْ بوّابة) |
| حالاتُ `deletetruth` | ١٢ | **٢٨** (+٤ للزرّ، +١٢ للخادم) |
| حالاتُ `authbridge` | ٢٢ | **٢٢** (٦/٦ طافرات مقتولة) |
| حالاتُ `authserver` | ٤٧ | **٤٧** (٦/٦ طافرات مقتولة) — لم تُمَسَّ البوّابةُ ولا حمرَّت |
| `app.js` | ١٬١٤٣٬٤٦٧ | **١٬١٤٧٬٨٣٠** (+٤٣٦٣) |
| `CORE_BYTES` | ١٬٩٧٩٬١٣٢ | **١٬٩٨٣٬٤٩٥** |
| `sw.js` | ٤٤٬٢٦٦ | **٤٤٬٢٦٦** (رقمانِ تحرّكا لا غير) |
| ختمُ `sw.js` | `096451a1…79a4` | **`f90db007…712a`** |
| مفاتيحُ مخزنٍ جديدة | — | **٠** |
| متغيّراتُ بيئةٍ جديدة | — | **٠** (`grep -c process.env api/auth-delete.js` ⟹ **٠**) |
| مسُّ `pc:` | — | **٠** خارجَ التعليقِ، ومقيسٌ داخلَ البوّابة: `eq(pc, [], 'pc: keys this route touched at all')` |
| مفاتيحُ i18n | — | **+١٢** (٦ بالعربيّةِ و٦ بالإنجليزيّةِ في الموضعِ النسبيِّ نفسِه) |
| الأسطر | — | **+٨٠٩ / −١٦** في ثمانيةِ ملفّات |

**والنصوصُ العربيّةُ الستّةُ لم تُصَغْ ولم تُكتَبْ باليد**: انتُزِعَتْ برنامجيًّا من ملفِّ الأمرِ نفسِه (`**…**` في أسطرِها الستّة) وكُتِبَتْ في `app.jsx` كما هي — فلا تشكيلَ اختلفَ ولا حرفَ سقط.

---

## ٤ · المحوُّ الثلاثيُّ — والترتيبُ هو التصميم

`deleteAccount(key, sessionId)` في `lib/auth/account.js:231`:

1. **`acctidx:v1:email:<sha256>` أوّلًا** — والبصمةُ مشتقّةٌ من **بريدِ السجلِّ المقروءِ نفسِه**، لا من مدخلٍ يرسلُه العميل. وتُحذَفُ **فقط إن كانَ المدخلُ يشيرُ إلى هذا الحسابِ بعينِه**: الفهرسُ يُكتَبُ `NX` ويظلُّ يسمّي **أوّلَ** حسابٍ أثبتَ العنوان، فقد يسمّي حسابًا آخَرَ **حيًّا** — ونزعُه حينَها يقتلعُ لحمةَ قارئٍ قائم.
2. **`acct:v1:<provider>:<sub>`**.
3. **`sess:v1:<id>`** عبرَ `revokeSession` — §٣-٣: استيقظَتْ هنا، وكانت مشحونةً بلا نداءٍ منذُ جولةِ الدخول.

**ولماذا الفهرسُ أوّلًا**: لو سقطَ بعدَ حذفِ السجلِّ لبقيَ **سهمًا إلى حسابٍ ميّت** ولا يبقى في المخزنِ ما يدلُّ على البريدِ لإعادةِ المحاولة. وبتقديمِه: فشلُه يُبقي السجلَّ قائمًا والعنوانَ مشتقًّا والإعادةَ ممكنة — **وهذا مقيسٌ**، لا مقول.

**الهويّةُ من الجلسةِ وحدَها**: لا `provider` ولا `sub` ولا بريدٌ من الجسم. مقيسٌ من الوجهَين — بطلبٍ يحملُ الثلاثةَ لحسابٍ آخَرَ فلا يتغيّرُ شيء، وبمسحِ نصِّ المنفذِ من `body.email` و`body.provider` و`body.sub` و`body.accountKey`.

**لا كشفَ ولا تعداد**: جلسةٌ لم تكنْ قطُّ، وجلسةٌ منتهية، ومخزنٌ لا يُقرأ، وحسابٌ محذوفٌ سلفًا — **أربعتُها `401` بالرمزِ نفسِه والجسمِ نفسِه**، مقيسًا بمقارنةِ الأجسامِ بعضِها ببعض.

**يفشلُ مغلقًا**: كلُّ محوٍ يُفحَصُ، ومخزنٌ يرفضُ أيَّها ⟹ `503` مسمّى، **لا `{ ok: true }` أبدًا**.

---

## ٥ · «انزعْه فعلًا وجرِّبْ» — §٥

نُزِعَ كلُّ محوٍّ من الثلاثةِ **على القرصِ فعلًا**، وأُجريَتِ البوّابةُ، ثمّ أُعيدَ الملفُّ وتُحُقِّقَ من مطابقتِه بايتةً ببايتة. وتغيُّرُ البايتاتِ مطبوعٌ في كلِّ صفٍّ حتّى لا يُقبَلَ «نجاحٌ» من طفرةٍ لم تُطبَّقْ أصلًا.

| # | النصُّ الذي نُزِعَ من `lib/auth/account.js` | البايتات | ما فعلَتْه `deletetruth` |
|---|---|---|---|
| **م١** | `if (!(await deleteKey(idxKey))) return { ok: false, code: 'auth-delete-index' };`<br>`index = true;` ⟵ أُبقيَ وحدَه | `13304 → 13217` | **`EXIT=1`** · سقطَتْ **٣** حالات |
| **م٢** | `if (!(await deleteKey(key))) return { ok: false, code: 'auth-delete-account' };` | `13304 → 13264` | **`EXIT=1`** · سقطَتْ **٣** حالات |
| **م٣** | `if (!(await revokeSession(sessionId))) return { ok: false, code: 'auth-delete-session' };` | `13304 → 13254` | **`EXIT=1`** · سقطَتْ **٢** حالة |

والحالاتُ الساقطةُ بأسمائِها:

```
م١  [FAIL] the account, the email index and the session are ALL THREE erased, by name
    [FAIL] the index is found through the record OWN address, not through the body
    [FAIL] a store that refuses a delete is a NAMED 503, never { ok: true }

م٢  [FAIL] the account, the email index and the session are ALL THREE erased, by name
    [FAIL] an index entry pointing at ANOTHER LIVE account is left exactly where it is
    [FAIL] a store that refuses a delete is a NAMED 503, never { ok: true }

م٣  [FAIL] the account, the email index and the session are ALL THREE erased, by name
    [FAIL] the route reuses revokeSession -- the call lib/auth/account.js was waiting for
```

ثمّ: `restored; tree byte-identical to before the experiment`.

**فالحالةُ الأولى تسقطُ بنزعِ أيٍّ من الثلاثة** — وهذا هو المطلوبُ حرفيًّا: فحصٌ يستطيعُ أن يفشل.

> والتأكيداتُ ليست قراءةَ نصٍّ: المنفذُ الحقيقيُّ يُرفَعُ من القرصِ وتُبدَّلُ **جملُ الاستيرادِ وحدَها**، ويُقادُ فوقَ مخزنٍ من `Map` يُبلَغُ عبرَ `__setAuthStoreForTest` — البذرةِ التي ينشرُها `lib/auth/store.js` لهذا الغرضِ بعينِه. و`account.js` و`store.js` و`daycap.js` و`attempts.js` تُستورَدُ **حقيقةً**؛ والمزيَّفُ الوحيدُ `lib/ratelimit.js`، لأنّه يبني عميلَ Upstash عندَ نطاقِ الوحدةِ وهو العائلةُ التي **تفشلُ مغلقةً**، فكانَ سيردُّ `429` على كلِّ طلبٍ في آلةٍ بلا اعتماداتٍ — ورفضُه حينَ لا يستطيعُ العدَّ **مقيسٌ** على الجذعِ نفسِه.

---

## ٦ · الزرُّ — خطوتانِ لا ضغطة

- **لا يُرسَمُ إلّا لمن له جلسةٌ حيّة**: داخلَ فرعِ `session ?` وحدَه — مقيسٌ، ومعه النفيُّ أنّ الفرعَ الآخَرَ لا يحملُ منه شيئًا.
- **`armed` يفتحُ على `false`** — مقيسٌ نصًّا (`useState(false)`)، فلا يُفتَحُ الصفُّ مسلَّحًا.
- **الضغطةُ الأولى لا تبلغُ الشبكة**: `askDelete` خالٍ من `fetch` ومن `ezikAuthDelete` — مقيس.
- **زرُّ التنفيذِ لا وجودَ له قبلَ التسليح** — مقيسٌ في `deletetruth` نصًّا، **وفي `authbridge` رسمًا** على شجرةِ React حقيقيّة.
- **والجهازُ لا يتبعُ إلّا خادمًا قالَ نعم**: `clearAuthSession()` بعدَ فحصِ `if (!ok)` لا قبلَه — مقيسٌ بترتيبِ الموضعَين. فلو فشلَ الحذفُ لبقيَ القارئُ داخلًا ومعه سطرُ الفشلِ وإمكانُ الإعادة، بدلَ أن يُخرَجَ من حسابٍ ما زالَ قائمًا.
- بعدَ النجاح: `setSession(null)` ومحوُ `AUTH_SESSION_KEY` ⟹ حالُ «غيرِ مسجَّل».

والنصوصُ الستّةُ موجودةٌ في اللغتَين، غيرُ فارغةٍ وغيرُ مترجَمةٍ إلى نفسِها، وجسمُ التأكيدِ أطولُ من ١٠٠ حرفٍ — فلا يمرُّ نصٌّ نائب.

---

## ٧ · ناتجُ الطقمِ — حرفيًّا

```
=== SUITE: 99/99 EXIT=0 ===
recon:    SUMMARY   PASS=190   WARN=1   FAIL=0
tree after: 0 dirty path(s)
```

`evidence: C:\Users\passe\AppData\Local\Temp\ezik-gates\runs\2026-08-26T07-57-46-175Z-19080`

**وقياسُ البوّاباتِ قبلَ الإيداعِ أنتجَ حمرةً كاذبةً كما نبّهَتْ §٤-٤** — وتُسجَّلُ هنا لئلّا تُقرأَ لاحقًا انحدارًا: `recon` (انحرافُ تقريرِ التنفيذ: الشجرةُ متّسخة) و`chatux` (يقرأُ `git diff --name-only HEAD`، فرأى `sw.js` داخلَ نصفِ قطرِ الانفجارِ الدائم). **كِلتاهما اخضرَّتا فورَ الإيداعِ بلا تغييرِ حرفٍ فيهما.**

والإيداعاتُ ثلاثةٌ، بأسماءِ ملفّاتٍ صريحةٍ ولا `git add .` ألبتّة:

| الإيداع | الملفّات |
|---|---|
| `bd6464d` | `api/auth-delete.js` · `lib/auth/account.js` |
| `606b707` | `app.jsx` · `app.js` · `sw.js` · `quest-bank-integrity-guard.cjs` |
| `10b2946` | `tools/delete-truth-measure.cjs` · `tools/auth-bridge-measure.cjs` |

**صفرُ دفعٍ · صفرُ نشرٍ · صفرُ لمسٍ لحسابِ متجر.**

---

## ٨ · ثلاثةٌ للمالكِ — لم أُصلِحْ منها شيئًا

1. **جسمُ التأكيدِ يحيلُ إلى زرٍّ باسمٍ غيرِ اسمِه على الشاشة.** النصُّ المأمورُ يقول «لمحوِها استعملْ **«احذفْ بياناتي»**»، والزرُّ المشحونُ في `app.jsx:16243` مكتوبٌ عليه **«حذف كل البيانات وإعادة البدء»**. النصُّ يُنسَخُ ولا يُصاغُ (§٣)، وتسميةُ ذلك الزرِّ خارجَ الملكيّة — فتُرِكَ الأمرانِ كما هما ويُعرَضُ القرارُ.
2. **ربطُ الجهازِ ليس ربطًا بالجلسة.** `x-murabbi-device` **يُقرأُ ويُفحَصُ شكلُه ويُرفَضُ الطلبُ بدونِه**، كما في `api/auth-exchange.js:59` — لكنّ سجلَّ الجلسةِ يحملُ `{ accountKey, createdAt, expiresAt }` **ولا جهازَ فيه**، فليسَ ثمّةَ ما يُقارَنُ به. وإضافةُ حقلٍ إليه تحمِّرُ `authserver:828` التي تثبّتُ حقولَه الثلاثةَ بالضبط — وهي بوّابةٌ خارجَ ملكيّةِ هذه الجولة. وما يشتريه الاشتراطُ اليومَ مذكورٌ بصدقٍ في رأسِ المنفذ: أنّ البابَ لا يُجيبُ إلّا مسارَ الجلبِ المكبوحِ للتطبيقِ نفسِه، ولا يكلّفُ قارئًا شيئًا لأنّ الجلسةَ تسكنُ `localStorage` أصلًا.
3. **`authserver` R10 تفحصُ ثلاثةَ مسارات** بأسمائِها للتعليقِ الذي يعلّلُ غيابَ بوّابةِ موافقةِ الذكاء — و`api/auth-delete.js` رابعٌ لا تراه. التعليقُ مكتوبٌ فيه (والعلّةُ فيه أقوى: لا يُشترَطُ على قارئٍ أن يوافقَ على شيءٍ ليُنسى)، لكنّ إضافتَه إلى تلك القائمةِ مسٌّ لبوّابةٍ خارجَ الملكيّةِ لم يفرِضْه تناقض.

وكما تأمرُ §٢: **`privacy.html` و`delete.html` ما زالا ناقصَينِ في وصفِ الحساب، ولم يُمَسَّ منهما حرف.**

---

## ٩ · الختم

المختومُ هو **كلُّ ما فوقَ هذا السطرِ حتّى أوّلِ بايتةٍ في الملفّ** — أي الحمولةُ منتهيةً بسطرِ العنوانِ `## ٩ · الختم` وسطرِه الجديد، قبلَ إلحاقِ هذه الفقرة.

| | |
|---|---|
| حمولةُ التقرير | **18839** بايتة |
| `sha256` | **`a2367121d0120f09d4360c6bc04f5ab1ad53268664668e25fb549c0de416f169`** |

ويُعادُ التحقُّقُ منه هكذا:

```
node -e "const s=require('fs').readFileSync('EZIK-CC-ACCOUNT-DELETE-REPORT-2026-08-26.md','utf8'); const p=s.slice(0, s.indexOf('
'+'المختومُ هو')); console.log(Buffer.byteLength(p,'utf8'), require('crypto').createHash('sha256').update(p,'utf8').digest('hex'));"
```
