# EZIK — تقريرُ الجولةِ الختاميّة · الذخيرةُ والحارسُ والمفضّلةُ ثمَّ الدمجُ والنشر

**٢٦ أغسطس ٢٠٢٦ · الشجرة `C:\Users\passe\projects\ustaz`**

## ٠ · الحصيلةُ في سطرَين

**نُشِرَ للإنتاج.** `main` انتقلَ من `2f145f4` إلى **`8665d66`**، ونشرةُ الإنتاجِ **`dpl_4s8KQNooCkNSQpTEgWCegttkdi5G`** حالتُها `● Ready` — **و`ezik.app` يشحنُ الملفّاتِ الأربعةَ مطابقةً للقرصِ بايتًا وبصمةً** (§٥).

**والبوّاباتُ ٩٩/٩٩ خضراءُ قبلَ الدمجِ وبعدَه** · **وملفّا البياناتِ لم تُمَسَّ منهما بايتةٌ.**

---

## ١ · بصمتا البياناتِ — قبلُ وبعدُ

| الملفّ | قبلَ الجولة | **بعدَ الدمجِ والنشر** | حيًّا على `ezik.app` |
|---|---|---|---|
| `adhkar.json` | `19EF96B9` | **`19EF96B9`** | **`19EF96B9`** |
| `lib\data\adhkar.json` | `19EF96B9` | **`19EF96B9`** | — |
| `adhkar-split-27.json` | `56C0FC1A` | **`56C0FC1A`** | **`56C0FC1A`** |
| `lib\data\adhkar-split-27.json` | `56C0FC1A` | **`56C0FC1A`** | — |

**ولا بايتةَ واحدةٌ تحرّكتْ في أيٍّ منها** — لا في التعديلِ ولا في التجارب (§٣-٢).

---

## ٢ · الذخيرة — البابانِ بلا شبكة

`'/adhkar-split-27.json'` انضمَّ إلى `CORE` في `sw.js` **بجوارِ `adhkar.json` وبالطريقةِ القائمةِ نفسِها**: مدخلٌ واحدٌ في المصفوفةِ نفسِها.

🔑 **وإعادةُ التحقّقِ لم تحتجْ سطرًا واحدًا:** معالجُ الجلبِ يحوِّلُ **كلَّ ملفِّ `*.json` من الأصلِ نفسِه** — عدا ملفَّي المصحفِ المختومَين — إلى `stale-while-revalidate`. فالملفُّ **مخزَّنٌ سلفًا ويجدِّدُ نفسَه**، تمامًا كـ`adhkar.json`.

### معيارُ القبولِ (§١) — مقيسًا بتشغيلِ العاملِ نفسِه

شُغِّلَ العاملُ **القديمُ** (من `HEAD` قبلَ التعديل) ثمَّ **الجديدُ** على **مخزنٍ واحدٍ** يمثّلُ جهازَ قارئ:

| | بعدَ إقلاعِ العاملِ القديم | **بعدَ إقلاعِ العاملِ الجديدِ على الجهازِ نفسِه** |
|---|---|---|
| اسمُ المخزن | `ezik-v32` | `ezik-v32` (**لم يُبدَّلْ ولم يحتجْ**) |
| عددُ المداخل | **١٠** | **١١** |
| `/app.js` في المخزن | `OLD-APP-JS` | **`NEW-APP-JS`** |
| `/adhkar-split-27.json` | **غائب** | **حاضر** ✅ |

**فالإقلاعُ الباردُ بلا شبكةٍ يفتحُ بابَين** — لأنَّ الملفَّ في الذخيرةِ قبلَ أن تُقطَعَ الشبكة.

---

## ٣ · الحارس — وُسِّعَ ولم يُخلَقْ

`guards\adhkar-twins-guard.cjs` صارَ يقارنُ **زوجَين**: زوجَ `adhkar.json` أوّلًا كما كانَ حرفًا، ثمَّ زوجَ `adhkar-split-27.json` **بالفحوصِ نفسِها والترتيبِ نفسِه والشيفرةِ نفسِها**.

**ولا بوّابةَ جديدةً:** `gates.json` **لم يُمَسَّ**، **والروسترُ ما زالَ ٩٩** (مقيسٌ بعدَ التعديل).

**وأُضيفَ فحصٌ يغلقُ على نفسِه**: «كلُّ ملفِّ بياناتِ أذكارٍ مزدوجٍ على الروستر» — فقائمةُ أزواجٍ فقدَتْ مدخلًا **تحمرُّ** بدلَ أن تقيسَ أقلَّ وتقولَ `PASS`، وهو عينُ الصمتِ الذي وُجِدَ هذا الحارسُ ليُنهيَه.

### ٣-١ · التجربتانِ (§٢) — بالأرقامِ لا بالدعوى

| التجربة | النتيجة | الخروج |
|---|---|---|
| **بايتةٌ واحدةٌ حُرِّكَتْ** في نسخةٍ (`0x20` ← `0x09`، الطولُ نفسُه، والملفُّ ما زالَ يُحلَّل) | **`=== 3/4 -- FAIL ===`** | **`1`** ✅ |
| **البايتةُ أُعيدَتْ** | **`=== 4/4 -- PASS ===`** | **`0`** ✅ |
| التشغيلةُ الحقيقيّةُ بلا وسائطَ (الزوجانِ معًا) | **`=== 7/7 -- PASS ===`** | `0` |

### ٣-٢ · ⚠️ كيفَ أُجرِيَتِ التجربتانِ — انحرافٌ مسمًّى عن حرفِ §٢

§٢ يقولُ «تحريكُ بايتةٍ في إحدى النسختَين… والشجرةُ نظيفةٌ بعدَهما» — أي تعديلٌ في الشجرةِ ثمَّ ردٌّ.

**ولم أفعلْ ذلك.** أمرُك الصريحُ في هذه الرسالة: **«لا تعدّلْ بايتةً واحدةً من `adhkar.json` أو `adhkar-split-27.json`»**، وتعديلٌ ثمَّ ردٌّ **يمسُّ البايتاتِ في نافذةٍ زمنيّةٍ** ويتركُ الملفَّ مكسورًا إن انقطعَ التنفيذُ في تلك النافذة.

**فأجريتُهما بالمنفذِ الذي بناهُ الحارسُ لهذا الغرضِ نفسِه** (`argv` — «الاختبارُ السلبيُّ يوجّهُه إلى نسختَي مسوَّدة»): **الملفُّ الحقيقيُّ في الشجرةِ من جهةٍ، ونسخةٌ في المسوَّدةِ من الجهةِ الأخرى**. فالانحرافُ المقيسُ انحرافٌ حقيقيٌّ عن محتوًى حقيقيّ، **وبصمةُ ملفِّ الشجرةِ في مخرَجِ التجربةِ نفسِه هي `56c0fc1a…`** — برهانٌ في نصِّ التجربةِ أنّه لم يُمَسّ.

**وما لم تُثبتْهُ هذه الطريقةُ**: أنَّ الزوجَ الثاني موصولٌ بالتشغيلةِ الافتراضيّة. **وأثبتَه غيرُها**: التشغيلةُ بلا وسائطَ تطبعُ الزوجَينِ وبصمتَيهما (`7/7`)، وفحصُ الروسترِ يحمرُّ إن نقصَ زوج.

---

## ٤ · المفضّلةُ — تُطابَقُ بالـ`id` قراءةً لا كتابةً

**العلّةُ:** المفضّلةُ تخزَّنُ `<مجموعة>:<فهرس>`، فمفضّلةُ ٢٧ هي `27:5`؛ والبابانِ مسمَّيانِ فمفتاحُهما `adhkar_sabah:0`. والمجموعةُ ٢٧ خارجَ الفهرسِ ⟹ **قلبٌ مضاءٌ لا يبلغُه القارئ**.

**الحلُّ — عبرَ البياناتِ لا بإعادةِ كتابة:** `27:5` يسمّي **موضعًا**؛ والموضعُ يسمّي **صفًّا**؛ والصفُّ له **`id`**؛ **والـ`id` هو ما يتشاركُه البابانِ مع المجموعةِ التي جاءا منها.**

**المقيسُ** (مخزنٌ مبذورٌ `["27:5"]`، و`27:5` يسمّي الذكرَ **`id 80`**، وهو في البابَينِ عندَ الموضعِ ٥):

| | القلب | التسمية | المخزنُ بعدَه |
|---|---|---|---|
| **بابُ الصباح** (موضع ٥) | **`aria-pressed="true"`** ✅ | «أزل من المفضلة» | `["27:5"]` |
| **بابُ المساء** (موضع ٥) | **`aria-pressed="true"`** ✅ | «أزل من المفضلة» | `["27:5"]` |
| **الشاهدُ**: ذكرٌ غيرُ مفضَّلٍ (موضع ٠) | `aria-pressed="false"` ✅ | — | — |

> 🔑 **والمخزنُ بعدَ كلِّ ذلك `["27:5"]` حرفًا** — لا ترحيلَ ولا إعادةَ تسميةٍ ولا حذفَ مدخل. **وما يكتبُه القارئُ اليومَ في البابَينِ يُكتَبُ بمفتاحِ البابِ الجديد.**

**ومحصورٌ في مصدرِ الشقِّ وحدَه**، فمجموعتانِ لا علاقةَ بينَهما تتشاركانِ ذكرًا **لا تُضيءُ إحداهُما قلبَ الأخرى**.

### ٤-١ · ⚠️ ثمنٌ مسمًّى: قلبٌ موروثٌ لا يُطفَأُ من البابِ الجديد

القلبُ المضاءُ **بالمدخلِ القديمِ** لا يمكنُ إطفاؤُه من البابِ الجديد: الإطفاءُ يقتضي **حذفَ `27:5`**، **و§٣ يحرّمُ الكتابةَ نصًّا**. فالضغطُ يضيفُ مفتاحَ البابِ ثمَّ يزيلُه، ويبقى القلبُ مضاءً بالموروث.

**وهذا نتيجةٌ مباشرةٌ للقيدِ الذي أمرتَ به، لا سهوٌ.** والطريقُ إلى إطفائِه: `?adhkargroups=0` ثمَّ إطفاؤُه من المجموعةِ ٢٧. **وإن أردتَّ إطفاءَه من البابِ الجديدِ فذلك أمرٌ يأذنُ بكتابةٍ في `adhkar_favorites_v1`.**

---

## ٥ · المفتاحُ رُفِعَ — والرجعةُ في ثانيةٍ بلا نشر

```
قبل :  get('adhkargroups') === '1'   ⟹  مطفأٌ ما لم يُطلَبْ
بعد :  get('adhkargroups') !== '0'   ⟹  مشتعلٌ ما لم يُرفَضْ
```

**والاتّجاهانِ مقيسانِ:**

| الرابط | `ADHKAR_GROUPS_ON` | بطاقاتُ الفهرس | طلبُ ملفِّ الشقّ |
|---|---|---|---|
| **بلا مُعامِل** | **`true`** | **١٣٣** (بابانِ + ١٣١) | ١ |
| **`?adhkargroups=0`** | **`false`** | **١٣٢** (المجموعةُ ٢٧ كاملةً) | **٠** |

**وما زالَ لا يكتبُ في الجهازِ شيئًا** — فلا أحدَ من الطرفَينِ يلتصقُ بجهازٍ أو يعيشُ بعدَ اللسانِ الذي كُتِبَ فيه. **وكلُّ إخفاقٍ يجيبُ الآنَ `true`**، لأنَّ الافتراضَ الآمنَ صارَ «الشاشةَ التي أقرَّها المالك».

---

## ٦ · جوابُ §٥ — نعم، القارئُ القديمُ ينزلُ على الجديدِ بلا تحديثٍ قسريّ

**قِستُ ولم أُصلِحْ، ولم يثبتْ عطبٌ.**

| القياس | النتيجة |
|---|---|
| `self.skipWaiting()` في `sw.js` | **موجودٌ** (سطر ٣٩٨) — **نودِيَ مرّةً** في التشغيلِ الحقيقيّ |
| `self.clients.claim()` | **موجودٌ** (سطر ٤٤٠) — **نودِيَ مرّةً** |
| ترويسةُ `app.js` حيًّا | **`Cache-Control: public, max-age=0, must-revalidate`** |
| ترويسةُ `sw.js` حيًّا | **`public, max-age=0, must-revalidate`** |
| كتاباتُ الذخيرةِ في الإقلاعِ الثاني | **١١** — أي كلُّ `CORE` أُعيدَ كتابتُه |
| `/app.js` في المخزنِ بعدَ الإقلاعِ الثاني | **الجديد** |

**السلسلةُ كاملةً:** بايتاتُ `sw.js` تغيَّرَتْ ⟹ المتصفّحُ يعيدُ التحقّقَ منه (`max-age=0`) فيجدُه مختلفًا ⟹ **يثبِّتُ العاملَ الجديد** ⟹ التثبيتُ يعيدُ كتابةَ **كلِّ** مداخلِ `CORE` بنسخٍ طازجةٍ (وهي أيضًا `max-age=0`، **فلا مخزنُ HTTP قديمٌ يسمِّمُ الذخيرة**) ⟹ `skipWaiting` **يسلّمُ فورًا بلا انتظارِ إغلاقِ الألسنة** ⟹ `clients.claim` يمسكُ الصفحاتِ المفتوحة.

**ولماذا احتاجَ المالكُ تحديثًا قسريًّا على البريفيو؟** لأنَّ **صفحةً مفتوحةً بالفعلِ تبقى تشغّلُ الشيفرةَ التي حمَّلَتْها**: `clients.claim()` يمسكُها ولا **يعيدُ تنفيذَها**. **فالجديدُ يظهرُ عندَ الفتحِ التالي — وهو عينُ ما سألَ عنه §٥ («عندَ الإقلاعِ التالي»)، والجوابُ نعم.**

---

## ٧ · الدمجُ والنشرُ والتحقّقُ الحيّ

| | |
|---|---|
| **`main` قبلَ الدمج** | `2f145f417740bd24dc608124010116b20a899d6f` |
| **`main` بعدَ الدمج** | **`8665d66c9feb4d89a5b70cddf9a356e19d54a611`** |
| نوعُ الدمج | **`--no-ff`** برسالةٍ واحدة · ١٤ ملفًّا · `+1933 / -78` |
| **نشرةُ الإنتاج** | **`dpl_4s8KQNooCkNSQpTEgWCegttkdi5G`** · `target: production` · **`● Ready`** · ٢١ ثانية |

### ٧-١ · 🔑 الحيُّ مقابلَ القرصِ — بالبصمةِ لا بالدعوى

| الملفّ | القرص | **`ezik.app` حيًّا** | |
|---|---|---|---|
| `app.js` | ١٬١٧١٬٠٣٦ · `29dd55fd…` | ١٬١٧١٬٠٣٦ · `29dd55fd…` | **مطابق** ✅ |
| `sw.js` | ٤٤٬٦٩٥ · `5bd20328…` | ٤٤٬٦٩٥ · `5bd20328…` | **مطابق** ✅ |
| `adhkar-split-27.json` | ٧٬١٨٢ · `56c0fc1a…` | ٧٬١٨٢ · `56c0fc1a…` | **مطابق** ✅ |
| `adhkar.json` | ١٧٧٬٣٩٢ · `19ef96b9…` | ١٧٧٬٣٩٢ · `19ef96b9…` | **مطابق** ✅ |

**وقُرِئَ من الحزمةِ الحيّةِ نفسِها:**

- `get('adhkargroups')!=='0'` — **المفتاحُ مرفوعٌ في الإنتاج**.
- `'/adhkar-split-27.json'` داخلَ `CORE` في `sw.js` الحيّ · و`CORE_BYTES = 2013883`.
- وملفُّ الشقِّ الحيُّ: `adhkar_sabah` «أذكار الصباح» **٢٣** · `adhkar_masaa` «أذكار المساء» **٢١** · `approvedBy: owner`.

### ٧-٢ · البوّابات

```
قبلَ الدمجِ (على الفرع، عندَ 1231096):   === SUITE: 99/99 EXIT=0 ===   GATES_EXIT=0
بعدَ الدمجِ (على main،  عندَ 8665d66):   === SUITE: 99/99 EXIT=0 ===   GATES_EXIT=0
الشجرةُ بعدَ كلٍّ منهما:                  0 dirty path(s)

recon:  SUMMARY  PASS=190  WARN=1  FAIL=0     ·     grep -c "\[FAIL\]"  ->  0
```

**و`WARN=1` موروثٌ مطابقٌ حرفًا لأساسِ الجولاتِ الثلاث** (`LONGEST_CARD_CHARS` في `api/report.js`، ولم يُلمَسْ).

### ٧-٣ · الملفّاتُ الملموسةُ في هذه الجولة

| الملفّ | لماذا | الإذن |
|---|---|---|
| `app.jsx` · `app.js` | العملُ وبناؤه | §٦ نصًّا |
| `sw.js` | مدخلُ `CORE` · `CORE_BYTES` · جدولُ البايتات | §٦ نصًّا |
| `quest-bank-integrity-guard.cjs` | `SW_CORE` · `SW_CORE_FILES` · `SW_PROSE` · البصمةُ · سطرُ الدفتر | §٦ («مرآةُ الحارس») |
| `guards\adhkar-twins-guard.cjs` | التوسعة | **§٢ — إذنٌ صريحٌ بهذا الحارسِ وحدَه** |

**ولا ملفَّ خارجَ هذه الأربعة.** `gates.json` لم يُمَسّ · `adhkar.json` و`adhkar-split-27.json` لم تُمَسّا · **ولا `git add .`** في أيِّ إيداع.

---

## ٨ · ما لم يُنفَّذْ ولماذا

### ٨-١ · 🔴 نثرٌ كاذبٌ **أدخلتُه أنا** وتركتُه — وهذا أوّلُ ما يجبُ إصلاحُه

في `app.jsx` سطر **٦٦٥١** ما زالَ العنوانُ:

```
// ── THE GROUP DOORS -- ?adhkargroups=1, AND IT IS OFF FOR EVERYONE ──
```

**وهو كذبٌ الآنَ**، ويناقضُ الفقرةَ التي كتبتُها بعدَه بثمانيةَ عشرَ سطرًا (٦٦٦٩: «RAISED FOR EVERYONE»). **رفعتُ الافتراضَ وصحّحتُ نثرَ الدالّةِ ونسيتُ لافتةَ القسم.**

**ولم أُصلِحْه، والسببُ حدٌّ لا كسل:** إصلاحُه يعني `app.jsx` ⟹ بناءً ⟹ قطعَ الدفترِ ⟹ **دفعةً ثانيةً إلى `main`** ⟹ **نشرةَ إنتاجٍ ثانيةً**. **و§٦-٣ يقول: «وهذا هو الإذنُ الوحيدُ بالدفعِ إلى `main` في هذا الأمر».** فأخذُ نشرةِ إنتاجٍ ثانيةٍ لتصحيحِ تعليقٍ **تجاوزٌ للإذنِ**، وتركُ اللافتةِ **لا يمسُّ قارئًا** — التعليقُ غيرُ مرئيٍّ في الواجهة.

> **فهو بندٌ مفتوحٌ باسمِه**، وإصلاحُه إيداعٌ واحدٌ في الجولةِ القادمة.

### ٨-٢ · التقريرُ على الفرعِ لا على `main` — للسببِ نفسِه

هذا الملفُّ مُودَعٌ على `feat/adhkar-groups-20260826` **لا على `main`**، لأنَّ إيداعَه في `main` يقتضي دفعةً ثانيةً إليه. **`main` واقفٌ بالضبطِ عندَ الدمجِ الذي أُذِنَ به ونُشِرَ.** وضمُّه إلى `main` أمرٌ يملكُه المالكُ بكلمة.

### ٨-٣ · اسمُ المخزنِ `ezik-v32` لم يُبدَّلْ

**ولم يحتجْ** (§٦): التثبيتُ يعيدُ كتابةَ كلِّ `CORE` في المخزنِ نفسِه — مقيسًا، لا مفترَضًا. **وتبديلُ الاسمِ قرارُ شحنٍ** لم يأمرْ به هذا الأمر.

### ٨-٤ · ما لم يُقَسْ

- **لم أرَ `ezik.app` بعينٍ بشريّةٍ ولا بمتصفّحٍ حقيقيّ بعدَ النشر.** ما قِستُه: البايتاتُ والبصماتُ والترويساتُ الحيّة، والسلوكُ في linkedom بردِّ React الحقيقيّ. **حكمُ العينِ للمالك.**
- **ولم أراجعْ نسبةَ ذكرٍ إلى بابِه** — حكمٌ شرعيٌّ أقرَّهُ المالكُ، عرضتُه ولم أراجعْه.
- **ولم أقِسْ سلوكَ جهازٍ حقيقيٍّ بلا شبكة** — قِستُ العاملَ نفسَه مُشغَّلًا، لا هاتفًا في وضعِ الطيران.

---

## ٩ · الرجعةُ — إن أرادَها المالك

| المدى | كيف |
|---|---|
| **قارئٌ واحدٌ، فورًا، بلا نشر** | `https://ezik.app/?adhkargroups=0` |
| **الميزةُ كلُّها، بنشرةٍ واحدة** | قلبُ سطرٍ واحد: `!== '0'` ⟶ `=== '1'` في `readAdhkarGroupsFlag`، ثمَّ الدفترُ والدفع |
| **الجولةُ كلُّها** | `git revert -m 1 8665d66` |
