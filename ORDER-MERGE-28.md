# EZIK — أمرُ الدمج · الجولةُ ٢٨ · ٢٤ أغسطس ٢٠٢٦

**فرعٌ واحدٌ يحملُ الوِردَ والختمةَ والعلامتَينِ وتصحيحَ النثر. والقطعُ والختمُ والرفعةُ تقعُ هنا مرّةً واحدة.**

> 🔴 كلُّ رقمٍ هنا **فرضيّةٌ حتّى تقيسَه**.

---

## ٠ · الهويّةُ والحدّ

| المفتاح | القيمة |
|---|---|
| الشجرة | `C:\Users\passe\projects\ustaz` |
| الأساس | `origin/main` بعدَ `fetch` — المتوقَّعُ `8b45a39` |
| فرعُ الدمج | `merge/round-28-20260824` يُقطَعُ من `origin/main` |
| المدموج | `feat/wird-khatmah-20260824` — الرأسُ المتوقَّعُ **`da0e6d0`** · **وعددُ إيداعاتِه فوقَ الأساسِ يُقاسُ ويُطبَعُ** (المتوقَّعُ اثنانِ فأكثر: الوِردُ ثمّ النثر) |
| 🔴 المحرَّم | **دفعٌ · نشرٌ** · لمسُ `main` · تعديلُ أيِّ حارس · فتحُ أيِّ بندٍ جديد |

---

## ١ · بوّابةُ الفتح

1. `status --porcelain` نظيفةً · `fetch` · `rev-parse origin/main` مطبوعًا ومقارَنًا.
2. رأسُ الفرعِ وعددُ إيداعاتِه مطبوعان.
3. الأرضيّةُ عندَ `origin/main` تُقاسُ وتُطبَعُ **قبلَ** الدمج.

---

## ٢ · الترتيبُ الملزَم

1. اقطعِ الفرعَ من `origin/main` · ثمّ **ادمجْ** بـ`--no-ff`.
2. **إيداعُ توفيقٍ واحدٌ**:
   - **`CORE_BYTES`** يُعادُ قطعُه بالمقيسِ بعدَ الدمج — **المتوقَّعُ `1870613 ⟶ 1895725` (`+25112`)** (يُقاسُ لا يُفترَض).
   - 🔵 **وهذه الجولةُ يتحرّكُ فيها الملفّانِ معًا**: `index.html` (`122568 ⟶ 122884`) و`app.js` (`1035264 ⟶ 1060060`) ⟹ **بيانا الحجمِ في نثرِ `sw.js` يُصحَّحانِ معًا، ومرآتاهما المُلزِمتانِ في `SW_PROSE` داخلَ حارسِ البنكِ تُصحَّحانِ معهما** — تحريكُ واحدٍ دونَ مرآتِه يقلبُ حمرةً إلى حمرتَين.
   - **رفعةُ اسمِ مخزنِ الشحنة**: `ezik-v26 ⟶ ezik-v27` في **كلِّ** مواضعِه · **و`ezik-mushaf-pages-v1` لا يُمَسُّ ألبتّة**.
   - **الختمُ** يُعادُ بإبدالِ بصمةِ `sw.js` في مكانِها داخلَ الحارس — ولا تُولَّدُ خريطةُ الختمِ جملةً.
   - **وما كانَ صحيحًا لا يُحرَّك** — تحريكُ ما لا يحتاجُ تحريكًا عطبٌ لا إصلاح.
3. ثمّ البوّاباتُ على **شجرةٍ نظيفةٍ بعدَ الإيداع** · ثمّ التقريرُ · ثمّ **قفْ**.

---

## ٣ · أفخاخٌ مقيسةٌ — لا تُكتشَفُ من جديد

1. الحرّاسُ يفحصونَ نظافةَ الشجرة ⟹ **الحكمُ بعدَ الإيداعِ لا قبلَه** · والملفّاتُ غيرُ المتتبَّعةِ تُنحّى قبلَ الطقمِ وتُعادُ بعدَه.
2. `bankintegrity` تحمرُّ عمدًا بينَ الدمجِ وإيداعِ التوفيق — **وإيداعُ التوفيقِ هو الذي يُصلِحُها**.
3. رأسُ الدمجِ يُتحقَّقُ بـ`merge-base --is-ancestor` لا بالمساواة.
4. `index.html` كلُّه CRLF ⟹ `bare LF = 0` بأداةٍ تقرأُ البايتات.
5. **حجمُ `sw.js` قد لا يتغيّرُ وبصمتُه تتغيّر** — الحجمُ وحدَه لا يشهدُ بشيء.
6. 🔴 **حارسُ الدروسِ يعُدُّ النصَّ الخام** ⟹ لا تكتبْ عبارةَ نداءِ التنقّلِ إلى شاشةِ الدروسِ حرفًا في أيِّ ملفٍّ ولا في تقريرِك.

---

## ٤ · شروطُ المرورِ — تُطبَعُ بنصِّها

| البوّابة | الشرط |
|---|---|
| الروستر | **٩٣** |
| `SUITE` | **93/93** بخروجِ عمليّةٍ **صفرًا** |
| `recon` | **PASS=184 WARN=1 FAIL=0** · والإنذارُ الوحيدُ المعروف |
| حارسُ الدروس | **٢٦٩** |
| `themecoverage` · `questux` · `wird` | **1351** · **61** · **1122** — لا تنقصُ أرضيّةُ أيٍّ منها |
| `bankintegrity` | 🟢 **خضراءُ بعدَ إيداعِ التوفيق** — وهو البرهان |
| اسمُ المخزن | `ezik-v26` **صفرُ مواضعَ** بعدَ الرفعة |

**ولا تُخفَضُ أرضيّةٌ ولا يُنزَعُ تأكيدٌ ولا يُوسَّعُ استثناء.** بوّابةٌ حمراءُ لا تُفهَمُ ⟹ قفْ واكتبْ.

---

## ٥ · التقرير

`EZIK-CC-MERGE-28-REPORT-2026-08-24.md` في جذرِ الشجرة، وفيه:

1. الأساسُ · رأسُ الفرعِ وعددُ إيداعاتِه · رأسُ الدمجِ · كلُّ إيداعٍ وما مسَّ · وعددُ التعارضات.
2. **`CORE_BYTES` قبلَ وبعدُ والفرقُ** · ومواضعُ بيانِ الحجمِ التي مُسَّتْ بأعيانِها **وما تُرِكَ لأنّه صحيح**.
3. بوّاباتُ §٤ بنصِّها.
4. **«ما تراهُ عينُ المالكِ بعدَ النشر»** بلغةِ إنسان: ما يظهرُ في الشاشةِ · وما تحتَ السطح · وما لم ينزلْ ولماذا.
5. **ما لم يُقَسْ** يُسمّى ولا يُطوى · وأيُّ فرضيّةٍ كذّبَها القياس.
6. ختمٌ: `PAYLOAD_BYTES` + `sha256`.

**وصفرُ دفعٍ — تقفُ عندَ التقريرِ وتنتظرُ أمرَ المالكِ بالنشر.**
