# تقريرُ كلود كود — الدخولُ أوّلًا + بابُ الضيف

**الشجرة:** `C:\Users\passe\projects\ustaz`
**الفرع:** `feat/login-first-20260827`
**التاريخ:** ٢٧ أغسطس ٢٠٢٦ (Asia/Kuwait)
**الحكم:** **نُفِّذَ الأمرُ كاملًا، والأرضيّةُ مطابقةٌ بصمةً: `SUITE 99/99 EXIT=0` و`recon PASS=190 WARN=1 FAIL=0`.**

---

## ١ · الرأسُ والفرعُ و`DIRTY`

| المقيس | القيمة |
|---|---|
| الرأسُ قبل | `170e040bf335ec44f18ea1ca8074c424900e6556` (على `main`) |
| الفرع | `feat/login-first-20260827` (مُنشَأٌ من `170e040`) |
| الرأسُ بعدَ إيداعِ التنفيذ | `50954808e5ea786523729322b84a108cb583c080` |
| `DIRTY` قبل | **٠** |
| `DIRTY` بعدَ الإيداعِ وبعدَ البطّاريّة | **٠** (`tree after: 0 dirty path(s)`) |
| الملفّاتُ المودَعةُ بأسمائِها | `app.jsx` · `app.js` · `sw.js` · `quest-bank-integrity-guard.cjs` · `theme-coverage-guard.cjs` · `guards/i18n-ui-guard.cjs` · `tools/delete-truth-measure.cjs` |

لم يُستعمَلْ `git add .` ألبتّة؛ سبعةُ ملفّاتٍ أُضيفَتْ بأسمائِها، ولا ملفَّ ثامنَ في الشجرة.
وهذا التقريرُ إيداعٌ توثيقيٌّ مستقلٌّ فوقَ إيداعِ التنفيذ.

---

## ٢ · رابطُ البريفيو

```
https://ustaz-5gk54jxl9-musaed-s-projects1.vercel.app
```

**الحالة:** `● Ready` · بيئةُ `Preview` · مبنيٌّ من `5095480`.
وقد تحقَّقتُ أنَّ البريفيو يحملُ البناءَ الجديدَ فعلًا لا القديم: `app.js` المخدومُ منه يحوي
`ezik_entry_v1` و`entry.guest` و`entry.guestWarn` و`entry.apple` و`ezikEntryAnswered`
و`ONBOARDING_DEFAULT_AGE`.

🔴 **البريفيو محميٌّ بـ SSO من فيرسل** — الرابطُ المجرَّدُ يردُّ `302` إلى `vercel.com/sso-api`
قبلَ أن يعملَ التطبيق. هذه حمايةٌ على مستوى المشروعِ لم أُغيِّرْها ولا أملكُ تغييرَها في هذه
الجولة. ففتحُه يحتاجُ أحدَ أمرين، وكلاهما بيدِك:

- أن تفتحَه في متصفِّحٍ أنتَ داخلٌ فيه بحسابِ فيرسل الذي يملكُ المشروع، أو
- أن تفتحَه مرّةً واحدةً برابطِ التجاوزِ الذي تملكُه:
  `<الرابط>/?x-vercel-protection-bypass=<سرُّك>&x-vercel-set-bypass-cookie=true`

ولم أكتبِ السرَّ في هذا التقريرِ ولا في أيِّ ملفٍّ من المستودَع — المستودَعُ عامٌّ على
GitHub، وكتابتُه فيه نشرٌ له. (طولُه ٣٢ وبصمتُه `26aed43d`، للتمييزِ لا غير.)

**وقد سِرتُ البريفيو بنفسي بكروم حقيقيٍّ (لا محاكاة DOM)** على مقاسِ هاتفٍ ٣٩٠×٨٤٤، وأدناه
ما قاسَه ذلك السير.

---

## ٣ · خطواتٌ تجرِّبُها بعينِك

> افتحِ الرابطَ أعلاه **في نافذةِ تصفُّحٍ خاصّةٍ** (أو امسحْ بياناتِ الموقعِ أوّلًا)،
> وإلّا فأنتَ زائرٌ عائدٌ وستقفزُ الشاشةُ عنكَ — وهذا هو المطلوبُ في البندِ الثامن، لا خللٌ.

1. **أوّلُ ما تراه شاشةُ دخول.** فيها «المتابعة مع Google» و«المتابعة مع Apple»،
   وتحتَهما جملةُ التنبيه، وتحتَها زرُّ **«متابعة كضيف»**. **لا يُسألُ اسمٌ ولا عمرٌ ولا شيءٌ
   آخرُ قبلَها.**
2. **اقرأِ السطرَ فوقَ زرِّ الضيف:** «في حال عدم التسجيل ستفقد وردك ومحادثاتك.» — هو لفظُك
   نفسُه، ظاهرٌ دائمًا، لا يُخفى خلفَ نقرة، ولا يُوقِفُ أحدًا.
3. **اضغطْ «متابعة كضيف» ضغطةً واحدة.** هي ضغطةٌ واحدةٌ لا اثنتان، وليس بينَها وبينَك
   نافذةُ تأكيدٍ ولا حائط.
4. **الآن تظهرُ شاشةُ الاسمِ والعمر.** تحتَها سطرٌ يقول «اختياريّةٌ كلُّها»، وزرُّ **«ابدأ»
   يعملُ وأنتَ لم تكتبْ شيئًا**، وتحتَه زرُّ **«تخطّي»** ظاهرٌ.
5. **اضغطْ «تخطّي».** يدخلُ التطبيقُ فورًا. الملفُّ المحفوظُ عمرُه **١٩** — بالغٌ فوقَ ١٨
   كما أمرت.
6. **جرِّبِ الاسمَ ثمّ التخطّي:** اكتبِ اسمَك ثمّ اضغطْ «تخطّي» — يُحفَظُ اسمُك. «تخطّي» تعني
   «لا تُلزِمْني بالإكمال»، لا «ارمِ ما كتبتُه».
7. **جرِّبْ سنةً خاطئة:** اكتبْ في خانةِ السنةِ `12` — يظهرُ سطرُ الخطأِ ويتعطَّلُ «ابدأ»
   وحدَه، و**«تخطّي» يبقى يعملُ**. لا حائطَ في أيِّ حال.
8. **أعدْ فتحَ الرابطِ في التبويبِ نفسِه.** لا تعودُ شاشةُ الدخول: أنتَ داخلٌ سلفًا.
9. **الميزاتُ بلا حساب:** من القائمةِ افتحِ المصحفَ والأذكارَ والفتاوى وأنتَ ضيف — تعملُ
   كما كانت. لم يُقفَلْ شيءٌ كان مفتوحًا.
10. **مسحُ البيانات** (خطوةٌ أطولُ، وهي كذلك من قبلِ هذه الجولة): «لوحة الأهل» — وهي تطلبُ
    رمزًا، فإن لم يكنْ لك رمزٌ أنشِئْ واحدًا — ثمّ «حذف كل البيانات». يعودُ الجهازُ إلى **فتحةٍ
    أولى حقيقيّة**، أي إلى شاشةِ الدخولِ نفسِها لا إلى نصفِها. ومحوُ المفتاحِ الجديدِ في هذا
    الزرِّ مبرهَنٌ ببوّابةِ `deletetruth` التي تُشغِّلُ `resetAll` الحقيقيّةَ وتقرأُ ما بقي؛
    وما لم أسِرْه بعينِي على البريفيو هو ضغطُ الزرِّ نفسِه، لأنَّه خلفَ رمزِ أهلٍ يُنشَأُ على
    الخادم.

**واحدةٌ لا تُجرَّبُ في المتصفِّح، وأقولُها صراحةً:** زرَّا Google وApple **معطَّلانِ في
المتصفِّح** ومكتوبٌ تحتَهما سببُ ذلك: «تسجيلُ الدخولِ يتِمُّ داخلَ التطبيق». والسببُ حقيقيٌّ:
`api/auth-return.js` يردُّ على مخطَّطِ `ezik://` الذي لا يستقبلُه إلّا الغلافُ الأصليّ، فزرٌّ
يبدو حيًّا في تبويبٍ زرٌّ لا يصلُ إلى شيء. أمّا في التطبيقِ نفسِه (وهو الذي يفتحُه مراجعُ
آبل) فالزرّانِ حيّانِ يفتحانِ صحيفةَ الدخول. ولم أبنِ لهما مسارَ ويبٍ جديدًا لأنَّ أمرَك يمنعُ
بناءَ دخولٍ جديدٍ صراحةً.

---

## ٤ · البوّابات

| المقيس | قبل (`170e040`) | بعد (`5095480`) |
|---|---|---|
| عددُ البوّابات | ٩٩ | **٩٩** |
| السويت | `SUITE: 99/99 EXIT=0` | **`SUITE: 99/99 EXIT=0`** |
| `recon` | `PASS=190 WARN=1 FAIL=0` | **`PASS=190 WARN=1 FAIL=0`** |
| `FAIL` | ٠ | **٠** |
| `WARN` | ١ | **١** |
| رمزُ الخروج | ٠ | **٠** |
| الشجرةُ بعد | `0 dirty` | **`0 dirty`** |

والـ`WARN` هو الموروثُ نفسُه حرفًا بحرف، لم يُصلَحْ ولم يَنمُ:

```
[WARN] LONGEST_CARD_CHARS = 3405 > longest card 3401 -> cap oversized/stale (re-derive in api/report.js)
```

**عددُ الفحوصِ لم ينقصْ داخلَ الحرّاسِ أيضًا** — وهي الأرضيّةُ الحقيقيّةُ كما نصَّ أمرُك. وقيسَ
عمودُ «قبل» بتشغيلِ الحرّاسِ فعلًا في شجرةٍ عاملةٍ عندَ `170e040`، لا باستنتاجٍ من الفارق:

| الحارس | قبل | بعد |
|---|---|---|
| `theme-coverage-guard.cjs` | ١٣٥١/١٣٥١ | **١٣٥١/١٣٥١** |
| `guards/i18n-ui-guard.cjs` | ٢٧٧/٢٧٧ | **٢٨٩/٢٨٩** (‏+١٢ فحصًا جديدًا) |
| `tools/delete-truth-measure.cjs` | ٢٨/٢٨ | **٢٨/٢٨** |
| `tools/auth-bridge-measure.cjs` | ٢٢ فحصًا · ٦ طفراتٍ مقتولة | **٢٢ · ٦** (بلا مساس) |
| `quest-bank-integrity-guard.cjs` | ٧٦/٧٦ | **٧٦/٧٦** |

**البوّاباتُ التي وُسِّعَتْ ولم تُخلَقْ** (البندُ ٣-٢): ثلاثةُ حرّاسٍ قائمين، وصفرُ بوّابةٍ جديدة،
وصفرُ سطرٍ في `gates.json`:

1. `tools/delete-truth-measure.cjs` — روسترُ المحو (البندُ ٣-١، أدناه).
2. `theme-coverage-guard.cjs` — دبّوسانِ في `Q4` تحرَّكا **لأنَّ أمرَك حرَّكَهما**:
   - `canStart` لم يعدْ يشترطُ اسمًا ولا اختيارَ مخاطبة — وهذا نصُّ البندِ الرابع. والدبّوسُ
     الجديدُ يُثبِّتُ القاعدةَ الجديدةَ كاملةً: السنةُ المكتوبةُ الخاطئةُ وحدَها تحجزُ الزرّ.
   - «الشاشةُ لا تكتبُ شيئًا بنفسِها» وُسِّعَتْ إلى «لا تكتبُ شيئًا إلّا قرارَ الدخول» ونصَّتْ
     على كتابةٍ واحدةٍ بالاسم. تركُها تقولُ «لا شيء» بينما تُكتَبُ كتابةٌ كذبٌ في حارس.
3. `guards/i18n-ui-guard.cjs` — مِشيةُ الفتحةِ الأولى صارت تمرُّ بالخطوةِ الجديدة، **وتقيسُ
   شروطَ أمرِك على تركيبٍ حقيقيٍّ**: لا سؤالَ قبلَها، بابُ الضيفِ حيٌّ وضغطةٌ واحدة، التنبيهُ
   بجانبِه، والبابانِ مرسومانِ ومحجوزانِ في التبويبِ مع ذكرِ السبب.

---

## ٥ · `CORE_BYTES`

| المقيس | قبل | بعد |
|---|---|---|
| `app.js` | ‏١٬٢٠٥٬٩٧٥ | **‏١٬٢١٦٬٨٦٨** (‏+١٠٬٨٩٣) |
| `CORE_BYTES` في `sw.js` | ‏**٢٬٠٤٨٬٨٢٢** | **‏٢٬٠٥٩٬٧١٥** (‏+١٠٬٨٩٣) |

والفارقانِ متساويانِ بالضبط، وهو ما يقولُه تعليقُ الختمِ نفسُه.

قُطِعَ بـ`node tools/core-bytes.cjs --write` كما أذِنتَ صراحةً، **ومعه ثلاثةُ مواضعَ أخرى في
الإيداعِ نفسِه** وإلّا حمُرَتْ بوّابةُ `bankintegrity` بـ`[B10] sw.js MOVED`:

1. جدولُ النثرِ في `sw.js` (`+ app.js <n> +`) — تابع.
2. مرآةُ `SW_PROSE` في `quest-bank-integrity-guard.cjs` (`{ n: <n>, of: 'app.js' }`) — تابعت.
3. ختمُ `SEALED['sw.js']` — أُعيدَ قطعُه **أخيرًا** بعدَ الثلاثةِ جميعًا:
   `fadfe252…` ← `a7026814…`. و`sw.js` يحملُ صفرَ بايتِ `CR` كما يشترطُ الختم.

⚠️ **ملاحظةٌ أذكرُها ولا أُقرِّرُها:** النثرُ فوقَ `CORE_BYTES` في `sw.js` يحجزُ هذا القطعَ
لجولةِ الدمجِ وحدَها («البندُ ١١٢ يملكُه»). أمرُك أذِنَ لي به صراحةً، فنفَّذتُ إذنَك؛ وإن رأيتَ
الحجزَ أولى فالقطعُ معزولٌ في هذا الإيداعِ ويمكنُ إسقاطُه وحدَه.

---

## ٦ · مفاتيحُ التخزينِ الجديدةُ وموضعُها من روسترِ المحو

**مفتاحٌ واحدٌ لا غير:**

| المفتاح | القيمة | يُكتَبُ متى |
|---|---|---|
| `ezik_entry_v1` | `'guest'` أو `'account'` | لحظةَ الإجابةِ على شاشةِ الدخول، بأيِّ بابٍ من البابين |

**وموضعُه من الروستر — الملفّانِ معًا في الإيداعِ نفسِه، بلا استثناء:**

1. `app.jsx` · `resetAll` — `localStorage.removeItem(ENTRY_CHOICE_KEY)` بجانبِ مفتاحِ الجلسة.
2. `tools/delete-truth-measure.cjs` · **`MUST_GO_ALREADY`** — مصنَّفٌ «يُمحى، ويجبُ أن يُمحى».

ولماذا `MUST_GO_ALREADY` لا `MUST_GO_NEW`؟ بقاعدةِ الملفِّ نفسِه: `MUST_GO_NEW` يربطُ كلَّ
محوٍ **ببندٍ من `delete.html`**، وهذا المفتاحُ لا تذكرُه تلك الصفحةُ بأيِّ لغة، فالاستشهادُ
ببندٍ لا وجودَ له كذب. ولمْ أُعدِّلْ `delete.html` حرفًا.

ولماذا يُمحى أصلًا وقد بقِيَ `PRAYER_NOTIFY_KEY`؟ لأنَّهما ليسا حالةً واحدة: ذاك **إذنٌ**
مُنِحَ للنظام، ومحوُه يتركُ الإذنَ قائمًا والمفتاحَ مطفأً بلا سطرٍ يُحذِّر. وهذا **جوابٌ عن
سؤال**، وجهازٌ مُحِيَتْ بياناتُه يجبُ أن يُعيدَ السؤالَ لا أن يُورِّثَ صاحبَه الجديدَ جوابَ
غيرِه. وهو كذلك حرفُ قاعدتِك المكتوبةِ في الملفِّ ذاتِه: «كلُّ مفتاحِ جهازٍ جديدٍ يُدخَلُ في
روسترِ الحذفِ ويُمحى معه».

**وصفرُ مفتاحٍ آخرَ أُضيف.** والحارسُ `authbridge` يُثبِّتُ أنَّ مفتاحَ الجلسةِ ما زال المفتاحَ
الوحيدَ الذي يلمسُه ذلك الوصل.

---

## ٧ · صفرُ دمجٍ وصفرُ نشرِ إنتاج — تأكيدًا

- **لم يقعْ دمجٌ.** `main` ما زال عندَ `170e040`، ولم يُلمَسْ.
- **لم يقعْ نشرُ إنتاج.** ولا أمرَ `vercel --prod` ولا `vercel deploy` صدرَ منّي. النشرةُ
  الوحيدةُ الجديدةُ بيئتُها `Preview`، أنشأَها دفعُ الفرعِ الذي أذِنتَ به وحدَه.
- **لم يُلمَسْ `murabbi-shell`** ولا أيُّ شجرةٍ أخرى. كلُّ تعديلٍ داخلَ `C:\Users\passe\projects\ustaz`.
- **صفرُ بناءٍ أصليٍّ وصفرُ `eas` وصفرُ رفعٍ لمتجر.** التغييرُ ويبٌ خالصٌ في `app.jsx`
  (و`app.js` مُشتَقُّه، و`sw.js` مِن قطعِ `CORE_BYTES`).
- **النصُّ الشرعيُّ لم يُمَسّ:** لا `adhkar.json` ولا `quran-uthmani.json` ولا متونُهما.

---

## ٨ · ما وجدتُه مخالفًا لظاهرِ الأمر — أذكرُه ولا أُخفيه

**١. «الزرّانِ موجودانِ حيّانِ سلفًا» — نصفُ ذلك كان صحيحًا.**
الذي كان موجودًا زرٌّ **واحدٌ** لا اثنان: «تسجيل الدخول بحساب Google»، **داخلَ الإعدادات**،
و**لا يُرسَمُ في المتصفِّحِ ألبتّة** (`if (!bridge) return null;`). أمّا Apple فكان موجودًا
**في الخادمِ وحدَه**: `lib/auth/oidc.js` يحملُ جدولَ `PROVIDERS` بصفَّينِ منذُ شحنِ الوصل،
و`api/auth-start.js` يقرأُ الاسمَ من الطلبِ لا من فرع. فالصفحةُ كانت الطرفَ الوحيدَ الذي
يعرفُ اسمًا واحدًا.

فما فعلتُه ليس بناءَ دخولٍ جديد، بل **قراءةَ بياناتٍ كانت عندَ الخادمِ ولم تقرأْها الصفحة**:
صارَ المزوِّدُ وسيطًا في `ezikAuthStartUrl`، وقيمتُه الغائبةُ ما زالت Google، فنداءُ الإعداداتِ
ذو الوسيطَينِ لم يتغيَّرْ حرفًا. وكلُّ ضغطةٍ في الشاشةِ الجديدةِ تمرُّ بدوالِّ الوصلِ نفسِها.

**٢. صفُّ الإعداداتِ لم أنقلْه ولم أُعِدْ استعمالَه، عمدًا.**
حارسُ `authbridge` يُثبِّتُ أنَّ `EzikSignInRow` يُرسَمُ **من `SettingsSheet` وحدَها ومن
لا أحدٍ غيرِها**، وأنَّ طفرتَه السادسةَ تموتُ إذا رُسِمَ خارجَ الغلاف. فبناءُ الشاشةِ الجديدةِ
على دوالِّ الوصلِ لا على ذلك الصفِّ هو ما أبقى الحارسَ حيًّا يستطيعُ أن يحمرَّ.
ولهذا تكرَّرَ سُلَّمُ المعالجةِ مرّةً واحدةً: الطفرةُ `م٤` تجدُ سطرَ مقارنةِ حالةِ العميلِ
**بنصِّه بعينِه** وتشترطُ تفرُّدَه، فرفعُه إلى دالّةٍ مشتركةٍ كان سيُعطِّلُ برهانًا حيًّا.
التكرارُ ثمنُ البرهان، مدفوعٌ عن قصدٍ ومكتوبٌ سببُه في مكانِه.

**٣. مبدِّلُ اللغةِ باقٍ على شاشةِ الدخول.**
البندُ الخامسُ يقول «ولا سؤالٌ آخرُ قبلَه». ومبدِّلُ اللغةِ **ليس سؤالًا** — لا يُطالِبُ بجوابٍ
ولا يحجزُ الشاشة — وهو الوحيدُ الذي بغيابِه لا يستطيعُ قارئٌ إنجليزيٌّ أن **يقرأَ** شاشةَ
الدخولِ نفسَها ليختار. فأبقيتُه، ورفعتُه **فوقَ الخطوتينِ معًا** لا داخلَ إحداهما. أذكرُه
لتنقُضَه إن رأيتَه سؤالًا.

**٤. لم أُنشئْ شاشةً جديدةً، ولم أُنشئْ بوّابةً جديدة.**
`screen === 'onboarding'` ما زال سطرًا واحدًا يوجِّهُ إلى `Onboarding`. الشاشةُ اكتسبتْ
خطوةً في وجهِها؛ لم تنقسمْ إلى مكوِّنَين. فلا سطرَ في روسترِ `themecoverage`، ولا سطرَ في
`EZIK-THEME-33-HANDOFF.md`، ولا جدولَ رجوعٍ تحرَّك.

**٥. العائدونَ على `ezik.app` اليومَ لا يرونَ هذه الشاشةَ أبدًا.**
`ezikEntryAnswered()` يقرأُ **الملفَّ والجلسةَ** بجانبِ المفتاحِ الجديد. ومن عندَه ملفٌّ فقد
أجابَ بالاستعمالِ منذُ زمن، فلا يُنصَبُ له حائطُ دخولٍ صبيحةَ الشحن. وهذا هو البندُ الثامنُ
مقاسًا لا مأمولًا.

**٦. ما كان يعملُ بلا حسابٍ يبقى يعملُ — مقاسًا لا موعودًا.**
`readAuthSession()` لا تُستدعى في `app.jsx` إلّا في موضعَين: `ezikEntryAnswered` وصفُّ
الإعدادات. **لا ميزةَ واحدةَ في الملفِّ كلِّه مشروطةٌ بجلسة**، فاختيارُ الضيفِ لا يُقفِلُ شيئًا.

---

## ٩ · ما قاسَه السيرُ الحقيقيُّ على البريفيو

كروم بلا واجهة، ٣٩٠×٨٤٤، على الرابطِ الحيِّ أعلاه — لا محاكاةَ DOM:

| اللحظة | المقيس | النتيجة |
|---|---|---|
| جهازٌ نظيف | خاناتُ الإدخالِ المعروضة | **٠** — لا اسمَ ولا عمرَ قبلَ الدخول |
| | الأزرارُ المرسومة | «العربية» · «المتابعة مع Google» (محجوز) · «المتابعة مع Apple» (محجوز) · **«متابعة كضيف» (حيّ)** |
| | جملةُ التنبيه | مرسومةٌ بلفظِها: «في حال عدم التسجيل ستفقد وردك ومحادثاتك.» |
| | البابُ كلُّه فوقَ الطيّ | نعم — بلا تمرير |
| ضغطةٌ واحدةٌ على الضيف | `ezik_entry_v1` | `"guest"` |
| | `child_profile` | `null` — لم يُصنعْ ملفٌّ بعد |
| | ما ظهر | خانتانِ + «ابدأ» + **«تخطّي»** |
| ضغطةُ «تخطّي» | `child_profile` | `{"name":"","age":19,"gender":null,"birthYear":2007,…}` |
| | الشاشة | غادرتِ التهيئةَ ودخلَ التطبيق |

`age: 19` بالغٌ فوقَ ١٨ كما أمرت، و`birthYear: 2007` مخزَّنٌ لا العمرُ، فالحسابُ **يكبرُ
بنفسِه** كلَّ سنةٍ بدلَ أن يتجمَّدَ على رقم.
