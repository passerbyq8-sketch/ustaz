# أمرُ عمل — ٤٣-ب · ٤٧-ب: شاشةُ إعداداتِ التذكير (ويبٌ بلا بناءِ غلاف)

**الشجرة:** `C:\Users\passe\projects\ustaz` · **الأساس:** `main = 8665d66` (بعدَ نشرِ الأذكار).
**فرعٌ جديد:** `feat/reminder-settings-20260826` · **و`main` لا يُمَسُّ في هذا الأمر.**
**النتيجةُ بريفيو** — يراهُ المالكُ بعينِه، **والدمجُ والنشرُ بأمرٍ لاحقٍ لا بهذا الأمر.**

**نصُّ البندِ في سجلِّ المالكِ حرفًا:** «تذكيرُ الأذكارِ والوِردِ والمحتوى اليوميّ — **القارئُ يضبطُ الوقتَ وعددَ المرّاتِ بنفسِه** · الأنبوبُ جاهزٌ ⟹ **شاشةُ إعداداتٍ تنزلُ فورًا بلا بناء** · **ويركبُها تصحيحُ اسمِ زرِّ «حذف كل البيانات» في نصِّ تأكيدِ حذفِ الحساب**».

---

## ٠ · قياسٌ أوّلًا — ولا تكتبْ سطرًا قبلَ إتمامِه

🔴 **القاعدةُ المقيسةُ أمس: موضعُ المكوِّنِ يُقاسُ ولا يُظَنّ** (أمرٌ سابقٌ قالَ «`SettingsSheet` في `index.html`» والصوابُ `app.jsx`).

1. **الأنبوب:** أينَ يُجدوَلُ التذكيرُ اليومَ في `app.jsx`؟ إشعارُ متصفّحٍ · مؤقّتٌ داخلَ الجلسةِ · أم جسرُ الغلاف؟ **اطبعْ أسماءَ الدوالِّ وأسطرَها**، وقلْ **ما الذي يصمدُ والتطبيقُ مغلق** وما الذي لا يصمد.
2. **الإعدادات:** أينَ يسكنُ `SettingsSheet` وكيفَ تُضافُ إليه قِطعةٌ جديدةٌ بالطريقةِ القائمة؟
3. **المخزن:** ما مفاتيحُ التذكيرِ الموجودةُ اليومَ إن وُجِدَتْ، وما شكلُها؟
4. **الأذكار:** بعدَ نشرِ اليومِ صارَ للصباحِ بابٌ وللمساءِ بابٌ (`adhkar_sabah` · `adhkar_masaa`) — **فالتذكيرانِ يشيرانِ إليهما لا إلى المجموعةِ ٢٧.**
5. ⛔ **وإن ثبتَ أنَّ الأنبوبَ لا يصمدُ والتطبيقُ مغلقٌ في المتصفّحِ: لا تبنِ أنبوبًا جديدًا ولا تَعِدِ القارئَ بما لا يقع.** ابنِ الشاشةَ وخزِّنِ الاختيارَ، **وقلْ في التقريرِ صراحةً ما الذي يعملُ بلا الغلافِ وما الذي ينتظرُه** — الوعدُ الكاذبُ في شاشةِ عبادةٍ أسوأُ من ميزةٍ متأخّرة.

---

## ١ · العمل — شاشةُ إعداداتِ التذكير

**قِطعةٌ داخلَ الإعداداتِ القائمةِ** فيها **أربعةُ تذكيراتٍ مستقلّة**، كلُّ واحدٍ بمفتاحِ تشغيلٍ ووقتٍ يختارُه القارئ:

| التذكير | يفتحُ على |
|---|---|
| **أذكارُ الصباح** | بابُ `adhkar_sabah` |
| **أذكارُ المساء** | بابُ `adhkar_masaa` |
| **الوِردُ اليوميّ** | شاشةُ الوِردِ القائمة |
| **المحتوى اليوميّ** | ما يفتحُه الأنبوبُ اليومَ |

**وعددُ المرّاتِ في اليومِ** يضبطُه القارئُ حيثُ يصحُّ (الوِردُ والمحتوى) — **والأذكارُ وقتٌ واحدٌ لكلِّ بابٍ لا عدد**.

**الافتراضُ عندَ أوّلِ فتحٍ: كلُّ التذكيراتِ مطفأة** — لا يُشتعِلُ شيءٌ في جهازِ قارئٍ بلا اختيارِه.
**والثيمُ:** صفرُ لونٍ جديدٍ وصفرُ خطٍّ جديد — رموزُ الثيمِ القائمةُ وحدَها.

---

## ٢ · ثلاثةٌ صغارٌ تركبُ هذه الجولةَ مجّانًا

1. 🔴 **اسمُ الزرِّ في نصِّ التأكيد:** النصُّ يحيلُ إلى «احذفْ بياناتي» **واسمُ الزرِّ على الشاشةِ «حذف كل البيانات وإعادة البدء»** — يُوحَّدُ النصُّ على ما يقرأُه القارئُ فعلًا.
2. 🔴 **لافتةٌ كاذبةٌ** في `app.jsx:6651`: `THE GROUP DOORS -- ?adhkargroups=1, AND IT IS OFF FOR EVERYONE` — **والمفتاحُ مرفوعٌ للجميعِ منذُ `8665d66`**. تُصحَّحُ لتقولَ الواقع.
3. **تقريرُ الجولةِ الماضيةِ** `EZIK-CC-ADHKAR-SHIP-REPORT-2026-08-26.md` **يُضَمُّ إلى هذا الفرعِ** ليصلَ `main` مع الدمجِ القادم.

⛔ **وشريطُ «نسخةٌ جديدةٌ جاهزة» ليسَ من هذه الجولة** — لم يأذنْ به المالكُ بعد.

---

## ٣ · الحدود

1. **الملفّاتُ المسموحة:** `app.jsx` وما يوجبُه دفترُ إعادةِ البناءِ تبعًا له (`app.js` · `CORE_BYTES` · جدولُ `sw.js` · مرآةُ الحارس — **والبصمةُ آخِرًا**). وكلُّ ملفٍّ خارجَها **يُسمّى في التقريرِ ويُعلَّلُ بالأثر**.
2. 🔴 **أيُّ مفتاحِ تخزينٍ جديدٍ في الجهازِ يُسجَّلُ في روسترِ «احذفْ بياناتي» ويُمحى معه** — كما فُعِلَ بـ`adhkar_place_v1`. **مفتاحٌ لا يُمحى = بوّابةُ `deletetruth` حمراءُ بحقّ.**
3. **بوّابةٌ تُوسَّعُ ولا تُخلَق** — `gates.json` يبقى **٩٩**.
4. **`adhkar.json` و`adhkar-split-27.json` لا تُمَسُّ منهما بايتة.**
5. **تشغيلةٌ كاملةٌ خضراءُ** قبلَ الدفعِ · `recon` بلا `[FAIL]` بالأقواس · **ولا `git add .`**.
6. **ادفعِ الفرعَ وحدَه** للبريفيو · ⛔ **لا دمجَ · لا نشرَ للإنتاجِ · لا لمسَ `main`.**

---

## ٤ · التقرير

`EZIK-CC-REMINDER-SETTINGS-REPORT-2026-08-26.md` في جذرِ الشجرة:

- **جوابُ §٠ كاملًا** — وأصدقُ سطرٍ فيه: **ما الذي يعملُ اليومَ بلا الغلافِ وما الذي ينتظرُ البناء**.
- **مفاتيحُ الجهازِ الجديدةُ بالاسم** وبرهانُ محوِها مع «احذفْ بياناتي» — **بتجربةٍ مقيسةٍ لا بدعوى**.
- **البابانِ الجديدانِ**: برهانُ أنّ تذكيرَ الصباحِ يفتحُ `adhkar_sabah` وتذكيرَ المساءِ يفتحُ `adhkar_masaa`.
- **الثلاثةُ الصغارُ**: نصُّ الزرِّ بعدَ التوحيدِ · اللافتةُ بعدَ التصحيحِ · وإيداعُ التقرير.
- **عددُ البوّابات (٩٩)** ونتيجةُ التشغيلةِ بالأرقام · **رابطُ البريفيو** · **وما لم يُنفَّذْ ولماذا**.
