# EZIK — تقريرُ ٤٣-ب · ٤٧-ب: شاشةُ إعداداتِ التذكير

**٢٧ أغسطس ٢٠٢٦ · الشجرة `C:\Users\passe\projects\ustaz` · الفرع `feat/reminder-settings-20260826`**
**الأساس `main = 8665d66`** — **و`main` لم يُمَسّ، ولا دمجَ، ولا نشرَ للإنتاج.**

---

## ٠ · الحصيلةُ في ثلاثةِ أسطر

**الشاشةُ نزلتْ.** أربعةُ تذكيراتٍ مستقلّةٍ داخلَ الإعداداتِ القائمة، كلٌّ بمفتاحِه ووقتِه، **وكلُّها مطفأةٌ عندَ أوّلِ فتحٍ** — مقيسًا على البريفيو نفسِه (§٣).

🔴 **وأصدقُ سطرٍ في هذا التقرير:** الأنبوبُ **لا يرنُّ في المتصفّحِ ألبتّة** — لا والتطبيقُ مفتوحٌ ولا مغلق. **الذي يصمدُ والتطبيقُ مغلقٌ هو غلافُ `murabbi-shell` وحدَه**، وهو حيٌّ على المتجرَين. فما شُحِنَ اليومَ: **الشاشةُ والمخزنُ والحمولةُ** — والرنينُ في التطبيقِ من المتجرِ لا في الويب (§١-٢ · §٣-٦).

**والبوّاباتُ ٩٩/٩٩ خضراءُ** · `recon` **PASS=190 WARN=1 FAIL=0** بلا `[FAIL]` · **شجرةٌ نظيفة** · **و`gates.json` لم يُمَسّ** (§٦).

---

## ١ · جوابُ §٠ كاملًا — قياسٌ قبلَ سطرٍ واحد

### ١-١ · الأنبوب: أينَ يُجدوَلُ التذكيرُ اليوم

**ليسَ إشعارَ متصفّحٍ ولا مؤقّتًا داخلَ الجلسة. هو جسرُ الغلافِ وحدَه.** بأسمائِه وأسطرِه (أرقامُ الأسطرِ قبلَ تعديلِ اليوم):

| الدالّة | السطر | ما تفعلُه |
|---|---|---|
| `ezikSchedBridge()` | `app.jsx:15044` | **الاختبارُ كلُّه**: `window.ReactNativeWebView`. لا `navigator.userAgent` ولا تخمين. |
| `ezikSchedPayload(items, nowMs)` | `:15076` | يبني الحمولةَ **بقائمةِ سماحٍ حقلًا حقلًا**، ويُسقِطُ الماضيَ ويَعُدُّه. |
| `ezikSchedSend(items, nowMs)` | `:15126` | يُرسِلُ — **أو يقولُ لماذا لم يُرسِلْ**: `no-shell` · `unchanged` · `bridge-refused`. |
| `ezikSchedItems()` | `:15551` | العلفُ. **كانَ يردُّ الأذانَ وحدَه خلفَ مفتاحِ الصلاة.** |
| `ezikSchedArm()` | `:15568` | إعادةُ بناءٍ ذرّيّةٌ كاملة، لا فرقٌ ولا إضافة. |
| `useEzikSchedRoot()` | `:15589` | **سِتَّةُ مشغّلات**: فتحُ الصفحة · عودةُ التطبيق (`rearm-request` + `pageshow`/`focus`/`visibilitychange`) · كتابةُ تبويبٍ آخرَ (`storage`) · تغيُّرُ اللسان · **ومنتصفُ الليلِ المحلّيّ** (مؤقّتٌ يعيدُ ضبطَ نفسِه). |
| `ezikNotifyAsk()` | `:15414` | **العمليّةُ الوحيدةُ التي ترفعُ نافذةَ إذنِ النِّظام** — بموضعِ نداءٍ واحدٍ تحرسُه البوّابة. |

**وعقدُ الطرفِ الآخرِ قُرِئَ من مصدرِه** لا من الذاكرة — `murabbi-shell/src/scheduler/core.js`:

```
TYPES        = ['adhan', 'daily', 'adhkar']     ← مجمّدةٌ ثلاثة، والرابعُ يُرفَضُ unknownType
WINDOW_DAYS  = 7
MAX_SCHEDULED= 64 - 4 = 60                       ← والقصُّ للأبعدِ زمنًا لا للأقرب
buildOpenPayload() → { op:'open', route, type, id }   ← الغلافُ يعيدُ الوجهةَ عندَ الضغط
```

### ١-٢ · ⛔ ما الذي يصمدُ والتطبيقُ مغلق — والجوابُ نصفانِ لا نصفٌ واحد

| | **متصفّحٌ (بريفيو · `ezik.app` في المتصفّح)** | **تطبيقُ عزك من المتجرَين** |
|---|---|---|
| `window.ReactNativeWebView` | **غيرُ موجود** — مقيسٌ: `undefined` (§٣-٦) | موجود |
| جوابُ `ezikSchedSend` | **`{ sent:false, reason:'no-shell' }`** | `{ sent:true }` |
| رنينٌ والتطبيقُ **مفتوح** | **لا** | نعم |
| رنينٌ والتطبيقُ **مغلقٌ أو الجهازُ أُعيدَ تشغيلُه** | **لا** | **نعم** — `expo-notifications` |

**فبنيتُ الشاشةَ وخزَّنتُ الاختيارَ ولم أبنِ أنبوبًا ثانيًا.** ولا `Notification` ولا `showNotification` ولا مؤقّتَ جلسةٍ في هذه الجولة — وهو نصُّ ⛔ §٠-٥.
**والقارئُ يُقرَأُ عليه ذلك في الشاشةِ نفسِها**، سطرًا ظاهرًا لا حاشيةً في تقرير (§٣-٦).

### ١-٣ · الإعدادات: أينَ تسكنُ وكيفَ تُضافُ إليها قطعة

🔴 **`SettingsSheet` في `app.jsx:16716` — لا في `index.html`.** (وهذه هي القاعدةُ المقيسةُ أمس، طُبِّقتْ ولم تُخالَفْ.)
والطريقةُ القائمةُ: `<EzShellGroup title hint>` — ثمانِ مجموعاتٍ قائمةٍ داخلَ `<div className="ezsh-grid">`. **فأُضيفتْ مجموعةٌ تاسعةٌ بالطريقةِ نفسِها**، بعدَ `<EzikSignInRow />` وقبلَ تفضيلاتِ الصلاة، **فلم يتحرّكْ موضعٌ واحدٌ تعلَّمَه القارئ**.

### ١-٤ · المخزن: ما مفاتيحُ التذكيرِ الموجودةُ اليوم

**واحدٌ لا غير:** `ezik_prayer_notify_v1` — **قيمةٌ واحدةٌ تعني «مشتعل» (`'on'`)، وكلُّ ما عداها مطفأ**. ولا مفتاحَ وقتٍ في الشجرةِ كلِّها، **وطبقةُ الوِردِ تقولُ في كلماتِها لماذا**:

> «There is no time in it… A dropdown that asked for a time while nothing in the app can ring is a PROMISE THE APP CANNOT KEEP… The time field is born on the day notifications ship and not one hour before it.» — `app.jsx:18151`

**وذلك اليومُ هو اليوم** — لأنّ البندَ ٦٧ شحنَ المحرّكَ وهذه الجولةُ تشحنُ الشاشة. **وحارسُ الوِردِ كانَ يحرسُ تلكَ الجملةَ بمنعِ `type="time"` في الملفِّ كلِّه**، فوُسِّعَ لا أُضعِفَ (§٥-٢).

### ١-٥ · الأذكار: البابانِ لا المجموعةُ ٢٧

`adhkar-split-27.json` يعلنُ بابَين: **`adhkar_sabah` (٢٣)** و**`adhkar_masaa` (٢١)**.
**فالتذكيرانِ يحملانِ مفتاحَي البابَينِ وجهةً (`route`)، ولا يذكرانِ ٢٧ بحرف** — مقيسٌ بالحمولةِ الحيّةِ في §٣-٢، وبحالةٍ في البوّابةِ تحمرُّ إن ظهرَ «27» في وجهةِ أيِّ تذكير.

---

## ٢ · العمل — أربعةُ تذكيراتٍ، مفتاحٌ ووقتٌ لكلٍّ

| التذكير | النوعُ الذي يقبلُه الغلاف | الوجهة | الافتراض | عددُ المرّات |
|---|---|---|---|---|
| **أذكارُ الصباح** | `adhkar` | **`adhkar_sabah`** | مطفأ · `06:00` | **وقتٌ واحدٌ لا عدد** |
| **أذكارُ المساء** | `adhkar` | **`adhkar_masaa`** | مطفأ · `17:00` | **وقتٌ واحدٌ لا عدد** |
| **الوِردُ اليوميّ** | `daily` | `mushaf` | مطفأ · `20:00` | **١ – ٣** |
| **المحتوى اليوميّ** | `daily` | `home` | مطفأ · `09:00` | **١ – ٣** |

**والوجهتانِ الأخيرتانِ اسمُ شاشةٍ لا اسمُ باب:** لوحةُ الوِردِ تسكنُ شاشةَ المصحف، ومحتوى اليومِ (آيةُ اليومِ · حديثُ اليوم) يسكنُ الرئيسيّة. **ولا وجهةَ منها تخترعُ شاشةً غيرَ قائمة.**

**الثيم:** **صفرُ لونٍ جديدٍ وصفرُ خطٍّ جديد.** خمسةُ مفاتيحِ نمطٍ أُضيفتْ، مبنيّةٌ كلُّها من رموزِ الثيمِ القائمةِ وحدَها — `var(--line)` · `var(--tint)` · `var(--ink)` · `var(--muted)` — ولا قيمةَ لونٍ حرفيّةً واحدة.

---

## ٣ · القياسُ الحيُّ — على البريفيو نفسِه، بمتصفّحٍ حقيقيّ

**الطريقة:** Chrome بلا رأسٍ عبرَ CDP على النشرةِ نفسِها، **بلا لمسِ سطرٍ من الصفحة**. الطرفُ الآخرُ وحدَه هو المُصطَنَع: `window.ReactNativeWebView` يجمعُ ما يُرسَل، **ويجيبُ `enable` بـ`result{inReplyTo:'enable'}` تمامًا كما يفعلُ الغلافُ في `core.js`**. والملفّاتُ الحيّةُ طوبِقتْ بالبايتِ أوّلًا:

| الملفّ | حيًّا | على القرص | |
|---|---|---|---|
| `/app.js` | `1193820B` `0a64a704` | `1193820B` `0a64a704` | **مطابق** |
| `/sw.js` | `44695B` `c6f6d26e` | `44695B` `c6f6d26e` | **مطابق** |
| `/adhkar-split-27.json` | `7182B` `56c0fc1a` | `7182B` `56c0fc1a` | **مطابق** |
| `/index.html` | `123047B` | `122884B` | يختلفُ بـ**١٦٣ بايتًا** فقط: وسمُ `vercel.live/feedback` المحقونُ **بعدَ `</html>`** في البريفيو وحدَه. وكلُّ بايتةٍ قبلَه مطابقة. |

### ٣-١ · أوّلُ فتحٍ — كلُّ شيءٍ مطفأ

```
switches = ["sabah:false","masaa:false","wird:false","daily:false"]
labels   = ["أذكارُ الصباح","أذكارُ المساء","الوِردُ اليوميّ","المحتوى اليوميّ"]
حقولُ الوقتِ المرسومةُ والمفاتيحُ مطفأة = 0
ezik_reminders_v1 قبلَ أيِّ ضغطة        = null
حمولاتٌ أُرسِلَتْ بمجرّدِ فتحِ الشاشة      = 0
```

**فلا مفتاحَ يُكتَبُ ولا رسالةَ تُرسَلُ لقارئٍ فتحَ الشاشةَ ولم يلمسْ شيئًا.**

### ٣-٢ · 🔴 برهانُ البابَين — البابانِ الجديدانِ بالوجهةِ لا بالدعوى

بعدَ ضغطتَينِ اثنتَينِ على مفتاحَي الأذكار:

```
ezik_reminders_v1 = {"sabah":{"on":true,"times":["06:00"]},
                     "masaa":{"on":true,"times":["17:00"]},
                     "wird" :{"on":false,"times":["20:00"]},
                     "daily":{"on":false,"times":["09:00"]}}

على السلك:  channel=ezik-scheduler  v=1  op=schedule  items=13
             adhkar → adhkar_masaa   × 7
             adhkar → adhkar_sabah   × 6      ← ٦ لا ٧: صباحُ اليومِ مضى، والأنبوبُ يُسقِطُ الماضي
```

**وأوّلُ عنصرٍ حرفًا كما عبرَ الجسر:**

```json
{"id":"adhkar:masaa:17:00:2026-08-27","type":"adhkar","at":1787839200000,
 "title":"أذكارُ المساء","body":"وقتُ أذكارِ المساء.","route":"adhkar_masaa"}
```

**وعنصرُ الصباحِ بعدَ أن حرّكَ القارئُ الوقتَ إلى `05:45`:**

```json
{"id":"adhkar:sabah:05:45:2026-08-28","type":"adhkar","at":1787885100000,
 "title":"أذكارُ الصباح","body":"وقتُ أذكارِ الصباح.","route":"adhkar_sabah"}
```
> **ويقعُ على الساعةِ `5:45` بالتوقيتِ المحلّيّ** — مقروءًا من `at` نفسِه بعدَ عبورِه.

**فتذكيرُ الصباحِ يشيرُ إلى `adhkar_sabah` وتذكيرُ المساءِ إلى `adhkar_masaa`، ولا يذكرُ أيٌّ منهما ٢٧.**

### ٣-٣ · ⚠️ وحدُّ هذا البرهانِ مسمًّى: الوجهةُ **تُحمَلُ** ولا **تُستَهلَك** بعدُ

الغلافُ يخزّنُ الوجهةَ في الإشعارِ ويعيدُها عندَ الضغطِ رسالةً `op:'open'` (`buildOpenPayload`، وهي موصولةٌ فعلًا في `murabbi-shell/src/SiteScreen.js:264`). **وهذا العميلُ لا يسجّلُ مستمِعًا لتلكَ الرسالةِ بعد** — والسببُ مكتوبٌ في الملفِّ نفسِه: **البندُ ٩٧ ألغى الرابطَ العميقَ إلى الأذكارِ مع الملاحةِ التي غذَّتْه** (`app.jsx:7055`).

**فاليومَ:** الضغطةُ تفتحُ التطبيقَ كما هو، والوجهةُ محمولةٌ في الحمولةِ محفوظةٌ عندَ الطرفِ الآخر. **وأمرُك يقولُ «يشيرانِ إليهما» (§٠-٤) وذلك واقعٌ ومقيسٌ.** أمّا أن **تُفتَحَ** الشاشةُ بالضغطةِ فيقتضي إعادةَ فتحِ رابطٍ عميقٍ ألغاهُ قرارٌ مسجَّل، **ولم يأذنْ به هذا الأمر** — فتُرِكَ، وهو بندٌ في §٧.

### ٣-٤ · العدد — حيثُ يصحُّ وحدَه

```
الوِردُ مشتعلٌ والعددُ رُفِعَ مرّتَين:
  ezik_reminders_v1.wird = {"on":true,"times":["20:00","21:00","22:00"]}
  حقولُ الوقتِ المرسومة = 5      (١ صباح + ١ مساء + ٣ وِرد)
  على السلك = 34 عنصرًا:  adhkar_masaa ×7 · adhkar_sabah ×6 · mushaf ×21
```

**والأذكارُ لا تعرضُ عدّادًا أصلًا** — لا زرَّ `+` ولا `−` في صفَّيهما، وهو نصُّ §١.

### ٣-٥ · إطفاءُ واحدٍ لا يُطفئُ القناة

```
المساءُ أُطفئ:  op=schedule   items=27   {"mushaf":21,"adhkar_sabah":6}
كلُّ العمليّاتِ المُرسَلةِ في الجلسةِ كلِّها = ["enable","schedule"]     ← ولا "cancel" واحدة
```

**فالوِردُ وأذكارُ الصباحِ نجَوا**، والمساءُ وحدَه غادرَ السلك. `ezikNotifyStop()` **لا يُنادى من هذه الشاشةِ ألبتّة** — فإطفاءُ أذكارِ المساءِ لا يُنهي تذكيراتِ الصلاةِ لقارئٍ لم يطلبْ ذلك.

### ٣-٦ · 🔴 المتصفّحُ بلا غلافٍ — الجولةُ الثانيةُ من القياس، بلا جسرٍ مصطنَع

```
window.ReactNativeWebView                = undefined
مفاتيحُ التذكيرِ المرسومة                = 4
مفتاحُ تذكيرِ الصلاةِ المرسوم             = 0    ← يردُّ null بلا جسر، وهذا سلوكُه المشحون
السطرُ الذي يقرؤُه القارئُ في الشاشة:
   «التذكيرُ يرنُّ في تطبيقِ عزك منَ المتجرِ وحدَه. وفي المتصفّحِ يُحفَظُ اختيارُك ولا يرنُّ شيءٌ.»

بعدَ ضغطةٍ واحدة:  aria-checked=true · حقولُ الوقت=["06:00"]
                   ezik_reminders_v1 = {"sabah":{"on":true,"times":["06:00"]}, …}
                   رسائلُ عبرتِ الجسر = 0        ← لا جسرَ أصلًا
بعدَ إعادةِ تحميلٍ كاملة:  المخزنُ كما هو
```

**فالاختيارُ يُحفَظُ ويعيشُ، ولا يُوعَدُ القارئُ برنينٍ لا يقع.** وهذا بعينِه ما أمرَتْ به §٠-٥.

---

## ٤ · مفاتيحُ الجهازِ الجديدةُ — بالاسمِ وببرهانِ المحو

**مفتاحٌ واحدٌ جديدٌ لا غير: `ezik_reminders_v1`** — سجلٌّ واحدٌ يحملُ التذكيراتِ الأربعةَ (`{ on, times[] }` لكلٍّ).

**وسُجِّلَ في روسترِ «احذفْ بياناتي» ويُمحى معه** — وهذا نصُّ §٣-٢ من أمرِك. **والبرهانُ تجربةٌ مقيسةٌ لا دعوى**: بوّابةُ `deletetruth` تبذرُ **٤٥ مفتاحًا** يعرفُها `app.jsx`، ثمَّ **تضغطُ `resetAll` الحقيقيَّ المرفوعَ من `app.jsx` نفسِه**، ثمَّ تقرأُ ما بقي:

```
[PASS] every key app.jsx knows is classified -- must go, or must stay, and never neither
        45 keys read out of app.jsx; 27 must go, 20 must stay, 0 unclassified
[PASS] the press erases the roster and nothing beyond it
        27 keys removed, exactly the 27 expected

what "delete all my data" ERASES (27):
   …
   - ezik_reminders_v1        ← هنا
   …
```

**وكانَ الروسترُ ٢٦ فصارَ ٢٧** — والمفتاحُ الجديدُ هو الفرقُ كلُّه.

### ٤-١ · ⚠️ وهذا قرارٌ معاكسٌ لسابقةِ `ezik_prayer_notify_v1` — عمدًا، وبأمرِك

`ezik_prayer_notify_v1` مسجَّلٌ في **`MUST_STAY`** بحجّةٍ مكتوبةٍ: محوُه يترُكُ إذنَ النِّظامِ ممنوحًا والمفتاحَ مطفأً صامتًا. **والحالتانِ ليستا واحدةً، ولذلك الجوابانِ كلاهما صحيح:**

* ذاكَ **إذنٌ** منحَه القارئُ للنِّظام؛
* وهذا **أربعُ ساعاتٍ من يومِ إنسان** — متى يقومُ ومتى يقرأُ ومتى يجلسُ لوِردِه — **وهو سجلُّ شخصٍ بعينِه**، من الجنسِ الذي وُجِدَ وعدُ الصفحةِ من أجلِه. وتركُه يسلّمُ لمالكِ الجهازِ التالي هاتفًا يرنُّ السادسةَ صباحًا لشخصٍ رحل.

**والحجّةُ مكتوبةٌ في الحارسِ نفسِه**، لا في هذا التقريرِ وحدَه.

---

## ٥ · الملفّاتُ التي مُسَّتْ — وكلُّ ما خرجَ عن `app.jsx` مسمًّى ومعلَّلٌ بالأثر

| الملفّ | لماذا | صنفُه في §٣-١ |
|---|---|---|
| `app.jsx` | الشاشةُ والمخزنُ والعلفُ والراكبانِ الصغيران | **مسموحٌ نصًّا** |
| `app.js` | مُوَلَّدٌ من `app.jsx` بـ`tools/build-app.cjs` | **دفترُ إعادةِ البناء** |
| `sw.js` | `CORE_BYTES` + جدولُ البايتات | **دفترُ إعادةِ البناء** |
| `quest-bank-integrity-guard.cjs` | مرآةُ `SW_PROSE` + **البصمةُ آخِرًا** | **دفترُ إعادةِ البناء** |
| `tools/schedule-payload-measure.cjs` | 🔴 **بوّابةٌ وُسِّعَتْ** — §٥-١ | **خارجَ القائمةِ ⟹ مسمًّى هنا** |
| `tools/wird-guard.cjs` | 🔴 **بوّابةٌ وُسِّعَتْ** — §٥-٢ | **خارجَ القائمةِ ⟹ مسمًّى هنا** |
| `tools/delete-truth-measure.cjs` | 🔴 **بوّابةٌ وُسِّعَتْ** — §٤ | **خارجَ القائمةِ ⟹ مسمًّى هنا** |

**والأثرُ الذي أوجبَ الثلاثةَ واحدٌ:** كلُّ واحدةٍ منها **كانتْ تحرسُ غيابَ ما شُحِنَ اليوم**، فلو تُرِكَتْ لحمَّرَتْ بحقٍّ على شيءٍ أمرْتَ به. **ولم تُخلَقْ بوّابةٌ واحدةٌ: `gates.json` لم يُمَسَّ والروسترُ ٩٩** (§٦).

### ٥-١ · `schedulepayload` — الدبّوسُ نزلَ درجةً ولم يُنفَق

**العلّة:** البوّابةُ تُثبّتُ `ezikSchedItems` على **جملتَينِ اثنتَينِ حرفًا**، وتُثبّتُ موضعَ نداءِ `ezikNotifyAsk` في `PrayerNotifyToggle`.

**والحلُّ توسعةٌ في الاتّجاهَينِ لا تخفيف:**

1. **الجملتانِ نزلتا معَ ما تصفانِه.** الصلاةُ انتقلتْ إلى `ezikAdhanFeed()` بحرفِها — `if (!readPrayerNotify()) return [];` ثمَّ `return ezikAdhanItems(new Date());` — **والدبّوسُ يُثبِّتُها هناك**. وأُضيفَ دبّوسٌ **ثانٍ** على الوصلِ نفسِه: `ezikSchedItems` **جملةٌ واحدةٌ** هي `return ezikAdhanFeed().concat(ezikReminderItems(new Date()));` — فلا مرشِّحَ ولا قصَّ يختبئُ بينَ العلفَينِ والأنبوب.
2. **موضعُ النداءِ ما زالَ واحدًا.** بدلَ نسخِه لمفتاحٍ ثانٍ، **رُفِعَ إلى `ezikNotifyRequest()`** — والمفتاحانِ كلاهما يبلغانِ النِّظامَ عبرَه. **والعددُ لم يُمَسّ (`=== 1`)**، وأُضيفَ ما كلّفَتْه النقلةُ: **كلُّ مُنادٍ لذلكَ المُرسِلِ يُسمّى ويُقاسُ أنّه ليسَ داخلَ أيِّ `useEffect`** — `['EzikReminderSettings','PrayerNotifyToggle']` حرفًا.
3. **وسبعُ حالاتٍ جديدةٍ للتذكيرات** — مطفأةٌ ببناءِ الشيفرةِ عبرَ ١٧ مخزنًا تالفًا وواحدٍ يرمي عندَ القراءة · النافذةُ والحقولُ والوجهة · البابانِ ولا «27» · العددُ وحدودُه والمكرَّرُ · استقلالُ المفاتيحِ الخمسة · إطفاءٌ بلا إلغاءِ قناة · والقاموسانِ.

```
schedulepayload:  51/51  →  58/58 cases hold
```

### ٥-٢ · `wird` — حظرُ «حقلِ الوقتِ» صارَ سماحًا مسمًّى ومَسنودًا

**العلّة:** حظرانِ لـ`type="time"` في الملفِّ كلِّه (A-2 وA-3)، وحظرُ كلمةِ «تذكير» في كلِّ نصٍّ ظاهرٍ إلّا في قيمِ `prayer.notify.*`.

**والتوسعةُ على الشكلِ الذي رسمَه البندُ ٦٧ نفسُه لهذا الحارس:**

1. **A-2 ضاقَ على ما كانَ عنه أصلًا: الجدول.** جدولُ الثلاثينَ يومًا أوقاتُه **محسوبةٌ** ويجبُ ألّا تصيرَ أوقاتًا يختارُها قارئ — **فصارَ الفحصُ أربعةَ مواضعَ مسمّاةٍ** (اللوحةُ · ضابطُها في الإعداداتِ · نصُّ الجدولِ · مسارُ الجدولِ المرفوعُ كلُّه) **وهو فيها أشدُّ ممّا كانَ عليه بحثٌ في الملفِّ كلِّه**.
2. **A-3 أخذَ النصفَ الآخر** — سماحٌ في **مكوِّنٍ واحدٍ** (`EzikReminderSettings`) **والعددُ خارجَه صفر**، **ومسنودٌ سطرًا سطرًا**: المكوِّنُ موجودٌ ومركَّبٌ مرّةً واحدةً · مخزنُه مطفأٌ ببناءِ الشيفرةِ · لا شيءَ يُجدوَلُ حتّى يُشعِلَ القارئُ واحدًا · يبلغُ النِّظامَ عبرَ المُرسِلِ المشترَكِ لا نسخةٍ منه · **لا يُنادي `ezikNotifyStop` ألبتّة** · ومفتاحُه يذهبُ مع «احذفْ بياناتي».
3. **الكلمةُ: بادئةٌ ثانيةٌ ولا حرفٌ أكثر** — `reminders.*` انضمّتْ إلى `prayer.notify.*`، **والبادئتانِ تُقرآنِ من القاموسِ لا تُكتَبانِ هنا**، والعددُ خارجَهما ما زالَ صفرًا.
4. **وعدّادانِ صُحِّحا:** كانا يُحصيانِ `ezikNotifyAsk()`/`ezikNotifyStop()` **في الملفِّ الخامِّ**، **فكانَتْ فقرةٌ تشرحُ القاعدةَ لا تُميَّزُ عن خرقٍ ثانٍ لها** — وهو فحصٌ يعاقبُ الشيءَ الوحيدَ الذي ينبغي أن يكافئَه. **صارا يُحصيانِ على الشيفرةِ بعدَ نزعِ التعليقات؛ والأرقامُ والاتّجاهاتُ كما كانتْ حرفًا.**

```
wird:  1131 فحصًا (١١٢٤ نجحَ + ٧ حمّرَتْ بحقٍّ لحظةَ التعديل)  →  1137/1137
```

### ٥-٣ · دفترُ إعادةِ البناء — بترتيبِه، والبصمةُ آخِرًا

```
1. node tools/build-app.cjs        app.js   1171036 → 1193820   (+22784)
2. node tools/core-bytes.cjs --write  CORE_BYTES 2013883 → 2036667   (+22784 — الفرقُ نفسُه بلا مدخلٍ جديد)
3. جدولُ البايتاتِ في sw.js:112       + app.js 1171036 + → + app.js 1193820 +
4. مرآةُ SW_PROSE في الحارس           { n: 1171036, of: 'app.js' } → { n: 1193820, … }
5. بصمةُ sw.js آخِرًا                 5bd20328… → c6f6d26e…
```

`bankintegrity` **EXIT=0**، و`sw.js` **يحملُ صفرَ بايتةِ `CR`** فالقرصُ والبصمةُ يتّفقانِ بلا مصالحة.

---

## ٦ · التشغيلة — بالأرقام

```
=== SUITE: 99/99 EXIT=0 ===
recon:    SUMMARY   PASS=190   WARN=1   FAIL=0
tree after: 0 dirty path(s)
```

**ولا `[FAIL]` بالأقواسِ في `recon`** · **`gates.json` ٩٩ مدخلًا ولم يظهرْ في فرقِ الفرعِ ألبتّة** · **ولا `git add .`** — كلُّ إيداعٍ بأسماءٍ صريحة.

**وملفّا البياناتِ لم تُمَسَّ منهما بايتة:**

| الملفّ | البصمة |
|---|---|
| `adhkar.json` · `lib\data\adhkar.json` | **`19EF96B9`** (كما في تقريرِ أمس) |
| `adhkar-split-27.json` · `lib\data\adhkar-split-27.json` | **`56C0FC1A`** (كما في تقريرِ أمس) |

---

## ٧ · الثلاثةُ الصغارُ

### ٧-١ · اسمُ الزرِّ في نصِّ التأكيد — وُحِّدَ على ما يقرؤُه القارئُ فعلًا

| | قبل | **بعد** |
|---|---|---|
| **عربيّ** | «لمحوِها استعملْ **«احذفْ بياناتي»**» | «لمحوِها استعملْ **زرَّ «حذف كل البيانات وإعادة البدء» في لوحةِ الأهل**» |
| **إنجليزيّ** | “use **“Delete my data”**” | “use the **«حذف كل البيانات وإعادة البدء»** button in the parents’ dashboard” |

**والاسمُ في السطرَينِ هو نصُّ الزرِّ على الشاشةِ حرفًا** (`app.jsx:17084`). **والإنجليزيّةُ تسمّيهِ بالعربيّةِ عمدًا**: لوحةُ الأهلِ عربيّةٌ في اللسانَينِ، فالقارئُ الإنجليزيُّ يرى هذا النصَّ بعينِه على الزرّ.

> ⚠️ **وثالثٌ وجدتُّه ولم يأذنْ به أمرُك:** `delete.html:88` يسمّي الزرَّ **«مسح كلّ البيانات»** — اسمٌ ثالثٌ لزرٍّ واحد. **و`delete.html` خارجُ الملفّاتِ المسموحةِ، و§٢-١ لم تذكرْه**، **وبوّابةُ `deletetruth` تقرأُ بنودَ تلكَ الصفحةِ بنصِّها**. فتُرِكَ ويُسمّى هنا.

### ٧-٢ · اللافتةُ الكاذبة — قالتِ الواقع

```
قبل:  // ── THE GROUP DOORS -- ?adhkargroups=1, AND IT IS OFF FOR EVERYONE ──────
بعد:  // ── THE GROUP DOORS -- ON FOR EVERYONE SINCE 8665d66, ?adhkargroups=0 THE WAY BACK ──
```
(`app.jsx:6698`) — **والنصُّ تحتَها كانَ صادقًا سلفًا**؛ العنوانُ وحدَه هو الذي بقيَ على حالِ ما قبلَ `8665d66`.

### ٧-٣ · تقريرُ الجولةِ الماضية

`EZIK-CC-ADHKAR-SHIP-REPORT-2026-08-26.md` **أُودِعَ في هذا الفرعِ بايتةً بايتة** (`ca394e6`). **وعلّةُ غيابِه عن `main` مسمّاة:** أُودِعَ في `feat/adhkar-groups-20260826` **بعدَ** الدمجِ الذي أخذَ شيفرتَه، **فحملَ `main` العملَ ولم يحملْ خبرَه**. ويصلُ `main` مع الدمجِ القادم.

---

## ٨ · ما لم يُنفَّذْ ولماذا

1. 🔴 **مستمِعُ `op:'open'` لم يُبْنَ.** الوجهاتُ محمولةٌ ومقيسةٌ (§٣-٢)، **لكنَّ الضغطةَ تفتحُ التطبيقَ كما هو** ولا تهبطُ بالقارئِ في البابِ نفسِه. **والسببُ قرارٌ مسجَّلٌ لا سهو:** البندُ ٩٧ ألغى الرابطَ العميقَ إلى الأذكارِ، وإعادةُ فتحِه توسعةٌ لم يأذنْ بها هذا الأمر. **وهي أوضحُ جولةٍ تالية** إن أردتَّها.
2. **شريطُ «نسخةٌ جديدةٌ جاهزة» لم يُقترَبْ منه** — ⛔ نصُّ أمرِك.
3. **`delete.html` لم يُمَسّ** — §٧-١.
4. ⚠️ **سقفُ الستّينَ عندَ الغلافِ يستحقُّ اسمًا.** بكلِّ شيءٍ مشتعلٍ: ٥ صلواتٍ + ١ صباح + ١ مساء + ٣ وِرد + ٣ محتوى = **١٣ في اليوم**، فالستّونَ تملأُ في **٤٫٦ يومًا** من نافذةِ السبعة. **والقصُّ للأبعدِ زمنًا** (`accepted.slice(0, MAX_SCHEDULED)` بعدَ ترتيبٍ تصاعديّ) **وإعادةُ التسليحِ تقعُ عندَ كلِّ فتحٍ وكلَّ منتصفِ ليل** — فالمقصوصُ يعودُ قبلَ أن يحينَ وقتُه. **وهذا تدهورٌ لطيفٌ مصمَّمٌ لا عطب**، لكنّه ليسَ سبعةَ أيّامٍ كاملةً وقارئٌ لم يفتحِ التطبيقَ خمسةَ أيّامٍ يفقدُ الأبعد.
5. **لا دمجَ · لا نشرَ للإنتاجِ · و`main` لم يُمَسّ** — وقفتُ عندَ البريفيو كما أمرت.

---

## ٩ · البريفيو

**الرابطُ الثابتُ للفرع:**

```
https://ustaz-git-feat-reminder-settings-20260826-musaed-s-projects1.vercel.app
```

**والنشرةُ بعينِها:** `https://ustaz-4z7pfzw52-musaed-s-projects1.vercel.app` · `● Ready` · من `0102905`.

**والطريقُ إلى الشاشةِ ثلاثُ ضغطات:** «القائمة» ← صفُّ **«الإعدادات»** ← المجموعةُ **«تذكيراتُ العبادة»**، تحتَ صفِّ الحسابِ وفوقَ تفضيلاتِ الصلاة.

> ⚠️ **وأنتَ تفتحُه في متصفّحٍ، فسترى السطرَ الصادقَ ولن يرنَّ شيء** — وهو الصوابُ لا عطب (§٣-٦). **الرنينُ يُجرَّبُ في تطبيقِ عزك من المتجرِ وحدَه.**

**والإيداعاتُ الأربعةُ على الفرع:**

```
0102905  fix(reminders): a second slot steps from the feed's own hour, not from midnight
ca394e6  docs: carry the adhkar ship report onto this branch
ff2df55  feat(reminders): the reader's own times for four reminders, stored and armed
fef7ac8  docs: the reminder-settings order
```
