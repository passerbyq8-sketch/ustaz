# تقريرُ تنفيذٍ — عزك · الشاملةُ · **٢٥-ب · التصحيح**: البطاقةُ تُشكَّلُ على الخادمِ لا في المتصفّح

**النافذة:** ن١ · وكيلُ Claude Code · `C:\Users\passe\projects\ustaz-cc-ui`
**التاريخ:** 2026-08-22
**الفرع:** `feat/lib-search-16b-20260822`
**استُؤنِفَ من:** `334d9e6e` — ولم يُعَدْ ضمُّ ٢٥-أ ولم تُعَدْ بناءُ الورقة.

> ✅ **الحصيلة: منجَزٌ ومودَعٌ.** `babel` **خضراءُ** ولم يُمَسَّ الحارسُ ولا بايتة. الحمراءُ الوحيدةُ `bankintegrity` برقمَي `sw.js` وحدَهما. الحارسُ `139 → 169`.

**وتشخيصُ صاحبِ الأمرِ مقيسٌ صحيحًا:** `sw.js` لا يحملُ في `CORE` مدخلًا واحدًا تحتَ `/lib/` — قِيسَ بالبحثِ عن `'/lib/` في الملفِّ كلِّه ⟹ صفر. فالورقةُ التي تجلبُ `lib/lib-source-card.js` وقتَ التشغيلِ **تنكسرُ بلا إنترنت**. البوّابةُ كانت محقّةً، والسطرُ الذي سمَّتْه ذهب.

---

## ٠ · نقلُ الأمر

`ORDER-LIBSEARCH-16B-B.md` نُقِلَ إلى `C:\EZIK-STAGE\orders\` **قبلَ أيِّ فعلٍ آخر** ⟹ `ORDER_MOVED=True`.

---

## ١ · بوّاباتُ ما قبلَ العمل

| المفتاح | المطلوب | **المقيس** | الحال |
|---|---|---|---|
| `CURRENT_BRANCH` | `feat/lib-search-16b-20260822` | `feat/lib-search-16b-20260822` | ✅ |
| `HEAD` | `334d9e6e` أو سليلُه | `334d9e6efd8b974f46a2965ff7b4bd0801c35498` (هو نفسُه) | ✅ |
| `DIRTY_LINES` | `0` | `0` (بعدَ نقلِ الأمر) | ✅ |
| `DYNAMIC_IMPORT_SITES` | يُطبَعُ بأسطرِه | **`1`** — `app.jsx:12404`: `if (!ezLibCardModulePromise) ezLibCardModulePromise = import(EZLIB_CARD_MODULE);` | ✅ |

---

## ٢ · مواضعُ `import(` — قبلَ وبعد

| | العدد | الموضع |
|---|---|---|
| **قبل** | `1` | `app.jsx:12404` |
| **بعد** | **`0`** | — |

`DYNAMIC_IMPORT_SITES_AFTER = 0` — في `app.jsx` **وفي `app.js` الذي يحمِّلُه المتصفّح**. وزيادةً: **صفرُ ذكرٍ لأيِّ مسارٍ تحتَ `lib/`** في أيٍّ منهما، فلم يعدْ في الورقةِ ما يقعُ خارجَ `CORE`.

وذهبَ معَ السطرِ كلُّ ما كان يخدمُه: `EZLIB_CARD_MODULE` · `ezLibCardModulePromise` · `ezLibCardModule()` · حالةُ `mod` في الورقةِ و`useEffect` الذي يجلبُها · وفرعُ «الوحدةُ قبلَ المسار» في `run`.

**و`babel` خضراءُ الآن**، ونصفُها الثاني — توكيدا البندِ ٣٢ — **يجري لأوّلِ مرّةٍ في هذا العمل** بعدَ أن كان الماسحُ يُخرِجُ قبلَهما:

```
OK: Babel transform succeeded with sourceType=script (no error reproduced)
=== IMPORT SCAN (block-relative + HTML line) ===
(no lines containing the word "import" in the babel block)
=== ITEM 32: GENERATED BUNDLE ===
OK: two builds in this run emitted identical bytes
OK: app.js is byte-for-byte what the shipped JSX builds
```

**ولم يُمَسَّ `babel-gate.cjs` ولا بايتةً** — `git diff --numstat 334d9e6e..HEAD` لا يذكرُه.

---

## ٣ · الخادمُ يُشكِّلُ البطاقة

`api/lib-search.js` — `+51/-2` سطرًا.

**الاستيرادُ اتّسع** إلى `buildSourceCard` و`renderSourceCard` و`REFUSED_TEXT` و`DEGRADED_TEXT` معَ القائمتَينِ اللتَينِ كان يستوردُهما. وهو استيرادُ عقدةٍ عاديٌّ على مسارٍ **لا يمرُّ ببابل ألبتّة**.

**والإضافاتُ ثلاثةُ أسماءٍ لا رابعَ لها، وهي مصدَّرةٌ بالاسمِ ليقرأَها الحارس:**

```js
export const ADDED_HIT_FIELD = 'source_card';
export const ADDED_RESPONSE_FIELDS = Object.freeze(['refused_text', 'degraded_text']);
```

### شكلُ `source_card` كما يُرجِعُه الخادمُ فعلًا

إصابةٌ **غيرُ** قابلةٍ للإحالةِ (`hit_not_citable`، وهي تحملُ في الفيكستشرِ `volume: 9407` و`page_start: 7301` و`page_end: 7302`):

```json
{
 "page_citable": false,
 "book_title": "مجموع الفتاوى",
 "author": "ابن تيمية",
 "heading_path": ["كتاب الصلاة", "فصل في السهو"],
 "line": "مجموع الفتاوى · ابن تيمية · كتاب الصلاة ← فصل في السهو"
}
```

**لا مفتاحَ صفحةٍ ولا رقمَ صفحةٍ، لا في البطاقةِ ولا في السطرِ المعروض** — والمسارُ المذكورُ بدلَه، فالبطاقةُ ليست مُفرَّغةً بل مُحالةً إلى الباب.

وإصابةٌ قابلةٌ للإحالةِ تحصلُ على صفحتِها:
`"line": "المغني · ابن قدامة المقدسي · ج 9407 · ص 7301-7302"`.

### الحقولُ السبعةَ عشرَ لم تُمَسّ

`source_card` **إضافةٌ لا بديل**: الإصابةُ نفسُها ما زالت تحملُ `page_start` و`page_end` و`volume` كما أرسلَها الخادمُ الأصليّ — مقيسٌ بتوكيدٍ صريحٍ في القسم F. والقائمةُ البيضاءُ **تجري أوّلًا** والبطاقةُ تُبنى من **مخرجِها**، فحقلٌ اخترعَتْه الخدمةُ لا يبلغُ بطاقةً ولو بطريقٍ غيرِ مباشر.

### قاعدةُ الصفحةِ ما زالت في مكانٍ واحد

`buildSourceCard` وحدَها تقرِّر. وما أُضيفَ في الدالّةِ **ليس قرارًا ثانيًا** بل شبكةُ أمانٍ: إن عادتْ بطاقةٌ تحملُ مفتاحَ صفحةٍ معَ `page_citable !== true`، **لا تُرسَلُ البطاقةُ أصلًا**. بطاقةٌ غائبةٌ صدقٌ، وإحالةٌ لا يجدُها القارئُ ليست كذلك.

### الجملتان

`refused_text` و`degraded_text` تُكتَبانِ **من ثابتَي الوحدةِ نفسِها**، وكلٌّ منهما **حينَ تنطبقُ حالتُها فقط** — مقيسٌ: جسمُ الرفضِ لا يحملُ `degraded_text`، وجسمُ البتْرِ لا يحملُ `refused_text`، و`200` عاديٌّ لا يحملُ أيًّا منهما.

**وصفرُ تسريب:** لا التوكنُ ولا اسمُ المضيفِ في أيِّ بايتةٍ من الجوابِ — مقيسٌ على الجسمِ المسلسَلِ كاملًا.

---

## ٤ · العميلُ يرسمُ بياناتٍ فقط

`app.jsx` — `+63/-87` سطرًا (الكتلةُ صغرَتْ لأنّ آليّةَ الاستيرادِ ذهبت).

* الورقةُ ترسمُ `hit.source_card.line` **كما وصل**، و`refused_text`/`degraded_text` **كما وصلا**.
* `ezLibSentence(state, payload)` لا تنتجُ كلمةً من عندِها: الحالُ تسمّي **حقلًا**، والدالّةُ تقرؤُه؛ وإن لم يكنْ نصًّا فالجوابُ سلسلةٌ فارغةٌ ولا يُرسَمُ شيء.
* **`CARD_TEXT_IN_APPJSX = 0`** — مقيسٌ على مجموعتَين: الجملُ الثلاثُ التي تملكُها الوحدة، **والجملُ الأربعُ التي يرسلُها `api/lib-search.js`** مقروءةً من مصدرِ الدالّةِ نفسِه لا مُعادةَ الكتابةِ في الحارس.
* رسالتا `503` و`502` صارتا **من الخادمِ** (`payload.error.message`)، بعدَ أن كانتا مكتوبتَينِ في `app.jsx` في الجولةِ السابقة.
* إصابةٌ لم تصلْ بطاقتُها **لا تُرسَمُ أصلًا** — لا تُقتَبَسُ بلا نسبة. مقيسٌ على الشاشةِ المرسومةِ بجسمٍ حُذِفَ منه `source_card`.

### ما بقيَ من نصٍّ في `app.jsx`، وهو زينةُ شاشةٍ لا خبرُ نتيجة

عنوانٌ · رجوعٌ · ترويسةُ مجموعةٍ · تلميحٌ · نائبُ نصٍّ · «ابحث» · «النتائج» · «يجري البحث…» · «أعد المحاولة». **ولا واحدةَ منها تقولُ ماذا حدثَ للبحث.**

> ⚠️ **تفسيرٌ اتّخذتُه ويُسجَّلُ صراحةً:** §٣-٣ تمنعُ «نصَّ بطاقةٍ **أو رسالةٍ**» في `app.jsx`. فحذفتُ `EZLIB_NONE` («لا نتيجة لهذا البحث.») لأنّها خبرُ نتيجةٍ يكتبُه العميل، والخادمُ لا يرسلُ حقلًا لها. **والأثر:** بحثٌ يرجعُ بصفرِ إصاباتٍ يعرضُ الآن الاستمارةَ ولا جملةَ تحتَها. هذا ما يقتضيه «غابَ الحقلُ ⟹ لا يُرسَمُ شيءٌ ولا يُختلَقُ بديل»، **وإن أرادَ صاحبُ الأمرِ سطرًا للحالةِ الفارغةِ فمكانُه الخادمُ** — حقلٌ ثالثٌ بجانبِ الاثنَين، لا نصٌّ يعودُ إلى الورقة.

---

## ٥ · الحارسُ — `139 → 169`

`guards/lib-search-16a-guard.cjs`، وهو حارسٌ قائمٌ **غيرُ مسجَّلٍ في `gates.json`** فلا يُغيِّرُ عددَ البوّابات (٩٠ كما هي).

| | قبل | **بعد** |
|---|---|---|
| عددُ التأكيدات | `139` | **`169`** |
| الحصيلة | `139/139 - PASS` | **`169/169 - PASS`** |

**ولم يُخفَّفْ تأكيدٌ واحد.** توكيدانِ في القسم C كان لا بدَّ من **إعادةِ توجيهِهما** لأنّ «كلُّ مفتاحٍ من الأحدَ عشرَ / السبعةَ عشرَ» صارَ كاذبًا لحظةَ انتقالِ البطاقةِ إلى الخادم. والبديلُ **أضيقُ لا أوسع**: يسمّي الإضافاتِ المسموحةَ بالاسمِ من قائمتَي الدالّةِ المصدَّرتَين، فاسمٌ رابعٌ يظهرُ يسقطُ كما كان يسقط. ومعهما أُضيفتْ ثلاثةُ توكيداتٍ جديدةٍ تثبِّتُ الأسماءَ الثلاثةَ وتُثبِتُ أنّ الإضافةَ ليست استبدالًا.

**والجديدُ الملزِمُ (§٤ من الأمر)، وقد قِيسَ:**

* `PAGE_SHOWN_WHEN_NOT_CITABLE = 0` **في البطاقةِ التي بناها الخادم**
* **و`0` في السطرِ الذي صيَّرَه الخادم**
* **ولا مفتاحَ صفحةٍ في البطاقةِ أصلًا** — لا مجرَّدَ قيمةٍ فارغة
* والمسارُ مذكورٌ بدلَه · والإصابةُ القابلةُ ما زالت تُظهِرُ صفحتَها · والسبعةَ عشرَ باقيةٌ على الإصابة

**والقسمُ F0 جديدٌ كلُّه** وهو التوكيدُ الذي وُجِدَ التصحيحُ لأجلِه: صفرُ `import(` في `app.jsx` وفي `app.js`، وصفرُ مسارٍ تحتَ `lib/` في أيٍّ منهما، **وأنّ `sw.js` فعلًا لا يحملُ `'/lib/`** — فالتوكيدُ يقيسُ العلّةَ لا الأثر.

### طفرةُ العضّ — المطلوبةُ في §٤، على القرصِ ثمّ رُدَّتْ

| الخطوة | النتيجة |
|---|---|
| جُعِلَ الخادمُ يمرِّرُ الصفحةَ في الحالةِ غيرِ القابلة (`page_citable: true` + الحقولُ الثلاثة) | `MUTATION_APPLIED` — والحارسُ يُثبِتُ أنّ المصدرَ **تغيَّرَ فعلًا** |
| جريانُ الحارسِ على المطفَّر | **`162/169 - FAIL`**، خروجٌ `1`. سقطَ **سبعةُ** توكيدات: أربعةٌ على البطاقةِ والسطرِ، **واثنانِ على الشاشةِ المرسومة**، وواحدٌ هو الفحصُ الذاتيُّ للطفرة |
| رُدَّ الأصلُ | `RESTORED_IDENTICAL=true` ⟹ **`169/169 - PASS`** |

وفي الحارسِ نفسِه طفرتانِ داخليّتانِ دائمتانِ: خادمٌ مطفَّرٌ في الذاكرةِ (عبرَ `data:` URL) يجبُ أن يُسرِّبَ، وعرضٌ مطفَّرٌ يجبُ أن يُمسَك — وكلتاهما تتحقّقُ أوّلًا من أنّ الطفرةَ **ليست عاطلة**.

---

## ٦ · شرطُ القبول

| المفتاح | **المقيس** |
|---|---|
| `DYNAMIC_IMPORT_SITES_AFTER` | **0** — في `app.jsx` و`app.js` |
| `babel` | **خضراء**، والحارسُ لم يُمَسّ |
| `LIB_HOST_IN_APPJSX` | **0** — و`0` في `app.js` |
| `CALLS_ON_BOOT` | **0** — بشجرةِ البناءِ (موضعُ نداءٍ واحدٌ، سلسلتُه `LibrarySheet > run`) **وعلى ورقةٍ مرسومةٍ لم يُكتَبْ فيها شيء: `gNet.length === 0`**، أي لا نداءَ للمسارِ ولا لوحدة |
| `PAGE_SHOWN_WHEN_NOT_CITABLE` | **0** — في البطاقةِ، وفي السطرِ، وعلى الشاشةِ المرسومة |
| `CARD_TEXT_IN_APPJSX` | **0** — على جملِ الوحدةِ الثلاثِ وجملِ الدالّةِ الأربع |
| `node tools/build-app.cjs --check` | `959845` بايتة · `f2c9718822134a6c140f2be7b10aa0bb62cd8b10c1702728f7d3ef714ec001a0` — مطابق |
| `TDZ_COUNT` | **0** · `SCOPES=1355` · `MODE=script` · **بمسارٍ صريحٍ مطبوع:** `C:/Users/passe/projects/ustaz-cc-ui/app.jsx` (`SRC_CHARS=1019666`، مطابقٌ للقرص) |
| الإيداعُ أوّلًا | نعم، ثمّ السلسلةُ على شجرةٍ نظيفة (`0 dirty path(s)`) |
| الدفع | **صفر** |

### الأرضيّاتُ — والمسمَّاةُ في §٥ منها

| | قبل (`334d9e6e`) | **بعد** (`5fd92712`) |
|---|---|---|
| `run-gates` | 88/90 | **89/90** |
| `recon` | `PASS=182 WARN=1 FAIL=0` | `PASS=182 WARN=1 FAIL=0` |
| `babel` | **حمراء** | ✅ `EXIT=0` |
| `themecoverage` | `EXIT=0` | ✅ `EXIT=0` |
| `wird` | `EXIT=0` | ✅ `EXIT=0` |
| `i18nui` | `EXIT=0` | ✅ `EXIT=0` |
| حارسُ ١٦-أ | `139/139` | ✅ **`169/169`** |

**لم تنخفضْ أرضيّةٌ، ولم يسقطْ فحصٌ كان أخضر، وارتفعتْ بوّابةٌ كانت حمراء.**

### الحمراءُ الوحيدةُ — `bankintegrity`، نصُّها حرفًا

```
  FAIL [B12] CORE_BYTES = 1781136 but CORE weighs 1793243 bytes on disk (+12107).
        The quota pre-check is measured against a number that stopped being true.
        Repair it with `node tools/core-bytes.cjs --write` and re-cut the sw.js
        digest in the SAME commit -- never by hand from the comment table.
  FAIL [B14] sw.js prose says app.js is 947738 bytes; the disk says 959845 (+12107).
        A comment that states a size is what the next person re-cuts CORE_BYTES from,
        so it is wrong in exactly the way the constant used to be.
FAIL  74 checks passed, 2 failed.
```

رقمانِ في `sw.js` وحدَهما، وكلاهما `+12107` وهو نموُّ `app.js` بعينِه (`947738 → 959845`). لا ثالثَ لهما. **صفرُ بايتةٍ في `sw.js`.**

---

## ٧ · ما لم يُقَسْ، بعلّتِه

1. **لم يُجرَّبْ نداءٌ حيٌّ — التوكنُ في الإنتاجِ وحدَه.** كلُّ ما في هذا التقريرِ أجسامٌ أنتجَتْها الدالّةُ الحقيقيّةُ من فيكستشراتٍ محلّيّة. أوّلُ نداءٍ فعليٍّ لـ`https://lib.ezik.app/search` لم يقعْ ولن يقعَ في معاينة: كلُّ معاينةٍ بلا `SEARCH_API_TOKEN` تُجيبُ `503`.
2. **لم يُقَسْ سلوكُ الورقةِ بلا إنترنتٍ في متصفِّحٍ حقيقيّ.** المقيسُ أنّ سببَ الكسرِ ذهب: صفرُ جلبٍ لأيِّ مسارٍ تحتَ `lib/` من الورقة، وصفرُ نداءٍ عندَ الفتح. أمّا تشغيلُ عاملِ خدمةٍ حقيقيٍّ وقطعُ الشبكةِ فلم يقعْ، لأنّ `sw.js` محظورةٌ في هذه الجولةِ فلم أُشغِّلْ ما لا أملكُ إصلاحَه.
3. **`CALLS_ON_BOOT=0` مقيسٌ على ورقةٍ مركَّبةٍ وحدَها، لا على إقلاعِ التطبيقِ كاملًا** إلى الرئيسةِ ثمّ إلى البلاطة. المقيسُ: موضعُ نداءٍ واحدٌ في شجرةِ البناءِ كلِّها، وورقةٌ مرسومةٌ صفرُ طلباتٍ منها.
4. **لم تُقَسْ فروقُ الخطِّ ولا اتّجاهُ النصِّ على شاشةٍ حقيقيّة.** `linkedom` بلا `getComputedStyle`، ولم تُلتقطْ صورة.
5. **لم يُقَسْ حجمُ الجوابِ بعدَ الإضافة.** `source_card` يُكرِّرُ عمدًا حقولًا موجودةً على الإصابةِ (`book_title` · `author` · وربّما `volume`/الصفحات)، فجسمُ عشرِ إصاباتٍ صارَ أكبرَ ممّا كان. لم يُقَسْ كم، لأنّ قياسَ الزمنِ والحجمِ ممنوعٌ في هذه الجولة، ولأنّ `MAX_BYTES` في الدالّةِ يزنُ جوابَ الخدمةِ لا جوابَنا.
6. **الحالةُ الفارغةُ بلا سطرٍ الآن** (§٤ أعلاه). لم يُقَسْ أثرُ ذلك على قارئٍ، وهو تفسيرٌ لا حكم.
7. **لم يُقَسْ زمنٌ ألبتّة.**

---

## ٨ · الرأسُ الأخير

| المفتاح | القيمة |
|---|---|
| الرأسُ المُستأنَفُ منه | `334d9e6efd8b974f46a2965ff7b4bd0801c35498` |
| إيداعُ التصحيح | **`5fd92712022255749224f7eaa9eaeae1bc930662`** |
| `FINAL_HEAD` | **`5fd92712022255749224f7eaa9eaeae1bc930662`** — وعليه قِيسَتِ السلسلةُ على شجرةٍ نظيفة |
| الدفع | **صفر** |

ويُودَعُ هذا التقريرُ فوقَه، وSHA إيداعِه مطبوعٌ على الطرفيّةِ بعدَ الختم.

---

## ٩ · في الختمِ وسطورِه

🔴 **الختمُ محسوبٌ على بايتاتِ LF.** و`core.autocrlf` **مفعَّلٌ** في هذه الشجرة، فسحبٌ جديدٌ على وِندوز يكتبُ هذا الملفَّ بـCRLF على القرصِ ولن يُعيدَ الرقمَ. **فيُعادُ إنتاجُ الختمِ من كائنِ git لا من نسخةِ العمل:**

```
git show <commit>:EZIK-CC-LIBSEARCH-16B-B-REPORT-2026-08-22.md
```

وقد قِيسَ في هذا الجريانِ أنّ الكائنَ والقرصَ يعطيانِ الرقمَ نفسَه، لأنّ نسخةَ العملِ ما زالت LF كما كُتِبَتْ.

<!-- PAYLOAD-END -->

```
PAYLOAD_BYTES = 18754
SHA256        = 838e0619e451ed22d6e643694945143a22ea2d1dfffdb5863b3a54cb30008907
ALGO          = sha256 over every byte of this file up to and including the line <!-- PAYLOAD-END --> and its newline
ENCODING      = LF. core.autocrlf is on in this tree: reproduce from the git object, not the working copy.
```
